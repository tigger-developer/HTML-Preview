#+TITLE: W011 - Native Org conversion validation
#+SCHEMA: 2
#+STARTUP: content
#+OPTIONS: toc:2 num:nil
#+TODO: AMBER RED | GREEN

* Automated regression

** GREEN RT011.1 - Format routing :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-1-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Native Org, explicit reader, linked Org, text/code wrappers and packaged symlink cases pass; Markdown and other reader preservation tests pass.
:END:

*** Earlier test-admission execution

- State :: RED.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Native .org, explicit Org reader and text/code wrappers still invoke Pandoc; guarded CLI/service tests reject them. The built executable rejects the missing private worker entry point.

- Criteria :: See [[file:spec.org::#rt-011-1][RT011.1 - Format routing]].

** GREEN RT011.2 - Semantic HTML :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-2-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Semantic output checks pass for metadata, outline/navigation, declared task categories, checkbox variants and explicit footnote structure/roles/relationships.
:END:

*** Earlier test-admission execution

- State :: GREEN.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Initially GREEN preservation: explicit footnote nesting, roles, ordered definitions, backlinks and multi-paragraph assertions pass against the Pandoc baseline. Existing metadata, navigation and semantic heading checks also passed. TestCheckboxRendering and checkbox rows of TestHTTPBlockAnnotations verify all task states, spacing and whole-item structure.

- Criteria :: See [[file:spec.org::#rt-011-2][RT011.2 - Semantic HTML]].

** GREEN RT011.3 - Literal code :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-3-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Known-language highlighting and exact literal/copy payloads pass; source and example blocks remain inside multi-paragraph footnotes.
:END:

*** Earlier test-admission execution

- State :: GREEN.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Initially GREEN preservation: existing exact literal/code-copy and highlighting cases plus JavaScript, Python and Java cases passed against the Pandoc baseline.

- Criteria :: See [[file:spec.org::#rt-011-3][RT011.3 - Literal code]].

** GREEN RT011.4 - Block placement :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-4-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Whole-block HTTP placement, short definition, nested/list/task/container cases, repeated notes and rejection of unverified targets pass without weakening source proof.
:END:

*** Earlier test-admission execution

- State :: GREEN.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Initially GREEN preservation: whole-block placement pack, newly added short definition, unverified-target rejection and repeated notes passed through real HTTP saves.

- Criteria :: See [[file:spec.org::#rt-011-4][RT011.4 - Block placement]].

** GREEN RT011.5 - Footnote lifecycle :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-5-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Ordinary and repeated multi-paragraph footnotes pass identification, edit, reload and deletion; BOM, legacy reading, source preservation and sidecar cases pass.
:END:

*** Earlier test-admission execution

- State :: GREEN.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Initially GREEN preservation: ordinary-note editing/retries/conflicts, deletion, read-only sidecar endnotes and rendered markup checks passed through existing HTTP helpers. The additional repeated two-paragraph fixture passed identification, edit, reload association and delete with exact unrelated-source preservation.

- Criteria :: See [[file:spec.org::#rt-011-5][RT011.5 - Footnote lifecycle]].

** GREEN RT011.6 - Trust boundaries :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-6-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: No-fetch/no-execution publication checks and valid/malformed/duplicate private-record controls pass under the native converter.
:END:

*** Earlier test-admission execution

- State :: RED.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: Published-page passive-content and no-fetch checks pass on the baseline; the new valid owned-literal-record case fails because the worker scaffold cannot convert it. Invalid and duplicate records must be assessed together with this valid control.

- Criteria :: See [[file:spec.org::#rt-011-6][RT011.6 - Trust boundaries]].

** GREEN RT011.7 - Limits and failure recovery :testdef:
:PROPERTIES:
:CUSTOM_ID: rt011-7-result
:REVISION: e171898; full race suite at 216bee6, affected race checks at de0cb56 and the corpus checkpoint, current converter coverage pack
:COMMAND: make test; affected code/footnote race command recorded below
:EVIDENCE: Native protocol, long lines, exact/excess input/output bounds, runtime memory abort, started-child cancellation/output flood, reaping, cleanup and recovery all pass.
:END:

*** Earlier test-admission execution

- State :: RED.
- Revision :: 64b4b81 plus W011 test package and worker scaffold.
- Observation :: The worker scaffold rejects valid empty, ordinary and long-line requests and subsequent/concurrent valid requests. The native worker failure injection receives HTTP 200 because the baseline never invokes that worker; required existing failure classification is 422.

- Criteria :: See [[file:spec.org::#rt-011-7][RT011.7 - Limits and failure recovery]].

* Test-admission execution, 20 September 2026

- New code :: =native_org_test.go= in preview and integration;
  =internal/orgconvert/worker_test.go=; one additional short-definition row in
  =annotation_blocks_test.go=; controlled converter rejection in the existing
  preview test Host.
- Scaffold :: =internal/orgconvert/worker.go= declares the private request and
  a worker that always returns failure. TestMain dispatches that worker so the
  tests compile and exercise behaviour. No production routing or conversion
  is implemented at this checkpoint.
- Primary command :: =go test ./internal/orgconvert ./internal/preview -run 'TestRT011|TestHTTPBlockAnnotations|TestHTTPBlockRejectsUnverifiedTargets|TestHTTPRepeatedBlockNotesAndHeadingAnchor|TestRT009_7_EditOrdinaryFootnotes|TestHTTPDeleteFootnote|TestRT009_8_ReadOnlySidecarUsesNativeEndnotes|TestRT009_8_EditableNotesPreserveRenderedMarkup|TestRT002_1_CodeFidelity|TestRT004_3_ConsolidatedFrontmatter|TestFoldingSemanticHeadingClasses|TestRT005_2_NavigationStructure|TestRT006_8_CancelledLastWaiterReleasesWorker' -count=1=.
- Additional commands :: =go test ./integration ./internal/preview -run 'TestRT011_1_PackagedNativeWorker|TestRT011_7_NativeWorkerFailureAndRecovery' -count=1=;
  =go test ./internal/orgconvert -run 'TestRT011_6_Owned|TestRT011_7_Concurrent' -count=1=.
- Classification correction :: The page renderer's existing generic conversion
  failure is HTTP 422, not 503. The recovery test expectation was corrected and
  rerun independently with =go test ./internal/preview -run TestRT011_7_NativeWorkerFailureAndRecovery -count=1=;
  it still fails meaningfully with observed HTTP 200 before native routing exists.
- Result :: Executed behavioural RED in native routing/worker cases; retained
  semantics passed on the existing converter. No compilation/setup failure is
  classified as RED. The invalid-request controls alone do not prove validation
  while the scaffold rejects everything.
- Static check :: =golangci-lint run ./internal/orgconvert/... ./internal/preview/... ./integration/...= passed.
- Environment :: Local macOS; existing Go module/build caches isolated under
  operating-system temporary directories. No browser or live service was used.

* Additional test-admission execution, 20 September 2026

- Command :: =go test ./internal/orgconvert ./internal/preview -run 'TestRT011|TestCheckboxRendering|TestRT002_2_PassiveHighlighting' -count=1=.
- Native behavioural RED :: Exact serialized input allowance and ordinary-memory
  controls fail because conversion is not implemented. The new service cases
  observe HTTP 200 instead of the specified 504/413: native routing does not yet
  invoke the real blocked/flooding child. These tests subsequently check its
  start acknowledgement, reaping, scratch cleanup and reuse of service capacity.
- Memory boundary :: A 2.3 MiB structured document must convert under the ordinary
  ceiling; the same input under a valid private 16 MiB allowance must report a
  content-free memory-limit error without partial output.
- Initially GREEN preservation :: =TestCheckboxRendering= and the existing
  passive-highlighting checks passed. Repeated multi-paragraph footnote lifecycle
  passed after correcting the deletion oracle to retain the blank lines outside
  the note definition. No application behaviour changed.
- Confirmation command :: =go test ./internal/preview -run 'TestRT011_5_Repeated|TestCheckboxRendering' -count=1= passed.
- Static check :: Scoped =golangci-lint= passed for the converter, preview and
  integration packages.

* One-off tests

** GREEN OT011.1 - Full-pipeline benchmark :testdef:
:PROPERTIES:
:CUSTOM_ID: ot-011-1-result
:REVISION: e171898 production checkpoint against 96a8a2ce165333045a377c38da044bb501461e4f
:EVIDENCE: Complete 108-operation final comparison passes all GET thresholds and source-preserving POST checks; 33 preliminary/interrupted operations retained separately. Final results below.
:END:

** GREEN OT011.2 - Packaging and dependencies :testdef:
:PROPERTIES:
:CUSTOM_ID: ot-011-2-result
:REVISION: e171898 corpus compatibility checkpoint; prior package trials retained below
:EVIDENCE: Four release targets built; licence bytes matched; isolated Darwin arm64 service rendered through a symlink without Go/Rust on PATH. Actual-code and unreplaced-upstream vulnerability scans both reported no vulnerabilities.
:END:

* User tests

** AMBER UT011.1 - Reader review :testdef:
:PROPERTIES:
:CUSTOM_ID: ut-011-1-result
:REASON: Human browser review follows implementation and automated validation; no browser automation is authorized.
:END:

** AMBER UT011.2 - Annotation review :testdef:
:PROPERTIES:
:CUSTOM_ID: ut-011-2-result
:REASON: Human annotation interaction review follows implementation; HTTP results do not establish presented UX.
:END:

* Packaging execution, 20 September 2026

- Candidate :: =216bee67bd61455f7ed859a635feb041b7b7ea26=, native converter
  checkpoint. Archives were built immediately before that checkpoint with the
  same production files; their embedded revision label names the preceding
  checkpoint. No live installation or manager restart occurred.
- Environment :: Darwin arm64, Go =1.26.8=, Pandoc =3.9.0.2=.
- Build command :: =VERSION=0.1.0 make release= produced the four supported
  archives. This version is a local packaging-test value, not a release tag.
- Native trial :: Unpacked Darwin arm64 archive; executable symlink outside the
  checkout; isolated config, document root and runtime socket; PATH containing
  Pandoc and system utilities but no Go, Cargo or Rust compiler. The real
  service returned HTTP 200 with Org content, task text, highlighted Go and
  six embedded font faces, then shut down successfully.
- Notices :: Parser, Chroma, regexp2, project and both font licences in every
  archive matched tracked bytes. All 16 copied upstream Org files were compared
  with the pinned module: only =document.go= differs, for the documented scanner
  allowance. Upstream module metadata confirms the recorded commit hash.
- Vulnerabilities :: =make vulncheck= reported =No vulnerabilities found.= for
  both linked application code and the locked unreplaced-upstream module scan.
  The initial module-only command rejected package patterns; the corrected
  documented invocation passed. No scanner finding was suppressed.
- Static checks :: =make lint= passed with the documented StyLua =2.5.2=
  executable provisioned only in OS temporary storage. Its archive SHA-256
  matched the upstream release digest. Initial missing-tool execution was
  inconclusive, not a lint success. No browser code or styles changed.
- Coverage :: Existing targeted worker/HTTP tests instrumented through
  =go test -coverpkg=./internal/orgconvert= and combined =set= profiles cover
  87.7% of the new package statements. Source-preservation and legacy-note
  cases supplement the focused native pack. This measurement does not replace
  the full race suite or human interaction review.

| Package target | Executable bytes |
|----------------+------------------|
| macOS amd64 | 22,936,880 |
| macOS arm64 | 21,735,490 |
| Linux amd64 | 22,509,073 |
| Linux arm64 | 21,147,262 |

The preserved macOS arm64 Pandoc baseline at =96a8a2c= is 16,985,122 bytes.
The candidate adds 4,750,368 bytes (28.0%), including the parser, highlighter and
adapter; this is not an isolated library-size measurement. Linux/WSL runtime
and desktop behaviour remain untested here; cross-compilation does not qualify
those platforms.

* Final regression and package refresh, 20 September 2026

- Full suite :: =make test= passed at the native converter checkpoint with
  =go test -race -timeout=20m ./...=. All packages passed; the preview package
  took 1,161.022 seconds. No race detector finding occurred.
- Additional compatibility case :: An Org =SRC= block following the first
  paragraph of a footnote lost its containing note because preservation added
  a blank line. The added case failed on missing footnote markup, then passed
  after removing that generated separator, matching the =EXAMPLE= correction.
- Final affected command :: =go test -race ./internal/preview -run 'TestRT009_8_EditableNotesPreserveRenderedMarkup|TestRT002_1_CodeFidelity|TestHTTPBlockAnnotations|TestRT011_3' -count=1=
  passed in 72.520 seconds at =de0cb56=. The non-race execution passed in
  4.536 seconds. No unrelated suite rerun was needed for that single spacing
  correction.
- Packaging refresh :: The four archives and isolated macOS symlink/service
  trial were regenerated after that correction. Licences and content checks
  passed again. Final Linux arm64 size is 21,147,286 bytes; the other three
  sizes above are unchanged. Their embedded revision now identifies =de0cb56=.
- Final static checks :: =make lint= passed after the correction. Dependency
  graphs and reachability are unchanged from the successful two-part scan.
- Documentation :: Converter ownership is updated in the README, Vision,
  Architecture, annotation and service guides. Notices and parser provenance
  cover the added dependencies. CLI help and format mappings are unaffected:
  public invocation, Pandoc installation and admitted readers remain unchanged.
  Technical prose passed =sanitize oed=.

* Corpus compatibility verification, 20 September 2026

- Preliminary comparison :: The frozen large document exposed wrapped =p. 32=
  losing its prefix, ordinary list items acquiring invented definition terms,
  and same-document Org heading links being treated as file URLs. These
  candidate timings are retained separately and do not qualify performance.
- Focused RED :: =TestRT011_2_CorpusListSemantics= and
  =TestRT011_2_LocalHeadingSearches= reproduced those failures before correction.
- Affected GREEN :: =go test -race ./internal/preview -run 'TestRT011_2|TestRT001_7|TestHTTPBlockAnnotations|TestCheckboxRendering|TestRT009_8_EditableNotesPreserveRenderedMarkup' -count=1=
  passed in 89.474 seconds. This includes whole-block placement and existing
  footnote markup cases, with unchanged source-write proof.
- Patch provenance :: The final local upstream diff now includes =list.go= as
  well as the previously documented =document.go= scanner allowance. The two
  list corrections and synthetic reproducer are documented in the local-module
  README; the earlier scanner-only comparison above records its earlier build.
- Static checks :: =make lint= passed after these corrections.
- Heading search follow-up :: Source markup and rendered-text spellings both
  resolve to the final custom ID. Missing and duplicate headings stay inactive.
  The affected heading/native lifecycle race checks passed in 34.693 seconds.
- Source placement follow-up :: Three added HTTP cases save notes after wrapped
  =p. 32= prose, a definition, and its neighbouring ordinary list item. The
  focused race execution passed in 8.676 seconds.
- Final package refresh :: After =e171898=, all four release targets rebuilt.
  The isolated Darwin arm64 symlink/service trial passed, with licence bytes
  matching tracked files and no Go/Rust executable on the runtime PATH.
  Final executable sizes are 22,937,104 bytes (Darwin amd64), 21,735,698 (Darwin
  arm64), 22,509,534 (Linux amd64) and 21,147,331 (Linux arm64). Only
  =document.go= and =list.go= differ from the pinned upstream Org package;
  the upstream licence remains byte-identical. =make lint= passed again.
  The final =make vulncheck= also passed both application and unreplaced-upstream
  scans with no vulnerabilities reported.
- Coverage refresh :: Current native, block-placement, checkbox, preserved Org
  and legacy-import executions pass with converter instrumentation. Combined
  =set= profiles cover 87.4% of current =internal/orgconvert= statements.
  The focused placement additions passed under the race detector separately;
  the full historical suite is not being repeated for a coverage counter.
- Repeated-reference cross-check :: A direct Pandoc =org= to =html5= conversion
  of two references to one named, two-paragraph note emits two numbered
  definitions with separate reference/backlink pairs. The native adapter's
  corresponding output therefore retains that reader behaviour. The persistent
  HTML-contract and repeated-note lifecycle cases cover these relationships.

* Benchmark checkpoint, 20 September 2026

- Revisions :: Baseline =96a8a2ce165333045a377c38da044bb501461e4f=;
  corrected candidate =e171898=. Go =1.26.8=, Pandoc =3.9.0.2=,
  Darwin arm64. The live installation was not changed.
- Progress :: 75 retained final-build operations, plus 33 preliminary or
  interrupted operations, initially reached the agent-authored cap of 108.
  The script stopped there and OT011.1 was AMBER. The verification estimate was
  amended to allow necessary replacement measurements after remediation.
  The remaining 33 measurements then ran, giving 108 retained final operations
  and 33 preliminary/interrupted operations overall.
- Method :: Fresh disposable source copies and service contexts; one warm-up
  and five measured samples per completed row. GET and POST retain every
  normal conversion/proof pass. CLI timing ends at captured browser handoff;
  it includes the child CLI startup and capture overhead, excludes the browser
  and subsequent cleanup grace. No browser was opened.
- Source preservation :: Every retained execution checked unchanged source on
  GET/CLI or an exact original prefix with only the added native reference and
  appended definition on POST. Prior note text and counts were checked, and
  both saved UTF-8 paragraphs were present in the refreshed HTML.
- Semantic comparison :: The corrected large source has the same 347 headings
  and two references. It offers 953 verified blocks versus the baseline's 945;
  counts are descriptive, not a substitute for endpoint proof. Remaining main
  text differences are native recognition of valid verbatim markup and explicit
  diagnostics for two source heading links without an exact destination.
  The work ledger differs only by preserving literal underscores in one branch
  name. The specification's normalized main text matches the baseline.
- Conversion counts :: Large GET and POST/refresh use three converter calls.
  The work ledger GET uses two, POST/refresh three. The initial specification
  GET uses two. The CLI subprocess capture does not instrument converter calls;
  its raw counter value zero is not a claim of zero conversion work.
- Qualification :: Final GET medians are 8.36%, 8.48% and 10.32% of baseline
  for the large document, work ledger and specification respectively. All
  required warm-ups and five measured samples per operation/build/document
  are present. The large threshold and both smaller-document requirements pass.

| Document | Operation | Pandoc median seconds (range) | Native median seconds (range) |
|----------+-----------+-------------------------------+-------------------------------|
| Large Org | GET | 7.951 (7.891–7.968) | 0.665 (0.663–0.679) |
| Large Org | POST and refresh | 8.046 (8.041–8.106) | 0.793 (0.768–0.797) |
| Large Org | CLI handoff | 2.425 (2.398–2.457) | 0.311 (0.307–0.314) |
| Work ledger | GET | 0.558 (0.556–0.566) | 0.047 (0.047–0.050) |
| Work ledger | POST and refresh | 0.858 (0.849–0.872) | 0.074 (0.073–0.078) |
| Work ledger | CLI handoff | 0.454 (0.447–0.456) | 0.206 (0.205–0.211) |
| Specification | GET | 1.415 (1.341–1.662) | 0.146 (0.145–0.152) |
| Specification | POST and refresh | 1.959 (1.936–1.971) | 0.198 (0.195–0.201) |
| Specification | CLI handoff | 0.745 (0.735–0.798) | 0.224 (0.219–0.243) |

Frozen input SHA-256 identities and byte sizes:

| Corpus | Bytes | SHA-256 |
|--------+-------+---------|
| Authorized large Org | 531757 | 5a8cdaed11ed712c7a75d9e3281df0102eb4ee9864f797edfc400ef1a050adfb |
| Work ledger | 15063 | fce97c20afac69c1005e748ed467b7b3a3b828aa29f5d8c113f7d57ac3163ed1 |
| Reader/annotation specification | 108554 | 0b48eea39ce4fb4a4d5223f56e892ef1a6812fdc2cdcdff50a9545358fe59de7 |

Private raw corpus, normalized output and per-sample logs remain in the
OS-allocated temporary benchmark directory. Aggregate results contain no
document text. Process-level peak resident measurements are recorded separately;
they do not measure only the parser or establish a hard worker memory ceiling.

#+TITLE: W004 - Fold generated Org drawers
#+SUBTITLE: Unified defect correction specification
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil

* Fold generated Org drawers
:PROPERTIES:
:CUSTOM_ID: summary
:END:

- Outcome :: An enhanced Org preview *starts every generated Org drawer folded*.
- Before :: *Every generated Org drawer starts open*.
- After :: JavaScript *folds all generated Org drawers* while passive HTML remains
  fully readable.
- Changes :: The renderer identifies generated Org drawers; browser enhancement
  applies their initial closed state and retains existing reveal and print rules.
- Unchanged :: Source-authored HTML details retain their authored state; section
  controls, no-JavaScript output and document navigation remain unchanged.
- Edge cases :: Unheaded, empty, nested, LOGBOOK and named drawers, global modes,
  source marker-like HTML, printing and page-lifecycle teardown retain defined
  behaviour.
- Decisions :: The operator classified this as a *defect*. This amendment adopts
  Org's all-drawer startup model rather than a PROPERTIES-only exception.
- Next step :: Definition gate, then operator sign-off.

-----

* Context
:PROPERTIES:
:CUSTOM_ID: context
:END:

** Problem

Org uses drawers for metadata and secondary material that readers normally do
not need to see. The current renderer emits every generated Org drawer open and
the browser module does not distinguish them. That leaves drawer content expanded
in an enhanced preview, contrary to Org's normal drawer startup model.

** Scope

*** In scope

- Mark every generated Org drawer without trusting source-supplied HTML
  attributes.
- Close each marked drawer during browser enhancement's initial state.
- Preserve each marked drawer's normal native summary control, print
  expansion/restoration and page-lifecycle teardown.
- Add regression and user validation evidence for the defect.

*** Out of scope

- Changing any source document.
- Folding source-authored HTML =details= elements.
- Adding a user setting, a new browser harness or an Org parser dependency.

*** Unchanged

- Generated HTML with JavaScript disabled keeps every drawer open and readable.
- Existing section visibility, global outline controls, copied code and contents
  behaviour keep their current contracts.
- Existing non-drawer fragment revelation remains governed by W001/W002. Drawer
  text has no generated fragment mapping and is outside this defect correction.
- Existing =showall= and =VISIBILITY=all= directives retain their W001 meaning:
  they leave generated Org drawers open after enhancement.

** Authority and starting point

- Project profile :: [[file:../../.sdlc/project.yaml][=.sdlc/project.yaml=]].
- Product authority :: [[file:../../docs/VISION.md][Vision]], Org fidelity and
  direct document navigation.
- Architecture authority :: [[file:../../docs/ARCHITECTURE.md][Architecture]],
  browser folding responsibility.
- Historical requirement :: [[file:../001-local-document-preview/spec.org][W001 - Installable Markdown and Org previews]], which requires drawers to be
  open without JavaScript and enhancement to apply initial folding.
- Historical delivery :: [[file:../002-code-and-outline/spec.org][W002 - Code
  highlighting, copying and document navigation]], which owns enhanced folding
  and states its historical drawer-open rule. W004 *supersedes that rule for all
  generated Org drawers*; source-authored HTML details remain outside the rule.
- Org authority :: The current [[https://orgmode.org/manual/Drawers.html][Org
  drawer manual]] describes drawers as material readers do not normally see. Its
  [[https://orgmode.org/manual/In_002dbuffer-Settings.html][startup settings]]
  provide =hidedrawers= and =nohidedrawers= for all drawers. This 2026-09-10
  amendment supersedes W004's earlier PROPERTIES-only interpretation.
- Current implementation :: =internal/preview/org.go= emits every drawer as
  =details open=; =assets/web/preview.js= only opens drawer state.

** Constraints and dependencies

- Use existing Go Org pre-processing, owned browser JavaScript and native HTML
  details elements; introduce no runtime dependency.
- Treat source content as untrusted; only application-generated metadata may
  select initial drawer folding.
- Keep no-JavaScript content readable and preserve print accessibility.

** Decisions

- Every generated Org drawer begins closed only after JavaScript enhancement.
- Existing =showall= and =VISIBILITY=all= are higher-precedence source requests:
  their generated Org drawers remain open. In every other existing startup mode,
  the enhancement closes generated Org drawers after outline visibility is set.
- Org's =hidedrawers= and =nohidedrawers= keywords are editor-startup settings;
  this previewer does not support them as source directives.
- The native =summary= interaction and the existing global/section, fragment and
  print paths remain valid ways to reveal drawers; no additional control is added.
- The reserved application marker is =data-hp-org-drawer=. It is
  added only by the post-sanitization renderer from an Org pre-pass provenance
  record. Source HTML cannot create, retain or select this marker.

** Unresolved blockers

- None.

* Acceptance Criteria
:PROPERTIES:
:CUSTOM_ID: acceptance-criteria
:END:

** AC004.1 - Generated Org drawers fold after enhancement
:PROPERTIES:
:CUSTOM_ID: ac-004-1
:END:

- Criterion :: Given a generated Org drawer, the *enhanced preview starts that
  drawer closed*.
- Proven by :: [[#rt-004-1][RT004.1 - Marked Org drawers]] and
  [[#ut-004-1][UT004.1 - Browser drawer state]].

** AC004.2 - Source HTML details retain authored state
:PROPERTIES:
:CUSTOM_ID: ac-004-2
:END:

- Criterion :: Given a source-authored HTML =details= element, the *enhanced
  preview retains its authored open or closed state*.
- Proven by :: [[#rt-004-1][RT004.1 - Marked Org drawers]] and
  [[#ut-004-1][UT004.1 - Browser drawer state]].

** AC004.3 - Passive reading remains available
:PROPERTIES:
:CUSTOM_ID: ac-004-3
:END:

- Criterion :: Generated HTML *keeps all drawers open before JavaScript runs*.
- Proven by :: [[#rt-004-1][RT004.1 - Marked Org drawers]] and
  [[#ut-004-1][UT004.1 - Browser drawer state]].

** AC004.4 - Existing reveal and print state remains coherent
:PROPERTIES:
:CUSTOM_ID: ac-004-4
:END:

- Criterion :: Existing global/section and native-summary controls may reveal a
  marked drawer. Print *shows drawer content* and, afterwards, restores each
  marked drawer to its own immediately preceding open or closed state. Existing
  non-drawer fragment revelation remains unchanged; drawer-text fragments are
  not a supported destination.
- Proven by :: [[#ut-004-1][UT004.1 - Browser drawer state]].

* Test Definitions
:PROPERTIES:
:CUSTOM_ID: test-definitions
:END:

** RT004.1 - Marked Org drawers
:PROPERTIES:
:CUSTOM_ID: rt-004-1
:END:

- Covers :: [[#ac-004-1][AC004.1 - Generated Org drawers fold after
  enhancement]], [[#ac-004-2][AC004.2 - Source HTML details remain open]] and
  [[#ac-004-3][AC004.3 - Passive reading remains available]].
- Boundary :: The Org input through the generated HTML document.
- Setup :: Render one fixed Org fixture containing an unheaded PROPERTIES drawer;
  immediate and later PROPERTIES drawers; an immediate LOGBOOK; a named drawer;
  empty drawers; drawers beneath nested headings; a heading with no drawer; and
  raw HTML details, one authored open and one authored closed, carrying
  =data-hp-org-drawer= and a marker-like class. Render the same fixture with
  showall, overview and content startup metadata. Include a generated body target
  under an initially folded non-drawer heading for the existing fragment-revelation
  check.
- Action :: Render the document through the existing test command and inspect the
  generated DOM.
- Expected :: Every generated Org drawer, including unheaded, later, LOGBOOK,
  named, empty and nested-heading cases, carries exactly the owned
  =data-hp-org-drawer= marker. Source-supplied details lacks it. The generated
  Org drawers retain =open= in passive HTML across startup variants. The source
  details retain respectively its authored =open= attribute and its absence.
  Drawer text and raw source remain intact or pass through the established
  sanitization path.
- Retention :: Persistent regression suite under =make test=. Observe RED before
  production changes, then GREEN.

** UT004.1 - Browser drawer state
:PROPERTIES:
:CUSTOM_ID: ut-004-1
:END:

- Covers :: [[#ac-004-1][AC004.1 - Generated Org drawers fold after
  enhancement]], [[#ac-004-2][AC004.2 - Source HTML details remain open]],
  [[#ac-004-3][AC004.3 - Passive reading remains available]] and
  [[#ac-004-4][AC004.4 - Existing reveal and print state remains coherent]].
- Reviewer :: Operator.
- Procedure :: Open the provided synthetic Org fixture in the default browser
  with JavaScript enabled, then reload with JavaScript disabled. With JavaScript
  enabled, tab to the native summary, inspect its visible focus, use Space or
  Enter to open and close it, use existing section/global controls, print once
  while closed and once while open, and return from each print preview. Navigate
  to the fixture's generated non-drawer body target under its folded ancestor,
  then navigate away and back through the browser history to exercise
  pagehide/bfcache. Inspect every generated drawer and both source-supplied
  details after enhancement. Repeat for =showall= and =VISIBILITY=all= fixtures.
  With JavaScript disabled,
  inspect the drawer text and summary semantics.
- Expected :: Every generated drawer begins closed only when enhanced; each summary
  remains keyboard operable with visible focus; existing controls reveal content;
  print shows drawer text and restores respectively closed and open pre-print
  states; history restoration creates no duplicate controls and leaves passive
  drawers open. Each source-authored detail retains its authored open or closed
  state after enhancement. In =showall= and =VISIBILITY=all= fixtures, generated
  Org drawers remain open. The non-drawer fragment reveals its folded ancestor
  without changing drawer scope. No-JavaScript output remains open and readable.
- Record :: Write the dated result to =validation.org=.

* Edge Cases
:PROPERTIES:
:CUSTOM_ID: edge-cases
:END:

** Drawer origin and placement

- Condition :: The input has unheaded, empty, PROPERTIES, LOGBOOK and named Org
  drawers; drawers beneath nested headings; source marker-like HTML; or no drawer.
- Required behaviour :: Every generated Org drawer is marked and closed only in
  the ordinary enhanced preview. Source HTML details retain authored state.
- Covered by :: [[#rt-004-1][RT004.1 - Marked Org drawers]].

** Nested, global and print state

- Condition :: A marked drawer appears under a folded ancestor, global/section
  controls reveal it, or the reader prints while it is closed or open.
- Required behaviour :: Existing controls reveal content; print returns to the
  actual pre-print state. Drawer-internal fragment targets are out of scope
  because preserved drawer text has no generated fragment mapping.
- Covered by :: [[#ut-004-1][UT004.1 - Browser drawer state]].

* Solution Design
:PROPERTIES:
:CUSTOM_ID: solution-design
:END:

** Architecture context

- Components :: Org pre-processing owns source-to-HTML drawer recognition;
  browser enhancement owns interactive initial state.
- Existing pattern :: Org drawer HTML is application-generated and open in the
  passive document. The browser module already preserves print state and opens
  fragment ancestors.

** High-level design

*** Flow

1. The Org pre-pass recognizes every Org drawer and emits an owned marker on its
   details element.
2. The generated document retains =open= on all drawers for passive reading.
3. At enhancement start, browser JavaScript applies existing outline visibility,
   then closes owned marked drawers except those in a =showall= or
   =VISIBILITY=all= section. Existing global/section, print and teardown logic
   continues to operate on native details elements and preserves the pre-print
   state.

*** Interfaces and configuration

- Interface :: Internal =data-hp-org-drawer= marker only; no CLI
  option or public setting. The renderer adds it after sanitization from a
  provenance record, and browser code selects this exact attribute only.
- Compatibility :: Generated Org drawers change only after enhancement;
  source-authored details retain authored state. =hidedrawers= and
  =nohidedrawers= are unsupported source directives. Markers are removed or
  ignored safely by non-enhanced reading.

** Architecture impact

*** Security impact

- Threat boundary :: Source-derived drawer names and raw HTML stay untrusted;
  only Go emits the marker.
- Data and secrets :: No new data collection, storage or logging.
- Access :: No access or permission change.
- Conclusion :: Existing sanitization and browser-only execution boundary remain
  sufficient.

*** Other relevant qualities

- Accessibility :: Native summary interaction remains keyboard accessible; the
  no-JavaScript state stays readable.
- Portability :: Uses existing browser-standard details behaviour on supported
  macOS, Linux and WSL browsers.
- Maintainability :: A semantic marker avoids positional browser heuristics and
  keeps recognition alongside existing Org drawer handling.

* Delivery Handoff
:PROPERTIES:
:CUSTOM_ID: delivery-handoff
:END:

- Required reading :: =internal/preview/org.go=, =assets/web/preview.js=,
  =internal/preview/graph_test.go=, W001/W002 specifications and this document.
- First implementation step :: Write [[#rt-004-1][RT004.1 - Marked Org drawers]]
  and observe the missing marker before production changes.
- Definition evidence :: [[file:audits.yaml][W004 - Definition audit record]].
- Validation evidence :: [[file:validation.org][W004 - Generated Org drawer
  validation]].
- Documentation impact :: Update README and Architecture only if their drawer
  descriptions become inaccurate; otherwise record their review.

#+TITLE: W013 - UX fixes and improvements
#+SCHEMA: 8
#+SUBTITLE: Open paired-development record
#+STARTUP: overview
#+OPTIONS: num:nil toc:2
#+OPERATOR_SIGNOFF: Paired development authorized 2026-09-20; closure pending

* Synopsis
:PROPERTIES:
:CUSTOM_ID: summary
:END:
- Outcome :: Track *explicitly agreed UX changes* during paired development.
- Pending increment :: Give new Org and Markdown footnotes their *own configurable section*,
  separate from the document's final content section when folding in Emacs.
- Current increment :: Restore *annotation targets in wrapped descriptions and
  indented quotes*, including the corresponding description labels.
- State :: *Keep this ticket open*. Append subsequent authorized increments and
  their evidence here; no design audit is requested at this stage.

* Context and boundaries
- Authorities :: [[file:../../docs/VISION.md][Vision]],
  [[file:../../docs/ARCHITECTURE.md][Architecture]] and
  [[file:../../.sdlc/project.yaml][Project profile]].
- Baseline :: [[file:../009-reader-annotation-ux/spec.org][W009 - Reader and annotation UX]]
  defines native footnotes and source preservation.
  [[file:../../docs/ANNOTATIONS.md][Annotation guidance]] and
  [[file:../../docs/SERVICE.md][Service configuration]] describe current use.
- Mode :: *Paired development*, authorized on 20 September 2026. This skeleton
  records agreed behaviour, not a completed design or implementation.
- Scope control :: The title reserves space for later UX increments; it does
  *not authorize unspecified changes*. Record each agreed addition before coding.
- Preservation :: No migration or rearrangement of existing footnotes. Retain
  native reference IDs, definitions, attribution and unrelated source bytes.
- Separate correction :: The duplicate unplaced-footnote correction remains
  recorded under [[file:../011-conversion-performance/validation.org][W011 - Native Org preview validation]].

* Backlog: agreed Org and Markdown footnote section
** First footnote gets a configurable section
:PROPERTIES:
:CUSTOM_ID: ac-013-1
:END:
- Reserved requirement :: AC013.1 - Configurable section. *Pending implementation*;
  retained outside the active correction's acceptance/test inventory.
- Default :: When creating the *first footnote in an Org or Markdown document*,
  append a top-level heading and a *blank line* before the first definition.
  Use =* Footnotes & Annotations= in Org and =# Footnotes & Annotations= in Markdown.
- Configuration :: =annotations.section_title= defaults to
  =Footnotes & Annotations=. A custom string changes the title; an *empty string
  disables* automatic heading creation.
- Title syntax :: The configured value contains *title text only*, without
  =* = or =# =. The writer supplies the heading marker for the destination format.
- Repetition :: Later footnotes reuse the section without duplicating its heading.
- Existing documents :: Preserve existing footnote organization and headings.
- Boundary :: This increment concerns *Org and Markdown source organization*.
  The block-end insertion policy remains unchanged.

#+BEGIN_SRC yaml
annotations:
  section_title: "Footnotes & Annotations"
#+END_SRC

* Verification to retain
** RT013.2 - Wrapped descriptions and indented containers :testdef:
:PROPERTIES:
:CUSTOM_ID: rt-013-2
:END:
- Covers :: [[#ac-013-2][AC013.2 - Reachable block annotation targets]].
- Check :: Serve synthetic descriptions containing multiline verbatim/code and
  an indented quote. Select targets using the browser's nearest-block rules,
  save through HTTP, verify placement and unchanged source syntax, then repeat.
  Test the authorized personal document only through a disposable external copy.

** Planned section creation and source preservation
:PROPERTIES:
:CUSTOM_ID: rt-013-1
:END:
- Covers :: [[#ac-013-1][AC013.1 - Configurable section]].
- Reserved test :: RT013.1 - Section creation and source preservation. Not yet
  implemented or executed; activate with the configurable-section increment.
- Check :: In both Org and Markdown, default, custom and disabled configuration;
  native top-level heading syntax, a blank line before the definition, first and subsequent
  saves; existing native footnotes and section headings; unrelated source bytes.
- Boundary :: Configuration and authenticated annotation writes with synthetic
  documents in temporary storage. No browser automation or personal fixtures.
** Planned native folding and annotation workflow
:PROPERTIES:
:CUSTOM_ID: ut-013-1
:END:
- Covers :: [[#ac-013-1][AC013.1 - Configurable section]].
- Reserved test :: UT013.1 - Native folding and annotation workflow.
  Pending the configurable-section increment.
- Check :: Review generated Org and Markdown source and refreshed previews;
  inspect Org folding in Emacs and
  verify the section folds independently and annotations remain usable.

** UT013.2 - Description and quote interaction :testdef:
:PROPERTIES:
:CUSTOM_ID: ut-013-2
:END:
- Covers :: [[#ac-013-2][AC013.2 - Reachable block annotation targets]].
- Check :: In annotation mode click the description label, wrapped paragraph
  and indented quote. Verify pencil cursor, selected-block outline, successful
  save and a reference after the block. No automated desktop interaction.

* Increment ledger
- 2026-09-20 :: *Skeleton created*. Footnote-section behaviour recorded;
  implementation and validation pending. Additional UX work awaits explicit
  paired instructions. Design review and ticket closure are deferred.
- 2026-09-20 :: Prioritize *missing annotation targets* in wrapped descriptions
  and indented quotes before the pending configurable section increment.
- 2026-09-20 :: Extend the pending section heading to *Markdown as well as Org*,
  superseding the initial Org-only boundary. The shared configuration stores
  title text; writers supply the native top-level heading marker.

* Agreed correction: reachable annotation blocks
** AC013.2 - Reachable block annotation targets :ac:
:PROPERTIES:
:CUSTOM_ID: ac-013-2
:END:
- Wrapped descriptions :: Multiline inline verbatim/code must not prevent a
  rendered description paragraph from being an annotation target. Its label
  should reach the same verified description boundary.
- Indented containers :: Quotes inside list descriptions remain whole-block
  targets. Save the reference *after the closing delimiter*, preserving list
  indentation and surrounding items; subsequent references reuse that paragraph.
- Preservation :: Keep source/HTML correspondence checks and rejection of
  unverified targets. No private source content enters tracked fixtures.
- Baseline :: Restores the existing whole-block interaction described in
  [[file:../011-conversion-performance/spec.org][W011 - Native Org preview compatibility]].

* Current correction: implementation notes
- Inline probes :: A temporary line-end marker inside multiline verbatim/code
  is rendered literally. Remove only generated unparsed markers from the probe
  DOM before comparing it with the original. Such markers never authorize a
  write; only parsed reference hits at verified block ends can do so.
- Indented blocks :: Preserve closing-line indentation in the temporary probe
  and saved reference paragraph. Preserve the original following line endings
  so a new blank line does not split the enclosing list. The native converter's
  invisible probe prefix follows that indentation too.
- Labels :: A description term gets a distinct interactive ID referring to its
  first verified content boundary. Paragraph and label selection remain distinct;
  neither inserts a footnote into the description delimiter.
- Security :: Existing revision checks, shape/text correspondence, request
  authentication and source-preserving write checks remain active. Literal or
  otherwise unverified probe markers do not become client-writeable offsets.
- Evidence :: [[file:validation.org][Increment validation]]. Configurable-section
  work and retrospective design review remain pending; the ticket stays open.

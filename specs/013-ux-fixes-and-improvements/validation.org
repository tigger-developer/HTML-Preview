#+TITLE: W013 - UX fixes and improvements: validation
#+SCHEMA: 1
#+TODO: AMBER RED | GREEN
#+STARTUP: overview
#+OPTIONS: num:nil toc:2

* Current increment: annotation targets
** GREEN RT013.2 - Wrapped descriptions and indented containers :testdef:
:PROPERTIES:
:REVISION: 2a08374 plus the recorded wrapped-description and indented-block correction
:COMMAND: go test -race ./internal/annotation ./internal/orgconvert ./internal/preview -run 'TestHTTPWrappedDescriptions|TestHTTPAnnotationTargetContract|TestHTTPBlockAnnotations|TestHTTPBlockRejectsUnverifiedTargets|TestHTTPRepeatedBlockNotesAndHeadingAnchor|TestHTTPQuotedFootnotes|TestPreviewFootnotesOrgContainers' -count=1 -timeout=3m
:EVIDENCE: Affected served-page race pack passed in 141.288 seconds, including five new target/save cases, repeated block notes and rejection of unverified targets. Initial four-case reproduction was RED at 2a08374.
:END:

*** Observations and correction, 20 September 2026
- Reproduction :: Synthetic fixture =testdata/annotation-targets/wrapped-descriptions.org=.
  The initial served-HTML check failed for multiline verbatim, multiline code,
  description-label selection and the indented quote. Neighbouring ordinary
  list items and descriptions were retained as preservation assertions.
- Cause :: Line-end proof references inside multiline literal spans stayed as
  text in the probe DOM and invalidated its comparison. Indented Org blocks had
  no candidate boundary. Supporting them also required preserving indentation
  before the native converter's invisible probe prefix and avoiding an extra
  blank line that split the enclosing description list.
- Label handling :: A description term now reaches its content block's verified
  boundary through a distinct interactive ID. Selecting paragraph content does
  not highlight its label instead.
- Focused evidence :: The corrected served-page test passed before adding the
  separate quote-label case to the final affected pack. Saves append two notes
  to the correct boundary; removing only those additions recovers the original
  fixture bytes. No generated proof token appears in the served document.
- Private check :: A disposable copy of the authorized source passed selection
  checks for the reported label, wrapped paragraph and quote. An HTTP save to
  that copy returned 201 and placed its reference after the indented closing
  delimiter. The original file stayed byte-identical during the check; private
  source and scratch tests are outside the repository and audit evidence.
- Other checks :: Complete annotation and native converter race packages passed
  in 5.101 and 18.852 seconds respectively. =make lint= and a fresh production
  build passed. No browser automation or live-service restart was performed.

** AMBER UT013.2 - Description and quote interaction :testdef:
:PROPERTIES:
:REASON: Awaiting browser review of the corrected build; HTTP and DOM checks do not establish live pointer, focus or highlight behaviour.
:END:
- Report :: The original build allowed annotation through the last ordinary
  bullet, then did not offer the description label, wrapped paragraph or draft
  quote as targets. The screenshot and source inspection corroborated the
  affected structures.
- Next check :: Rebuild and restart the service, reopen the document, and test
  those three locations. Keep the ticket open for subsequent paired increments.

* Current increment: annotation input guards
** GREEN OT013.1 - Input-guard build and persistence compatibility :testdef:
:PROPERTIES:
:REVISION: 2d60e9f plus annotation-input.js, composer/panel integration and embedded bundle update
:EVIDENCE: JavaScript lint, production build, Go vet, existing HTTP limits test and full annotation writer race package passed on macOS arm64; served Org and Markdown pages returned 200 and their bundled module passed lint.
:END:

*** Run 2026-09-20
- Environment :: Go 1.26.8, oxlint 1.82.0 and Biome 2.5.12 on macOS arm64.
  Build caches, executable, service runtime and synthetic review documents are
  in OS-allocated temporary directories. No private document or installed service
  was changed; no browser automation was used.
- Persistence :: =go test -race ./internal/annotation -count=1 -timeout=2m=
  passed in 5.466 seconds. It includes native creation/editing, sidecars,
  attribution, trailing newlines, literal markup and rejected boundary breakouts.
- HTTP :: =go test -race ./internal/annotation ./internal/preview -run 'TestRT007_7_10_EventSchemaAndLimits|TestNative|TestEditable|TestEdit|TestFootnote' -count=1 -timeout=3m=
  passed the preview package in 6.740 seconds. The annotation package matched no
  tests in that initial selection, so the complete writer package was then run
  separately as recorded above. HTTP rejection checks retain original source bytes.
- Packaging :: =go build -o TEMP/htmlpreview ./cmd/htmlpreview= and
  =go vet ./internal/preview= passed. The isolated executable served Org and
  Markdown review pages with annotation targets. The emitted module was extracted
  with =htmlq= and passed =oxlint --deny-warnings=.
- JavaScript :: =oxlint --deny-warnings= on the three changed JS modules passed;
  =biome lint --only=correctness/noUnreachable= passed. Initial lint identified an
  unexported cross-module function; exporting it resolved the warning.
- Limits :: These are compatibility and packaging checks, not executed browser
  interaction coverage. No server limit, request contract, persistence format or
  dependency changed. The earlier block-target regression evidence remains
  applicable: this increment changes the editor, not target generation or writes.
- Org block boundary probe :: Six authenticated HTTP saves on separate synthetic
  sources tested =BEGIN_EXAMPLE=, =BEGIN_SRC text= and =BEGIN_QUOTE= containing two
  internal blank lines. All three first-line openers returned HTTP 409
  =invalid_footnote=, leaving the source byte-identical; the same bodies with a
  leading newline returned HTTP 201. This confirms the guard's first-line rule
  against the current public write boundary. The legacy intermediate serializer
  wraps literal text, but the final readable serializer and its reparse still
  enforce the native definition boundary.

** AMBER UT013.3 - Annotation length input guard :testdef:
:PROPERTIES:
:REASON: Awaiting user validation in the real browser; automated browser and CLI JavaScript execution are excluded by the project instructions.
:END:
- Prepared review :: Disposable Org and Markdown documents each contain an
  existing 3,999-code-point footnote. Test the next two typed characters,
  selection replacement, deletion, oversized paste, undo/redo and composition.
- Remaining evidence :: New-note and existing-note input handling, supplementary
  and combining characters, progressive reduction of legacy oversized notes,
  feedback accessibility, stable layout, autosave and reopening.

** AMBER UT013.4 - Org paragraph input guard :testdef:
:PROPERTIES:
:REASON: Awaiting user validation of textarea events, clipboard handling and feedback in Org, embedded Markdown and a read-only Markdown source with an Org sidecar.
:END:
- Prepared review :: The Org page includes copyable one-blank-line and
  two-blank-line samples, and links to Markdown and sidecar review documents.
- Remaining evidence :: Typing and paste rejection, whitespace-only lines and
  CRLF, preserved block content, selection, and successful save after rejection.

* Pending section-heading increment
- The reserved configurable-section requirement and its planned tests remain
  unimplemented. They are retained in [[file:spec.org][the skeleton backlog]],
  outside this correction's execution inventory. No success is claimed for them.

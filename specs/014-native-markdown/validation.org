#+TITLE: W014 - Native Markdown validation evidence
#+SCHEMA: 2
#+STARTUP: content
#+OPTIONS: toc:2 num:nil
#+TODO: AMBER RED | GREEN

* Automated regression

** RED RT014.1 - Public native routing :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-1-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Native CLI previews reject absent Pandoc with exit 1; served Markdown invokes Pandoc twice. Expected native output without these calls.
:END:

- Definition :: [[file:spec.org::#rt014-1][RT014.1 - Public native routing]].

** RED RT014.2 - Semantic render compatibility :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-2-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Fenced divs still become containers; unsupported native qualifiers return CLI 0 and HTTP 200/415 instead of 2/400. Invalid metadata can publish a page. Semantic heading and footnote adaptation preservation checks passed.
:END:

- Definition :: [[file:spec.org::#rt014-2][RT014.2 - Semantic render compatibility]].

** RED RT014.3 - HTML omission and warning delivery :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-3-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Inline/block raw HTML is retained and CLI/page omission notices are absent, including optional Markdown readers, service registration and refreshed pages.
:END:

- Definition :: [[file:spec.org::#rt014-3][RT014.3 - HTML omission and warning delivery]].

** RED RT014.4 - Clickable block creation :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-4-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Definition-list bodies and multi-paragraph quotations have no browser-selectable verified target; creation returns HTTP 409. Other isolated/mixed block creation and post-save second insertions pass; forged/stale targets are rejected without changing bytes.
:END:

- Definition :: [[file:spec.org::#rt014-4][RT014.4 - Clickable block creation]].

** GREEN RT014.5 - Footnote lifecycle :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-5-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Repeated references including a quote map to one stored note. Multi-paragraph edit, idempotent retry, stale conflict and deletion preserve unrelated source. Existing Org/Markdown ordinary-note, rendered-markup and read-only-sidecar regressions passed. This is preservation coverage: no artificial failure is introduced.
:END:

- Definition :: [[file:spec.org::#rt014-5][RT014.5 - Footnote lifecycle]].

** RED RT014.6 - Conditional dependency behaviour :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-6-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Native formats and input listing reject absent Pandoc; native service cannot start. Removing Go from the optional highlight catalogue incorrectly disables bundled Go highlighting. Failed catalogue reporting remains a passing preservation check.
:END:

- Definition :: [[file:spec.org::#rt014-6][RT014.6 - Conditional dependency behaviour]].

** RED RT014.7 - Failure and trust boundaries :testdef:
:PROPERTIES:
:CUSTOM_ID: rt014-7-result
:REVISION: 1d32001 - approved native Markdown baseline, plus test-only W014 working tree
:COMMAND: go test ./internal/preview -run 'TestRT014_|TestHTTPAnnotationTargetContract|TestCheckboxRendering|TestRT009_7_EditOrdinaryFootnotes|TestRT009_8_ReadOnlySidecar|TestRT009_8_EditableNotes|TestRT007_7_AnnotationHTTPAuthority|TestRT003_1_FormatContentsDefaults|TestRT011_7_StartedNative|TestRT011_1_ServedOrgAndMarkdown' -count=1 -v
:EVIDENCE: Markdown requests bypass the native-worker fault boundary and return HTTP 200 instead of conversion failure; cancellation/output-flood cases likewise return 200. Raw HTML remains in the passive-content fixture. Existing Org worker cancellation/reaping and annotation authority checks passed.
:END:

- Definition :: [[file:spec.org::#rt014-7][RT014.7 - Failure and trust boundaries]].

* User tests

** AMBER UT014.1 - Reading interface :testdef:
:PROPERTIES:
:CUSTOM_ID: ut014-1-result
:REASON: Pending user validation after implementation.
:END:

- Definition :: [[file:spec.org::#ut014-1][UT014.1 - Reading interface]].

** AMBER UT014.2 - Annotation workflow :testdef:
:PROPERTIES:
:CUSTOM_ID: ut014-2-result
:REASON: Pending user validation after implementation.
:END:

- Definition :: [[file:spec.org::#ut014-2][UT014.2 - Annotation workflow]].

* One-off tests

** AMBER OT014.1 - End-to-end comparison :testdef:
:PROPERTIES:
:CUSTOM_ID: ot014-1-result
:REASON: Pending candidate implementation.
:END:

- Definition :: [[file:spec.org::#ot014-1][OT014.1 - End-to-end comparison]].

** AMBER OT014.2 - Packaging and documentation :testdef:
:PROPERTIES:
:CUSTOM_ID: ot014-2-result
:REASON: Pending candidate implementation.
:END:

- Definition :: [[file:spec.org::#ot014-2][OT014.2 - Packaging and documentation]].

* Execution context

- Date :: 2026-09-20.
- Environment :: macOS arm64; Go 1.26.8; isolated temporary Go build/module caches.
- Method :: Regression groups executed in focused invocations before production edits.
  The COMMAND properties give the equivalent combined selector; the actual runs
  split the native package, preservation cases and final test additions.
- Preservation :: Existing Org/Markdown browser-target, checkbox, contents-default,
  read-only sidecar, ordinary-footnote and authorization regressions passed.
- Limits :: No visual tests, live installation, service restart or performance
  claims are represented by these regression results.

** Run 2026-09-20 - Extended native contract coverage

- Revision :: =e147668= - retained review evidence, plus the test-only working tree.
- Commands :: Focused =go test ./internal/preview -run ... -count=1 -v= groups
  cover the new metadata, copy, fragments, omission, resource, selector and
  repeated-note checks. Raw execution logs accompany the retained review inputs.
- Native routing :: Page load, advertised refresh, note creation and post-save
  rendering invoked Pandoc five times; expected zero.
- Semantics :: Native-only selector rejection fails with dependency exit 1,
  not invocation exit 2. Served qualifiers consult Pandoc and return 415,
  not 400. Metadata-only structured display is absent. Fenced-code copy and
  intra/cross-document fragment checks pass as preservation evidence.
- Notices :: Multi-region/multi-document and followed-link checks find no
  omission notice. Native HTML/plaintext without Pandoc fails at startup.
- Resources :: An out-of-root Markdown image receives no served asset URL.
- Footnotes :: Two successive paragraph notes retain their exact source boundary
  and two correctly labelled sidebar records. Existing repeated heading, code,
  table and task-note checks also pass.
- Optional formats :: The existing real DOCX mapping check passes with Pandoc.
  The unavailable-lexer check now uses JSX, whose mapped =javascriptreact=
  lexer is absent from the pinned Chroma registry; the old Pandoc-catalogue
  fault no longer models native lexer availability. The baseline omits the
  required literal-fallback notice.
- Browser isolation :: Regression CLI helpers intercept =Host.Execute= for the
  absolute system opener. Service helpers allocate private runtimes and ephemeral
  loopback ports. The separate exploratory timing executable is disabled after
  its PATH-only opener stub proved ineffective on macOS. No timing result from
  that interrupted run establishes the end-to-end comparison.

** Run 2026-09-20 - Baseline HTTP and CLI timing

- Revision :: =720e635= - pre-migration master; temporary build with the existing
  =htmlpreview_test_desktop= tag. The tag intercepts desktop handoff and captures
  the HTML through HTTP; no browser is launched.
- Method :: Seven measurements after one warm-up per corpus. Each GET uses a
  fresh registered source; each POST creates a native Markdown footnote in a
  fresh disposable file. CLI timing includes the capture adapter's HTTP GET.
- README :: 18,120 bytes; 8 headings, 64 writable targets, 0 prior note references.
  Median CLI 220.863 ms; uncached GET 208.553 ms; annotation POST 13.475 ms.
- Long synthetic document :: 183,780 bytes; 1,200 headings, 4,800 writable targets,
  0 prior note references. Median CLI 2,307.520 ms; uncached GET 2,285.629 ms;
  annotation POST 31.189 ms.
- Footnote corpus :: 17,320 bytes; 120 headings, 240 writable targets and 240
  prior note references. Median CLI 425.322 ms; uncached GET 412.462 ms;
  annotation POST 18.561 ms.
- Isolation :: Private temporary runtime, control socket and served directory;
  ephemeral loopback port. Service exited successfully and removed its temporary
  directory. The installed service remained running on its different port.
- Status :: Baseline evidence only. The candidate comparison remains pending;
  these measurements do not establish a performance improvement.

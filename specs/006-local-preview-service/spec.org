#+TITLE: W006 - Local preview service and automatic fallback
#+SCHEMA: 1
#+SUBTITLE: Unified change specification
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil
#+OPERATOR_SIGNOFF: Taḋg, 2026-09-12: full amended W006 design approved; explicit delivery instruction releases the prior implementation hold, after W008.

* Synopsis
:PROPERTIES:
:CUSTOM_ID: summary
:END:

** Outcome

- Readers *follow supported local document and code links on demand*, with
  single-document fallback when serving is unavailable or disallowed.

** Before

- The command *opens temporary file URLs*.
- Optional linked browsing *pre-converts a bounded graph* and occupies the
  terminal until interrupted.

** After

- An optional *per-user Go service serves HTTP only on 127.0.0.1*.
- Eligible inputs open *random-token/root-relative-path URLs* and the CLI returns.
- Without a service, or outside its configured roots, the command uses the
  existing file preview and quick/read lifetime, subject to explicit client-root
  restrictions.

** Changes

- *Configured roots, private discovery and token authorization* govern serving.
- *On-demand rendering, bounded caching and authorized assets* support browsing.
- *All W008-supported formats* work for direct inputs and linked targets.
- *Native HTML preserves passive authored appearance* and rewrites document links.
- Org heading/custom-ID searches resolve when their targets are visited.
- *Homebrew service support, a LaunchAgent and a Linux user service* are packaged
  for explicit operator activation; WSL retains Windows-browser handoff.

** Unchanged

- Sources remain *read-only* and the default browser opens previews.
- Converted pages retain source headers, fonts, navigation, metadata, drawers,
  highlighting and copying; native HTML retains its own layout.
- *No source scripts or automatic external requests* are permitted. Only native
  HTML gains bounded passive source styling through W008.
- *No remote bind option, TLS, implicit activation or directory listing* exists.

** Edge cases
:PROPERTIES:
:CUSTOM_ID: synopsis-edge-cases
:END:

- *Escaped paths, symlink escapes and outside-root links* cannot bypass grants.
- *Missing/ambiguous Org destinations* remain explained without disk indexing.
- *Conversion errors* remain errors; they do not trigger successful fallback.
- *Unavailable WSL loopback* selects file fallback without host-network changes.
- *Restarts revoke tokens*; cache eviction preserves authorized URLs.
- *Malformed format selectors and stale/foreign media* expose no denied bytes.

** Decisions

- The operator approved *HTTP, loopback, configured roots, random tokens and
  fallback* on 11 September 2026, including restart-scoped tokens and replacement
  of pre-generated browsing.
- The 12 September amendment adds *W008 format routing and passive native HTML*.
- The *11 September approval and hold remain history*. On 12 September Taḋg
  approved the amended design and explicitly released the hold through delivery.

** Next step

- *Check delivery admission*, then implement after W008 under
  [[#delivery-handoff][Delivery Handoff]]. Native qualification remains delivery work.

-----

* Context
:PROPERTIES:
:CUSTOM_ID: context
:END:

** Problem

- A reader opening a ledger wants to *follow its linked specifications* without
  manually invoking a converter for every file.
- The current graph mode *converts unvisited documents in advance*. Quick file
  cleanup prevents later navigation; foreground reading ties lifetime to the CLI.

** Outcome

- A *running local service renders only requested documents*. The CLI selects
  HTTP or file delivery without starting a service implicitly.
- A *configured directory allowlist limits served content*. An explicit command
  outside that allowlist still produces a local single-document preview.

** Scope

*** In scope

- *Service/client coordination, HTTP rendering, token URLs, local references,
  caching, failure handling and service packaging* on the existing platforms.
- *User-authored configuration for allowed roots* and operator-controlled
  activation, restart and stop instructions.

*** Out of scope

- *Remote serving, configurable bind addresses, TLS, tunnelling guidance,
  uploads, editing, live-reload watchers, whole-disk scanning and publication*.
- *Global Org-ID indexing*, fetching remote documents or resources, a Windows
  native binary, root daemons and changes to host firewall/WSL settings.
- *Closure of earlier work or remediation of its unrelated warnings/audits*.

*** Unchanged

- *Go and Pandoc remain the application stack*. Lua remains inside Pandoc;
  no command-line JavaScript or new browser test framework is introduced.
- *Both delivery paths use the same renderer* and the existing source-identity,
  presentation, copy, folding, print and passive-source semantics.

** Authority and starting point

- Project profile :: [[file:../../.sdlc/project.yaml][Project profile]] declares
  *no external infrastructure owner*. This is an optional local user service.
- Product authority :: [[file:../../docs/VISION.md][Vision]], reading experience,
  linked-document browsing and installation; its initial background-service
  exclusion is *superseded for this bounded local feature on sign-off*.
- Architecture authority :: [[file:../../docs/ARCHITECTURE.md][Architecture]],
  reference resolution, platform handoff and lifetime. File delivery remains;
  HTTP becomes an *additional transport with on-demand conversion*.
- Historical requirements :: [[file:../001-local-document-preview/spec.org][W001 - Local document preview]],
  especially AC001.9 - Source-relative links, AC001.23 - Actual heading targets,
  AC001.31 through AC001.36 - Graph budgets and lifetime, and AC001.37 through
  AC001.46 - Passive rendering and platforms. Graph discovery and its third
  mode are replaced; *source-relative meaning and file retention remain*.
- Presentation baseline :: [[file:../002-code-and-outline/spec.org][W002 - Code and outline]],
  [[file:../004-fold-initial-property-drawers/spec.org][W004 - Metadata and controls]],
  [[file:../005-navigation-and-metadata/spec.org][W005 - Navigation and metadata]]
  remain authorities for *rendered content*, not approval of this service.
- Current implementation :: At =df8bf96716680eaa549682be541dbc1524b34e1a=,
  [[file:../../internal/preview/session.go][Session coordination]],
  [[file:../../internal/preview/links.go][Reference resolution]],
  [[file:../../internal/preview/source.go][Source confinement]],
  [[file:../../internal/preview/platform.go][Desktop adapter]] and
  [[file:../../internal/preview/html.go][HTML policy]] implement *file delivery*.
- Regression baseline :: [[file:../../internal/preview/graph_test.go][Graph tests]],
  [[file:../../internal/preview/references_test.go][Reference tests]],
  [[file:../../internal/preview/lifecycle_test.go][Lifecycle tests]] and
  [[file:../../integration/cli_test.go][Installed CLI tests]] protect the affected
  boundaries. Their *graph-only expectations require explicit migration*;
  they are evidence, not authority to retain an extra public mode.
- External contracts :: [[https://docs.brew.sh/Formula-Cookbook#service-files][Homebrew service definitions]],
  [[https://learn.microsoft.com/en-us/windows/wsl/networking][WSL localhost networking]],
  [[https://orgmode.org/manual/Search-Options.html][Org file-search syntax]],
  [[https://pkg.go.dev/net/http][Go HTTP]], and
  [[https://pkg.go.dev/os#Root][Go rooted filesystem access]].

** Constraints and dependencies
:PROPERTIES:
:CUSTOM_ID: constraints-and-dependencies
:END:

- *W008's format pipeline must be delivered before W006 service integration is
  qualified*. [[file:../008-input-formats/spec.org][W008 - Input formats]] owns
  detection, reader selection, code/text wrappers, JSON display, binary resources
  and passive HTML. W006 owns HTTP delivery and linked navigation; W008 can be
  delivered in file mode without W006 or W007.
- *Bind only to literal IPv4 loopback, 127.0.0.1*. An OS-assigned port avoids
  collisions. Neither a flag, environment value nor config key can change the
  address. This operator requirement overrides the generic configurable-address
  advice in the Go standards; no IPv6 listener is required in this revision.
- *Use HTTP without certificates*. No host trust-store changes are required.
- *YAML configuration requires a maintained parser*, compiled into the binary;
  it does not add a runtime executable. Use the YAML organization's maintained
  Go package, pin a reviewed stable release at delivery, and record its licence
  and transitive dependencies. Do not hand-write a YAML parser.
- *All real service activation is operator-controlled*. Definition changes only
  specifications and documentation; packaging does not silently start a service.

** Decisions

- Template conversion, 12 September 2026 :: The active amended candidate adopts
  *template schema 1*, including Synopsis and harness-only audit state. This is
  part of the authorized definition update; prior approval remains recorded.
- Amendment, 12 September 2026 :: The operator requested *all Pandoc input formats
  and supported linked targets*, including W008 code/text and native HTML. They
  accepted ordinary JSON pretty printing with original-text copying, explicit
  specialist reader selection and passive authored HTML.
- Supersession :: This amendment replaces *Org/Markdown-only document admission*
  and the earlier download-only treatment of .txt, .csv, .json, .yaml, .yml,
  .html, .htm, .css and .js. These use W008's defined routes. The former list
  and prior approval remain recorded here as history, not current policy.
- Operator direction, 11 September 2026 :: *Prefer an existing service*, otherwise
  perform a single-file conversion. *Configured trees are the serving boundary*;
  sources outside them use the same fallback. HTTP and random tokens are approved.
- URL direction :: The operator explicitly permits
  =/<random-token>/<relative-path>=. *Relative names may appear after the token*;
  full filename concealment is no longer required. A filename hash is not used
  as an access credential and the port is not treated as authentication.
- Candidate lifetime :: *Tokens survive cache eviction but expire at restart*.
  There is no idle timer, tab tracking or persisted token restoration. This is
  a proposed default for sign-off, not an independently confirmed user answer.
- Candidate compatibility :: *Two transports replace graph mode*. Legacy graph
  settings are validated and diagnosed as described below. This proposal is
  explicit so delivery need not guess how old scripts should behave.
- Definition mode :: *ATTENDED*. The two candidate choices above were presented
  as clarification questions; the final sign-off must cover them if unanswered.

** Unresolved blockers

- *No unresolved product question remains*. Full amended-design approval and
  explicit delivery authority were supplied on 12 September 2026.
- Sign-off resolution :: On *11 September 2026*, Taḋg approved the design after
  clarification of its asset restrictions: "makes sense. design is approved but
  don't implement yet". This resolves the sign-off requirement above, including
  the lifetime and migration proposals. Implementation was deferred pending a
  subsequent explicit instruction. That approval predates the format amendment.
- Renewed authority :: On *12 September 2026*, Taḋg said "ok, 006 007 008
  approved", then "$deliver-change 008 006 007". This approves the amended
  format contract and *releases the implementation hold*. Current definition
  evidence in =audits.yaml= and the W008 dependency still govern admission.

* Acceptance Criteria
:PROPERTIES:
:CUSTOM_ID: acceptance-criteria
:END:

** AC006.1 - Eligible inputs use the available service
:PROPERTIES:
:CUSTOM_ID: ac-006-1
:END:
- Criterion :: Given a compatible, reachable service and an allowed source,
  the CLI *opens its token-prefixed HTTP URL in the default browser and returns*,
  without pre-rendering linked documents or retaining a foreground reading job.
- Proven by :: [[#rt-006-1][RT006.1 - Automatic transport selection]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.2 - Unavailable or excluded sources use single-file delivery
:PROPERTIES:
:CUSTOM_ID: ac-006-2
:END:
- Criterion :: For an otherwise valid explicit input, *absent/unreachable service,
  absent/empty service roots or exclusion by service roots select file delivery*.
  An explicit client =HTMLPREVIEW_ROOT= is an input restriction checked first;
  exclusion by that restriction is an error under AC006.17 - Failure precedence.
- Proven by :: [[#rt-006-1][RT006.1 - Automatic transport selection]],
  [[#rt-006-2][RT006.2 - Configuration and compatibility]].

** AC006.3 - HTTP never listens outside loopback
:PROPERTIES:
:CUSTOM_ID: ac-006-3
:END:
- Criterion :: Every service HTTP listener *binds only to 127.0.0.1*, including
  installed service launches. No public setting can change this invariant.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.4 - Tokens and roots authorize each served request
:PROPERTIES:
:CUSTOM_ID: ac-006-4
:END:
- Criterion :: *Only a valid capability for an allowed root* can reveal that
  root's permitted content. Absent or forged credentials reveal no source data.
  Credential scope includes its immutable rendering settings.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#rt-006-4][RT006.4 - Rooted path access]].

** AC006.5 - Requested documents render on demand
:PROPERTIES:
:CUSTOM_ID: ac-006-5
:END:
- Criterion :: An eligible W008-supported request *returns its preview at the original
  source extension*, using conversion or passive native HTML as appropriate. Following a new local link renders that target
  when requested. Cycles and repeated names retain the correct source context.
- Proven by :: [[#rt-006-5][RT006.5 - Lazy mixed-document navigation]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.6 - Relative references preserve their source meaning
:PROPERTIES:
:CUSTOM_ID: ac-006-6
:END:
- Criterion :: *Links and image references resolve from their containing source*,
  including spaces, Unicode, literal delimiters, subdirectories and parent
  paths. Allowed cross-root links use the target root's capability. Outside-root
  references are *inactive with an explanation*, never an HTTP escape or
  server-triggered file preview. External web/mail links remain unchanged.
- Proven by :: [[#rt-006-5][RT006.5 - Lazy mixed-document navigation]],
  [[#rt-006-4][RT006.4 - Rooted path access]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.7 - Org destinations remain meaningful
:PROPERTIES:
:CUSTOM_ID: ac-006-7
:END:
- Criterion :: *Unique custom-ID and heading searches reach the target's actual
  generated anchor*. Ambiguous, unknown and unsupported searches show the target
  with an explanation. Fileless IDs resolve only when unique among known rendered
  pages; unknown IDs remain explained and inactive without filesystem scanning.
- Proven by :: [[#rt-006-6][RT006.6 - Org destination resolution]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.8 - Passive presentation survives HTTP delivery
:PROPERTIES:
:CUSTOM_ID: ac-006-8
:END:
- Criterion :: *Both transports preserve the inherited presentation contract*
  for converted pages, enumerated under RT006.7 - HTTP content and assets, with
  original logical source identity. Native HTML preserves its own layout under
  W008's passive HTML contract, without injected application chrome.
- Proven by :: [[#rt-006-7][RT006.7 - HTTP content and assets]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.9 - Refresh and concurrent requests remain bounded
:PROPERTIES:
:CUSTOM_ID: ac-006-9
:END:
- Criterion :: *Refresh reflects the current readable source snapshot*, including
  changes that leave the file size and modification time unchanged.
- Proven by :: [[#rt-006-8][RT006.8 - Cache, concurrency and resource limits]].

** AC006.10 - Service lifecycle preserves ownership
:PROPERTIES:
:CUSTOM_ID: ac-006-10
:END:
- Criterion :: *Only one service owns a runtime directory at a time*; a second
  instance cannot replace its active control socket or instance lock.
- Proven by :: [[#rt-006-9][RT006.9 - Lifecycle and discovery]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.11 - Diagnostics protect tokens and explain failures
:PROPERTIES:
:CUSTOM_ID: ac-006-11
:END:
- Criterion :: *Routine logs exclude tokens, URL paths and document bodies*.
  CLI stdout may print the requested preview URL as deliberate user output.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#rt-006-7][RT006.7 - HTTP content and assets]],
  [[#rt-006-9][RT006.9 - Lifecycle and discovery]].

** AC006.12 - Service installation remains optional and per-user
:PROPERTIES:
:CUSTOM_ID: ac-006-12
:END:
- Criterion :: *Each distribution contains its specified user-service artefacts*
  with fixed installed executable arguments, the empty-root example and licence
  notices. The packaging layout below defines each destination.
- Proven by :: [[#rt-006-10][RT006.10 - Packaged service artefacts]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]],
  [[#ut-006-2][UT006.2 - Service setup and fallback workflow]].

** AC006.13 - Legacy options have a documented migration
:PROPERTIES:
:CUSTOM_ID: ac-006-13
:END:
- Criterion :: *Legacy graph settings never enable pre-generation*. Valid old
  values receive one migration notice per invocation; malformed values retain
  status 2, as defined in the migration contract.
- Proven by :: [[#rt-006-1][RT006.1 - Automatic transport selection]],
  [[#rt-006-2][RT006.2 - Configuration and compatibility]].

** AC006.14 - Served handles remain within the authorized filesystem
:PROPERTIES:
:CUSTOM_ID: ac-006-14
:END:
- Criterion :: *Every served document or asset is a regular file within its
  effective root and filesystem*. Traversal, symlink replacement and special
  files cannot disclose outside-root bytes.
- Proven by :: [[#rt-006-4][RT006.4 - Rooted path access]].

** AC006.15 - Browser requests cannot create access grants
:PROPERTIES:
:CUSTOM_ID: ac-006-15
:END:
- Criterion :: *Only same-user private control requests can register
  previews*. HTTP browser requests cannot create credentials or alter allowed
  roots.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#rt-006-9][RT006.9 - Lifecycle and discovery]].

** AC006.16 - Configuration is strict and service-authoritative
:PROPERTIES:
:CUSTOM_ID: ac-006-16
:END:
- Criterion :: *Configuration follows the stated schema and validation rules*;
  invalid input cannot grant access. The running service's root snapshot
  remains authoritative until restart, regardless of later client config
  edits.
- Proven by :: [[#rt-006-2][RT006.2 - Configuration and compatibility]].

** AC006.17 - Failure precedence prevents accidental fallback
:PROPERTIES:
:CUSTOM_ID: ac-006-17
:END:
- Criterion :: *Invalid input/configuration, client-root exclusion,
  permission, protocol and conversion failures remain errors*. They never
  select successful file fallback. Only service absence/unreachability or
  service-root exclusion selects fallback after client input validation
  succeeds.
- Proven by :: [[#rt-006-1][RT006.1 - Automatic transport selection]],
  [[#rt-006-2][RT006.2 - Configuration and compatibility]].

** AC006.18 - Foreign HTTP authorities and peers are refused
:PROPERTIES:
:CUSTOM_ID: ac-006-18
:END:
- Criterion :: *Requests violating the stated peer, Host, Origin or fetch-site
  policy receive 403 without source data*, even with a valid token.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.19 - Source content remains passive
:PROPERTIES:
:CUSTOM_ID: ac-006-19
:END:
- Criterion :: *HTTP delivery retains the inherited passive-content policy*:
  source scripts/includes cannot execute or expand, and source-directed remote
  resources do not load automatically.
- Proven by :: [[#rt-006-7][RT006.7 - HTTP content and assets]],
  [[#ut-006-1][UT006.1 - Linked reading across platforms]].

** AC006.20 - Registered assets follow the exact content policy
:PROPERTIES:
:CUSTOM_ID: ac-006-20
:END:
- Criterion :: *Only registered, authorized assets are served*, with the
  raster allowlist, signature checks, download disposition and mismatch
  outcomes in the asset contract below. Source active content never receives
  an executable same-origin response.
- Proven by :: [[#rt-006-7][RT006.7 - HTTP content and assets]],
  [[#rt-006-4][RT006.4 - Rooted path access]].

** AC006.21 - Request work respects finite limits
:PROPERTIES:
:CUSTOM_ID: ac-006-21
:END:
- Criterion :: *Connections, conversions, bytes, state and deadlines stay
  within their stated inclusive limits*. Overflow receives the stated failure
  without partial successful HTML or cross-request source/settings
  contamination.
- Proven by :: [[#rt-006-8][RT006.8 - Cache, concurrency and resource limits]].

** AC006.22 - Service restart revokes prior credentials
:PROPERTIES:
:CUSTOM_ID: ac-006-22
:END:
- Criterion :: *Tokens from a previous service instance cannot serve content*
  after restart, and stale discovery cannot establish protocol compatibility.
- Proven by :: [[#rt-006-9][RT006.9 - Lifecycle and discovery]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.23 - Shutdown cleans only owned state
:PROPERTIES:
:CUSTOM_ID: ac-006-23
:END:
- Criterion :: *Normal shutdown cancels and reaps converters and removes only
  owned temporary state*. Cleanup failure reports remaining owned paths;
  unrelated files and another service's socket remain unchanged.
- Proven by :: [[#rt-006-9][RT006.9 - Lifecycle and discovery]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.24 - HTTP responses suppress credential propagation
:PROPERTIES:
:CUSTOM_ID: ac-006-24
:END:
- Criterion :: *Every HTTP response carries the stated no-referrer, no-store,
  nosniff and frame-ancestor protections*, including errors and redirects.
- Proven by :: [[#rt-006-7][RT006.7 - HTTP content and assets]].

** AC006.25 - Public responses follow the specified protocol
:PROPERTIES:
:CUSTOM_ID: ac-006-25
:END:
- Criterion :: *Methods, routes, status codes and content types follow the
  declared contract*, including HEAD, health, reserved paths and Org
  redirects.
- Proven by :: [[#rt-006-3][RT006.3 - Network and capability boundary]],
  [[#rt-006-6][RT006.6 - Org destination resolution]],
  [[#rt-006-7][RT006.7 - HTTP content and assets]].

** AC006.26 - Service activation is explicit and unprivileged
:PROPERTIES:
:CUSTOM_ID: ac-006-26
:END:
- Criterion :: *Installation does not activate serving or grant roots*.
  Supported activation runs as the invoking user without a privileged daemon,
  and stop/rollback follow the specified manager contract.
- Proven by :: [[#rt-006-10][RT006.10 - Packaged service artefacts]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]],
  [[#ut-006-2][UT006.2 - Service setup and fallback workflow]].

** AC006.27 - Installation preserves user configuration
:PROPERTIES:
:CUSTOM_ID: ac-006-27
:END:
- Criterion :: *Repeated package/source installation preserves active user
  configuration and service-manager state*. Only packaged example/template
  artefacts may be replaced as part of their managed installation.
- Proven by :: [[#rt-006-10][RT006.10 - Packaged service artefacts]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

** AC006.28 - Mixed batches retain identity and file lifetime
:PROPERTIES:
:CUSTOM_ID: ac-006-28
:END:
- Criterion :: *Mixed HTTP/file batches retain each source's identity and
  presentation settings*, existing partial-input exit semantics, and file
  quick/read retention. An HTTP-only invocation never waits for file
  retention.
- Proven by :: [[#rt-006-1][RT006.1 - Automatic transport selection]].

** AC006.29 - Packaged guidance explains the two-mode contract
:PROPERTIES:
:CUSTOM_ID: ac-006-29
:END:
- Criterion :: *Packaged help and documentation accurately describe selection,
  root precedence, activation, rollback and legacy-option migration*, with no
  claim of remote serving or unverified platform qualification.
- Proven by :: [[#ot-006-2][OT006.2 - Documentation and compatibility review]],
  [[#ut-006-2][UT006.2 - Service setup and fallback workflow]].

** AC006.30 - Cache eviction preserves authorized URLs
:PROPERTIES:
:CUSTOM_ID: ac-006-30
:END:
- Criterion :: *Evicting cached content does not revoke its live capability*;
  a subsequent authorized request re-renders the source through the same URL.
- Proven by :: [[#rt-006-8][RT006.8 - Cache, concurrency and resource limits]].

** AC006.31 - All supported input kinds use the same service route
:PROPERTIES:
:CUSTOM_ID: ac-006-31
:END:

- Criterion :: *Every W008-supported input kind* can be registered for HTTP
  preview, including an explicit installed Pandoc reader. Service unavailability
  or a service-root exclusion retains that format in the single-file fallback.
- Proven by :: [[#rt-006-11][RT006.11 - Cross-format service navigation]].

** AC006.32 - Supported linked targets render on demand
:PROPERTIES:
:CUSTOM_ID: ac-006-32
:END:

- Criterion :: Following a supported *relative document anchor in any rendered
  format, including native HTML*, produces the target preview. Ordinary JSON
  displays formatted code; an explicit htmlpreview-format=json selector renders
  a Pandoc AST document. The target never inherits its parent's selected reader.
- Proven by :: [[#rt-006-11][RT006.11 - Cross-format service navigation]],
  [[#ut-006-3][UT006.3 - Cross-format browser reading]].

** AC006.33 - Format selection never expands authorization
:PROPERTIES:
:CUSTOM_ID: ac-006-33
:END:

- Criterion :: *Reader selection cannot create a root or asset grant, run code,
  bypass confinement or change another target's interpretation*. Malformed,
  duplicate or unsupported selectors are rejected under the protocol below.
- Proven by :: [[#rt-006-11][RT006.11 - Cross-format service navigation]].

** AC006.34 - Native HTML and extracted media obey the passive contract
:PROPERTIES:
:CUSTOM_ID: ac-006-34
:END:

- Criterion :: *Native HTML preserves permitted appearance and rewritten links*
  without scripts, form submissions or automatic external requests. Container
  media is served only through its parent document's authorization and revision;
  it cannot expose temporary paths or become an arbitrary asset endpoint.
- Proven by :: [[#rt-006-12][RT006.12 - Native HTML and container media]],
  [[#ut-006-3][UT006.3 - Cross-format browser reading]].

* Test Definitions
:PROPERTIES:
:CUSTOM_ID: test-definitions
:END:

** RT006.1 - Automatic transport selection
:PROPERTIES:
:CUSTOM_ID: rt-006-1
:END:
- Covers :: [[#ac-006-1][AC006.1 - Eligible inputs]],
  [[#ac-006-2][AC006.2 - File fallback]], [[#ac-006-13][AC006.13 - Migration]],
  [[#ac-006-17][AC006.17 - Failure precedence prevents accidental fallback]],
  [[#ac-006-28][AC006.28 - Mixed batches retain identity and file lifetime]].
- Boundary :: *Installed executable*, real local service and existing desktop
  capture adapter; fixture sources and runtime/config paths use OS temp storage.
- Action :: Open nested Org/Markdown with service ready, stopped and unreachable;
  inside/outside roots; mix both transports, duplicate inputs and one bad input.
  Include incompatible protocol, registration failure and failed browser handoff.
- Expected :: *Correct scheme, source, setting snapshot and exit status*; HTTP
  CLI returns after handoff, file output retains configured grace/read lifetime.
  No child service is started; graph links are not pre-rendered. All publication
  preparation precedes browser opening, preserving batch failure semantics.
- Intended RED :: Baseline rejects =--serve= and returns file URLs instead of the required HTTP entry.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.2 - Configuration and compatibility
:PROPERTIES:
:CUSTOM_ID: rt-006-2
:END:
- Covers :: [[#ac-006-2][AC006.2 - File fallback]],
  [[#ac-006-13][AC006.13 - Migration]],
  [[#ac-006-16][AC006.16 - Configuration is strict and service-authoritative]],
  [[#ac-006-17][AC006.17 - Failure precedence prevents accidental fallback]].
- Boundary :: *CLI startup and service registration*.
- Action :: Exercise absent/empty/default/explicit config; invalid YAML, duplicate
  and unknown keys, wrong types, over-limit root lists, relative/missing/file
  roots, duplicate/overlapping roots, bad permissions, stale client config and
  configured restart. Cover legacy options and TOC values at both bounds.
- Expected :: *Strict documented validation and precedence*, no implicit root
  grant, canonical duplicate handling and no work on malformed settings.
  Service-authoritative roots prevail; a CLI root override can only narrow them.
  Help/version bypass config and discovery with no side effects.
- Intended RED :: Baseline rejects the new config/runtime settings or cannot validate their YAML contract.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.3 - Network and capability boundary
:PROPERTIES:
:CUSTOM_ID: rt-006-3
:END:
- Covers :: [[#ac-006-3][AC006.3 - Loopback]],
  [[#ac-006-4][AC006.4 - Authorization]], [[#ac-006-11][AC006.11 - Diagnostics]],
  [[#ac-006-15][AC006.15 - Browser requests cannot create access grants]],
  [[#ac-006-18][AC006.18 - Foreign HTTP authorities and peers are refused]],
  [[#ac-006-25][AC006.25 - Public responses follow the specified protocol]].
- Boundary :: *Real listener and HTTP request/response*, plus the existing host
  seam only for peer-address cases unavailable on the test host.
- Action :: Inspect the actual bound socket address; send absent/invalid tokens,
  filename hashes, root listings, forged Host/Origin, cross-site fetch metadata,
  unsupported methods and attempts to register a root over TCP. Try invalid
  bind settings and capture stdout/stderr for successful and rejected requests.
- Expected :: *Loopback binding and specific rejection codes*, no credential or
  source disclosure, no wildcard CORS, and no HTTP route for token creation.
  Separate capabilities cannot substitute roots or settings. At the returned
  URL boundary, assert *43 unpadded base64url characters decoding to 32 bytes*;
  different contexts and restarted instances return distinct tokens, and a
  changed token is rejected. Entropy-source review belongs to the implementation
  audit; these wire checks do not claim statistical proof of randomness.
  Exercise GET/HEAD health responses, tokenless data requests, unsupported
  methods and health payload fields; health never lists roots or grants.
- Intended RED :: Baseline creates no service listener/control endpoint; the required authorized HTTP response is absent.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.4 - Rooted path access
:PROPERTIES:
:CUSTOM_ID: rt-006-4
:END:
- Covers :: [[#ac-006-4][AC006.4 - Authorization]],
  [[#ac-006-6][AC006.6 - Relative references]],
  [[#ac-006-14][AC006.14 - Served handles remain within the authorized filesystem]],
  [[#ac-006-20][AC006.20 - Registered assets follow the exact content policy]].
- Boundary :: *HTTP requests against synthetic rooted filesystems*.
- Action :: Cover literal/encoded/double-encoded traversal, separators, absolute
  paths, NUL, invalid UTF-8, spaces, Irish text, percent/hash/question marks,
  sibling-prefix directories, symlinked roots and targets, symlink replacement
  during reads, devices/FIFOs/directories and disallowed filesystem boundaries.
- Expected :: *Only authorized regular-file bytes are returned*; outside sentinel
  bytes never appear. Both documents and assets receive the same containment
  checks. No directory listing or redirect leaks a token/path outside its root.
- Intended RED :: Baseline cannot start the rooted HTTP route; the authorized control case fails before exercising its denial cases.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.5 - Lazy mixed-document navigation
:PROPERTIES:
:CUSTOM_ID: rt-006-5
:END:
- Covers :: [[#ac-006-5][AC006.5 - Lazy rendering]],
  [[#ac-006-6][AC006.6 - Relative references]].
- Boundary :: *CLI entry followed by actual returned HTTP anchors* and real Pandoc.
- Setup :: Nested Org/Markdown graph with cycles, repeated basenames, aliases,
  cross-root links, literal link lookalikes and an unvisited malformed target.
- Action :: Request the entry, then create/change the linked target before
  following its generated URL; follow back links, queries and fragments.
- Expected :: *Target content reflects its first request*, the bad unvisited
  document does not fail the entry, and every fetched page preserves its source
  context. Literal code is not interpreted as links; external links stay exact.
- Intended RED :: Baseline returns a file preview instead of a lazy HTTP link whose target is rendered on first request.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.6 - Org destination resolution
:PROPERTIES:
:CUSTOM_ID: rt-006-6
:END:
- Covers :: [[#ac-006-7][AC006.7 - Org destinations]],
  [[#ac-006-25][AC006.25 - Public responses follow the specified protocol]].
- Boundary :: *Returned links and HTTP redirects* against real converted Org.
- Action :: Follow plain files, custom-ID searches, headings with TODO/tags,
  duplicate headings/IDs, unsupported searches, same-page fragments and fileless
  IDs before/after their target has entered the bounded rendered catalogue.
  Change a target and repeat after cache eviction.
  Exercise *GET and HEAD* for =_org= lookup and =_file= escape routes, source
  documents/directories named =_org= and =_file=, literal percent/query characters,
  duplicate/missing selector parameters and an over-limit selector.
- Expected :: *Actual unique anchors or an explicit explanation*; one bounded
  redirect with no eager graph scan. Unresolved IDs never guess a file. No stale
  catalogue entry bypasses validation when a target changes or disappears.
- Intended RED :: Baseline has no deferred Org lookup route or HTTP redirect to the target catalogue.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.7 - HTTP content and assets
:PROPERTIES:
:CUSTOM_ID: rt-006-7
:END:
- Covers :: [[#ac-006-8][AC006.8 - Presentation]],
  [[#ac-006-11][AC006.11 - Diagnostics]],
  [[#ac-006-19][AC006.19 - Source content remains passive]],
  [[#ac-006-20][AC006.20 - Registered assets follow the exact content policy]],
  [[#ac-006-24][AC006.24 - HTTP responses suppress credential propagation]],
  [[#ac-006-25][AC006.25 - Public responses follow the specified protocol]].
- Boundary :: *HTTP headers, parsed rendered DOM and returned asset bytes*.
- Action :: Request Org/Markdown and referenced rasters, PDF, text, HTML and SVG;
  cover every allowlisted extension/MIME/signature, truncated and mismatched
  signatures, unregistered asset paths, disallowed extensions and CR/LF/Unicode
  download names. Include malicious scripts/attributes, remote images, source-authored service
  URLs, includes, attachment type spoofing and unsupported raw reference forms.
- Expected :: *The inherited invariants below hold*, local rasters load, attachments
  have download disposition, native HTML remains script-free and SVG remains a download,
  owned CSP and privacy headers are present on successes and errors. No source
  content, permissions or unrelated files change. HTTP pages contain no file URLs
  for denied resources and no source-originated automatic network access.
  HEAD matches the corresponding GET status/representation headers and has
  no body, including document, media, raster, download, lookup, health and error routes.
- Inherited DOM invariants :: *Original logical source identity*, all six embedded
  font faces/notices, passive Markdown structure and Org task/tag/drawer values
  (W001); literal code bytes, highlighting spans, unique heading IDs and TOC
  targets (W002); independent title/subtitle/author/date and one semantic H1 for
  titled Org, one leading frontmatter panel and drawer ownership markers (W004);
  format-labelled nav and property rows without repeated delimiters (W005).
  Check TOC default-on/depth-three and explicit overrides in both formats.
- Inherited browser invariants :: UT006.1 - Linked reading verifies W002 code/path
  copy and fragment revelation, W004 title/bar folding and initially closed
  drawers, and W005 responsive nav/tag/metadata styling. Print, keyboard,
  no-JavaScript reading and unavailable-clipboard fallback remain human checks.
- Intended RED :: Baseline has no HTTP document/asset response with the required content and headers.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.8 - Cache, concurrency and resource limits
:PROPERTIES:
:CUSTOM_ID: rt-006-8
:END:
- Covers :: [[#ac-006-9][AC006.9 - Refresh and bounded work]],
  [[#ac-006-21][AC006.21 - Request work respects finite limits]],
  [[#ac-006-30][AC006.30 - Cache eviction preserves authorized URLs]].
- Boundary :: *Concurrent real HTTP clients*, real Pandoc and controlled process
  boundary doubles for deterministic stalls/output floods.
- Action :: Refresh unchanged/edited/replaced/deleted files; change same-size
  content with restored mtime. Evict cached pages; make duplicate and distinct
  requests with different settings. Exercise exact/over byte, count, path,
  request, token and timeout limits; abort clients during conversion.
- Expected :: *Current bytes or the specified error*, no mixed settings,
  bounded workers/cache/scratch, duplicate in-flight work coalesced, and no
  leaked converters. An evicted page re-renders through its existing URL.
- Intended RED :: Baseline has no long-running HTTP cache/worker boundary; concurrent request setup fails.
- Retention :: Persistent under =make test - race enabled by the target=.

** RT006.9 - Lifecycle and discovery
:PROPERTIES:
:CUSTOM_ID: rt-006-9
:END:
- Covers :: [[#ac-006-10][AC006.10 - Lifecycle]],
  [[#ac-006-11][AC006.11 - Diagnostics]],
  [[#ac-006-15][AC006.15 - Browser requests cannot create access grants]],
  [[#ac-006-22][AC006.22 - Service restart revokes prior credentials]],
  [[#ac-006-23][AC006.23 - Shutdown cleans only owned state]].
- Boundary :: *Service executable, private control socket and HTTP listener*.
- Action :: Start twice with one runtime path; try wrong-owner/symlinked runtime
  state, stale sockets, occupied ports and malformed discovery replies. Stop
  during idle and conversion, restart, reuse old tokens, and inject cleanup error.
- Expected :: *One owner, bounded shutdown, stale-token refusal and safe fallback
  only for unavailable discovery*. Private state remains inaccessible to other
  users; no unrelated socket/file removal. Logs identify operation and error
  class without bearer credentials or source bodies.
- Intended RED :: Baseline rejects service startup and supplies no private discovery or restart-scoped credential.
- Retention :: Persistent under =make test=; *TDD before production changes*.

** RT006.10 - Packaged service artefacts
:PROPERTIES:
:CUSTOM_ID: rt-006-10
:END:
- Covers :: [[#ac-006-12][AC006.12 - Optional installation]],
  [[#ac-006-26][AC006.26 - Service activation is explicit and unprivileged]],
  [[#ac-006-27][AC006.27 - Installation preserves user configuration]].
- Boundary :: *Existing staged install/release commands and archive contents*.
- Action :: Install twice under temporary DESTDIR/prefix and generate each
  supported archive/formula. Assert the exact archive and installed paths in
  Packaging layout and manager contracts. Parse the generated plist/XML, unit
  and YAML example;
  invoke staged executable help. Preserve a pre-existing configuration sentinel.
- Expected :: *Fixed absolute executable arguments, user-service scope, declared
  Pandoc and licences*, safe staging/repetition, no configuration overwrite and
  no service activation. Generated formula service resolves its installed binary.
  Assert launchd Label/ProgramArguments/KeepAlive and systemd ExecStart/Restart/
  WantedBy against the specified values; DESTDIR never appears in their runtime
  arguments. Existing config and active user-service sentinels remain unchanged.
- Intended RED :: Baseline generated archives omit the named service templates/example and formula service support.
- Retention :: Extend existing Go integration suite; *no runtime service manager
  or Homebrew invocation in persistent tests*.

** UT006.1 - Linked reading across platforms
:PROPERTIES:
:CUSTOM_ID: ut-006-1
:END:
- Covers :: [[#ac-006-5][AC006.5 - On-demand document rendering]],
  [[#ac-006-1][AC006.1 - HTTP opening]],
  [[#ac-006-6][AC006.6 - Relative references]], [[#ac-006-7][AC006.7 - Org destinations]],
  [[#ac-006-8][AC006.8 - Presentation]],
  [[#ac-006-19][AC006.19 - Source content remains passive]].
- Reviewer :: Operator, on the existing W001 platform matrix: macOS/Safari,
  Ubuntu/Firefox, Windows/Edge with WSL1 and WSL2 including WSLg-present handoff.
- Procedure :: Open nested synthetic documents from another cwd through an
  installed command; follow Org/Markdown links, parent paths, cross-root links,
  folded anchors, images and downloads. Use Back, refresh after editing, copy,
  keyboard navigation, narrow/wide light/dark, print and JavaScript-disabled views.
- Expected :: *Correct content and source header, usable navigation and unchanged
  presentation*. No browser warning, unintended remote fetch or Linux-browser
  substitution in WSL. Denied links explain their boundary.
- Record :: *Exact platform/browser/Pandoc versions and dated human result* in
  [[file:validation.org][W006 - Service validation]]. Missing platforms stay pending.

** UT006.2 - Service setup and fallback workflow
:PROPERTIES:
:CUSTOM_ID: ut-006-2
:END:
- Covers :: [[#ac-006-12][AC006.12 - Optional installation]],
  [[#ac-006-26][AC006.26 - Service activation is explicit and unprivileged]],
  [[#ac-006-29][AC006.29 - Packaged guidance explains the two-mode contract]].
- Reviewer :: Operator on macOS and Linux/WSL.
- Procedure :: Follow packaged guidance to configure roots and explicitly start,
  stop and restart the user service. Open eligible/excluded files and try a stale
  browser URL after restart. Check the migration notice from old graph settings.
- Expected :: *Clear activation and fallback*, usable diagnostics and no need
  for elevated privileges, host configuration changes or a foreground CLI.
- Record :: Dated result and environment in [[file:validation.org][W006 - Service validation]].

** OT006.1 - Native service and network qualification
:PROPERTIES:
:CUSTOM_ID: ot-006-1
:END:
- Covers :: [[#ac-006-3][AC006.3 - Loopback]],
  [[#ac-006-10][AC006.10 - Lifecycle]], [[#ac-006-12][AC006.12 - Installation]],
  [[#ac-006-18][AC006.18 - Foreign HTTP authorities and peers are refused]],
  [[#ac-006-22][AC006.22 - Service restart revokes prior credentials]],
  [[#ac-006-23][AC006.23 - Shutdown cleans only owned state]],
  [[#ac-006-26][AC006.26 - Service activation is explicit and unprivileged]],
  [[#ac-006-27][AC006.27 - Installation preserves user configuration]].
- Boundary :: *Actual launchd/Homebrew and systemd user activation*, plus native
  Windows-to-WSL loopback access; synthetic documentation roots only.
- Procedure :: Use a staged candidate and installed user-service recipe; inspect
  actual listeners, reject host-interface access, verify default-browser HTTP
  handoff and stop/restart behaviour. On WSL, verify Windows loopback access and
  absence of LAN exposure without changing networking. Also test service absence
  and an unavailable Windows-facing endpoint selecting file fallback.
- Bounds :: At most *two start/stop cycles per installation route per platform*,
  20 local requests per cycle, five-second individual probe deadlines, 15 minutes
  per platform. No remote/metered service; activation requires the operator's
  explicit authority for the host, with its prior service state restored.
- Expected :: *Loopback-only listeners and correct user ownership*, no implicit
  boot/root activation, clean supported shutdown and truthful fallback.
- Record :: Revision, native commands, outcomes and cleanup in
  [[file:validation.org][W006 - Service validation]]. Never add to CI or =make test=.

** OT006.2 - Documentation and compatibility review
:PROPERTIES:
:CUSTOM_ID: ot-006-2
:END:
- Covers :: [[#ac-006-29][AC006.29 - Packaged two-mode guidance]].
- Boundary :: *Packaged help, README, Vision, Architecture and service guide*.
- Procedure :: One candidate review checks two-mode selection, config paths,
  root grants, temporary tokens, graph-option migration, WSL limitations,
  activation/stop instructions and qualification claims; sanitize edited prose.
- Expected :: *Commands match the candidate interface*, histories identify
  superseded behaviour, and no tunnel, remote bind or certificate workaround is
  recommended. No public package publication is claimed.
- Bounds :: One review per final candidate; *no new persistent prose tests*.
- Record :: Dated result in [[file:validation.org][W006 - Service validation]].

** RT006.11 - Cross-format service navigation
:PROPERTIES:
:CUSTOM_ID: rt-006-11
:END:

- Covers :: [[#ac-006-31][AC006.31 - Service format admission]],
  [[#ac-006-32][AC006.32 - Linked formats]],
  [[#ac-006-33][AC006.33 - Selection confinement]].
- Boundary :: *CLI registration and actual returned HTTP anchors*, real Pandoc.
- Setup :: Reuse W008's valid fixture for every installed reader. Link a synthetic
  Org/Markdown/HTML entry to those fixtures using natural mapped suffixes or an
  explicit selector. Include code, .txt, ordinary JSON, Pandoc AST JSON, cycles,
  cross-root links, an unreadable target and an unvisited malformed container.
- Action :: Register inputs and follow returned anchors. Repeat registration
  with unavailable service/outside service roots and explicit --from. Test
  duplicate/empty/invalid/unknown selectors, reader paths and query encoding,
  escaped filenames, fragments and a format-contract mismatch.
- Expected :: *On-demand preview and source identity match W008*, no eager
  conversion, no parent-reader inheritance, no download substitution for known
  formats. Authorized alternate readers produce distinct cache results.
  Existing bounds, denied-root behaviour and fallback precedence remain.
- Intended RED :: The current application has no HTTP registration/rendering;
  the original W006 definition also omitted broad format/selector routing.
- Retention :: Persistent under =make test=; *TDD before service implementation*.

** RT006.12 - Native HTML and container media
:PROPERTIES:
:CUSTOM_ID: rt-006-12
:END:

- Covers :: [[#ac-006-34][AC006.34 - Passive native HTML and owned media]].
- Boundary :: *Returned HTTP DOM, CSP, asset bytes and cache revalidation*.
- Setup :: W008 native-HTML hostile fixture with in-root stylesheet/raster and
  document anchors; DOCX/ODT/EPUB with embedded rasters, duplicate media names,
  rejected SVG and references outside the configured root.
- Action :: Follow HTML document links; fetch extracted media with GET/HEAD,
  guessed IDs, other document/token ownership and stale revisions. Change the
  source and the inlined local stylesheet separately; repeat requests.
- Expected :: *Passive native appearance, correct links and fresh permitted
  assets*. No raw script endpoint or conversion-side network fetch; stylesheet
  changes invalidate output. No stale/foreign media is served. Existing resource
  and response limits apply before publication; denied assets expose no bytes.
- Intended RED :: No native HTML service representation or scoped media route
  exists on the current implementation.
- Retention :: Persistent under =make test=; native browser checks are UT006.3.

** UT006.3 - Cross-format browser reading
:PROPERTIES:
:CUSTOM_ID: ut-006-3
:END:

- Covers :: [[#ac-006-32][AC006.32 - Linked formats]],
  [[#ac-006-34][AC006.34 - Passive native HTML and owned media]].
- Reviewer :: Operator.
- Procedure :: On the same four environments as UT006.1, follow Org/Markdown
  links to DOCX, ODT, code, .txt, ordinary JSON and authored HTML. From HTML,
  follow relative document links and a specialist Pandoc JSON AST selector.
  Exercise Back/refresh, fragments, keyboard and JavaScript-disabled navigation.
  Inspect the browser network panel on the hostile HTML fixture.
- Expected :: *Readable target previews, original source identity and permitted
  HTML styling*, without source script execution, form submissions or automatic
  external requests. The WSL browser remains the Windows default.
- Record :: [[file:validation.org][validation.org]], per native environment.

* Edge Cases
:PROPERTIES:
:CUSTOM_ID: edge-cases
:END:

** Multiple roots and aliases
- Condition :: *Sibling roots, overlapping roots or a symlink alias* change how
  a relative path is interpreted.
- Required behaviour :: Select the longest containing configured canonical root;
  keep the logical parent separately. Canonical and logical context must both
  remain within the effective grant. Cross-root links receive another authorized
  token; never reinterpret =../= as permission to escape the current token.
- Covered by :: [[#ac-006-4][AC006.4 - Authorization]],
  [[#rt-006-4][RT006.4 - Rooted path access]].

** Missing and denied linked files
- Condition :: A *linked file is missing, unreadable, outside a root or too large*.
- Required behaviour :: The entry still renders. Outside-root links are inactive;
  allowed missing links lead to a bounded 404, unreadable files to 403 and size
  violations to 413. HTTP never opens an OS application or runs the CLI itself.
- Covered by :: [[#ac-006-6][AC006.6 - Relative references]],
  [[#rt-006-5][RT006.5 - Lazy mixed-document navigation]].

** Configuration and service replacement
- Condition :: Roots are changed while a service runs, or a second binary uses
  the same runtime directory.
- Required behaviour :: *Restart applies configuration and revokes old tokens*.
  The active service's root snapshot is authoritative. A protocol mismatch is
  diagnosed; a missing/refused/timed-out socket means unavailable service.
- Covered by :: [[#ac-006-2][AC006.2 - Fallback]],
  [[#rt-006-9][RT006.9 - Lifecycle and discovery]].

** WSL loopback is unavailable
- Condition :: The Linux service is healthy but Windows cannot reach the same
  authenticated service instance through loopback.
- Required behaviour :: A *bounded Windows-side probe selects file fallback*
  and explains why. No wildcard bind, IP-address bypass, firewall modification,
  alternate browser or networking reconfiguration is attempted.
- Covered by :: [[#ac-006-2][AC006.2 - Fallback]],
  [[#ot-006-1][OT006.1 - Native service and network qualification]].

* Solution Design
:PROPERTIES:
:CUSTOM_ID: solution-design
:END:

** Architecture context

- Components :: *Extract rendering from file-session publication*. Preserve
  source snapshots, Org preservation, Pandoc, destination catalogue, sanitizer
  and final template. Give URL rewriting explicit file and HTTP resolvers.
- Ownership :: *One binary owns the CLI, service and protocol*. launchd/systemd
  supervise it; they do not implement conversion or access decisions.
- Replacement :: *Lazy HTTP replaces breadth-first pre-generation*. The file
  path remains a single-document renderer. No source scanning is needed to start
  the service or create a capability.

** High-level design

*** Flow

1. The operator *starts the optional service explicitly*. It validates its config,
   Pandoc, runtime directory and exclusive instance lock before reporting ready.
2. The CLI validates inputs/settings and contacts the *private Unix-domain
   control socket*. Discovery/registration has a five-second overall deadline.
3. The service checks allowed roots and captures rendering settings, returning
   *authorized HTTP URLs or explicit outside-root results*. It does not render.
4. The CLI verifies eligible entry responses before opening any batch input;
   this *preflights explicit inputs only*. Prepare fallback file entries in the
   same batch, then use the existing platform opener. Failed individual inputs
   may coexist with successful inputs; publication failure opens none.
5. HTTP requests *validate token, root, path and file handle*, obtain a bounded
   current snapshot and render/cache the response. References receive token
   routes without converting their target documents.
6. *CLI return does not stop the service*. SIGINT/SIGTERM or the user-service
   manager ends it, invalidating capabilities and cleaning owned scratch data.

*** Interfaces and configuration

- Commands :: Preserve =htmlpreview FILE [FILE ...]= and add
  =htmlpreview --serve= as a sole operation. =--= still permits a literal
  filename beginning with a dash. Service mode opens no browser and requires
  no desktop session. No =open= command or file association is installed.
- Configuration file :: =HTMLPREVIEW_CONFIG= selects an absolute YAML path;
  unset/empty uses =os.UserConfigDir()/htmlpreview/config.yaml=. The initial
  schema is the following, with *no directory grants by default*:

#+begin_src yaml
version: 1
serve:
  roots: []
#+end_src

- Root schema :: Up to *32 existing absolute directory paths*, at most 4096
  UTF-8 bytes each; no tilde/environment expansion. Canonical duplicates collapse;
  overlaps use longest containment. A missing default config means empty roots;
  an explicitly selected missing file is an error. Maximum config size is 64 KiB.
  Reject duplicate/unknown fields, wrong types, aliases/tags and extra documents.
  Reject config not owned by the current user or writable by group/others.
- Configuration lifetime :: *Read once per service start*. Missing/empty roots
  allow an idle service but serve no documents. Invalid config exits 2 before
  listening. The CLI validates a present config but does not use a changed file
  to override the running service's snapshot. Restart is the reload mechanism.
- Runtime path :: =HTMLPREVIEW_RUNTIME_DIR= selects an absolute private directory;
  default =os.UserCacheDir()/htmlpreview/runtime=. Reject symlinks, other owners
  and group/other permissions instead of chmodding existing paths. New runtime
  directories are 0700. Long Unix-socket paths fail with an actionable diagnostic.
- Discovery :: =control.sock= is a *0600 Unix-domain socket* inside that directory,
  guarded by an exclusive process-held lock. Authenticate peer UID as the service
  user as well as checking directory ownership. No TCP registration endpoint,
  PID scan, port scan, service autostart or environment-proxy use is permitted.
- Control protocol :: HTTP over that Unix socket, *version 1*, with JSON bodies.
  =GET /v1/status= returns ={protocol:1,instance,origin,ready,format_contract:1,input_formats:[...]}=;
  input_formats contains the installed reader names. =instance= is a
  non-secret random instance ID and =origin= is the bound loopback HTTP origin.
  =POST /v1/previews= accepts ={paths:[absolute logical paths],settings:{...},from:optional-reader,format_contract:1}=
  with up to the existing MAX_FILES limit, a 64 KiB request body and 256 KiB
  response. The optional from value uses W008's validated reader grammar and
  applies only to explicit registered inputs. Reject unknown/duplicate fields
  and wrong types. The settings object
  contains only toc, toc_depth, source/output byte limits, deadline and optional
  narrowing root; arbitrary environment or Pandoc arguments are rejected.
- Registration response :: HTTP 200 returns ={protocol:1,results:[...]}= in input
  order, each with =path= and =url= or a stable =error= code. Codes include
  =outside_root=, =invalid_source= and =capacity_exceeded=. Only outside_root
  selects per-file fallback. Invalid request is 400, version conflict 409,
  excessive body 413 and service unavailable 503. Error bodies are bounded JSON
  ={error:code}=; clients do not parse prose. No automatic POST retry occurs.
- Public routes :: *GET and HEAD only*. =/<token>/<root-relative-path>= returns
  document HTML or registered assets. =/_health= returns only protocol/instance
  identity for readiness/WSL verification; no roots, tokens or filenames. Other
  methods return 405; unknown routes/tokens return a generic 404 without listing.
  HEAD performs the same authorization, validation and rendering/cache lookup
  as GET, returning *the same status and representation headers with no body*.
  Health returns 200 application/json with exactly protocol, instance and ready
  when ready, otherwise 503; it contains no source or capability metadata.
- URL handling :: *Percent-encode path segments exactly once*. Separate query
  and fragment before filesystem resolution; reject malformed encoding, decoded
  separators, NUL, invalid UTF-8, absolute paths and decoded dot segments.
  Renderer-generated parent links are normalized into a root-relative route
  before output. Ordinary source query strings remain inert except the reserved
  htmlpreview-format key defined below; fragments retain their anchor meaning.
- Org lookup route :: A file-search link uses
  =/<token>/_org?path=<encoded-root-relative-path>&search=<encoded-selector>=.
  The reserved route is processed before content paths; a source named =_org=
  is accessed through =/<token>/_file/<encoded-path>=, also reserved for collisions.
  Resolve the target only on request, then send 303 to its actual anchor when
  unique. Otherwise serve target HTML with an escaped explanation, status 200.
  Search and path are single values; duplicates/oversized selectors return 400.
- Existing settings :: *TOC defaults and overrides remain unchanged* and are
  snapshotted per capability. Source and output limits/deadlines cannot exceed
  the service ceilings below. =HTMLPREVIEW_ROOT=, if supplied, further narrows
  the configured trees and retains its existing explicit-input admission check;
  it can never add a served tree.
- Root precedence :: *Validate the client input restriction first*. For example,
  client ROOT=/project/docs rejects /project/notes.org with status 1 even if the
  service serves /project. With no client ROOT, service roots containing only
  /project/docs cause /project/notes.org to use file fallback instead. When an
  explicit client ROOT permits the input, intersect it with configured service
  roots for HTTP grants; service exclusion still selects fallback. The client
  restriction applies independently of service availability. State both cases
  in help, RT006.1 - Transport selection and RT006.2 - Configuration tests.
- Migration :: =HTMLPREVIEW_LINKS= and =HTMLPREVIEW_MAX_DEPTH= remain accepted
  with their old validation ranges but *have no graph effect*, with one concise
  deprecation notice when non-empty. =HTMLPREVIEW_MODE= continues quick/read
  retention for file fallback; it never starts/stops the service. MAX_FILES and
  total-source/output-byte budgets still apply to explicit batch preparation;
  HTTP requests then use per-request limits. Preserve validation of inactive
  settings. Document these intentional replacements before release.
- Fallback classification :: *Missing/refused/timed-out control endpoint* means
  unavailable service. Unsafe runtime permissions, malformed replies, different
  protocol, invalid config and registered conversion failures are errors.
  A disappearing HTTP service before handoff permits one fresh availability
  check; confirmed unavailability selects file fallback. No endless retries.
- CLI output :: *Explicit preview URLs remain stdout output*. Treat these as
  deliberate bearer disclosure to the invoking user, not log events. Diagnostics
  may identify the user's explicit source path but never include token URLs;
  service logs contain only operation, status, duration and a safe request ID.

*** Asset contract

- Registration :: *Only a rewritten local anchor or image in an authorized
  rendered document registers an asset*. Registration records capability,
  normalized logical path and usage (image or download), after checking the
  target's root. A guessed HTTP asset path without this registration returns
  404. Asset access repeats regular-file, root, filesystem and 10 MiB checks.
- Inline image allowlist :: Case-insensitive =.png=, =.jpg=, =.jpeg=, =.gif=,
  =.webp= and =.avif= with matching signatures. Responses use image/png,
  image/jpeg, image/gif, image/webp or image/avif respectively, always nosniff.
- Signature rules :: PNG requires its eight-byte magic; JPEG starts FF D8 FF;
  GIF starts GIF87a or GIF89a; WebP starts RIFF and has WEBP at bytes 8-11.
  AVIF requires a well-formed initial ISO-BMFF ftyp box containing avif or avis
  as major/compatible brand, with declared box size between 16 and 4096 bytes
  and fully present in the bounded header. Do not infer type from source-supplied
  metadata or accept HTML/SVG merely labelled as a raster.
- Signature mismatch :: An image-route request whose extension and signature
  disagree returns *415 with no file bytes*. Truncated signature data fails the
  same way. The browser retains the image's alternative text; a failed linked
  image does not invalidate the containing document's successful response.
- Download allowlist :: Registered anchor targets ending in =.pdf= or =.svg=
  are *downloads only* unless an explicitly selected installed reader accepts
  that target under W008. Without a supported document route, use application/octet-stream, nosniff and Content-Disposition:
  attachment. Use safe quoted ASCII filename plus RFC 5987 filename* encoding
  for Unicode; reject CR/LF rather than allowing header injection. No content
  sniffing promotes these files to inline executable content.
- Other references :: Raster anchors use the verified inline-image policy;
  W008-supported anchors use their selected document/code/text/native HTML route. Unsupported image/download
  extensions are *inactive with an explanation* during rewriting, and direct
  registered requests return 415. Directory references never list contents.
  An allowed extension does not bypass registration, containment or file-size
  limits. This explicit HTTP subset leaves file-preview references unchanged.

*** Format routing and extracted resources
:PROPERTIES:
:CUSTOM_ID: format-routing
:END:

- Shared contract :: *W008 classifies each original source independently*.
  Its reader/language/native HTML policy, binary-safe snapshots and bounded
  media extraction apply before publication. The service owns the installed
  reader catalogue; the client must not assume another Pandoc installation
  has matching capabilities.
- Compatibility :: The private status and registration protocol require
  *format_contract:1*. An absent/different contract returns 409 and a restart/
  upgrade diagnostic, not fallback. No service implementation precedes this
  amendment, so this extends the proposed version-1 schema without a migration
  of deployed state. Reader names are bounded to 256 entries of 128 bytes each;
  discovery exceeding the existing control-response limit fails startup.
- Selector :: Exactly one =htmlpreview-format= query value uses W008's validated
  reader grammar, maximum 256 UTF-8 bytes after URL decoding. Decode once using
  URL-query rules; a literal plus in reader extensions must be encoded as %2B.
  Empty/duplicate/invalid values return 400, an unavailable reader or unsupported
  reader extension returns 415, and invalid document data returns 422.
  Preserve unrelated query parameters and fragments, but never forward any
  query as Pandoc options or a remote fetch instruction.
- Entry selection :: A registered explicit reader is encoded in the returned
  entry URL using that selector; it is not made the token's default reader.
  Cache keys include the resolved reader/extensions or source language.
  Reserved Org searches carry an optional validated format selector through
  the lookup URL and its redirect; do not reinterpret genuine Org search syntax
  as a filesystem path or propagate the parent reader to the target.
- Source links :: Rewrite document anchors *before returning HTML*, so navigation
  works with JavaScript disabled. Source-authored service URLs remain untrusted.
  Resolve local links relative to the original document (or admitted HTML base);
  use existing configured-root and cross-root rules. The reserved selector
  changes representation only, never authority or allowed trees.
- HTML styling :: Inline only *sanitized authorized local CSS*, resolving its
  raster URLs from the stylesheet's original directory under existing root and
  same-filesystem checks. Do not serve raw stylesheets from a generic endpoint.
  Track at most 256 inlined resource dependencies per page, within 8192 total
  registered assets. Hash and reauthorize dependencies before cache reuse.
- Media route :: =/<token>/_media/<document-id>/<source-revision>/<asset-id>=
  returns an admitted embedded raster from a registered converted document.
  IDs are opaque service-owned identifiers, never disk/extraction paths.
  Registration binds token, logical source, resolved format, source SHA-256
  and media ID. Reauthorize/re-read the parent source on GET/HEAD; a changed or
  unavailable parent revokes old media (404). Guessing/unregistered IDs returns
  404; signature/type failure returns 415. Paths beginning with _media use
  the existing _file escape route for real source-name collisions.
- Media lifetime :: Cache admitted bytes within *existing cache and scratch
  ceilings*. If evicted while the parent revision is unchanged, a request may
  reconvert under the same two-worker/deadline budgets. Stable media IDs derive
  from the parent conversion and bounded media content, so that URL remains
  usable after cache eviction. No route reads arbitrary scratch files.
- Original kind :: *Generated Org wrappers are not Org source documents*.
  They and non-Org/Markdown readers remain outside W007's annotation-store scope.

*** Capabilities and bounded state

- Capabilities :: Generate *32 random bytes with crypto/rand*, encoded as
  unpadded base64url (43 characters). This is a chosen 256-bit secret, not a
  filename hash, encryption scheme or configurable entropy policy. Tokens map
  in memory to canonical root, logical context and immutable rendering settings.
  Reuse a token for equivalent root/context/settings within one service lifetime.
- Scope :: A token grants *previewing regular W008-supported files in its root*,
  not listing or arbitrary raw-file retrieval. Non-document assets must first
  be registered by an authorized rendered document reference. A cross-root link
  may mint/reuse a separate token only for another configured, permitted root.
- Lifetime :: *Restart destroys all capabilities*. Cache eviction does not.
  At 128 distinct capability contexts, reject new contexts with capacity_exceeded;
  retain existing tokens. A service restart is the explicit reset operation.
- Reads :: Preserve *logical source location separately from canonical identity*.
  Use rooted file handles and revalidate regular-file type, containment and the
  configured root filesystem before each read. Do not admit symlink escapes,
  sockets/FIFOs/devices or implicit mount crossings. Separate configured roots
  can explicitly authorize different filesystems. Handle-safe confinement must
  survive a symlink rename between checking and opening.
- Cache :: Cache *rendered document plus its destination catalogue*, keyed by
  capability, logical path, SHA-256 of the bounded current source snapshot and
  application asset revision, resolved reader/extensions or source language and
  format-contract revision. Revalidate inlined stylesheet dependencies and media
  ownership before reusing output. Re-read/hash on every request before reuse;
  mtime alone is insufficient. Assets are read through authorization each time.
  Evict least-recently-used entries within 50 pages and 100 MiB of cached bytes.
- Org IDs :: *Fileless ID lookup covers only the current bounded catalogue* of
  rendered pages under the same effective roots/settings. It never enumerates
  directories. Revalidate matching candidates and catalogue freshness before
  issuing a redirect; absent/ambiguous IDs produce an explained inactive label.
- Budgets :: At most *two Pandoc conversions concurrently*, with no unbounded
  work queue; excess distinct requests return 503 with Retry-After: 1. Duplicate
  in-flight conversions for the same snapshot/settings share a result. Each
  conversion has a maximum 60-second deadline, 10 MiB source and 50 MiB output
  including embedded assets, and the existing Pandoc 512 MiB heap limit.
- Aggregate bounds :: *128 active HTTP connections*, 16 KiB headers, 16 KiB URL,
  4096-byte filesystem path and Org selector, 8192 registered assets and 100 MiB
  total owned scratch output. An asset is at most 10 MiB and streamed from its
  verified handle. Control requests have five-second deadlines. HTTP header/idle
  timeouts are 5/30 seconds; total response lifetime is 70 seconds. Over-capacity
  registration/request is explicit, never an eviction of live authorization.
- Scratch :: *Each conversion owns an OS-temporary private directory*. Cleanup
  follows conversion cancellation or result publication; the bounded cache is
  in memory. Runtime lock/socket are removed only by their owning instance.
  Abrupt termination may leave exact scratch paths for manual cleanup; no scan
  or deletion by filename prefix is introduced.

*** Failure behaviour

- Public HTTP :: *404* for absent/unknown token/path; *403* for known-capability
  denied content, Host/Origin/peer violations; *400* for malformed URL/search;
  *413* for source/body limits; *415* for asset type/signature refusal;
  *422* for invalid source encoding/conversion;
  *503* for capacity/dependency unavailability; *504* for conversion timeout.
  No partial success response is sent. Errors use escaped readable HTML for
  browser requests and contain no stack trace, token or absolute source path.
- Startup :: *Pandoc incompatibility or missing runtime tools exits 1* before
  ready; bad configuration exits 2. A second instance fails visibly without
  replacing the active socket or lock. Stale owned sockets are recoverable only
  after exclusive lock acquisition and verification of their type/ownership.
- Cancellation :: *Cancel and reap Pandoc on shutdown/deadline*. For shared work,
  a single disconnected waiter does not cancel other live waiters; when all
  depart, cancel the work. Shutdown stops admission immediately and has a
  five-second cleanup deadline before reporting remaining owned scratch.
- WSL :: Perform a *five-second Windows-side health probe* through the existing
  fixed PowerShell bridge with proxying and redirects disabled. Match the exact
  instance/protocol from the private socket before passing a token URL to the
  Windows browser. Probe failure is diagnosed unavailability and uses file
  fallback; it never changes the loopback-only bind requirement.

** Architecture impact

*** Security impact

- Threat boundary :: *HTTP exposes a local reading capability to browser clients*.
  Loopback excludes direct non-local peers, but is not user authentication.
  Private Unix-socket registration plus unpredictable tokens provides that
  additional boundary. Same-user malicious processes and a compromised browser
  remain outside the application's isolation guarantee.
- Origin checks :: Require the *exact advertised 127.0.0.1 authority and port*;
  reject unrelated Host, absolute-form authority, non-loopback peers and an
  explicit foreign/null Origin. Reject Sec-Fetch-Site: cross-site; allow absent
  fetch metadata for ordinary navigation/compatible clients. No wildcard CORS,
  proxy forwarding trust or service-directed network fetch is enabled.
- Browser policy :: For converted pages preserve *only hash-authorized packaged scripts/styles*;
  change HTTP image permission to the service origin and raster data. Strip
  source-authored service-origin URLs so only owned rewriting creates automatic
  local requests. Retain disabled includes, source-script removal and no remote
  automatic loading. Native HTML instead uses W008's *script-free authored-layout
  policy*, including bounded sanitized source CSS. No source scripts execute.
- Response protection :: All HTTP responses send *Referrer-Policy: no-referrer*,
  *Cache-Control: no-store*, X-Content-Type-Options: nosniff and CSP frame-ancestors
  'none'. Source images require matching approved raster type/signature. Other
  registered download links use application/octet-stream and attachment
  disposition with an escaped filename. Native HTML is passive; source SVG and
  JavaScript never execute. CSS source anchors render as code, while admitted
  native-HTML stylesheets are sanitized and inlined. Converted pages retain the
  deliberately visible original source header.
- Authorization renewal :: *Every request revalidates the allowed root and source
  handle*. Configuration changes take effect on restart; old tokens then fail.
  Allowed roots are explicit user grants, not inferred cwd/home/repository scans.
- Data and secrets :: *Tokens and content remain in memory/private state* apart
  from necessary browser delivery and explicit CLI URL output. No access log of
  full URLs, cookie/session persistence, telemetry, crash upload or token backup.
  Browser history may retain visited URLs; server restart revokes them.
- Conclusion :: *Loopback is hardcoded with no escape hatch*. Service templates
  run under the user, never root. No SSH tunnel or remote-serving workaround is
  documented or recommended. The public change remains a local reader.

*** Other relevant qualities

- Reliability :: *Fallback is deliberate and classified*. Service conversion
  errors do not silently become successful alternate output.
- Performance :: *No graph pre-generation or filesystem index*. Cache reuses
  immutable rendered snapshots; budgets protect the long-running process.
- Operability :: Add *packaged per-user launchd and systemd service templates*
  using fixed absolute executable/config paths and direct arguments. Extend the
  generated Homebrew formula's service block. Source/prefix installs place
  templates and an empty-root example alongside notices, not into active service
  directories; never overwrite a user config or enable a service automatically.
- macOS activation :: The guide documents *brew services start htmlpreview* for
  the generated installed formula, without claiming an existing published tap.
  Non-Homebrew users install the generated LaunchAgent and use launchctl
  bootstrap/bootout in their GUI domain. No root LaunchDaemon or sudo route.
- Linux activation :: Ship a *systemd user unit* for explicit installation/start;
  a foreground =htmlpreview --serve= works without a user service manager.
  WSL with no functioning systemd user manager uses that same explicit command;
  installation never enables systemd or changes WSL settings.
- Packaging :: Retain *current install locations, four release targets, embedded
  fonts and licences*. Resolve installed paths at build/install time using
  structured templates; never discover commands into a scheduler at runtime.
  Service stdout/stderr use user-manager logs with redacted diagnostic content.
- Accessibility :: *Ordinary links and fragments work without browser JavaScript*.
  Existing enhancements reveal folded targets and retain keyboard/copy/print
  contracts; errors are readable text, not notification-only failures.
- Portability :: Preserve *macOS, Linux and Windows-default-browser WSL* through
  existing adapters. Native checks, not cross-compilation alone, establish each
  platform's qualification; unavailable environments remain pending.
- Maintainability :: Go's *net/http, crypto/rand, os.Root and Unix sockets* supply
  the service machinery. YAML is the sole proposed dependency addition; review
  the maintained [[https://github.com/yaml/go-yaml][YAML Go implementation]] and
  select/pin its stable supported API during delivery. Keep its licence in
  THIRD_PARTY_NOTICES and run the existing vulnerability gate. No new shell
  application logic, routing framework, CLI JS or persistent browser harness.

*** Packaging layout and manager contracts

- Maintained sources :: Add =packaging/service/org.htmlpreview.agent.plist.tmpl=,
  =packaging/service/htmlpreview.service.tmpl=,
  =packaging/service/config.example.yaml= and =docs/SERVICE.md=. Extend the existing
  =packaging/htmlpreview.rb.tmpl= and Go buildtool. These are *delivery artefacts*,
  not files implemented by this definition.
- Archive layout :: Darwin archives contain
  =share/htmlpreview/service/org.htmlpreview.agent.plist.tmpl=; Linux archives
  contain =share/htmlpreview/service/htmlpreview.service.tmpl=. Both contain
  =share/htmlpreview/config.example.yaml= and =share/htmlpreview/SERVICE.md= beside
  the existing root executable and licence files. Templates have only named,
  documented installation substitutions for executable/config paths.
- Installed templates :: Explicit PREFIX installation copies those files under
  =PREFIX/share/htmlpreview/= and expands the service template into the sibling
  =service/org.htmlpreview.agent.plist= or =service/htmlpreview.service= using
  =PREFIX/bin/htmlpreview= and the resolved default config path. DESTDIR stages
  the destination only; it never enters installed executable/config arguments.
  Default source installation stages the same files under =~/.local/share/htmlpreview/=
  with the resolved checkout executable. Preserve existing active user files.
- launchd contract :: Label =org.htmlpreview.agent=; ProgramArguments is the
  fixed absolute installed executable followed by =--serve=. Use a user
  LaunchAgent with RunAtLoad=true, KeepAlive.SuccessfulExit=false and
  ThrottleInterval=5. The user's documented copy destination is
  =~/Library/LaunchAgents/org.htmlpreview.agent.plist=. Only explicit bootstrap
  into =gui/<uid>= activates it; bootout of the same domain/plist stops it.
  Document checking existing labels first and preserving conflicting plists.
- systemd contract :: Unit =htmlpreview.service= uses Type=simple, the fixed
  absolute ExecStart with =--serve=, Restart=on-failure, RestartSec=5 and
  WantedBy=default.target. The user copies it to
  =~/.config/systemd/user/htmlpreview.service=, explicitly runs user daemon-reload
  and enable --now; disable --now reverses activation. No system unit or linger
  change is installed. WSL without the user manager uses foreground --serve.
- Service environment :: Templates set HTMLPREVIEW_CONFIG to the resolved
  config path. PATH is a fixed installation-time value containing the known
  Pandoc directory and platform system binaries; no shell wrapper, profile or
  runtime executable discovery is placed into a service-manager command.
- Homebrew contract :: Formula =htmlpreview= gains a service block running
  =[opt_bin/"htmlpreview", "--serve"]= with keep_alive limited to unsuccessful
  exits and the package-manager standard service PATH for Pandoc. Formula install
  copies shared examples/guidance but never writes a user's config or registers
  launchd. The operator invokes brew services start/stop htmlpreview without sudo.
- Rollback :: *Stop/disable the selected user service first*. Restore a previous
  package version or remove only the operator-installed unit/plist after stop;
  preserve config and source trees. A service-manager reload follows unit-file
  changes. With no service, the CLI's file-preview fallback remains available.
  Package removal does not itself authorize deletion of user config or documents.
- Qualification :: RT006.10 - Packaged artefacts examines these *generated outputs*
  structurally and through staged commands. OT006.1 - Native qualification proves
  manager activation/stop, actual listener ownership and restored prior state.

* Delivery Handoff
:PROPERTIES:
:CUSTOM_ID: delivery-handoff
:END:

- Required reading :: This specification, [[file:../008-input-formats/spec.org][W008 - Input formats]],
  *the named project authorities and
  affected baseline components*, plus the platform contracts linked above.
  Historical W001 graph tests must be reconciled explicitly, not silently dropped.
- Entry conditions :: *Renewed operator sign-off and explicit release of the
  implementation hold*; read current definition evidence in =audits.yaml=.
  Verify [[#constraints-and-dependencies][W008's delivered format pipeline]]
  using its committed implementation and recorded qualification before service
  integration qualification. Design approval alone does not lift the hold.
- First implementation step :: Write
  [[#rt-006-1][RT006.1 - Automatic transport selection]] and
  [[#rt-006-3][RT006.3 - Network and capability boundary]], *observe intended RED*,
  then add the service boundary around the existing renderer.
- Definition evidence :: [[file:audits.yaml][W006 - Definition audit record]].
- Validation evidence :: [[file:validation.org][W006 - Service validation]] begins
  *PENDING*. No implementation or native qualification is claimed by this draft.
- Documentation impact :: On delivery, update *README/help, Vision, Architecture,
  service configuration/activation guide and packaging notices*. Retain explicit
  supersession of graph/no-service contracts. Reconcile CLI examples and legacy
  settings; do not present proposed service commands as currently implemented.
- Artefact inventory :: Deliver the *four named service source/guide files*,
  generated archive/install destinations and Homebrew service block specified
  under Packaging layout and manager contracts; do not invent another layout.
- Closure boundary :: *Amended design approval and delivery authority were
  supplied on 12 September 2026*. Prior approval and deferral remain history.
  Activation on the operator's machine requires its own authority. Required RTs,
  implementation gate, browser/platform validation and operator closure remain
  distinct.

* Hands-off mode decision log
:PROPERTIES:
:CUSTOM_ID: hands-off-mode-decision-log
:VISIBILITY: folded
:END:

- *No hands-off invocation occurred* during this definition.

#+TITLE: W009 - Reader and footnote validation
#+STARTUP: overview

* Automated regression evidence

- Native reading :: RT009.6 first failed for both formats with zero recognized
  notes. The v2 service-state test failed with HTTP 404. These were expected
  baseline failures; the current endpoint is now covered by the HTTP checks below.
- Current codec :: The focused annotation package now passes native recognition,
  literal exclusion and twenty current-value replacements, including label changes.
- Username defaults :: The native-browser runner first failed because the
  default-ID helper was absent. After implementation it passed 48 assertions,
  including the specified accent/apostrophe examples, case collisions and suffixes.
- Environment :: 14 September 2026, macOS arm64, Go 1.26.8; browser identified
  itself as Chrome 147.0.0.0. These are increment checks, not final delivery evidence.

* Current integration checks - 14 September 2026

- Scope :: Working tree following =5fd43a8= (persistent reader controls and native
  endnote view). These are implementation checks, not operator acceptance.
- Legacy import :: Save tests first failed with =unsupported_store= for Org and
  Markdown. Current checks preserve authored bytes, latest text, author and dates;
  discard superseded draft values; preserve a read-only legacy source when writing
  its sidecar; and reject equal-version conflicts across stores.
- Served endnotes :: The sidecar HTTP check first failed because its comment was
  absent from the page. Both formats now retain one native endnotes section,
  resolved virtual positions and unchanged source bytes/body revision. Changed
  source text produces visible unplaced notes without false source links.
- Retired writer :: The old-protocol check first returned HTTP 201. It now returns
  HTTP 409 =annotation_upgrade_required= without changing source bytes. The legacy
  filesystem writer was removed; existing permission, recovery, locking and
  capacity checks exercise current-value replacement instead.
- Replacement boundary :: An injected hard link during staging first bypassed the
  final check. The writer now rechecks source/destination aliases and metadata
  before rename; focused race checks pass. Platform qualification remains separate.
- Static/security :: =make lint= passes with temporary StyLua 2.5.2; the official
  macOS arm64 archive matches the earlier recorded SHA-256. =make vulncheck=
  reports no vulnerabilities. =oxlint --deny-warnings assets/web= passes after
  the guarded-transition changes. No browser-fixture execution was used.
- Regression scope :: The full Go race suite passed before the last save/refresh
  corrections (preview package: 627.318 seconds). After those corrections,
  =go test -race ./internal/annotation ./internal/preview -run '^TestRT007_|^TestRT009_' -count=1=
  passed (annotation: 3.614 seconds; preview: 17.905 seconds). These focused
  results supplement the earlier full suite; they are not a new full-suite run.
- Source entry :: Service pages without annotation polling now fetch the existing
  authorized page before displaying plaintext. JavaScript lint passes; executed
  interaction remains part of the paired source-view test.
- Rendered output :: The rebuilt service response at =fb9f71e= (current native
  footnote checkpoint) passes =tidy -errors -quiet -utf8= and
  =htmltest --skip-external=. =htmlq= confirms the info bar, reader, frontmatter,
  navigation, native endnotes and literal-source regions. These checks do not
  execute JavaScript or establish link navigation in a browser.
- Final Go run :: =make test= passed with the race detector (integration:
  29.493 seconds; annotation: 4.025 seconds; preview: 622.740 seconds). It tested
  the =fb9f71e= implementation plus the help correction committed in =1f9db0e=.
  The later CSS-only change in =27d218d= (navigation placement and folding
  chevrons) was not part of that compiled test binary. No Go or JavaScript
  behaviour changed after the run began.
- Remaining :: Corrected paired browser interaction and documentation wrap-up
  remain to be recorded. Linux/WSL remain pending as previously agreed. Review
  evidence is retained only in [[file:audits.yaml][the harness-owned audit record]].

* Pending human and native qualification


- Testing amendment, 14 September 2026 :: The operator stopped browser-fixture
  execution and directed the standard WEB.md/TESTING.md tools. Earlier browser
  runs above are historical increment evidence. Current interaction changes
  remain pending paired human validation; no further browser automation was run.
- Current-value writer :: Focused Go checks cover twenty HTTP autosaves in both
  formats, rename, clearing and retry, injected write/directory-sync failure,
  native export, Unicode points and read-only sidecar source preservation.
- Generated output :: The disposable Org response passed =tidy= diagnostics and
  =htmltest --skip-external=. =htmlq= confirmed the info bar, reader, metadata,
  navigation, native endnotes and literal-source regions. External links were
  not checked by that =htmltest= run.
- Lint :: =oxlint --deny-warnings assets/web= and native =biome lint= for
  =assets/web/preview.css= pass after the current reader changes.
- Candidate :: Working tree following =3ab50f4= (current footnote writer and
  insertion-point validation). These checks do not establish full W009 delivery;
  broader regression qualification and paired user validation remain open.

** Provisional paired feedback - 14 September 2026

- Reviewer :: Taḋg; desktop screenshot =screenshot-26-09-14-0302-48.png=,
  1989 by 1255 image pixels, macOS. Browser version and zoom were not captured.
- Candidate :: =fb9f71e= (current native footnote checkpoint), before the CSS
  correction. The user reported missing/broken controls and overlapping text.
  The screenshot shows the navigation links over the frontmatter.
- Observation :: FAIL for the reviewed layout. The shared reader selector had
  greater specificity than the responsive navigation placement, putting the
  navigation in the article column. Expanded folding chevrons were hidden.
- Correction :: Use a low-specificity child selector for shared reader sizing,
  allowing the sidebar placement to apply; retain a visible downward chevron on
  expanded folding bars. Both bar and heading click handlers remain present.
- Evidence :: At =27d218d= (navigation placement and folding chevrons), native
  Biome CSS lint passes. The rebuilt service opened the same disposable sample;
  its response passes =tidy -errors -quiet -utf8= and
  =htmltest --skip-external=. Corrected interaction and visual validation remain
  PENDING; no browser automation was invoked.

** UT009.1 - Responsive reading and composition
- Status :: PENDING.
- Requirement/procedure :: Follow [[file:spec.org::#ut-009-1][Responsive reading and composition]].
- Expected :: Reachable sticky controls, usable panes and native footnote navigation.

** UT009.2 - Point insertion, save feedback and recovery
- Status :: PENDING.
- Requirement/procedure :: Follow [[file:spec.org::#ut-009-2][Point insertion and recovery]].
- Expected :: Accurate pointer/keyboard placement, secondary ID field and retained drafts on failure.

** UT009.3 - Original source inspection
- Status :: PENDING.
- Requirement/procedure :: Follow [[file:spec.org::#ut-009-3][Original source inspection]].
- Expected :: Literal eligible source, current embedded comments and no substituted sidecar text.

** UT009.4 - Footnote source and portability
- Status :: PENDING.
- Requirement/procedure :: Follow [[file:spec.org::#ut-009-4][Footnote source and portability]].
- Expected :: Readable native definitions and normal footnote references in both formats.

** OT009.1 - Native replacement qualification
- Status :: PASS for macOS on the 14 September final batch below; Linux/WSL PENDING.
- Requirement/procedure :: Follow [[file:spec.org::#ot-009-1][Native replacement qualification]].
- Expected :: Durable acknowledged values, preserved permissions/metadata and bounded failure.
- macOS result :: PASS on 14 September 2026, macOS 26.6 (25G5065a), arm64,
  Pandoc 3.9.0.2, rebuilt =fb9f71e= (current native footnote checkpoint).
  A separate service used only an OS-created temporary document root and private
  runtime directory. Four disposable documents (two per format) and eleven
  mutation attempts stayed within the stated bound.
- Observed :: Org and Markdown create/update retained one current value and
  preserved owner/group/mode. Added xattrs and restrictive ACLs caused HTTP 409
  =storage_metadata_unsupported= with bytes and metadata unchanged. Read-only
  source saves selected native sidecars and preserved the original bytes/mode.
  SIGTERM after handing a mutation to the HTTP socket, followed by restart,
  retained a readable current record; every earlier acknowledged value survived.
  This does not establish safety against every possible interruption instant.
- Evidence :: Native check output and bounded runner were retained under
  =hp9q-20260914-56708-fcpimq= in the OS temporary directory. The check service
  was stopped; the human review service is separate. The first setup attempt
  made no mutations and reported a too-long Unix socket path; the successful
  attempt used a shorter independently allocated runtime directory.
- Platforms :: Overall status remains PENDING for Linux/WSL as agreed.

** Provisional paired refinement - 14 September 2026

- Operator observation :: Taḋg reported the corrected reader was "working quite
  well" at =c84a094= (layout evidence checkpoint), then requested clearer save
  feedback, removal of X/check controls, outside-click closing, direct exits from
  plaintext, an insertion cursor and a document marker instead of the snippet.
  This is provisional feedback, not a full responsive/accessibility test pass.
- Changed interaction :: Current JavaScript/CSS uses the existing close/refresh
  lifecycle for those actions. The visual marker is an overlay and does not edit
  document text. Its DOM position follows the shared canonical text traversal.
- Verification :: JavaScript execution, pointer placement, refresh/reflow marker
  tracking, focus transitions and two-second dark-mode save feedback remain
  PENDING paired validation under [[file:spec.org::#ut-009-1][Responsive reading]]
  and [[file:spec.org::#ut-009-2][Point insertion and save feedback]]. No browser
  runner or CLI JavaScript runtime is invoked for this iteration.

** Editable-footnote paired increment - 14 September 2026

- Scope :: The operator authorized editing supported native footnotes, including
  ordinary notes and closed HTMLPreview annotations, preserving ID, references,
  markup and attribution. This supersedes the earlier read-only editing boundary.
- Regression evidence :: The new HTTP ordinary-note test first failed because
  editable content was absent. Its Org and Markdown cases now pass through GET,
  edit POST, repeated save, stale-save refusal and reload. Both rendered copies
  of a repeated reference retain the same native label. Real-file tests cover
  multiline/CRLF markup, Unicode labels, retained author/creation identity after
  reopening with a fresh writer, duplicate labels, stale source/note, read-only
  refusal, failed replacement and editing an existing sidecar.
- Objective checks :: Focused W007/W009 race checks passed (annotation 4.036s;
  preview 19.558s). Subsequent markup-preservation checks passed after moving
  conversion markers into their own leading paragraph; lists and literal blocks
  retain their structure. The build, complete static lint and vulnerability
  checks pass. The full =make test= race suite passed at =0d754ac=
  (native footnote editing): integration 28.621s, annotation 4.619s and preview
  633.121s. The later =aae3e27= help correction changes no Go/JavaScript behaviour.
  The rebuilt command exposes that corrected help. The live HTML response passes
  =tidy -errors -quiet -utf8= and =htmltest --skip-external=; =htmlq= confirms
  native labels for ordinary and generated notes. No browser automation ran.
- Review sample :: The rebuilt foreground service uses the same isolated
  OS-temporary document root and opens its Org index through the real CLI and
  system default browser. Existing sample annotations were retained.
- Human checks :: PENDING under the existing responsive/composer/source user
  tests: open Edit on an ordinary footnote and a saved annotation, change text,
  observe Auto saved, click outside and reopen. Confirm the fixed ID, retained
  attribution, source markup, reference outline and normal endnotes outside
  annotation mode. Exercise Copy draft / Use current footnote after a competing
  edit. JavaScript interaction has not been executed by an automated browser.

* Paired refinement: native attribution and clickable footnotes

- Operator observation, 14 September 2026 :: "looks really good" is provisional
  feedback on the preceding candidate. The operator requested removal of COMMENT
  metadata, native timestamps, optional attribution, clickable shaded footnotes
  and the sidebar heading "Annotations & Footnotes". This does not establish
  keyboard, accessibility or platform qualification.
- Initial regression :: The new current-save case failed because a saved native
  footnote contained hidden COMMENT metadata. The implementation was then amended.
- Focused checks :: Current W007/W009 Go writer and HTTP regressions passed in
  annotation and preview packages. These cover current saves, retry refusal and
  acknowledgement, conflicts, edits after reopening, optional native attribution,
  sidecar rendering and source preservation. Native lint checks also passed.
- Review sample :: The checkout binary was rebuilt and the isolated service
  restarted. The actual CLI opened a native Org footnote sample in the system
  default browser, with a linked Markdown sample. Attributed, date-only and
  unattributed notes are present. No browser automation was invoked. Tidy and
  htmltest passed for the served HTML; htmlq found all three native source labels.
- Pending on this candidate :: Full race-enabled regression suite,
  implementation review and paired browser observation of
  the new clickable blocks and date presentation. Linux/WSL remains unqualified.

* Subsequent paired attribution amendment and regression evidence

- Operator, 14 September 2026 :: Requested replacement of a matching final
  attribution line on saved edits with the current reviewer and local date/time,
  always using inactive Org timestamp syntax.
- Historical full-suite evidence :: The preceding candidate's complete
  =make test= passed: annotation 4.834 s, integration 29.416 s and preview
  628.148 s. Its dependency vulnerability check also reported no vulnerabilities.
  These results precede the subsequent corrections below.
- RED observations :: Updated writer tests retained the previous author/time
  instead of refreshing them. Editing another footnote interrupted an active
  draft with =composer_conflict=. The HTTP multiline round-trip returned literal
  fences instead of the saved text; the Org case could not render the saved note.
- Current focused evidence :: W007/W009 writer and HTTP regressions pass in both
  packages (annotation 1.585 s, preview 8.528 s). The HTTP case creates markup
  with multiple paragraphs, reloads it, edits as a different registered reviewer
  and checks current text and attribution. Native EXAMPLE/fence fixtures preserve
  their authored syntax on edits. The separate live-composer regression passes.
- Current objective evidence :: The full race-enabled =make test= passed
  (annotation 4.339 s, preview 621.735 s; integration reused its passing cache).
  After the cursor-only CSS refinement, =make lint= and =make build= passed.
  The rebuilt isolated service opened the same native Org sample through the
  actual CLI and default browser; its served HTML passed tidy and htmltest.
- Cursor refinement :: The operator requested =✎= (U+270E). A 32-pixel SVG
  embeds that glyph with white edging and the hotspot at its lower-right tip.
  Native raster inspection confirms the glyph and orientation. Existing CSP
  permits the embedded image; no external cursor asset or broader policy is needed.
- Pending :: Paired review of the pencil cursor, click targets, focus outlines,
  attribution display and current save feedback. Linux/WSL qualification remains
  pending. No automated browser interaction was performed.

* Paired refinement: Created and Edited attribution

- Operator scope :: Use Created on first save and Edited after changing the
  comment. Batch remaining implementation and run the combined audit at the end.
- RED evidence :: Org and Markdown attributed-note edit regressions first failed
  because the updated native line still said Created.
- Current checks :: Focused W007/W009 race regressions passed (annotation 5.214 s,
  preview 19.678 s). Cases cover first save, changed drafts, unchanged closes,
  reopening and repeated edits with a fresh writer, harmless retries, optional
  attribution and sidecar conversion preserving Edited in Org and Markdown.
  The HTTP multiple-save expectation now uses Edited. Lint and build passed.
- Review sample :: The rebuilt isolated service reopened the existing native Org
  sample in the system default browser. Served HTML passed tidy and htmltest.
  Existing sample files and annotations were retained. No browser automation ran.
- Evidence boundary :: The previous full race-suite result precedes this wording
  refinement; current verification targets the affected writer and service paths.
  Human cursor, editing and visual checks, plus Linux/WSL qualification, remain
  pending. The combined implementation review is batched under operator direction.

* Final paired batch: consolidation and native qualification

- Implementation :: =35dd645= (Created/Edited native attribution), plus the
  help-text correction. The current specification consolidates earlier paired
  amendments; it introduces no new product scope. Earlier requirement wording
  remains available at that Git checkpoint.
- OT009.1 macOS result :: PASS through the actual built service on 14 September
  2026, macOS arm64. A bounded one-off used two disposable files per format and
  11 mutation attempts, below the 20-attempt limit. Org and Markdown retained
  current values, owner/mode and acknowledged saves after restart. The interrupted
  request left a readable current value. Read-only originals stayed unchanged
  while sidecars received notes. Extended attributes and ACLs caused explicit
  metadata-preservation refusal without changing original bytes or metadata.
  The check stopped its own service and removed its document/runtime directories.
  The operator's separate demo remained running. Linux/WSL remains PENDING.
- Static checks :: Final =make lint= and =make build= passed; =make vulncheck=
  reported no vulnerabilities. A one-off document review found all 45 custom IDs
  unique, all 94 fragment references resolved and all 19 local spec links present.
- Provisional latest feedback :: Taḋg reported a demo save error, then "weird,
  it's working now" and "i cannot reproduce it currently". The observed candidate
  was the native attribution build. Service reads showed newly saved notes.
  The earlier error's cause remains unconfirmed; no speculative fix or general
  save-recovery qualification is inferred. Browser version and viewport were not
  captured for this observation.
- Documentation :: The specification, Vision, Architecture, command help and
  work ledger are reconciled with editable native notes and current attribution.
  The annotation guide already describes that behaviour. README now reflects
  editable notes, left-aligned filename and current native/manual test boundaries. Historical validation entries
  above remain evidence for their explicitly named earlier candidates.
- Human boundary :: The UT entries remain provisional/pending until the operator
  confirms the consolidated observations. General positive feedback does not
  establish keyboard, screen-reader, phone-keyboard or Linux/WSL qualification.
- Later sidecar regression :: The final source review found native sidecar preview
  still decoding ordinary EXAMPLE syntax through the former literal decoder.
  The added regression observed the block disappear, then passed after that
  decoding was removed. Ordinary authored sidecar text now reaches conversion
  unchanged. Focused W007/W009 race checks passed (annotation 4.733 s,
  preview 19.928 s).
- Full-suite sequencing :: The preceding full =make test= passed (integration
  29.954 s, annotation 5.218 s, preview 632.906 s). The final full race-enabled
  run after the sidecar correction also passed: integration 29.978 s, annotation
  5.722 s and preview 628.188 s, exit status 0. This verifies the implementation
  committed in =ea9bada= (native sidecar preservation and paired consolidation).

* Trailing-newline save regression, 14 September 2026

- Scope :: Operator-authorized remediation of a self-review finding: a non-empty
  comment ending in a line break must save in Org and Markdown. Attribution must
  contain Created or Edited; the earlier invalid-attribution probe is withdrawn.
- RED :: HTTP creation and ordinary-note editing returned 409 =invalid_footnote=
  in both formats. Writer regressions also failed for embedded and sidecar saves.
- Change :: The shared writer normalizes terminal CR/LF after validating the
  original request. Internal paragraphs, spaces and native markup are retained;
  the definition-breakout guard is unchanged. Save/retry/close comparisons use
  the same normalized value, preserving attribution for unchanged content.
- GREEN :: The affected W007/W009 race regressions passed: annotation 5.213 s,
  preview 21.170 s. They cover repeated HTTP saves/reloads, ordinary and attributed
  edits, read-only sidecars, CR/LF, internal paragraphs, significant trailing
  spaces, retry/close attribution and unchanged unrelated source bytes.
- Static/build :: =make lint= and =make build= passed. The first lint invocation
  used an incomplete PATH and could not locate golangci-lint; restoring the
  existing tool path allowed the normal lint target to run successfully.
- Boundary :: The preceding full-suite result remains historical evidence.
  This correction used affected regression checks; no browser automation or
  independent audit ran. Browser and Linux/WSL qualification remain pending.

* Extended-attribute emergency regression evidence - 15 September 2026

- Authority :: Operator BYPASS-GATE-7 for README.md saves returning HTTP 409
  =storage_metadata_unsupported=; see [[file:spec.org::#extended-attribute-saves][Extended-attribute save correction]].
- RED :: New writer regressions failed for Org and Markdown with
  =storage_metadata_unsupported=. The concurrent-change regression confirmed
  refusal happened before the injected staging action.
- GREEN :: Focused writer and HTTP tests with the race detector passed:
  annotation 1.228s; preview 2.176s. They include attribute persistence,
  concurrent metadata edits, atomic replacement failures, retry synchronization,
  alias rechecks and existing read-only-source handling. HTTP saves returned 201.
  Added sidecar-attribute regression passed separately in 0.036s.
- Static :: Scoped golangci-lint passed. No browser automation or full-suite run.
- Review scope :: Descriptor-only metadata access, copying before synchronization,
  original/staged verification before rename, bounded allocations, preservation
  of empty/binary values, existing source and permission checks, and retained ACL
  refusal were checked locally. External audit was not rerun under the operator's
  standing instruction to proceed with self-review; no independent verdict is
  claimed by this record.
- Limitation :: The check covers macOS execution. Linux/WSL runtime verification
  remains pending, separately from cross-compilation.

* User test - README annotation with extended attributes - PENDING

- Requirement :: [[file:spec.org::#extended-attribute-saves][Extended-attribute save correction]].
- Reviewer :: Taḋg, in the existing browser workflow.
- Procedure :: Retain any unsaved draft, restart the service using
  =make service-stop= followed by =make serve=, reopen README.md, and create or
  edit an annotation. Inspect the saved native footnote and service log.
- Expected :: Autosave succeeds without =storage_metadata_unsupported= for the
  ordinary =com.apple.lastuseddate#PS= attribute; the attribute remains present.
- Status :: PENDING; automated HTTP evidence does not imply human validation.

- Final build checks :: Native =make build= and Linux amd64 cross-compilation
  passed. =govulncheck ./internal/annotation/...= reported no vulnerabilities.
  The installed command symlink points to the rebuilt checkout executable.
  The running LaunchAgent was not restarted by the agent.

* User test - explicit section folding controls - PENDING

- Requirement :: [[file:spec.org::#explicit-section-folding][Explicit section folding controls]].
- Authority :: Operator BYPASS-GATE-7, 16 September 2026.
- Procedure :: With the rebuilt service and a freshly opened preview, click
  section heading text in reading and annotation modes. Click the bar/indicator
  and Show more button; focus these controls and use Enter/Space.
- Expected :: Heading text never folds. Explicit controls still fold/unfold.
  Hyperlink navigation and annotation eligibility are unchanged.
- Verification choice :: This change affects browser event dispatch and hover
  styling; it is classified as manual UT. No automated browser or source-text
  tests were run, and no RED/GREEN browser claim is made.
- Static evidence :: Oxlint completed with the existing unused =detailIdentities=
  warning, confirmed present in the parent revision. That unrelated function
  was left unchanged. The two folding-control listeners remain in the code;
  source review does not substitute for this pending interaction check.
- Review :: Local delta review under the operator's standing self-review route;
  no independent audit verdict is claimed.

- Build evidence :: =make build= passed for the explicit section-folding change.
  The running service was not restarted; the human interaction check remains pending.

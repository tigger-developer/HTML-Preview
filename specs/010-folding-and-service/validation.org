#+TITLE: W010 - Folding and service verification

* Focused Go checks - PASS

- Candidate :: 15 September 2026 working tree, recorded with the associated commit.
- Command :: =go test ./internal/preview ./internal/buildtool -run 'TestConfiguredHomeRoots|TestFolding|TestLaunchAgentLifecycle' -count=1=
- Result :: Preview 0.994s; build tool 0.009s. Config, rendering/inheritance and
  LaunchAgent generation checks passed. =make build= also passed.
- RED :: Folding YAML was rejected before implementation. Missing home-expansion
  and LaunchAgent functions produced compilation failures before their implementation.
- Scope :: *Focused tests only*. The full suite was not rerun.

* OT010.1 - Current configuration startup - PASS

- Requirement :: Start with the user's existing folding and home-relative roots.
- Environment :: macOS, rebuilt checkout executable, 15 September 2026.
- Procedure :: Start once with the existing user config and an OS-allocated
  temporary runtime. GET =/v1/status= through its private Unix socket with curl.
- Result :: *ready=true*. The instance received TERM and exited; its temporary
  directory was cleaned up. No browser was opened and no LaunchAgent registered.

* UT010.1 - LaunchAgent and folding interaction - PENDING

- Reviewer :: Taḋg, using the shared desktop at a time of his choosing.
- Procedure :: =make serve=; preview Org and Markdown; inspect header, drawer and
  frontmatter defaults. Toggle bars and headings, Show all, visit a folded fragment,
  edit a footnote to trigger refresh, and check print then return. Stop with
  =make service-stop=.
- Expected :: Service starts/stops; configured initial state appears; manual
  controls remain functional and identified state survives annotation refresh.
- Status :: No human result has been inferred from automated output.

* Browser test removal and classification

- Authority :: Operator's repeated instruction, reaffirmed on 15 September 2026.
- Removed :: The automated browser runner, its JavaScript/HTML fixtures and
  =make test-browser=. Historical evidence and harness-owned audit records remain.
- RT boundary :: Config admission, output HTML, links, HTTP responses, writes,
  conflicts and attribution are covered through Go and HTTP boundaries.
- UT boundary :: Browser gestures, focus, pointer hit targets, visual layout,
  keyboard behaviour, clipboard UI, live autosave feedback, folding and print.
- Incident :: Prohibited browser runs occurred before this correction. The older
  annotation assertions stopped at panel activation; the folding assertions
  reached eight successes but the runner failed during teardown. Neither result
  is accepted as current validation or as a substitute for UT.

* Logging, TMPDIR and SSE - focused regression evidence

- Candidate :: 15 September 2026 paired logging/SSE working tree, preserved in
  the associated commit; manual interaction remains untested.
- RED :: SSE request returned application/json rather than text/event-stream;
  annotation refusal produced no diagnostic; generated plist lacked output logs.
  TMPDIR roots were rejected before parser implementation.
- GREEN :: =go test -race ./internal/preview ./internal/buildtool -run 'TestDocumentEvents|TestAnnotationFailureLogging|TestLaunchAgent|TestTMPDIRConfiguration|TestConfiguredHomeRoots' -count=1=
  passed: preview 2.026s, build tool 1.019s.
- HTTP evidence :: Notifications after edits, atomic replacement and sidecar
  creation; cancellation; foreign Origin, duplicate/invalid event selector and
  unknown capability refusal. Save-refusal logs retain HTTP status/error code
  without source names, author, text or capability URL.
- Static checks :: Native oxlint passed; scoped golangci-lint passed after
  documenting the bounded log/test-file reads. =govulncheck ./internal/preview/...=
  reported no vulnerabilities. No full-suite rerun and no browser automation.
- Dependency :: fsnotify 1.10.1 and x/sys 0.47.0 are pinned; their upstream BSD
  licences are packaged. Platform documentation supports directory watching
  across atomic replacement; actual Linux/WSL filesystems remain untested here.

* UT010.2 - Live logging and push refresh - PENDING

- Reviewer :: Taḋg, who confirmed the live service was stopped.
- Procedure :: =make serve= regenerates the plist and starts the rebuilt service.
  Reopen a preview to load the current JavaScript. Inspect the service log; edit
  a disposable document externally and check automatic refresh. Compose a draft
  during an external change and check preservation; hide/show the tab and retry
  after disconnect. The Network panel should show a persistent event stream,
  with state fetches on changes rather than every second.
- Expected :: Current source appears; the draft survives; a failed save has a
  non-2xx response and a corresponding diagnostic. No assertion here establishes
  that the reported insertion-point mapping defect has been corrected.

* Final candidate checks - 15 September 2026

- Build :: =make build= passed.
- Startup :: The rebuilt executable accepted the current user configuration,
  including TMPDIR, and returned =ready=true= through its private Unix socket.
  This bounded instance used an OS-allocated temporary runtime and log, received
  TERM, and exited. Timestamped startup and shutdown messages were recorded.
  The live LaunchAgent was not registered or started.
- Focused recheck :: After adjusting notifications for loss of a watched parent
  directory, =go test ./internal/preview -run ^TestDocumentEvents -count=1=
  passed in 0.796s. No broader suite or browser run was repeated.

* Semantic heading configuration - 18 September 2026

- Scope :: Paired breaking change from scalar headers to default/todo/done
  mapping, retaining version 1. The preceding scalar-config evidence remains
  historical and does not establish compatibility with this schema.
- RED :: The updated config/preview tests rejected valid mappings and accepted
  the now-invalid scalar open/closed form before implementation (0.022s).
- GREEN :: Focused folding and existing outline metadata tests passed with the
  race detector (14.681s). YAML cases include omitted fields, partial/empty
  mappings, invalid values/types, unknown/duplicate keys and scalar migration
  guidance. File rendering, distinct HTTP capabilities and linked previews carry
  category settings. Real Pandoc conversions classify custom TODO/SEQ_TODO/
  TYP_TODO sequences, including reversed DONE/TODO word meanings.
- Boundary :: These Go tests prove parsing, transport and rendered semantic
  markers. Initial browser folding, fallback precedence and restored manual
  state remain manual UT; no browser automation was used. Full-suite execution
  is not claimed. The JavaScript lint reports the existing unused
  detailIdentities function; this increment does not introduce that warning.
- Pending UT :: With headers default=open, todo=open, done=closed, open the
  disposable semantic-folding fixture. Unmarked and active headings should be
  open, completed headings closed, drawers closed. Check a completed parent,
  manual bars, Show all, fragment navigation and annotation refresh. Configured
  choices apply initially rather than locking the controls.
- Final checks :: Focused Go lint and make build passed. Git whitespace checks
  passed. The live service and external configuration were not changed.
- Provisional user validation :: The operator reported "looks good" after saving
  the nested configuration globally and reviewing the semantic-folding fixture
  for candidate 45ff309 (semantic heading fold configuration). Record this as
  acceptance of the displayed initial folding. Browser/version and individual
  gesture results were not supplied; this does not establish the separate
  fragment, print or annotation-refresh checks above.
- Additional provisional user validation :: The operator explicitly confirmed
  headings, folding and configuration changes as PASS. The subsequent defect
  report concerns list checkboxes and annotation targets, not semantic heading
  folding. This confirms those reviewed behaviours; it does not infer separate
  print or platform checks that were not reported.

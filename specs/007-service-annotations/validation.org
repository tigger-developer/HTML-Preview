#+TITLE: W007 - Annotation browser and filesystem validation
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil

* Planned qualification

- Specification :: [[file:spec.org][W007 - Attributed autosaved annotations]].
- Evidence boundary :: The original definition-only boundary is superseded by
  the implementation evidence below. Native browser usability, accessibility
  and filesystem qualification remain individually pending.
- Prerequisite :: *W006 delivered revision and required qualification must be
  recorded before W007 implementation*, under OT007.1 below.
- Recording :: Each result needs *candidate revision, exact environment,
  procedure, observed evidence and reviewer*. Human tests require human authority.
  All required platforms remain PENDING until observed individually.

* Implementation admission, 13 September 2026

- Service baseline :: =3819952= (portable service configuration and review
  evidence), with runtime =7d890c0= and synthetic linked fixture =1b040aa=.
  [[file:../006-local-preview-service/validation.org][Service validation]] records
  race-enabled regression, lint, vulnerability and actual loopback/browser
  handoff evidence. [[file:../006-local-preview-service/audits.yaml][Service
  implementation review]] retains the independent review evidence.
- Operator observation :: Taḋg confirmed project-link navigation on macOS and
  instructed continuation of the remaining implementation. When asked about
  pending native qualification, the operator confirmed, "linux/wsl will remain
  pending as i can't test it here".
- Admission :: The specification records this operator sequencing direction.
  It permits W007 implementation on the working service without converting
  unperformed platform or manager checks into passes. All W007 browser and
  filesystem results below remain pending until individually observed.
- OT007.1 dependency review :: The service code and running linked fixture,
  recorded local checks, operator observation and explicit sequencing direction
  provide the pre-implementation evidence. The later delivered-guidance portion
  remains pending; this entry is not a full OT007.1 pass.

* Implementation regression evidence, 13 September 2026

- Candidate :: The implementation checkpoint following =84ea26e= (operator
  admission with pending platform qualification); subsequent records identify
  the final reviewed revision.
- Environment :: macOS arm64, Go 1.26.8, Pandoc 3.9.0.2. Go cache and module
  cache were redirected into the allocated temporary workspace.
- Initial RED :: On the delivered service baseline, the new annotation
  registration HTTP test received 404. The browser runner then reached its
  intended missing-composer failure. Its first attempt had timed out without
  returning assertions; a complete fixture header and module-load error reporting
  were added before retrying. That timeout is not a product or browser pass.
- Further RED/GREEN :: Controlled browser tests exposed duplicate concurrent
  flushes; filesystem tests exposed loss of the live composer after a failed sync,
  missing secret verification and missing file locking on accepted retries.
  HTTP tests exposed the missing shared polling snapshot and loss of an explicit
  Markdown reader during refresh. Framing/projection tests exposed incorrect
  oversized-frame classification and loss of valid events before a conflicting
  sequence. Each observed failure was followed by implementation and a passing
  affected test. Canonical explicit-heading spans initially failed to compile
  because their new owned mapping interface was absent; its implementation and
  shared text fixtures then passed.
- Targeted Go result :: =go test -race ./internal/annotation ./internal/preview
  -run TestRT007_ -count=1= passed after these corrections. Tests exercise actual
  temporary files and service requests; append/sync faults use the owned file
  operation boundary. No metered API or provider is in the regression suite.
- Browser result :: =make test-browser= returned 35 passing native assertions
  from Chrome 147.0.0.0. The runner loaded modules extracted from an actual
  rendered page. Assertions cover shared Unicode text fixtures, debounce, IME,
  single-flight coalescing, identical retry payloads, close ordering, accessible
  composition, inert saved text, source refresh, explicit-ID folds, default-folded
  drawers, failed close, navigation protection and unsaved-text print exclusion.
  This is Chrome evidence, not Safari or screen-reader qualification.
- Check limitations :: Additional focused tests extend the broader initial RED
  boundary; no independent pre-change failure is claimed for every individual
  assertion. Full-suite, final static checks and the delivered browser sample
  are recorded separately when their current runs return.
- Vulnerability check :: =make vulncheck= reported no vulnerabilities against
  the implementation candidate. No runtime dependency was added by annotations.
- Native matrix :: Linux, WSL1, WSL2, Safari and assistive-technology results
  remain PENDING. The operator's platform sequencing instruction is preserved
  above; it does not manufacture qualification results.

** Candidate verification after the implementation checkpoint

- Revisions :: =fb19048= introduced the annotation candidate. The subsequent
  =49bd9aa= synchronization checkpoint contains the boundary corrections and
  their tests. The working tree was clean when that checkpoint was inspected.
- Regression results :: Two full =make test= runs returned successfully, with
  the preview package reporting 617.219 seconds and 619.717 seconds. Subsequent
  =go test -race ./internal/annotation ./internal/preview -run
  'TestRT007_|TestRT006_' -count=1= passed at =49bd9aa= (41.100 seconds for the
  preview package). The final uncached =go test -race -count=1 -timeout=20m
  ./...= run passed for that checkpoint (626.349 seconds for the preview
  package). Later response-validation changes have the focused browser evidence
  below; that earlier full run is not presented as covering those changes.
- Browser result :: The latest native Chrome 147 run returned 38 passing
  assertions. It additionally checks continuous typing's two-second bound and
  the print appendix before the next poll. The latter first failed, then passed
  after the presentation included the composer's acknowledged receipt.
  An earlier inert-comment assertion regressed when its fake service changed
  events without changing the full revision; the fixture now follows the
  specified revision contract and that assertion passes.
- Response validation :: A later native browser assertion first failed because
  malformed state with an empty body revision enabled Add comment. Validating
  the required revision and write-state fields made all 39 assertions pass.
  =make lint= also passed for this correction.
- Compatibility :: The new HTTP compatibility fixture checks that only a 404
  registration response triggers one old-schema retry and an unavailable notice.
  Its first execution lacked the control protocol's JSON content-type header;
  correcting the fixture header allowed the intended compatibility checks to run.
- Storage coverage :: Combined annotation-unit and service HTTP regressions
  measured 78.2% statement coverage in =internal/annotation=. Added persisted-byte
  cases for duplicate/malformed headers, orphan events, unknown versions,
  oversized frames and valid-prefix retention passed without production changes.
  The same combined profile now measures 80.2%. This is statement coverage for
  that package, not a whole-application or browser branch-coverage claim.
- Further boundaries :: Mixed predominant source line endings no longer obscure
  an intact owned-tail boundary. Cancellation during synchronization does not
  acknowledge an event, and an identical later retry remains available. Oversized
  comment fields return HTTP 413; malformed or forged fields return HTTP 400.
  These cases each have observed failing and passing regression executions.
- Build and lint :: =make build= and =make lint= returned successfully. The built
  executable reports =htmlpreview dev (49bd9aa432cb)=. The existing user-local
  symlink points to this checkout's =bin/htmlpreview=; no installation outside
  the checkout was changed by the agent.

** UT007.1 - Attributed persistence
:PROPERTIES:
:CUSTOM_ID: ut-007-1
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-1][UT007.1 - Attributed persistence]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.2 - Read-only and sidecar workflow
:PROPERTIES:
:CUSTOM_ID: ut-007-2
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-2][UT007.2 - Read-only and sidecar workflow]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.3 - Autosave and append-only composition
:PROPERTIES:
:CUSTOM_ID: ut-007-3
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-3][UT007.3 - Autosave and append-only composition]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.4 - Failure and navigation recovery
:PROPERTIES:
:CUSTOM_ID: ut-007-4
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-4][UT007.4 - Failure and navigation recovery]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.5 - Selection and changed documents
:PROPERTIES:
:CUSTOM_ID: ut-007-5
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-5][UT007.5 - Selection and changed documents]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.6 - Accessible and safe presentation
:PROPERTIES:
:CUSTOM_ID: ut-007-6
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-6][UT007.6 - Accessible and safe presentation]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.7 - Reader and print regression
:PROPERTIES:
:CUSTOM_ID: ut-007-7
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-7][UT007.7 - Reader and print regression]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.8 - Service boundary and lifecycle
:PROPERTIES:
:CUSTOM_ID: ut-007-8
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-8][UT007.8 - Service boundary and lifecycle]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** UT007.9 - Live refresh while annotating
:PROPERTIES:
:CUSTOM_ID: ut-007-9
:CATEGORY: USER
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ut-007-9][UT007.9 - Live refresh while annotating]].
- Platforms :: *macOS/Safari, Linux/Firefox, WSL1/Windows Edge and
  WSL2/Windows Edge: PENDING*. UT007.6 also requires VoiceOver and NVDA.
- Evidence :: *Not executed*. Record dated observations against the specification.

** OT007.1 - Dependency and delivered guidance
:PROPERTIES:
:CUSTOM_ID: ot-007-1
:CATEGORY: ONE-OFF
:RESULT: PASS
:END:

- Definition :: [[file:spec.org::#ot-007-1][OT007.1 - Dependency and delivered guidance]].
- Candidate :: =21f3551= (damaged-store regression checkpoint), with unchanged
  production from =a80fc4e= (annotation response validation); macOS arm64,
  13 September 2026.
- Procedure :: The initial admission review above and one delivered-guidance
  comparison of README, help, Vision, Architecture, SERVICE and ANNOTATIONS
  against registration, storage, browser behaviour and package inputs.
- Evidence :: Attribution precedence, reading-only exclusions, autosave/close,
  refresh/recovery, source and sidecar ownership, limits and external-editor
  overwrite risk agree with the implementation. ANNOTATIONS.md is included in
  installation packaging. Changed documentation passed =sanitize oed= with
  no remaining transformation. No remote-hosting capability is claimed.
- Packaging check :: Two =DESTDIR= staged =make install= runs succeeded in a
  unique OS temporary directory. The packaged ANNOTATIONS.md matched its source
  byte-for-byte. This verifies creation and repeated installation of the same
  managed symlink; it does not claim replacement of a different existing target.
- Boundary :: No service activation or model invocation occurred under this OT.
  The separately authorized foreground sample is recorded below.

** OT007.2 - Native filesystem qualification
:PROPERTIES:
:CUSTOM_ID: ot-007-2
:CATEGORY: ONE-OFF
:RESULT: PENDING
:END:

- Definition :: [[file:spec.org::#ot-007-2][OT007.2 - Native filesystem qualification]].
- macOS APFS :: *PASS*, 13 September 2026, production =a80fc4e= with regression
  checkpoint =21f3551=, ordinary user on macOS arm64. Native =Statfs= reported
  =apfs=, mount flags =0x4909080= and filesystem identifier =[16777232 26]=.
- Procedure :: A temporary Go fixture runner called the delivered annotation
  boundary in an OS-created synthetic tree. Fifteen append attempts and one
  fresh-process recovery completed in under one second; no personal source
  files, elevated permissions, mounts or service-manager changes were involved.
- Observed :: Writable sources appended embedded records; read-only sources
  and an ACL denying write/append used mode-0600 sidecars. An unwritable parent,
  symlink sidecar, hard-linked source and externally replaced source were refused.
  Short-write and sync-failure injection preserved existing bytes; a damaged tail
  refused another append, and an identical uncertain-write retry succeeded.
  A real advisory lock produced immediate busy refusal. A fresh process retried
  an existing operation without duplicate bytes and refused a new revision of
  its recovered draft. Source prefixes and modes were checked around every call.
- Runner correction :: The initial runner stopped before any append because its
  filesystem-identification command rejected a directory argument. It was changed
  to native Statfs; only the subsequent bounded execution is qualification evidence.
- Cleanup :: The runner removed only its own OS-created synthetic tree. Its
  scratch source and execution log were kept outside tracked project artefacts.
- Other platforms :: *Linux ext4, WSL Linux storage and Windows-mounted WSL
  storage remain PENDING*. This entry's overall result remains pending until
  each required filesystem is observed.

* Browser review handoff, 13 September 2026

- Candidate :: The rebuilt executable is reached by the existing
  =~/.local/bin/htmlpreview= symlink to this checkout's =bin/htmlpreview=.
  The foreground candidate serves the already authorized checkout on loopback;
  its explicit runtime directory is temporary. Native service-manager activation
  remains pending.
- Sample :: =.agents/annotation-review.girrXX/index.org= links disposable Org,
  Markdown, DOCX, plain-text and Go fixtures, plus a mode-0400 Org file for
  sidecar review. The CLI opened it using the system default browser with
  the display name Taḋg. Review comments do not modify tracked fixtures or specs.
- Automated check :: After the implementation review, =make test-browser=
  again returned 39 passing assertions in Chrome 147. The race-enabled combined
  annotation/service regression selection passed (40.874 seconds for preview).
  These results do not replace the pending human browser and accessibility tests.
- Vulnerability result :: The final =make vulncheck= reported no vulnerabilities.

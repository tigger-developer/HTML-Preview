#+TITLE: W001 - Local document preview audit evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

- Artefact :: [[file:spec.org][W001 - Installable local Markdown and Org previews]].
- Initial status :: Before local round 1, the draft recorded unresolved operator
  decisions affecting scope, retention, packaging, and source safety; no audit
  had been run.
- Required components :: =audit-spec=, =audit-design=, and
  =audit-test-definitions= in this authoring context, followed by one retained
  external context after a local PASS.
- External profile :: Codex with =gpt-5.6-luna=; timeout eight minutes, resolved
  from the installed non-secret SDLC configuration. The retained external
  context and its revision-specific results are recorded below.

** Local round 1 - Product decisions prevent a delivery-ready definition
:PROPERTIES:
:CUSTOM_ID: definition-local-1
:ROUND: 1
:DATE: 2026-09-07
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable local Markdown and Org previews]].
- Revision :: =e670be74f7a5592f5f6d3b724a3b8d74db8c1f27f09eda6b542a22595176b018=.
- Context :: Authoring context; all three component reviews were applied
  locally. No independent audit has occurred.
- Verdict :: FAIL.

#+begin_example
GATE: definition
REVISION: e670be74f7a5592f5f6d3b724a3b8d74db8c1f27f09eda6b542a22595176b018
VERDICT: FAIL

1. [audit-spec] [BLOCKING] Linked-browsing scope is unresolved.
2. [audit-design] [BLOCKING] Retention and cleanup behaviour are unresolved.
3. [audit-design] [BLOCKING] Release and compatibility contracts are unresolved.
4. [audit-design] [BLOCKING] Source trust and its negative tests are unresolved.
5. [audit-test-definitions] [BLOCKING] The dependent outcome and failure-path coverage cannot yet be finalized.
#+end_example

*** Findings and required resolution

- D001 - Linked browsing :: Select whether W001 includes local document
  traversal. If included, finish the opt-in interface, directory and filesystem
  boundary, budgets, symlink contexts, ambiguous targets, partial failures, and
  traced tests. If excluded, state how unconverted document links behave.
- D002 - Preview lifetime :: Select how the command remains active or returns,
  how the reader ends a session, and when files expire. Then define normal,
  interrupted, failed-handoff, cleanup-failure, and slow-browser outcomes with
  quantitative bounds and tests. A successful opener call is not page-readiness
  evidence.
- D003 - Release contract :: Select the platform and installation route. Then
  define the supported architectures, OS/browser/Pandoc versions, exact input
  dialects, public settings, build/install interfaces, and installation evidence.
  Record the replacement checking contract for the operator's exclusion of CLI
  JavaScript, which overrides ESLint/stylelint defaults.
- D004 - Source trust :: Select the source-document safety policy. Complete its
  raw HTML/CSS, script, URL-scheme, include, metadata-resource, and remote-resource
  boundaries. Add tests demonstrating both retained passive content and rejection
  or neutralization of unsupported active content.
- D005 - Dependent test coverage :: Add acceptance criteria and traced tests for
  the selected traversal, retention, compatibility, installation, and trust
  outcomes. Resolve duplicate identifier and search ambiguity. The existing
  RT/UT definitions are a draft baseline, not sufficient final coverage.

*** Disposition

- Remediation :: No operator-controlled policy has been silently selected.
  Four free-form questions were sent during definition; their answers remain
  pending. The specification records reviewable proposals and dependent gaps.
- Autonomous boundary :: =AUDITS.md= requires stopping a gate loop at a
  human-controlled decision. Resume local composite review after the directions
  are incorporated. This is one failed local round, not an external verdict.
- External context :: None. Start the single retained external context only
  after all three local reviews pass.

** Definition-stage inspection evidence

- Scope :: Repository authorities, relevant prototype code, selected standards,
  and the maintained-regression baseline were inspected. No maintained
  application suite or earlier numbered project specification was found.
- Org probe :: A bounded synthetic =pandoc -f org -t json= probe used an isolated
  data directory on Pandoc 3.9.0.2. It confirmed the ID, planning, drawer, literal
  block, and link-suffix observations recorded in the specification's fidelity
  contract. These observations guide design; they are not application tests.
- Validation ledger :: The selected browser tests are recorded as PENDING in
  [[file:validation.org][W001 - Local document preview validation evidence]].
- Document checks :: =sanitize= processed all four changed Org files without
  changes. Pandoc parsed all four; a one-off AST inspection found unique heading
  identifiers, 77 resolving local links, and reciprocal links between 14 draft
  criteria and 11 test definitions. These are structural checks, not a
  definition-gate PASS or evidence of implemented behaviour.

** Local review - Round 2: template correction and concrete candidate
:PROPERTIES:
:CUSTOM_ID: definition-local-2
:ROUND: 2
:DATE: 2026-09-07
:END:

- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =216bd3d5675ca67ec813f0e5d95fc887c90cd46d027a6df49e7c224b4f7b2409=.
- Audits :: =audit-spec=, =audit-design=, and =audit-test-definitions= in the
  authoring context.
- Verdict :: FAIL.

#+begin_example
GATE: definition
REVISION: 216bd3d5675ca67ec813f0e5d95fc887c90cd46d027a6df49e7c224b4f7b2409
VERDICT: FAIL

1. [audit-spec] [BLOCKING] D006 - Browser procedures still defer conditions that the candidate now defines.
2. [audit-design] [BLOCKING] D007 - Local formula generation and its transport-only validation delta need an exact contract.
3. [audit-test-definitions] [BLOCKING] D008 - Pending validation records and licence assertions do not yet cover the revised criteria.
4. [audit-spec] [CONDITION] D009 - Replace the bare UT001.3 reference with its descriptive link and align the root setting description.
#+end_example

- Remediation required :: Pin browser viewing conditions in all user tests;
  define local archive/formula generation without publishing; refresh pending
  records and licence assertions; correct the named reference and root label.
- Presentation correction :: The operator reported that the prior draft did
  not match the template. Round 1's structural checks had not established ABC
  or template compliance. The revised candidate restores the fixed section
  hierarchy, improves the independent meaning of bold fragments, and separates
  compound criteria. Template and semantic checks must precede the next verdict.
- Candidate authority :: Earlier scope questions were not answered and no
  answer is inferred. The resumed definition now supplies explicit authoring
  proposals for final operator approval. This records a reviewable candidate,
  not resolved operator decisions or implementation permission.
- Identifier lineage :: Existing acceptance identifiers remain allocated.
  Source identity, concurrency, execution, font use, notices, references,
  accessibility, and failure components were separated into AC001.15 through
  AC001.28. AC001.29 through AC001.43 define the concrete installation, limits,
  retention, trust, outline, embedded-asset, and contrast contracts. Existing test IDs
  remain; new tests use IDs above the previously allocated values.
- Moved evidence :: The previous draft's inspection environment was Go 1.26.3,
  Pandoc 3.9.0.2/Lua 5.4, and macOS 26.6 arm64. Its user-owned profile amendment
  selected LUA and remains outside agent commits. These are authoring provenance,
  not application requirements.
- Org probe detail :: An ID-only heading retained =only-id=; a heading with
  both ID and CUSTOM_ID retained only =public-id= in Pandoc heading attributes.
  Planning lines and LOGBOOK were dropped; LOGBOOK_ survived as a custom drawer;
  source/example blocks retained literal text. The earlier detailed observation
  has moved here from the solution design without becoming a regression result.
- External context :: None has been started. Local PASS remains a prerequisite.

** Local review - Round 3: composite candidate review
:PROPERTIES:
:CUSTOM_ID: definition-local-3
:ROUND: 3
:DATE: 2026-09-07
:END:

- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =a5dd737adf809e5b09f05d5e2303f108062c5e5ee6c150f34ae856031923541f=.
- Context :: Authoring context; specification, design, and test definitions
  reviewed together against the named authorities and selected standards.
- Verdict :: *Local PASS*. Independent review and operator sign-off remain
  separate requirements.

#+begin_example
GATE: definition
REVISION: a5dd737adf809e5b09f05d5e2303f108062c5e5ee6c150f34ae856031923541f
VERDICT: PASS
#+end_example

- Specification review :: *The summary represents the concrete candidate*;
  title, fixed hierarchy, description lists, semantic emphasis, criterion
  identifiers, authority boundary, and delivery handoff were reviewed. The
  candidate makes proposals reviewable without claiming operator approval.
- Design review :: *Temporary ownership, graph admission, source-relative
  resolution, Org preservation, passive rendering, lifetimes, installation,
  and failure contracts are explicit*. Publication failure aborts before any
  browser opening; preflight and conversion have bounded process boundaries.
  The tooling deviation and supported platform remain visible for sign-off.
- Test-definition review :: *Every criterion has reciprocal coverage* through
  executable/output regression or named human/one-off validation. Normal
  conversion uses real Pandoc; doubles force stated external failures. Browser
  readiness, rendering, and actual Homebrew installation are not inferred from
  mocks. No implemented test result is required or claimed at this gate.
- Round 2 receipt :: *D006 through D009 are addressed*: browser conditions are
  pinned; the generated formula and local transport are specified; pending
  validation and licence assertions match the candidate; the UT reference and
  root label resolve correctly. Later consistency edits bound preflight output,
  distinguish cancellation from retention, and cover publication failure.
- Document evidence :: *sanitize left all four documents unchanged*. Pandoc
  parsed them; a one-off structure/trace inspection verified seven ordered
  template sections, eight summary fields, 43 criteria, 20 reciprocal test
  definitions, unique heading IDs, and 207 resolving local links.
  =git diff --check= passed. These checks support document integrity, not
  application behaviour or independent audit.
- Remaining approval :: *Operator sign-off is PENDING*. Candidate platform,
  browsing, retention, packaging, passive-source, and tooling choices have not
  been adopted by the operator merely because this local review passes.

** External review
:PROPERTIES:
:CONTEXT: /root/definition_audit
:END:

- Audits :: *Combined audit-spec, audit-design, and audit-test-definitions* in
  the single retained external context.
- Start :: 2026-09-07 20:27 UTC; eight-minute timeout.
- Revision :: =a5dd737adf809e5b09f05d5e2303f108062c5e5ee6c150f34ae856031923541f=,
  committed in =924371a866009d125ccf718e7e847c581f853282=.
- Input boundary :: Current specification, project profile, named authorities,
  selected standards, and focused repository evidence. No drafting conversation
  or local verdict rationale was supplied.
- Verdict :: *PASS*, returned before 20:29 UTC within the eight-minute limit.

*** External round 1 - Composite definition PASS
:PROPERTIES:
:CUSTOM_ID: definition-external-1
:ROUND: 1
:DATE: 2026-09-07
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =a5dd737adf809e5b09f05d5e2303f108062c5e5ee6c150f34ae856031923541f=.
- Context :: =/root/definition_audit=, Codex =gpt-5.6-luna=; one retained
  independent context for all three component audits.
- Verdict :: *PASS with two advisories*. No blocking finding or required
  correction remains.

#+begin_example
GATE: definition
REVISION: a5dd737adf809e5b09f05d5e2303f108062c5e5ee6c150f34ae856031923541f
VERDICT: PASS

All three audits were applied: audit-spec, audit-design, and audit-test-definitions.

1. [audit-test-definitions] [ADVISORY] TD-FILE-MODES - RT001.2 verifies owner-private session directories but does not explicitly verify the design's generated-file mode 0600 requirement (spec.org:1303-1305). Add that assertion if the file-mode detail remains part of the delivery contract.
2. [audit-test-definitions] [ADVISORY] TD-TIMER-BOUNDARY - RT001.12 describes quick-preview retention checks but does not define an observable timing tolerance around the configured grace period (spec.org:837-840). A bounded tolerance would reduce flaky interpretation while preserving the contract.
#+end_example

- Advisory disposition :: *Both advisories remain visible for operator review*.
  The file-mode assertion and timing-observation tolerance are test-detail
  improvements; the auditor did not require a specification correction. No
  material content changed after this verdict.
- Effective gate :: *Definition PASS is recorded* in the specification and its
  work item moves to REVIEW. Only lifecycle metadata changes after the audited
  hash; this does not invalidate the verdict under =AUDITS.md=.
- Approval boundary :: *Operator sign-off remains PENDING*. Candidate defaults
  still require explicit approval; no implementation, application regression,
  user validation, or Homebrew one-off result is claimed.
- Presentation :: *sdlc-preview returned 0 and reported opening spec.org*.
  Its generated adjacent HTML remained after return and was moved into this
  workflow's OS-temporary scratch directory. This records command handoff,
  not human inspection or browser-rendering approval.
- Integrity receipt :: *The audited content is unchanged*. Replacing only the
  PASS gate field with its audited PENDING value reproduces the audited SHA-256.
  The final documentation was processed with sanitize; whitespace checks pass.

** Local review - Round 4: Iosevka Custom amendment
:PROPERTIES:
:CUSTOM_ID: definition-local-4
:ROUND: 4
:DATE: 2026-09-08
:END:

- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =49047d7b3a5162342732a729d5c56a101fa2b879e936477f7a8f20a35d296659=.
- Authority :: The operator requested *Iosevka Custom WOFF2 assets from the local
  font archive* and explicitly requested the specification use them for
  fixed-width text and code blocks. This amends the existing definition; it
  does not sign off the whole candidate or authorize renderer implementation.
- Supersession :: *The earlier external PASS is historical for its audited
  revision*. The font change resets the gate to PENDING; operator sign-off
  remains PENDING and the work item remains REVIEW.
- Context :: Authoring context, applying *audit-spec, audit-design, and
  audit-test-definitions* to the changed scope and adjacent font, licensing,
  output-budget, packaging, and validation contracts.
- Verdict :: *Local PASS*; resume the same retained external context.

#+begin_example
GATE: definition
REVISION: 49047d7b3a5162342732a729d5c56a101fa2b879e936477f7a8f20a35d296659
VERDICT: PASS
#+end_example

- Specification review :: *Summary, criteria, design, and authorities name
  Iosevka Custom consistently*. AC001.19 - Fixed-width text uses Iosevka Custom
  refines the existing family requirement; AC001.20 - Embedded font licences
  includes both copyrights. AC001.44 - Iosevka Custom faces are embedded uses
  the next never-used criterion identifier, verified against current records
  and Git history. No earlier identifier was recycled.
- Template review :: *The current canonical template uses the change title as
  its opening headline*. The summary headline now follows that form while
  preserving its CUSTOM_ID and all eight summary fields; the remaining major
  sections and their order are unchanged.
- Design review :: *Four static normal-width faces map to normal/italic styles
  at 400/700*. Packaged data URLs, a generic monospace fallback, both font
  licences, and output-budget accounting are explicit. The amendment adds no
  runtime, remote font access, or renderer implementation.
- Test-definition review :: *RT001.5 - Embedded assets and licence payload*
  covers six exact font hashes, face declarations, data URLs, and notices.
  *UT001.1 - Document typography and readability* checks actual Iosevka faces,
  fixed-width elements, four styles, aligned columns, and Irish glyphs. Its
  pending validation record matches the definition; no application or human
  validation PASS is inferred from the asset probes.
- Asset evidence :: *Four copies match the source bytes and SHA-256 values*.
  Temporary decompression and OpenType inspection identify Iosevka Custom
  34.1.0, normal width, weights 400/700, fixed-pitch flags, and no variation
  axes. HarfBuzz found no missing glyphs in the recorded Irish/code sample;
  sampled letters, digits, and spaces have equal advances in all four faces.
- Licence evidence :: *Embedded metadata identifies SIL OFL 1.1*. The retained
  upstream licence has matching copyright holder/years and permits embedding
  with the notice and licence retained. Only trailing whitespace was normalized
  in the copied licence; the WOFF2 bytes were not modified. Full provenance and
  hashes are in [[file:../../assets/fonts/iosevka-custom/README.md][Iosevka Custom asset documentation]].
- Document evidence :: *sanitize processed every authored document without
  changes*. Pandoc parsed ten relevant Org/Markdown documents; a one-off check
  verified the current template, 44 criteria, 20 reciprocal test definitions,
  unique heading IDs, 238 resolving local links, and matching typography-test
  procedures. =git diff --check= passed.
- Retained context :: =/root/definition_audit=. Its next review is limited to
  the amendment and enough surrounding contract to assess it safely.

** External review - Round 2: Iosevka Custom definition PASS
:PROPERTIES:
:CUSTOM_ID: definition-external-2
:ROUND: 2
:DATE: 2026-09-08
:CONTEXT: /root/definition_audit
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =49047d7b3a5162342732a729d5c56a101fa2b879e936477f7a8f20a35d296659=,
  committed in =34d93ca8b26826c51078d0f7e404e133f3131ba5=.
- Context :: *The same retained Codex gpt-5.6-luna context*, applying
  audit-spec, audit-design, and audit-test-definitions together.
- Start :: 2026-09-07 23:51 UTC (8 September in the operator's timezone);
  verdict returned within the configured eight-minute limit.
- Verdict :: *PASS for the specification and committed font assets*. The
  separate working-tree anomaly below is excluded from this integrity claim.

#+begin_example
GATE: definition
REVISION: 49047d7b3a5162342732a729d5c56a101fa2b879e936477f7a8f20a35d296659
VERDICT: PASS

All three component audits were applied: audit-spec, audit-design, and audit-test-definitions.

1. [audit-spec] [ADVISORY] FONT-AMENDMENT-SCOPE - The amendment records Iosevka Custom as the fixed-width default while retaining Asap as the proportional default, with implementation and browser evidence still pending.
2. [audit-test-definitions] [ADVISORY] FONT-TRACEABILITY - RT001.5 checks all six hashes, CSS mappings, embedded data URLs, notices, and package contents; UT001.1 checks actual browser face selection and fixed-width coverage.
#+end_example

- Format receipt :: The first returned verdict used a combined audit-name
  label for one finding. It was *not accepted as a valid verdict*. The retained
  reviewer returned the normalized component names above for the same revision;
  no specification or asset correction was required by that review.
- Advisory disposition :: *The two font entries confirm the reviewed scope and
  traceability*. The earlier generated-file-mode and timer-tolerance advisories
  remain unchanged and non-blocking; no unrelated remediation was introduced.
- Effective gate :: *Definition PASS is restored* for this specification
  revision. Operator sign-off remains PENDING; only lifecycle metadata changes
  after the audited content. No renderer implementation or human font-validation
  result is claimed.
- Presentation :: *sdlc-preview returned 0 and reported opening the specification*.
  Its adjacent generated HTML remained after return and was moved into the
  workflow's OS-temporary scratch directory. Preview handoff does not establish
  human inspection or font validation.

*** Working-tree font anomaly

- Observation :: After verified import and commit, *the working bold-italic
  WOFF2 became empty* at 2026-09-07 23:51:45 UTC. The committed asset and the
  operator's source still contain 385,208 bytes and retain the documented hash.
- Isolation :: The auditor reported only checksum, size, and base64 reads.
  A bounded reproduction using an OS-temporary copy found that those same
  read operations left the file unchanged. *The writer has not been identified*;
  neither tool behaviour nor operator action is asserted as the cause.
- Preservation :: *The unexpected change remains unstaged and untouched*
  while its ownership or restoration authority is clarified. The independent
  reviewer explicitly excluded this working-tree state from the committed-asset
  integrity PASS. All other authorized documentation and asset work continues.

** Local review - Round 5: desktop platforms and embedded asset lookup
:PROPERTIES:
:CUSTOM_ID: definition-local-5
:ROUND: 5
:DATE: 2026-09-08
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =bdd95bb5bd2d08d418a958bccf0a2d82a9d3c6454d8b24072c33a0e22d7186c7=.
- Authority :: The operator required *macOS, Linux, and WSL support* and confirmed
  *system default-browser opening*, including Windows from WSL. Embedded font
  lookup addresses the installation/symlink question without system font writes.
- Supersession :: *The previous PASS remains revision-specific history*. The
  platform amendment resets the gate to PENDING; operator sign-off remains
  PENDING and the ledger remains REVIEW. No implementation is authorized.
- Context :: Authoring context, applying *audit-spec, audit-design, and
  audit-test-definitions* to the changed scope and adjacent lifecycle, security,
  URL, asset, packaging, and validation contracts.
- Verdict :: *Local PASS*; resume the same retained external context.

#+begin_example
GATE: definition
REVISION: bdd95bb5bd2d08d418a958bccf0a2d82a9d3c6454d8b24072c33a0e22d7186c7
VERDICT: PASS
#+end_example

- Specification review :: *The summary, authorities, and detailed contracts
  reflect the required platforms and default browser*. The qualification matrix
  and packaging recipe remain explicit authoring proposals for sign-off.
  AC001.45 - Default-browser opening and AC001.46 - Windows-readable references
  use new identifiers; prior identifiers and audit history are retained.
- Design review :: *Font lookup occurs at build time and uses embedded data at
  runtime*. WSL translates browser-facing paths after Linux admission; source
  file authorities cannot claim the generated WSL exception. Fixed PowerShell
  command text receives URL data through child stdin, with bounded helper calls.
  The design diagnoses unrepresentable paths and preserves private output,
  root/device boundaries, selected retention, and existing system configuration.
- Test-definition review :: *Installation tests cover symlink invocation and
  all four native release targets*. RT001.17 - Platform handoff and Windows file
  URLs covers adapter policy, failures, encoding, and trust boundaries;
  UT001.4 - Installed previews across desktop platforms requires real desktop
  and WSL observations. All typography/navigation/passive-content user tests
  now require the qualification environments. Mocks and cross-compilation do
  not establish browser compatibility; all runtime results remain PENDING.
- Documentation evidence :: *sanitize processed the four authored documents*.
  Pandoc parsed them with unique heading IDs; one-off checks verified the seven
  ordered template sections, eight summary fields, 46 criteria, 22 reciprocal
  test definitions, and matching procedures/expectations for all four user-test
  records. =git diff --check= passed. Git history contains no earlier allocation
  of the new criterion/test identifiers.
- Asset boundary :: *No font bytes were changed in this amendment*. The existing
  unattributed empty working-tree bold-italic file and operator profile edit
  remain untouched and excluded from staging; their preservation boundary from
  the earlier audit remains applicable.
- Retained context :: =/root/definition_audit=, with the configured eight-minute
  timeout. Review only this amendment and enough adjacent material to assess it.

** External review - Round 3: desktop platform definition PASS
:PROPERTIES:
:CUSTOM_ID: definition-external-3
:ROUND: 3
:DATE: 2026-09-08
:CONTEXT: /root/definition_audit
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =bdd95bb5bd2d08d418a958bccf0a2d82a9d3c6454d8b24072c33a0e22d7186c7=,
  committed as =7e3d2a5=.
- Context :: *The same retained Codex gpt-5.6-luna reviewer*, applying
  audit-spec, audit-design, and audit-test-definitions together. No drafting
  conversation or local verdict rationale was supplied.
- Start :: 2026-09-08 00:21:40 UTC; the verdict returned within the configured
  eight-minute timeout.
- Verdict :: *PASS with one new operational advisory*. No required correction
  remains for this amendment.

#+begin_example
GATE: definition
REVISION: bdd95bb5bd2d08d418a958bccf0a2d82a9d3c6454d8b24072c33a0e22d7186c7
VERDICT: PASS

All three component audits were applied: audit-spec, audit-design, and audit-test-definitions.

1. [audit-test-definitions] [ADVISORY] PLATFORM-MATRIX-EXECUTION - RT001.14 requires native regression on all four release targets while the specification does not define how unavailable target environments are scheduled or recorded beyond the separate pending UT matrix (spec.org:936-959, 1468-1486). This is an operational planning advisory and does not block definition sign-off.
#+end_example

- Review scope :: The reviewer confirmed *traceability through the authorities,
  criteria, regression definitions, and pending user tests*. Linux admission
  remains separate from WSL URL conversion; helper calls are bounded; unsafe
  paths are rejected; private output and the four release targets are explicit.
  No implementation or platform execution evidence was inferred.
- Advisory disposition :: *Plan native target availability during delivery*;
  unavailable environments cannot be reported as executed. The earlier file-mode
  assertion and timing-tolerance advisories remain visible in their original
  records. No material specification correction followed this verdict.
- Effective gate :: *Definition PASS is recorded*; operator sign-off remains
  PENDING and the work item remains REVIEW. Only the gate field changes after
  the audited revision. Browser, installation, and application tests remain
  unexecuted.
- Documentation receipt :: *249 local links resolve* across the five changed
  documents. Template, reciprocal traceability, parsing, and sanitizer checks
  are document evidence, not application validation.
- Asset boundary :: *The unexpected empty working-tree bold-italic file remains
  preserved and unresolved*. This audit makes no working-tree font-integrity
  claim and does not amend the earlier committed-asset evidence.
- Presentation :: *sdlc-preview reported opening the updated specification*.
  Its adjacent generated HTML remained after handoff and was moved to this
  amendment's unique OS-temporary scratch directory. This is presentation
  evidence, not human inspection or application browser validation.

** Source-header amendment - Local review 1
:PROPERTIES:
:CUSTOM_ID: definition-local-6
:ROUND: 1
:DATE: 2026-09-08
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =e2db66f35261830c34c1fa64f97cb1a618f67de960b8fa7d82cc7290dc98fe1b=.
- Authority :: After the platform amendment passed, the operator required *the
  original filename header and clicking it to copy*. This is the first local
  review of that new amendment, not a retry of an earlier failed candidate.
  Evidence identifiers remain monotonic; all earlier rounds are preserved.
- Baseline inspection :: *The personal template and its inline JavaScript were
  read in full*, together with the current preview command, HTML defaults, and
  stylesheet. The source template is
  =~/.local/share/pandoc/htmlpreview-template.html=; its source h1 contains
  source_dirname, a slash, and strong source_filename. The inline handler
  copies textContent after replacing line breaks and trimming, applies the
  copied class for 600 milliseconds on success, and logs failure only to the
  console. The stylesheet uses a compact right-aligned monospace header and
  separator, with stronger filename weight and blanket opacity.
- Prior gap :: *Source identity was required but copying remained optional*
  in the architecture; there was no criterion or user-test definition for it.
  The revised authorities preserve the interaction and add keyboard support,
  explicit failure feedback, accessible contrast, and a distinct full-path value.
- Supersession :: *The earlier PASS remains historical for its revision*.
  The gate resets to PENDING; sign-off remains PENDING and the ledger remains
  REVIEW. No renderer, browser script, or private configuration is changed.
- Context :: Authoring context, applying *audit-spec, audit-design, and
  audit-test-definitions* to the header amendment and adjacent source identity,
  typography, clipboard security, accessibility, and platform contracts.
- Verdict :: *Local PASS*; resume the existing independent reviewer.

#+begin_example
GATE: definition
REVISION: e2db66f35261830c34c1fa64f97cb1a618f67de960b8fa7d82cc7290dc98fe1b
VERDICT: PASS
#+end_example

- Specification review :: *Summary, authorities, and criteria require the source
  header and copying interaction*. AC001.15 - Source identity is refined;
  AC001.47 - Header activation copies the original path and AC001.48 - Manual
  copying when unavailable use new identifiers confirmed against Git history.
- Design review :: *The copy value is the original absolute logical source
  path*, including in WSL. Safe HTML data carries it separately from formatted
  text. Only the owned native control invokes writeText on user activation;
  it never reads the clipboard or trusts source-body copyable classes. Success
  awaits the API result, failure exposes manual copying, and no-JavaScript/print
  output retains plain source identity. Private helpers are not dependencies.
- Test-definition review :: *RT001.1 - Explicit inputs and source identity*
  verifies the emitted header, title, escaped path, and source-context value.
  *UT001.5 - Source header and clipboard interaction* verifies actual pasted
  values across the platform matrix, keyboard activation, failure feedback,
  manual copying, no-JavaScript/print behaviour, and clipboard isolation.
  Forced API failures test fallback handling; they do not substitute for actual
  successful writes. No browser harness or CLI clipboard dependency is added.
- Document evidence :: *sanitize leaves all four authored documents unchanged*.
  Pandoc parses each with unique heading IDs; 251 local links resolve. The
  canonical seven-section/eight-field shape, 48 criteria, 23 reciprocal test
  definitions, and matching procedures/expectations for all five user tests
  pass one-off checks. =git diff --check= passes.
- Evidence boundary :: *No application or browser result is claimed*. The
  unattributed empty working-tree font and operator profile edits remain
  preserved and unstaged. The retained reviewer remains =/root/definition_audit=
  with its eight-minute timeout.

** External review - Round 4: source-header definition PASS
:PROPERTIES:
:CUSTOM_ID: definition-external-4
:ROUND: 4
:DATE: 2026-09-08
:CONTEXT: /root/definition_audit
:END:

- Gate :: Definition.
- Artefact :: [[file:spec.org][W001 - Installable previews with linked browsing]].
- Revision :: =e2db66f35261830c34c1fa64f97cb1a618f67de960b8fa7d82cc7290dc98fe1b=,
  committed as =70f7e77=.
- Context :: *The same retained Codex gpt-5.6-luna reviewer*, applying
  audit-spec, audit-design, and audit-test-definitions together. The reviewer
  received the current specification, authorities, applicable standards, and
  permission to inspect the operator-named baseline template/stylesheet/command;
  no drafting conversation or local verdict rationale was supplied.
- Start :: 2026-09-08 00:39:51 UTC; verdict returned within the configured
  eight-minute timeout.
- Verdict :: *PASS with one new visual-test advisory*. No required correction
  remains for the source-header amendment.

#+begin_example
GATE: definition
REVISION: e2db66f35261830c34c1fa64f97cb1a618f67de960b8fa7d82cc7290dc98fe1b
VERDICT: PASS

All three component audits were applied: audit-spec, audit-design, and audit-test-definitions.

1. [audit-test-definitions] [ADVISORY] HEADER-VISUAL-DETAILS - The authority specifies a compact right-aligned header and separator above the document body, but UT001.5 does not name those two visual observations explicitly (docs/ARCHITECTURE.md:149-158; spec.org:1129-1148). This does not require correction for the source-path behavior gate.
#+end_example

- Review scope :: The reviewer confirmed *reciprocal source-identity and
  clipboard coverage*, the distinction between original paths and handoff URLs,
  native activation, clipboard isolation, accurate feedback, and manual/no-JS/
  print alternatives. Application and browser results remain unexecuted.
- Advisory disposition :: *The compact alignment and separator observations
  remain a visible test-detail advisory*. The prior file-mode, timing-tolerance,
  and target-availability advisories remain in their earlier records; no
  unrelated remediation or material post-verdict specification change occurs.
- Effective gate :: *Definition PASS is restored*. Operator sign-off remains
  PENDING and the ledger remains REVIEW. Only gate metadata changes after the
  audited specification hash.
- Asset boundary :: *The unexpected empty working-tree bold-italic font remains
  preserved*. This review makes no working-tree asset-integrity claim.
- Presentation :: *sdlc-preview returned 0 and reported opening the specification*.
  Its adjacent generated HTML remained after handoff and was moved into this
  amendment's unique OS-temporary scratch directory. This does not establish
  human inspection or a clipboard-test result.

* Implementation gate

- Status :: Not run. Implementation is outside this definition workflow.

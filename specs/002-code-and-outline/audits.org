#+TITLE: W002 - Audit Evidence
#+STARTUP: content
#+OPTIONS: toc:2 num:nil

* Definition gate

** Local review
:PROPERTIES:
:ROUND: 1
:END:

- Artefact :: [[file:spec.org][W002 - Highlighted code, copying and compact fold controls]].
- Revision :: Initial draft SHA-256
  =bb6580770596a2c1e4487d1063dbae04dde2b8686e72dceca405f432ec58e8a7=;
  retained in =7861849=, the initial code/outline definition checkpoint.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: FAIL.
- Findings :: The Markdown literal-value return channel is underspecified;
  private transport provenance needs an explicit collision-resistant boundary.
  Direct-code double-click selection can conflict with immediate copying.
- Steering :: During drafting, the operator replaced the triangle proposal with
  a clickable left margin bar: thicker/bright with a bottom plus when folded,
  normal when open, click again to close.
- Remediation :: Specify typed return records and activation semantics; adopt
  the operator's bar design before repeating the full local gate.

** Local review - margin bars and explicit code transport
:PROPERTIES:
:ROUND: 2
:END:

- Artefact :: [[file:spec.org][W002 - Highlighted code, copying and compact fold controls]].
- Revision :: SHA-256 =e122ffa42c8184fb66957fc1257e217d255fa154096b84fcbf74079c8e503cd0=.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No blocking findings. The proposed bar interaction explicitly
  replaces local three-state cycling; global modes and visibility remain.
  Code records have a private per-document token and a bounded, passive
  return channel. Selection guarantees distinguish drag/active selection from
  the initial click in a double-click sequence.
- Evidence :: Current template level-one heading sequence matches. Pandoc
  parsed the specification; no duplicate heading IDs or broken internal
  fragments were found. Requirements, design, baseline regressions and
  implementation boundaries were reviewed together.
- Advisory :: Narrow/nested bar geometry and real clipboard behaviour need
  the declared user tests; static HTML assertions cannot establish them.
- Remediation :: Round-one findings addressed. Independent gate not yet run.

** External review
:PROPERTIES:
:ROUND: 1
:CONTEXT: /root/code_outline_definition_audit
:END:

- Artefact :: [[file:spec.org][W002 - Code and navigation specification]].
- Revision :: SHA-256 =e122ffa42c8184fb66957fc1257e217d255fa154096b84fcbf74079c8e503cd0=.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: FAIL.
- Findings ::
  1. [audit-design] [BLOCKING] W002-DESIGN-TRANSPORT-CONTRACT - Name the private
     metadata key, lookup, encoding, precedence and rejection behaviour.
  2. [audit-test-definitions] [BLOCKING] W002-TEST-ALIASES - Cover sh and shell
     aliases and compare their highlighting/fidelity with Bash.
  3. [audit-test-definitions] [BLOCKING] W002-TEST-PASSIVE-BROWSER - Add a bounded
     browser page-load check for source-directed requests and execution.
- Remediation :: The next revision specifies the transport interface, alias
  cases and browser Network/Console observations. During remediation the
  operator also added default-on contents, depth 3 and hardcoded standalone.
  The earlier standalone-switch suggestion was explicitly withdrawn.
- Supersedes :: None.

** Local review - contents settings and external remediation
:PROPERTIES:
:ROUND: 3
:END:

- Artefact :: [[file:spec.org][W002 - Code highlighting, copying and document navigation]].
- Revision :: SHA-256 =74f384c113400136d327edcce9a890aa91f3f23040750e1cb71d1f981f887e58=.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No blocking findings. The exact private metadata and return
  channels, shell alias cases and passive browser checks address the external
  findings. Operator steering is reflected in the summary, settings contract,
  criteria, tests, handoff and validation record.
- Evidence :: Local Pandoc probes confirmed command metadata precedence and
  its Lua string representation, and rejection of TOC depth outside 1 to 6.
  The template's eight major sections match; Pandoc parsing found no duplicate
  heading IDs or unresolved internal fragments. All criteria have reciprocal
  test links. No production code or test implementation changed.
- Advisory :: Retargeting generated contents after Org ID restoration is an
  explicit design obligation, protected by generated-output tests.
- Remediation :: Resubmit to the retained external auditor.

** External review - contents and standalone
:PROPERTIES:
:ROUND: 2
:CONTEXT: /root/code_outline_definition_audit
:END:

- Artefact :: [[file:spec.org][W002 - Code highlighting, copying and document navigation]].
- Revision :: SHA-256 =74f384c113400136d327edcce9a890aa91f3f23040750e1cb71d1f981f887e58=.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: FAIL.
- Finding :: [audit-test-definitions] [BLOCKING]
  W002-TEST-STANDALONE-IMMUTABILITY - The auditor requested an explicit
  =HTMLPREVIEW_STANDALONE=0= comparison asserting identical complete output,
  with the setting ignored and no accepted standalone configuration path.
- Assessment :: Ignoring an unknown setting conflicts with W001 and the
  current configuration contract. The next revision instead makes rejection
  explicit: status 2, no allocation/opening; the ordinary invocation remains
  standalone. This demonstrates immutability without changing error handling.
- Supersedes :: External round 1 for its recorded revision.

** Local review - standalone rejection evidence
:PROPERTIES:
:ROUND: 4
:END:

- Artefact :: [[file:spec.org][W002 - Code highlighting, copying and document navigation]].
- Revision :: SHA-256 =ee7d917fbffb02b7776020b9d8999972656b7eeffcc2ea2867dfa07ebac83cc3=.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=.
- Verdict :: PASS.
- Findings :: No blocking findings. The explicit standalone-setting rejection
  now appears beside its no-output expectations. No interface or product
  behaviour changes from the previous proposal.
- Remediation :: Return the exact expectation and baseline rationale to the
  retained auditor; do not adopt its suggested silent-ignore behaviour.

** External review - standalone rejection accepted
:PROPERTIES:
:ROUND: 3
:CONTEXT: /root/code_outline_definition_audit
:END:

- Artefact :: [[file:spec.org][W002 - Code highlighting, copying and document navigation]].
- Revision :: SHA-256 =ee7d917fbffb02b7776020b9d8999972656b7eeffcc2ea2867dfa07ebac83cc3=,
  at =2ca95b7=, the standalone-rejection clarification checkpoint.
- Audits :: =audit-spec=, =audit-design= and =audit-test-definitions=, in the
  retained =gpt-5.6-luna= context.
- Verdict :: PASS.
- Findings :: No required correction reported.
- Supersedes :: External round 2 for its recorded revision. W001's
  unknown-setting rejection remains the selected contract.
- Recording :: Only definition-gate/summary status and the ledger lifecycle
  changed after this verdict. Operator sign-off and browser validation remain
  PENDING; no implementation result is inferred.

* Implementation gate

** Local review

- Definition-time status :: Not started at the definition gate. Delivery was
  subsequently authorized by the operator's recorded approval in spec.org.

** External review

- Status :: Not started.

** Local implementation review - semantic scope
:PROPERTIES:
:ROUND: 1
:END:

- Revision :: =7cd698d6d5ea42ca2f576107156d619b921d3889=, initial implementation.
- Audits :: =audit-test-code= and =audit-code= in the delivery context.
- Verdict :: FAIL.
- Test review :: The declared subprocess and installed-entry boundaries have
  real Pandoc RED/GREEN evidence. The neighbour list assertion now scopes to
  the document body so the added TOC does not replace its subject.
- Code finding :: [audit-code] [BLOCKING] W002-CODE-SEMANTIC-SCOPE - Code
  copying used the surrounding pre's text rather than the specified code-node
  value, and syntax selectors also matched code outside the document container.
- Remediation :: Capture code.textContent, scope token selectors to hp-document
  and supply the specified next-action tooltip on each folding bar.
- Advisory :: Browser interaction, geometry, printing and clipboard policy
  require the pending operator tests; generated HTML is not that evidence.

** Local implementation review - scoped candidate
:PROPERTIES:
:ROUND: 2
:END:

- Revision :: =3a77a328cdbc7ce56a25fac793306b2469a34898=, semantic-scope correction.
- Audits :: =audit-test-code= and =audit-code= in the delivery context.
- Verdict :: PASS.
- Findings :: No required local correction remains. Round-one scope changes
  match the signed-off design. Transport validation, token ownership, literal
  fallback, final heading binding, passive content, bounded process/session
  ownership, clipboard activation and outline lifecycle were reviewed together.
- Evidence :: The full race suite passed before the browser-only scope
  correction. Current focused generated-output checks and native JS/CSS static
  checks pass. Runtime coverage is 87.8%; make lint and make vulncheck pass.
  The validation record distinguishes these from unexecuted browser tests.
- Structure :: The transport coordinator retains one ordered validation and
  restoration pass. Outline setup keeps its shared state, handlers and teardown
  in one lifecycle; clipboard and code enhancement are separate functions.
- Remediation :: Round-one findings addressed; submit this candidate to the
  retained independent implementation gate.

** External implementation review - heading containers
:PROPERTIES:
:ROUND: 1
:CONTEXT: /root/code_outline_implementation_audit
:END:

- Target revision :: =f92f39e1b2d10a7b8c54b4472e51a3774404a937=, local evidence checkpoint.
  The returned revision text omitted one character; the request and repository
  identify the target above. The next retained-context review must confirm it.
- Audits :: =audit-test-code= and =audit-code=, configured gpt-5.6-luna.
- Verdict :: FAIL, returned within the eight-minute invocation bound.
- Finding :: [audit-code] [BLOCKING] W002-CODE-HEADING-CONTAINER - Pandoc
  headings inside blockquotes can be direct heading elements instead of section
  children. Requiring a section rejects those valid source documents.
- Advisory :: [audit-test-code] [ADVISORY] W002-TEST-HEADING-CONTAINERS - Add
  command coverage for headings in quotations and other non-section containers.
- Independent confirmation :: The new CLI regression returned status 1,
  no published page and "invalid heading association" before the correction.
- Remediation :: Restore both section-owned and bare heading nodes, retaining
  their identity through the existing catalogue wrapping step. The quoted/list
  heading regression and neighbouring W002 cases then pass.

** Local implementation review - container correction
:PROPERTIES:
:ROUND: 3
:END:

- Revision :: =8f5312569903564ef4308df7689e438c60d8dd7e=, container and raw-code correction.
- Audits :: =audit-test-code= and =audit-code= in the delivery context.
- Verdict :: PASS.
- Evidence :: The added public CLI case failed before the container correction
  and now passes; focused W002 and bare-HTML-heading checks pass in 8.321 s.
  make lint and the native browser-source checks pass.
- Scope review :: Multiple raw semantic code values sharing a preformatted
  element now keep independent code-node payloads and controls, using one
  wrapper. This follows the already-approved semantic-code scope. A synthetic
  example exposes it for the pending browser check. No test-only browser
  interface or new runtime was introduced.
- Remediation :: Resubmit to the same external context. Browser evidence
  remains PENDING; no operator closure is inferred.

** External implementation review - corrected candidate
:PROPERTIES:
:ROUND: 2
:CONTEXT: /root/code_outline_implementation_audit
:END:

- Revision :: =fb390c8b229fcd829faeb08f28378421206cf6fa=, remediation evidence checkpoint;
  production code is =8f5312569903564ef4308df7689e438c60d8dd7e=.
- Audits :: =audit-test-code= and =audit-code=, configured gpt-5.6-luna.
- Verdict :: PASS, returned within the eight-minute invocation bound.
  The retained reviewer reported the exact current revision.
- Findings :: No required correction remains in the reviewed delta.
- Advisories :: W002-TEST-BROWSER-QUALIFICATION and
  W002-CODE-BROWSER-QUALIFICATION retain actual browser interaction, geometry,
  clipboard policy, printing and accessibility as pending human evidence.
- Evidence boundary :: Quoted/list headings have an automated regression.
  Multiple raw code values sharing a preformatted block have an example and
  source review; their browser activation is not claimed as automated coverage.
- Supersedes :: External round 1 for its recorded revision. Final checks and
  the staged browser example follow this PASS; closure remains the operator's.

** Local implementation review - live-preview corrections
:PROPERTIES:
:ROUND: 4
:END:

- Revision :: =204e1281d39d9ad16007bb5cabce5ab20a795829=, folding and writer-link correction.
- Audits :: =audit-test-code= and =audit-code= in the delivery context.
- Verdict :: PASS after the corrections below.
- Observation :: The published candidate contained empty per-line highlighter
  anchors after source sanitization stripped their tabindex and aria-hidden
  attributes. This introduced unrequested empty links into keyboard navigation.
- Regression :: RT002.1 - Literal code now rejects those writer links. It failed
  for highlighted Markdown and Org before correction, then passed alongside all
  W002 cases, CSP checks and installed-prefix checks: runtime 8.427 s and
  integration 1.821 s. Only validated writer-owned code loses the empty links;
  ordinary source anchors retain the source policy.
- Operator finding :: The user reported that unsupported script/style notices
  remained visible when folded. Published HTML showed bare text children while
  outline refresh handled element children only. Enhancement now wraps visible
  direct section text for folding, and teardown restores those text nodes.
- Test boundary :: The reported browser failure and its correction are tracked
  in UT002.1 - Reading and outline presentation. Native source checks pass;
  no browser-execution claim is inferred from them.
- Presentation input :: The intermediate template no longer supplies an unused
  charset attribute that the source scrubber diagnosed. Final HTML retains its
  owned UTF-8 metadata and complete wrapper.
- Evidence :: make lint, oxlint and scoped Biome checks pass. The earlier full
  race suite passed at the prior candidate; final affected checks will follow
  the retained auditor's review of this correction.
- Supersession :: The round-two external PASS remains historical for its exact
  revision. Return this correction to the same external context.

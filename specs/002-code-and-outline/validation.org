#+TITLE: W002 - Browser validation
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil

* Automated implementation evidence

- Scope :: RT002.1 - Highlighted code and literal fidelity;
  RT002.2 - Passive rendering and budgets;
  RT002.3 - Standalone output and contents settings.
- RED :: Test checkpoint =cc65846= preceded production changes. Real Pandoc
  output lacked token spans in both formats, default contents were absent,
  inactive depth validation failed, and the installed symlink lacked the new
  output. Passive rendering and budget cases already passed.
- GREEN :: The focused subprocess and installed-entry checks pass. The full
  make test race suite passed: integration 8.687 s, runtime 134.630 s.
  Later browser-only scope corrections received current generated-output and
  static checks; final verification follows the independent implementation gate.
- Compatibility correction :: An existing nested-list assertion inspected the
  first page list, now the TOC. It now inspects the source document body and
  retains its original nested-list and table expectations.
- Static evidence :: make lint passed Go formatting, vet, configured Go
  linters, StyLua and the exact Pandoc Lua host. make vulncheck reported
  "No vulnerabilities found" with Go 1.26.8.
- Supplemental review :: Native oxlint 1.82.0 and Biome 2.5.12 checked browser
  source without executing JavaScript on the command line. The bounded Biome
  correctness check excluded its Qwik-only closure rule because this project
  has no Qwik runtime. Its initial Qwik diagnostic was not a product defect.
  These tools/configuration were not added to build targets or dependencies.
- Coverage :: The runtime package reports 87.8% statement coverage. This is
  structural evidence, not browser behaviour or a whole-project coverage claim.
- Palette :: Calculation against both page and panel backgrounds gives code
  token minima of 6.08:1 in light mode and 7.65:1 in dark mode. The folding
  accent gives at least 6.47:1 and 7.89:1 respectively. Actual font rendering,
  geometry and usability remain human checks.
- Environment :: macOS arm64, Go 1.26.8, Pandoc 3.9.0.2 with Lua 5.4.
  Test fixtures are synthetic; the local request counter observed no conversion
  requests. No browser or Linux/WSL qualification is inferred.

* UT002.1 - Reading and outline presentation
:PROPERTIES:
:CUSTOM_ID: ut-002-1
:RESULT: PENDING
:CATEGORY: USER
:END:

- Requirements :: [[file:spec.org::#ut-002-1][Presentation procedure and traced criteria]].
- Procedure :: Use the installed candidate and synthetic examples under the
  exact viewing conditions in the linked test definition.
- Expected :: Readable highlighted Iosevka code, faithful Org metadata,
  accessible margin-bar folding and contents navigation, standalone output,
  no-JS reading and full printing.
- Matrix :: macOS 26/Safari 26 arm64; Ubuntu 24.04/Firefox amd64;
  Windows 11/Edge with Ubuntu 24.04 WSL1; Windows 11/Edge with Ubuntu 24.04 WSL2.
  Each environment is PENDING; record its exact versions and candidate revision.
- Evidence :: None yet. The pre-definition example opened on 8 September 2026
  used W001 behaviour; the separate Pandoc study was not the W002 application.

* UT002.2 - Clipboard interaction and fallback
:PROPERTIES:
:CUSTOM_ID: ut-002-2
:RESULT: PENDING
:CATEGORY: USER
:END:

- Requirements :: [[file:spec.org::#ut-002-2][Clipboard procedure and traced criteria]].
- Procedure :: Use each matrix environment above and follow the linked
  activation, pasted-value, selection, navigation, denial and lifecycle checks.
  Include its bounded passive page-load/reload check with the hostile synthetic
  fixtures and developer-tools Network/Console observations.
- Expected :: Exact explicitly selected code/path payloads, honest feedback,
  manual fallback, retained keyboard/navigation behaviour, no source execution
  and no source-directed browser network requests.
- Matrix :: All four browser environments are PENDING.
- Evidence :: No W002 user-test result is claimed. Automated implementation
  evidence above does not establish browser clipboard or network behaviour.

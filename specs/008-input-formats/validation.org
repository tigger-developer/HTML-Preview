#+TITLE: W008 - Input format validation
#+STARTUP: overview
#+OPTIONS: toc:2 num:nil

* Planned qualification

- Specification :: [[file:spec.org][W008 - Preview Pandoc documents, source code and plain text]].
- Evidence boundary :: Definition feasibility and a *partial implementation
  checkpoint* are recorded separately below. No completed implementation or
  native qualification is claimed.

** UT008.1 - Native format reading
:PROPERTIES:
:CUSTOM_ID: ut-008-1
:CATEGORY: USER
:RESULT: PENDING
:END:

- Requirements :: [[file:spec.org::#ac-008-6][AC008.6 - Wrappers preserve original content]],
  [[file:spec.org::#ac-008-7][AC008.7 - Ordinary JSON is readable without changing its source]],
  [[file:spec.org::#ac-008-8][AC008.8 - HTML retains passive authored presentation]],
  [[file:spec.org::#ac-008-11][AC008.11 - Existing previews and lifetimes remain compatible]],
  [[file:spec.org::#ac-008-13][AC008.13 - Installed use remains portable and documented]].
- Procedure :: Follow the specification's DOCX/ODT/code/JSON/text/native HTML
  workflow through the installed command on macOS/Safari, Linux/Firefox and
  Windows/Edge via WSL1 and WSL2. Check layout, keyboard, copy, print and passive
  browser requests, including another cwd and PATH symlink invocation.
- Expected :: *Readable previews, exact original copy text and no source
  execution or automatic external requests*.
- Per-platform result :: *All four environments remain pending* until observed.

** OT008.1 - Guidance and reader coverage review
:PROPERTIES:
:CUSTOM_ID: ot-008-1
:CATEGORY: ONE-OFF
:RESULT: PENDING
:END:

- Requirements :: [[file:spec.org::#ac-008-13][AC008.13 - Installed use remains portable and documented]].
- Procedure :: Compare reader listing with the fixture catalogue and candidate
  guidance; inspect every mapping, selection example, licence/provenance and
  file/service distinction as specified in OT008.1.
- Bounds :: One local review per candidate, no external API or service activation.
- Expected :: *All installed readers accounted for and guidance matching the
  candidate's public behaviour*.

* Definition feasibility
:PROPERTIES:
:CUSTOM_ID: definition-feasibility
:END:

- Scope :: *Bounded design probes, not application implementation or RT/UT
  qualification*. Executed on 12 September 2026 with Pandoc 3.9.0.2 on macOS.
- Media probe :: Generated a valid 73-byte, one-pixel PNG using Go's image/png
  encoder in OS temporary storage. Created DOCX, ODT and EPUB fixtures containing
  that image as a data URI, with --sandbox and an isolated empty --data-dir.
- Conversion :: Read each fixture using a copy of the project's owned
  assets/pandoc/defaults.yaml (sandbox: true), the isolated data directory and
  a diagnostic owned Lua filter. The filter iterated pandoc.mediabag.items(),
  enforced one image/png item below 1024 bytes and wrote to a fixed owned
  exported.png path; it did not call fetch or extract-media.
- Observed :: *All three readers exported identical 73-byte PNG content*;
  cmp succeeded each time. Original and exported SHA-256 were
  =fcdbbd9628b73cc2c4191c294782f276e05b859847dd19c9f01b46057262e61d=.
  The corrected fixture produced no checksum warning.
- Earlier probe :: A manually supplied 68-byte PNG carried a CRC error. Its
  bytes were exported unchanged, but it was replaced with the encoder-generated
  fixture above before drawing a raster-validity conclusion.
- Limit :: This demonstrates *sandbox-compatible media-bag access and owned Lua
  export*, not the future renderer's media admission, confinement or complete
  platform behaviour. AC008.9 specifies a dependency error when export is unavailable.
- API reference :: [[https://pandoc.org/lua-filters.html#module-pandoc.mediabag][Pandoc media-bag API]].
- Language probe :: Compared every source-block language in the common-code
  table against =pandoc --list-highlight-languages=. *All non-plaintext names
  were present*; plaintext deliberately selects an unhighlighted block.

* Partial delivery checkpoint, 12 September 2026

- Baseline :: Approval checkpoint =fd4421f= (record delivery authority), followed
  by the implementation checkpoint containing this record, on macOS arm64 with
  Go 1.26.8 and Pandoc 3.9.0.2. Go caches were confined to OS temporary storage.
- Intended RED :: =go test ./internal/preview -run 'TestRT008_[2345]' -count=1=
  failed on the baseline: code/text/JSON inputs were refused by the extension
  check; explicit --from was refused by the option parser.
- Focused GREEN :: The same command passed after adding format selection,
  wrapper generation, original-payload restoration, token-preserving JSON
  formatting and the owned clipboard payload. Observed package duration:
  *10.734 seconds*.
- Evidence limit :: These cases exercise the CLI boundary with real Pandoc and
  captured browser handoff. They do not establish actual browser clipboard
  behaviour, complete mapping coverage, binary conversion or passive HTML.
- Broader regression result :: =go test ./internal/preview -count=1= *failed*
  after 38.364 seconds. RT002.2 passive-highlighting resource failure observed
  status 1, no browser opens and no cleanup timestamp. RT001.11 device-boundary
  testing then panicked when a directly constructed session reached format
  resolution with a nil catalogue. The run terminated; remaining package cases
  and the complete repository suite are unverified. Diagnose and reconcile these
  cases before continuing implementation.
- Remaining delivery :: Complete reader fixtures and mapping cases, binary
  snapshots/media and archive bounds, passive HTML/CSS/raster policy, graph and
  compatibility reconciliation, help/format documentation, static and security
  checks, and composite implementation audit. UT008.1 and OT008.1 remain pending.
- Continuation :: The native goal reached its *100,000-token budget* during
  delivery. W006 local service and W007 annotations have not been implemented
  under this invocation; their dependency and admission conditions remain.

* Resumed delivery checkpoint, 12 September 2026

- Environment :: macOS arm64, Go 1.26.8, Pandoc 3.9.0.2; Go and linter caches
  remain in the invocation's OS temporary directory. Native goal mode is off
  under the operator's instruction.
- Regression reconciliation :: The two initial failures were isolated to the
  fault adapter intercepting capability queries and the device-boundary fixture
  omitting the new format catalogue. Their focused run passed in 11.136 seconds.
  The next broader run reached only the superseded .txt rejection; that fixture
  now uses an unsupported suffix.
- Reader/resource RED :: The new catalogue initially exposed five hidden
  bibliography results, an unsupported inline-string spreadsheet fixture, three
  missing container images and absent archive admission. Native HTML tests
  independently failed on replaced layout and unvalidated raster headers.
- Reader/resource GREEN :: The reader, media and archive subset passed in
  *25.982 seconds*, covering every installed reader, DOCX/ODT/EPUB PNG bytes and
  the initial archive path cases. Shared-string XLSX passed independently.
- Native HTML GREEN :: Initial structure, passive styling, raster-signature and
  mixed-graph cases passed in *6.137 seconds*. Subsequent escaped-URL,
  metadata-event and large-data-image cases first failed as intended, then
  passed with the missing-export case in *1.506 seconds*.
- Broad regression evidence :: The preview package passed in *121.644 seconds*
  before the subsequent escaped-CSS, metadata and large-image corrections.
  That result is historical coverage, not the final full-suite result.
- Static evidence :: =go vet ./...= and the latest =make lint= passed. StyLua
  2.5.2 was provisioned only in OS temporary storage; its macOS arm64 release
  archive matched upstream SHA-256
  =92ff0889e16324801bc072692974bb67f8161e62010fc90f96c62a17f81f32c7=.
- Packaging RED :: The prefix-install test confirmed the new parser licence
  was missing. The installer now includes it; final packaging verification
  remains outstanding.
- Remaining :: Complete mapping and adversarial boundary coverage, current
  full race/static/security checks, reader/help guidance, implementation review
  and native qualification. *No implementation-gate or user-test result is
  inferred* from these development runs.

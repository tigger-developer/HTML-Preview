#+TITLE: Project Work Ledger
#+STARTUP: overview
#+TODO: TODO ACTIVE BLOCKED REVIEW | DONE ABANDONED
#+TYP_TODO: PENDING FAILING SUPERSEDED | HOLD ASSUMED_PASS
#+TAGS: feature defect maintenance migration legacy
#+OPTIONS: toc:2 num:nil

This is the project's canonical work and requirements index. Each fact has one
authoritative representation:

- *Current lifecycle state* :: The TODO keyword on the work-item headline.
- *Classification* :: One or more tags on that headline.
- *Stable metadata* :: The work item's property drawer.
- *Current definition* :: The linked =spec.org= file.
- *Evidence* :: The linked =audits.org= and =validation.org= files.
- *History* :: Git history and explicitly linked archived sources.

Do not repeat lifecycle state in a property, section, specification, or evidence
file. Work items remain under one heading when their state changes. Use one
monotonically increasing project-wide work number and never reuse an identifier,
including after abandonment, deletion, retirement, or archival.

* Ledger vocabulary
:PROPERTIES:
:VISIBILITY: folded
:END:

The file directives declare the permitted TODO states and standard tags. This
section defines their meanings.

** Lifecycle states

- =TODO= :: Defined but not started.
- =ACTIVE= :: Delivery is underway.
- =BLOCKED= :: Delivery cannot continue because of a named external dependency.
- =REVIEW= :: Human review, validation, or disposition is required.
- =DONE= :: Delivered, verified, and closed.
- =ABANDONED= :: Deliberately not delivered. Its identifiers remain reserved.

** Legacy requirement states

- =PENDING= :: The requirement or some required evidence remains unverified.
- =FAILING= :: Current evidence shows that the requirement does not hold.
- =SUPERSEDED= :: A named later requirement replaces or retires it.
- =HOLD= :: The requirement remains current and its recorded evidence passes.
- =ASSUMED_PASS= :: Migration inferred delivery under a recorded heuristic; it
  is not a contemporaneous test result.

** Standard tags

- =feature= :: New or changed product behaviour.
- =defect= :: Incorrect existing behaviour.
- =maintenance= :: Internal upkeep without intended product-behaviour change.
- =migration= :: Historical or framework migration work.
- =legacy= :: Material governed by an earlier delivery process. Apply it to the
  parent headline so the whole subtree inherits the classification.

Tags are non-exclusive classifications. Add a project-specific tag only when it
provides a durable cross-cutting category that the standard tags cannot express.

** Work-item structure

#+begin_example
** TODO WNNN - Concise descriptor :feature:
:PROPERTIES:
:CUSTOM_ID: w-nnn
:PRIORITY: P1
:SOURCE: operator
:CREATED: YYYY-MM-DD
:END:

- *Specification:* [[file:../specs/NNN-concise-descriptor/spec.org][WNNN - Concise descriptor]]
#+end_example

Use =OWNER= and =BRANCH= properties only when concurrent work needs explicit
coordination. Use =MIGRATION_DISPOSITION= only to preserve an immutable migration
classification; it is provenance, not current state.

* Work items

** REVIEW W099 - Additional Public Domains for Website Deployments :feature:migration:
:PROPERTIES:
:CUSTOM_ID: w-099
:LEGACY_ID: 001
:PRIORITY: P1
:SOURCE: docs/archive/sdlc-v2/specs/001-website-extra-domains/spec.md
:CREATED: 2026-09-03
:MIGRATION_DISPOSITION: unresolved
:END:

- *Specification:* [[file:archive/sdlc-v2/specs/001-website-extra-domains/spec.md][Additional Public Domains for Website Deployments]]
- *Migration evidence:* The specification remains Draft and its latest audit is FAIL; no delivery authorization or explicit abandonment is recorded.

** REVIEW W100 - Disposable Runpod GPU Lifecycle :feature:migration:
:PROPERTIES:
:CUSTOM_ID: w-100
:LEGACY_ID: 002
:PRIORITY: P1
:SOURCE: docs/archive/sdlc-v2/specs/002-runpod-gpu-lifecycle/spec.md
:CREATED: 2026-09-05
:MIGRATION_DISPOSITION: unresolved
:END:

- *Specification:* [[file:archive/sdlc-v2/specs/002-runpod-gpu-lifecycle/spec.md][Disposable Runpod GPU Lifecycle]]
- *Migration evidence:* The specification remains Draft and its latest audit is FAIL; no delivery authorization or explicit abandonment is recorded.

** REVIEW W101 - vps-list Runpod inventory :feature:
:PROPERTIES:
:CUSTOM_ID: w-101
:PRIORITY: P1
:SOURCE: operator BYPASS-GATE-7
:CREATED: 2026-09-07
:END:

- *Specification:* [[file:../specs/101-vps-list-runpod-inventory/spec.org][W101 - vps-list Runpod inventory]]
- *Evidence:* [[file:../specs/101-vps-list-runpod-inventory/audits.org][W101 - implementation audits]] and
  [[file:../specs/101-vps-list-runpod-inventory/validation.org][W101 - operator validation]].

* Legacy Acceptance Criteria (SDLC v1) :legacy:
:PROPERTIES:
:CUSTOM_ID: legacy-acceptance-criteria
:VISIBILITY: folded
:SOURCE_DATE: 2026-09-03
:END:

** Legacy ledger authority at migration

This file, =docs/ACs.org=, is the sole authority for requirements established
under the legacy ticket-led process, including current and superseded
requirements, provenance, and test traceability. Archived tickets, comments,
and =docs/ticket-migration.org= retain disposition, provenance, and rationale
only; they are not current requirement or AC authorities. Requirements
established or changed through SDLC v3 are governed by signed-off
=specs/*/spec.org= artefacts.


** Legacy status vocabulary at migration

- *HOLD:* The requirement remains current and its recorded evidence passes.
- *PENDING:* The requirement or some required evidence remains unverified.
- *FAILING:* Current evidence shows the requirement does not hold.
- *SUPERSEDED:* A named later requirement replaces or retires it.
- *ASSUMED PASS:* Migration inferred delivery under a named heuristic; this is not a contemporaneous test result.


The source ledger recorded this document as the canonical spec for criteria
introduced from 2026-05-20 onward. Pre-cutover criteria remained in their
originating issues until cited or migrated. Cutover: 2026-05-20. Last migrated:
AC98.1 - Pre-migration baseline: orderable Hetzner type/location validation,
from #98 on 2026-09-03. The source status key was: HOLD · PENDING · FAILING ·
SUPERSEDED.


** VPS provisioning (vps-create-nix)
:PROPERTIES:
:CUSTOM_ID: vps-provisioning-vps-create-nix
:END:
*** HOLD AC28.1 - vps-create-nix refuses to start a server when the resolved SSH_PUBLIC_KEY_PATH is not a readable file
:PROPERTIES:
:CUSTOM_ID: ac281---vps-create-nix-refuses-to-start-a-server-when-the-resolved-ssh_public_key_path-is-not-a-readable-file
:END:

**** Requirement

A run of vps-create-nix terminates with non-zero exit status before any Hetzner state-changing call when the operator's public key path (resolved from =--ssh-key-path=, =SSH_PUBLIC_KEY_PATH=, or the default =$HOME/.ssh/id_ed25519.pub=) does not point to a readable regular file.

**** Provenance

- *Introduced:* #28 (discovery, closed 2026-05-20)

**** Tests

- PASS RT-28.1 (resolve_ssh_key on missing path exits non-zero with diagnostic naming the path)

*** HOLD AC28.2 - .env is loaded as bash, not as a custom-parsed data file
:PROPERTIES:
:CUSTOM_ID: ac282---env-is-loaded-as-bash-not-as-a-custom-parsed-data-file
:END:

**** Requirement

Every production script that consumes =$REPO_ROOT/.env= loads it via bash =source=, so values in =.env= undergo bash shell expansion at load time (tilde-prefixed paths, =${VAR}= substitution, etc.). No production script in the repo references a custom =.env= parser or helper such as =lib/env_loader.sh= or =load_env_file=.

**** Provenance

- *Introduced:* #28 (discovery, closed 2026-05-20)

**** Tests

- PASS RT-28.2 (lint guard: each of the eight production scripts sources =$REPO_ROOT/.env= and none reference =env_loader.sh= or =load_env_file=)

*** HOLD AC28.3 - hcloud pre-flight failure aborts the run
:PROPERTIES:
:CUSTOM_ID: ac283---hcloud-pre-flight-failure-aborts-the-run
:END:

**** Requirement

When =hcloud_locations_for_type= cannot return data (hcloud API unreachable, authentication failure, etc.), =validate_type_location_combo= exits non-zero rather than warning and continuing. The pre-flight check is fatal, not advisory.

**** Provenance

- *Introduced:* #28 (discovery, closed 2026-05-20)

**** Tests

- PASS RT-28.3 (validate_type_location_combo with a stub hcloud that fails on =server-type list= returns non-zero)

*** HOLD AC51.1 - fresh caddy-hardened hosts have restic ready before hardening verification
:PROPERTIES:
:CUSTOM_ID: ac511---fresh-caddy-hardened-hosts-have-restic-ready-before-hardening-verification
:END:

**** Requirement

When =vps-create-nix= builds a new caddy-hardened NixOS host and the fleet restic secrets are present in the repo, that new host has restic backups installed and ready before the first hardening check runs.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)

**** Tests

- PASS RT-51.1 (Nix evaluation of the caddy-hardened host declares =restic-backups-hetzner.timer= when tracked restic secrets exist); PASS RT-51.2 (Nix evaluation declares the restic agenix secrets and makes =restic= plus =exodan-backup= available on PATH, with =hetzner-backup= retained as a compatibility alias); PASS RT-51.5 (Nix evaluation of a generated-host-shaped configuration keeps the requested hostname and declares the restic timer); PASS OT-51.1 (live fresh-host provisioning evidence captured from =theborg= provisioning log on 2026-06-17); PASS UT-51.1 (operator-accepted =theborg= provisioning log shows =restic_backup= passed before reboot)

*** HOLD AC51.2 - hosts without fleet restic do not pass restic verification
:PROPERTIES:
:CUSTOM_ID: ac512---hosts-without-fleet-restic-do-not-pass-restic-verification
:END:

**** Requirement

When a NixOS host is not configured to run fleet restic backups, the host does not pass the restic hardening check as if backups were working.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)

**** Tests

- PASS RT-51.3 (Nix evaluation of the base role does not declare =restic-backups-hetzner.timer=); PASS RT-51.11 (=fleet-verify --mode light <host> restic_backup= fixture distinguishes absent timer from absent agenix credentials and only passes when both timer and credentials are present)

*** PENDING AC51.3 - fleet restic uses the expected backup contract
:PROPERTIES:
:CUSTOM_ID: ac513---fleet-restic-uses-the-expected-backup-contract
:END:

**** Requirement

When fleet restic backups are enabled on a NixOS host, they use the expected schedule, retention policy, secret files, and host-specific snapshot tag.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)

**** Status qualification

- *Status qualification:* retention-pruning observation; the fixed schedule portion is superseded by AC81.5, fleet backup at 01:00 host-local with up to 45 minutes of spread
**** Tests

- +SUPERSEDED RT-51.4 (Nix evaluation declares =OnCalendar="*-*-* 05:00:00"=, =RandomizedDelaySec="1h"=, and =Persistent=true=) superseded by RT-81.15 under AC81.5+; PASS RT-51.6 (Nix evaluation declares =/run/agenix/restic-b2-env= and =/run/agenix/restic-password= with root/root =0400= ownership/mode intent); PASS RT-51.7 (Nix evaluation makes the =exodan-backup= wrapper and =hetzner-backup= compatibility alias available on PATH when secrets exist); PASS RT-51.8 (Nix evaluation declares =--host <config.networking.hostName>= for shared-repository host scoping); PASS RT-51.9 (Nix evaluation declares daily, weekly, monthly, and yearly keep policies); PASS UT-51.2 (=kepler452= timer is enabled/active and scheduled); PASS UT-51.3 (=kepler452= has both restic agenix files root-owned mode =0400=); PASS UT-51.4 (=kepler452= has =restic=, =exodan-backup=, and the =hetzner-backup= compatibility alias on PATH under =/run/current-system/sw/bin=); PASS UT-51.5 (=kepler452= snapshots are visible, latest =ac396d64= at =2026-06-17 05:11:30=); PASS UT-51.6 (=kepler452= recent backup status shows successful snapshot saves for 2026-06-15, 2026-06-16, and 2026-06-17); PASS UT-51.7 (=kepler452= snapshot listing is host-scoped to =kepler452=); PENDING UT-51.8 (retention pruning is observed once enough snapshot history exists)

*** HOLD AC51.4 - fresh provisioning verifies the host clone source before rebuild
:PROPERTIES:
:CUSTOM_ID: ac514---fresh-provisioning-verifies-the-host-clone-source-before-rebuild
:END:

**** Requirement

When =vps-create-nix= rebuilds a new host from =/opt/exodan=, the provisioning log identifies the source commit before hardening verification depends on that rebuild.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)

**** Tests

- PASS RT-51.10 (=HARDENING_ITEMS= includes =restic backup= and the expected total remains pinned); PASS RT-51.12 (=vps-create-nix= reports or verifies the host clone commit before =nixos-rebuild switch=, so stale source cannot silently masquerade as the expected generated host config); PASS UT-51.1 (operator-accepted =theborg= provisioning log shows fresh provisioning reached light-tier verification and =restic_backup= passed before reboot)

*** HOLD AC51.5 - the fleet backup set includes every deployed application's mutable state
:PROPERTIES:
:CUSTOM_ID: ac515---the-fleet-backup-set-includes-every-deployed-applications-mutable-state
:END:

**** Requirement

Given an application deployed to a NixOS fleet host, that application's state directory is inside the set of paths the fleet backup covers.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)
- *Backfilled:* 2026-08-23 under #86, which found that no deployed application's database or uploads had ever been backed up. AC51.1 through AC51.4 specify the schedule, retention, secrets, and snapshot tags, and say nothing about coverage, so a path list naming =/var/lib/private= while every app used =/var/lib/APP= satisfied all four.

**** Tests

- PASS RT-51.13 (Nix evaluation requires some declared backup path to cover a deployed application's state directory, expressed as a property rather than a literal list, so a future edit that drops app coverage fails)

*** HOLD AC51.6 - a scheduled fleet maintenance run is bounded
:PROPERTIES:
:CUSTOM_ID: ac516---a-scheduled-fleet-maintenance-run-is-bounded
:END:

**** Requirement

Given a scheduled fleet maintenance unit, a run that does not complete is terminated and recorded as failed rather than remaining active, so it cannot suppress its own timer.

**** Provenance

- *Introduced:* #51 (approved 2026-06-17)
- *Backfilled:* 2026-08-23 under #86. systemd does not trigger a timer whose service is still active. A backup stalled at =open repository= on 2026-08-20 and no backup ran for three days; no unit failed, so nothing reported it.

**** Status qualification

- *Status qualification:* , by declaration. Neither timeout has been demonstrated against a real hang.
**** Tests

- PASS RT-51.14 (Nix evaluation requires a start timeout on the backup service); PASS RT-51.15 (Nix evaluation requires a start timeout on the auto-upgrade service); PASS RT-51.16 (Nix evaluation requires a start timeout on the weekly prune service)

--------------

** Fleet host-health execution
:PROPERTIES:
:CUSTOM_ID: fleet-host-health-execution
:END:
*** PENDING AC90.1 - automatic NixOS upgrade runtime
:PROPERTIES:
:CUSTOM_ID: ac901---automatic-nixos-upgrade-runtime
:END:

**** Requirement

Given automatic NixOS upgrades are enabled, the upgrade service has a declarative runtime containing every program required for Git-over-SSH source synchronization and system activation, and completed work records success unless Git, Nix, or activation itself fails.

**** Provenance

- *Introduced:* #90 (closed 2026-08-25)

**** Status qualification

- *Status qualification:* regression remediation implemented under #95; live interoperability evidence pending
**** Tests

- PASS RT-90.1: Nix evaluation proves the upgrade unit exposes Git, OpenSSH, Nix, and nixos-rebuild in its service PATH.
- PASS RT-90.2: Nix evaluation proves the upgrade command retains the declared SSH remote workflow without ambient user configuration.
- PASS RT-90.12: subprocess execution proves a genuine Git synchronization failure remains terminal and suppresses activation.
- PASS RT-95.1: subprocess execution proves scheduled Git fetch/reset run as the declared checkout owner with explicit home and runtime PATH, while user-switch or Git failure remains terminal.
- PASS RT-95.2: Nix evaluation proves the checkout username/home and user-switching runtime are declared alongside Git, OpenSSH, Nix and nixos-rebuild.
- PASS OT-90.1: one authorized live upgrade on =kepler452= reaches Git-over-SSH and records the real terminal systemd outcome without authentication retry.
- PENDING OT-95.2: one scheduled upgrade followed by the standard operator rebuild proves both paths remain interoperable on the same live checkout.

*** HOLD AC90.2 - fleet-verifier runtime equivalence
:PROPERTIES:
:CUSTOM_ID: ac902---fleet-verifier-runtime-equivalence
:END:

**** Requirement

Given a supported host-local fleet-verifier entry point, its host observations use a declared Nix runtime dependency closure rather than an interactive caller PATH, and equivalent host state has an equivalent assessment across installed-command, service, and timer contexts.

**** Provenance

- *Introduced:* #90 (closed 2026-08-25)

**** Tests

- PASS RT-90.3: Nix evaluation accounts for every external program named by the verifier registry in the installed package or service runtime closure.
- PASS RT-90.4: subprocess tests execute the verifier with a minimal environment and prove undeclared ambient PATH entries cannot change the assessment.
- PASS OT-90.2: one authorized host comparison records equivalent installed-command and systemd-service assessments.

*** HOLD AC90.3 - optional host-service applicability
:PROPERTIES:
:CUSTOM_ID: ac903---optional-host-service-applicability
:END:

**** Requirement

Given Caddy, CrowdSec, or Caddy-specific confinement is enabled or disabled in evaluated Nix configuration, each corresponding verifier measure is respectively assessed or marked not applicable; an enabled component missing at runtime fails, while absent or malformed expectation data is unknown and non-zero.

**** Provenance

- *Introduced:* #90 (closed 2026-08-25)

**** Tests

- PASS RT-90.5: evaluated base-role configuration marks Caddy, CrowdSec, and Caddy-specific confinement not applicable.
- PASS RT-90.6: evaluated Caddy-role configuration marks all three measures applicable.
- PASS RT-90.7: Go fixtures cover enabled-and-healthy, enabled-but-missing, and disabled runtime states without inferring policy from command availability.
- PASS RT-90.8: missing, malformed, oversized, and unknown expectation fields are rejected as execution unknown with non-zero status.
- PASS RT-90.9: summary fixtures distinguish passed, failed, not-applicable, and unknown counts and details.
- PASS RT-90.13: evaluated configurations cover Caddy and CrowdSec enabled independently rather than only both disabled or both enabled.

*** HOLD AC90.4 - hourly hardening assessment and activation exclusion
:PROPERTIES:
:CUSTOM_ID: ac904---hourly-hardening-assessment-and-activation-exclusion
:END:

**** Requirement

Given a managed NixOS host, fleet hardening assessment is scheduled hourly with bounded jitter, persists across downtime, does not restart during operator activation, and leaves the latest completed outcome available to status consumers.

**** Provenance

- *Introduced:* #90 (closed 2026-08-25)

**** Tests

- PASS RT-90.10: Nix evaluation proves the hourly calendar, bounded jitter, persistence, and no-restart contract.
- PASS RT-90.11: Nix evaluation proves the timer targets the complete strict hostname-aware service command and the service leaves a systemd completion record.
- PASS RT-90.14: the wrapper-to-Go-to-OpenSSH operator path stops scheduled verification, restores it, and awaits a fresh verification only after successful activation.
- PASS RT-90.15: activation failure, timeout, or interruption restores the timer through a separately bounded cleanup context and suppresses post-activation verification.
- PASS RT-90.16: activation holds the declarative kernel lock and a persistent-timer request during that window is a clean verifier condition skip.
- PASS OT-90.3: an authorized =vps-rebuild kepler452= rebuilds and reloads Caddy while NixOS starts the persistent timer during activation, then completes with the explicitly awaited strict verifier healthy.

*Key:* PASS = passing= · PENDING = pending= · FAIL = failing= · SUPERSEDED = removed=

--------------

** Fleet status and operator health gates
:PROPERTIES:
:CUSTOM_ID: fleet-status-and-operator-health-gates
:END:
*** HOLD AC84.1 - complete fleet outcome report
:PROPERTIES:
:CUSTOM_ID: ac841---complete-fleet-outcome-report
:END:

**** Requirement

A single command reports, for every managed host, the state of each deployed app service, the last successful run of each app timer, and the last successful run of the host's restic backup, =nixos-upgrade=, and =fleet-verify=. Reported outcomes survive a host reboot, and a unit with no recoverable record is visibly distinct from one that has run and never succeeded.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Tests

- PASS RT-84.1: a stubbed fleet of two hosts renders one row per host with app, timer, and infra columns populated from collected data.
- PASS RT-84.2: a host whose collection fails is reported as unreachable rather than omitted.
- PASS RT-84.3: steady-state collection uses one remote invocation per host, never one per measure.
- PASS RT-84.29: a measure whose runtime record is absent recovers its last outcome from the host's persistent journal.
- PASS RT-84.30: a host whose measures all carry a runtime record is collected without the recovery invocation.
- PASS RT-84.31: a unit with no recoverable record renders distinctly from one that has run and never succeeded.

*** HOLD AC84.2 - timer-relative staleness thresholds
:PROPERTIES:
:CUSTOM_ID: ac842---timer-relative-staleness-thresholds
:END:

**** Requirement

The staleness threshold for a timer is derived from that timer's own cadence as reported by the host, rather than from a fixed period held by the monitor.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Tests

- PASS RT-84.4: a 5 minute interval timer last successful 20 minutes ago is failing, while a daily timer last successful 20 minutes ago is healthy.
- PASS RT-84.5: a timer whose cadence changes on the host changes its own threshold with no edit to the monitor.
- PASS RT-84.6: a timer that has never succeeded is failing regardless of cadence.

*** HOLD AC84.3 - severity controls exit status
:PROPERTIES:
:CUSTOM_ID: ac843---severity-controls-exit-status
:END:

**** Requirement

Failing measures set a non-zero exit status; degraded measures do not; measures that do not apply to a host set neither and are visibly distinct from both.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Tests

- PASS RT-84.7: a fleet containing one failing measure exits non-zero while still printing the complete report.
- PASS RT-84.8: a fleet whose worst measure is degraded exits zero.
- PASS RT-84.9: a non-NixOS host reports =nixos-upgrade= and =fleet-verify= as not applicable and does not affect the exit status.

*** SUPERSEDED AC84.4 - Superseded by AC91.3
:PROPERTIES:
:CUSTOM_ID: ac844---no_entry_sign-superseded-by-ac913
:END:

**** Requirement

+Given the fleet check reports a failing measure, =vps-create-nix= and =deploy-nix= refuse to act, before any provider, host, or repository state changes.+ Unhealthy interactive use may now proceed only after explicit acknowledgement; non-interactive use still fails closed.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Status qualification

- *Status qualification:* by AC91.3
**** Tests

- PASS RT-84.10: historical non-interactive refusal coverage remains active.
- PASS RT-84.12: historical refusal diagnostics coverage remains active.
- +SUPERSEDED RT-84.11: removed and superseded by RT-91.9.+

*** SUPERSEDED AC84.5 - Superseded by AC91.3
:PROPERTIES:
:CUSTOM_ID: ac845---no_entry_sign-superseded-by-ac913
:END:

**** Requirement

+Given the fleet check reports a failing measure, =vps-list= completes its listing and then exits non-zero.+ Inventory now completes before the shared interactive acknowledgement decision.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Status qualification

- *Status qualification:* by AC91.3
**** Tests

- +SUPERSEDED RT-84.13 and RT-84.14: removed and superseded by RT-91.11.+

*** SUPERSEDED AC84.6 - Superseded by AC91.3 and AC91.4
:PROPERTIES:
:CUSTOM_ID: ac846---no_entry_sign-superseded-by-ac913-and-ac914
:END:

**** Requirement

+Each of the three operator commands accepts a documented bypass that skips the fleet check, proceeds with the command's own work, and records that the check was skipped along with the measures that were failing.+ The universal bypass is removed.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Status qualification

- *Status qualification:* by AC91.3 and AC91.4
**** Tests

- +SUPERSEDED RT-84.15, RT-84.16, and RT-84.17: removed and superseded by RT-91.7, RT-91.10, RT-91.13, and RT-91.14.+

*** SUPERSEDED AC84.7 - Superseded by AC91.1 and AC91.2
:PROPERTIES:
:CUSTOM_ID: ac847---no_entry_sign-superseded-by-ac911-and-ac912
:END:

**** Requirement

+Operator credentials reach the tool through a bash-sourced environment rather than a custom parser, and a missing credential degrades the affected measure rather than failing the run.+ Bash still owns =.env=, while missing provider credentials are unknown and non-zero.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Status qualification

- *Status qualification:* by AC91.1 and AC91.2
**** Tests

- +SUPERSEDED RT-84.18, RT-84.19, and RT-84.20: removed and superseded by RT-91.3 through RT-91.6.+

*** HOLD AC84.8 - bounded scheduled Nix store garbage collection
:PROPERTIES:
:CUSTOM_ID: ac848---bounded-scheduled-nix-store-garbage-collection
:END:

**** Requirement

Managed NixOS hosts garbage-collect the Nix store on a schedule that is clear of the backup and auto-upgrade windows, retaining a bounded history.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Tests

- PASS RT-84.21: Nix evaluation declares a garbage collection timer whose retention bound and schedule are asserted exactly, and whose schedule does not overlap 01:00 or 03:00.
- PASS RT-84.22: the garbage collection unit does not restart during an operator rebuild.

*** SUPERSEDED AC84.9 - Superseded by AC90.4 and AC88.1
:PROPERTIES:
:CUSTOM_ID: ac849---no_entry_sign-superseded-by-ac904-and-ac881
:END:

**** Requirement

+Managed NixOS hosts run =fleet-verify --mode strict= daily, clear of the backup, upgrade, and garbage collection windows, and its result is readable by the fleet report.+ Verification is now hourly and latest completed outcomes remain readable.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Status qualification

- *Status qualification:* by AC90.4 and AC88.1
**** Tests

- +SUPERSEDED RT-84.23 and RT-84.24: removed and superseded by RT-90.10 and RT-90.11.+
- PASS RT-84.25: historical systemd status-source coverage remains active.

*** HOLD AC84.10 - inventory-free discovery
:PROPERTIES:
:CUSTOM_ID: ac8410---inventory-free-discovery
:END:

**** Requirement

The monitor holds no inventory: host, application, service, and timer names are discovered from fleet state and from the hosts themselves. The only unit names it carries are the fleet's own backup and system-upgrade units, and their absence on a managed NixOS host is a failing measure rather than a skipped one.

**** Provenance

- *Introduced:* #84 (closed 2026-08-23; approved 2026-08-27)

**** Tests

- PASS RT-84.26: a fixture fleet whose hosts, apps, and timers carry previously unseen names is reported in full without any change to the monitor.
- PASS RT-84.27: an app deployed with a new timer appears in the report on the next run, discovered rather than declared to the monitor.
- PASS RT-84.28: a managed NixOS host missing the fleet backup or system-upgrade unit reports that measure as failing, not as not applicable.

*** HOLD AC84.11 - bounded concurrent host collection
:PROPERTIES:
:CUSTOM_ID: ac8411---bounded-concurrent-host-collection
:END:

**** Requirement

Given operator-side fleet collection, host enquiries have bounded concurrency and one end-to-end deadline per host; the command waits for every host to return, fail, or time out, and presents the complete result in inventory order before returning control to the shell.

**** Provenance

- *Backfilled:* #84 on 2026-08-27 for the approved concurrent-collection correction.

**** Tests

- PASS RT-84.32: collection is concurrent, never exceeds four active hosts, waits for every host, and retains inventory order.
- PASS RT-84.33: the initial systemd enquiry and conditional journal recovery share one host deadline.
- PASS RT-84.34: a host deadline yields one visible unreachable-host result promptly.
- PASS RT-84.35: OpenSSH receives bounded connection and liveness settings without forcing IPv4 or IPv6.

*Key:* PASS = passing= · PENDING = pending= · FAIL = failing= · SUPERSEDED = removed=

--------------

*** HOLD AC88.1 - complete status with shared relative time
:PROPERTIES:
:CUSTOM_ID: ac881---complete-status-with-shared-relative-time
:END:

**** Requirement

Given managed-host status is displayed from the operator command or an interactive host login, the presentation identifies every deployed application, application timer, and applicable infrastructure measure with its own state, latest outcome, and the age of the latest completed cached hardening assessment whether successful or failed, using one shared relative-time vocabulary for host headings and measure cells.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)

**** Tests

- PASS RT-88.1: a generated structured manifest identifies every deployed application, timer, and applicable platform unit.
- PASS RT-88.2: rendering fixtures distinguish healthy, failing, degraded, not-applicable, unknown, and no-record states with per-measure marks.
- +SUPERSEDED RT-88.3: exact host-heading fixtures render latest-completed assessment at HH:MM, yesterday, and unavailable distinctly.+
- PASS RT-88.24: boundary-adjacent fixtures at 59s/1m, 59m/1h, 23h59m/24h, and 47h59m/48h prove the common relative-time library, host heading, and measure cell render =just now=, minutes, =today=, =yesterday=, and multiple days consistently, while a zero timestamp renders =status unavailable= in the heading without an independent valid-timestamp formatter.
- PASS RT-88.4: a healthy backup cell remains visibly healthy when upgrade or hardening assessment fails.

*** HOLD AC88.2 - exact declared application status identity
:PROPERTIES:
:CUSTOM_ID: ac882---exact-declared-application-status-identity
:END:

**** Requirement

Given an application is declared in host configuration, its status identity retains the declared application type and exact service unit; Go and Hugo applications therefore use their respective runtime and deployment units without monitor-owned application inventory.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)

**** Tests

- PASS RT-88.5: Nix evaluation derives application status entries from =services.deployedApps= and carries declared type and exact unit.
- PASS RT-88.6: Go and Hugo fixtures resolve respectively to =NAME.service= and =NAME-deploy.service=.
- PASS RT-88.7: a host with no deployed applications still carries its applicable platform measures.
- PASS RT-88.8: an application with a previously unseen name and timer appears without a Go source change.

*** HOLD AC88.3 - bounded, non-obstructing interactive login status
:PROPERTIES:
:CUSTOM_ID: ac883---bounded-non-obstructing-interactive-login-status
:END:

**** Requirement

Given an interactive SSH login to a managed host, host-local status appears only for the outermost interactive session and never obstructs the shell; internal collection failure is visible, local collection has a two-second hard deadline, and the session remains usable.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)
- *Result provenance:* the operator recorded the passing result through delivery roll-up #89; the criterion and test remain owned by #88.

**** Tests

- PASS RT-88.9: login-shell evaluation limits the greeting to outermost interactive SSH sessions.
- PASS RT-88.10: missing manifest, missing unit, and local collection failure leave the shell initialization successful.
- PASS RT-88.22: a stalled local collector is terminated at the two-second deadline and leaves shell initialization successful.
- PASS RT-88.11: non-interactive and nested-shell fixtures have no greeting output.
- PASS UT-88.1: the operator confirms the deployed login presentation is readable, unambiguous, and does not delay normal shell use.

*** HOLD AC88.4 - newest completed systemd outcome and explicit unknown state
:PROPERTIES:
:CUSTOM_ID: ac884---newest-completed-systemd-outcome-and-explicit-unknown-state
:END:

**** Requirement

Given systemd runtime state and persistent journal history, each scheduled measure reflects its newest completed outcome; genuine empty history remains distinct from journal access, transport, malformed-record, and timestamp failures.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)

**** Tests

- PASS RT-88.12: a newer failure supersedes an older success and a newer success supersedes an older failure.
- PASS RT-88.13: successful empty history becomes =no record=.
- PASS RT-88.14: journal permission, command, and transport failures become =unknown= with non-zero health.
- PASS RT-88.15: malformed JSON, invalid timestamp, oversized record, and unknown unit data become bounded =unknown= diagnostics.

*** HOLD AC88.5 - bounded explicit verifier refresh
:PROPERTIES:
:CUSTOM_ID: ac885---bounded-explicit-verifier-refresh
:END:

**** Requirement

Given explicit refresh mode, every reachable managed host has a newly completed fleet-verifier outcome before its refreshed status is displayed; unreachable, timed-out, and unsuccessful hosts remain visible and make the command non-zero, while ordinary status collection remains read-only.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)

**** Tests

- PASS RT-88.16: refresh subprocess fixtures use the documented systemd service entry point, wait for completion, and recollect once per host.
- PASS RT-88.17: bounded concurrent fixtures cover success, verifier failure, timeout, and unreachable hosts without omitting any result.
- PASS RT-88.18: ordinary mode fixtures contain no service-start operation.
- PASS RT-88.23: non-managed inventory entries remain visible but receive no service-start operation during refresh.
- PASS OT-88.1: one authorized =fleet-status --refresh= records a newly completed hardening timestamp on a real host.

*** HOLD AC88.6 - one shared Go status contract
:PROPERTIES:
:CUSTOM_ID: ac886---one-shared-go-status-contract
:END:

**** Requirement

Given operator-side and host-local presentations of the same collected state, both use one Go status contract for classification, outcome precedence, relative time, and diagnostic vocabulary, with no independent shell implementation of those rules.

**** Provenance

- *Introduced:* #88 (closed 2026-08-25)

**** Tests

- PASS RT-88.19: shared fixtures passed through fleet and host-local presenters retain identical classifications, timestamps, and diagnostic terms.
- PASS RT-88.20: Nix evaluation installs the same Go package used by the operator command and contains no generated shell status engine.
- PASS RT-88.21: Go package tests cover both presenters through public command boundaries.

*** HOLD AC91.1 - current Hetzner cloud-firewall health
:PROPERTIES:
:CUSTOM_ID: ac911---current-hetzner-cloud-firewall-health
:END:

**** Requirement

Given a managed Hetzner host, fleet status includes its cloud-firewall attachment as healthy only when at least one provider firewall is applied; detachment or attachments that are only pending fail; applied, pending, and detached observations carry their completed-enquiry time for relative presentation; and missing credentials, provider failure, ambiguous identity, and malformed provider data are visible unknown states with non-zero health.

**** Provenance

- *Introduced:* #91 (closed 2026-08-25)

**** Tests

- PASS RT-91.1: shared-client HTTP fixtures cover one applied firewall, pending-only attachments, no firewall, multiple applied attachments, provider error, malformed response, and ambiguous or absent server identity.
- PASS RT-91.2: fleet fixtures retain cloud state independently for multiple hosts and include exact host/measure diagnostics.
- PASS RT-91.3: missing and empty =HCLOUD_TOKEN= fixtures become unknown and non-zero rather than unchecked or healthy.
- PASS RT-91.16: deterministic-clock fixtures prove applied observations render =just now=, pending and detached observations render =failed just now=, and provider failures remain =unknown= without fabricated journal history.
- PASS OT-91.1: one guarded read-only provider enquiry records the actual firewall attachment for each managed Hetzner host.

*** HOLD AC91.2 - shell-owned operator environment loading
:PROPERTIES:
:CUSTOM_ID: ac912---shell-owned-operator-environment-loading
:END:

**** Requirement

Given the installed operator status entry point, shell configuration is sourced by its thin Bash wrapper before the Go command starts, values defined in the repository =.env= take precedence over the inherited environment while inherited values remain when the file or variable is absent, help and version need no environment loading, and no Go path interprets shell configuration syntax.

**** Provenance

- *Introduced:* #91 (closed 2026-08-25)

**** Tests

- PASS RT-91.4: wrapper subprocess fixtures cover quoted values, expansion, comments, missing =.env=, an =.env= value overriding an inherited token, and an inherited token retained when =.env= does not define it.
- PASS RT-91.5: help and version remain bounded and succeed without sourcing a synthetic =.env= that would fail or leave an observable side effect.
- PASS RT-91.6: installation fixtures place the wrapper at =fleet-status= and the Go executable at a distinct internal path without recursive invocation.

*** HOLD AC91.3 - explicit interactive acknowledgement and fail-closed automation
:PROPERTIES:
:CUSTOM_ID: ac913---explicit-interactive-acknowledgement-and-fail-closed-automation
:END:

**** Requirement

Given =vps-create-nix=, =deploy-nix=, or =vps-list= encounters unhealthy or unknown fleet state, a session with usable terminal input and diagnostic output requires explicit =y= after displaying every affected host and measure; empty, negative, or malformed answers abort, non-interactive use fails closed, and no universal bypass option is accepted.

**** Provenance

- *Introduced:* #91 (closed 2026-08-25)

**** Tests

- PASS RT-91.7: shared gate fixtures cover healthy, failing, unknown, both input-and-diagnostic TTYs, each single-descriptor TTY case, and fully non-interactive state with exact diagnostics.
- PASS RT-91.8: prompt fixtures cover =y=, =Y=, empty input, =n=, EOF, malformed input, and interrupted input.
- PASS RT-91.9: =vps-create-nix= refusal and acknowledgement both occur before provider mutation or prompting for provisioning inputs.
- PASS RT-91.10: =deploy-nix= refusal and acknowledgement both occur before generated files or host contact.
- PASS RT-91.11: =vps-list= prints complete live or cached inventory before its acknowledgement decision.
- PASS RT-91.12: each operator command rejects the former bypass option and documents the interactive contract.

*** HOLD AC91.4 - bounded deployment acknowledgement record
:PROPERTIES:
:CUSTOM_ID: ac914---bounded-deployment-acknowledgement-record
:END:

**** Requirement

Given an operator explicitly acknowledges unhealthy or unknown fleet state for a deployment, the generated deployment record identifies the acknowledgement time and every affected host and measure; healthy, aborted, and non-interactive-refused operations carry no false acknowledgement.

**** Provenance

- *Introduced:* #91 (closed 2026-08-25)

**** Tests

- PASS RT-91.13: acknowledged deployment fixtures persist the exact timestamp and complete failure/unknown set.
- PASS RT-91.14: healthy, negative-answer, EOF, and non-interactive-refusal fixtures persist no acknowledgement.
- PASS RT-91.15: multiple affected hosts and measures remain bounded and deterministic in the generated record.

*Key:* PASS = passing= · PENDING = pending= · FAIL = failing= · SUPERSEDED = removed=

--------------

** Active operator surface
:PROPERTIES:
:CUSTOM_ID: active-operator-surface
:END:
*** HOLD AC50.1 - general Ubuntu create and deploy entry points are absent
:PROPERTIES:
:CUSTOM_ID: ac501---general-ubuntu-create-and-deploy-entry-points-are-absent
:END:

**** Requirement

The active Exodan operator surface does not expose the general Ubuntu host creation or Ubuntu app deployment entry points.

**** Provenance

- *Introduced:* #50 (approved 2026-06-18)

**** Tests

- PASS RT-50.1 (install/help surface excludes =vps-create= and =deploy-app= while retaining current NixOS/shared commands); PASS RT-50.2 (active tests no longer assert =vps-template/create.sh= or =deploy-app.sh= as supported operator paths)

*** HOLD AC50.2 - Nextcloud and OS-agnostic fleet tooling remain available
:PROPERTIES:
:CUSTOM_ID: ac502---nextcloud-and-os-agnostic-fleet-tooling-remain-available
:END:

**** Requirement

While Ubuntu remains only as the =nc-tigger= exception, Nextcloud-specific tooling and OS-agnostic fleet tooling remain available.

**** Provenance

- *Introduced:* #50 (approved 2026-06-18)

**** Tests

- PASS RT-50.3 (active-tooling regression confirms =nc-aio=, =vps-destroy=, =vps-list=, =vps-create-nix=, =deploy-nix=, =cfdns=, =age-edit=, =cshost=, and =whatismyip= remain present); PASS RT-50.4 (=vps-destroy= remains available and is not described as Ubuntu-specific)

*** HOLD AC50.3 - active docs present NixOS by default and nc-tigger as the Ubuntu exception
:PROPERTIES:
:CUSTOM_ID: ac503---active-docs-present-nixos-by-default-and-nc-tigger-as-the-ubuntu-exception
:END:

**** Requirement

Active Exodan documentation presents NixOS as the default infrastructure model and documents =nc-tigger= as a live Ubuntu exception, not as a general supported Ubuntu path.

**** Provenance

- *Introduced:* #50 (approved 2026-06-18)

**** Tests

- PASS RT-50.5 (active docs do not present =vps-create= or =deploy-app= as supported workflows); PASS RT-50.6 (active docs identify =nc-tigger=/Nextcloud AIO as the remaining Ubuntu exception pending separate migration or maintenance decision); PASS UT-50.1 (operator reviewed the final install/help/documentation surface and confirmed no general Ubuntu workflow is presented as supported infrastructure)

--------------

** deploy-nix divergence guards
:PROPERTIES:
:CUSTOM_ID: deploy-nix-divergence-guards
:END:
*** HOLD AC30.1 - deploy-nix aborts when the exodan repo has uncommitted changes
:PROPERTIES:
:CUSTOM_ID: ac301---deploy-nix-aborts-when-the-exodan-repo-has-uncommitted-changes
:END:

**** Requirement

A run of deploy-nix exits non-zero before any SSH command to the host when =git status --porcelain= in the exodan repo root is non-empty.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.1 (validate_test.go: dirty-tree stub aborts with "uncommitted" in error)

*** HOLD AC30.2 - deploy-nix aborts when local HEAD is ahead of origin/master
:PROPERTIES:
:CUSTOM_ID: ac302---deploy-nix-aborts-when-local-head-is-ahead-of-originmaster
:END:

**** Requirement

When local HEAD's parent chain includes origin/master (operator has local commits not yet pushed), deploy-nix exits non-zero with a diagnostic naming "ahead" and instructing =git push=.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.2 (stub: origin/master is ancestor of HEAD; HEAD is not ancestor of origin/master)

*** HOLD AC30.3 - deploy-nix aborts when local HEAD is behind origin/master
:PROPERTIES:
:CUSTOM_ID: ac303---deploy-nix-aborts-when-local-head-is-behind-originmaster
:END:

**** Requirement

When origin/master's parent chain includes local HEAD (operator has not pulled), deploy-nix exits non-zero with a diagnostic naming "behind" and instructing =git pull --rebase=.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.3 (stub: HEAD is ancestor of origin/master; origin/master is not ancestor of HEAD)

*** HOLD AC30.4 - deploy-nix aborts when local HEAD and origin/master have diverged
:PROPERTIES:
:CUSTOM_ID: ac304---deploy-nix-aborts-when-local-head-and-originmaster-have-diverged
:END:

**** Requirement

When neither HEAD nor origin/master is an ancestor of the other (each has unique commits the other lacks), deploy-nix exits non-zero with a diagnostic naming "diverged".

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.4 (stub: neither ancestor check succeeds)

*** HOLD AC30.5 - deploy-nix proceeds when local HEAD == origin/master and the tree is clean
:PROPERTIES:
:CUSTOM_ID: ac305---deploy-nix-proceeds-when-local-head--originmaster-and-the-tree-is-clean
:END:

**** Requirement

The pre-flight check passes silently and the deploy continues into uploadBinary / applyNixConfig when the working tree is clean and HEAD resolves to the same SHA as origin/master.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.5 (stub: clean status, HEAD == origin/master returns nil error)

*** HOLD AC30.6 - the pre-flight check refreshes origin's remote-tracking ref before comparing
:PROPERTIES:
:CUSTOM_ID: ac306---the-pre-flight-check-refreshes-origins-remote-tracking-ref-before-comparing
:END:

**** Requirement

=git fetch origin master= is invoked before any =git rev-parse= comparison. A stale remote-tracking ref would otherwise mask real divergence.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.6 (stub records call order; asserts fetch index < first rev-parse index)

*** HOLD AC30.7 - a failed git fetch surfaces the underlying error
:PROPERTIES:
:CUSTOM_ID: ac307---a-failed-git-fetch-surfaces-the-underlying-error
:END:

**** Requirement

When =git fetch origin master= exits non-zero (network failure, auth failure, etc.), deploy-nix exits non-zero with the underlying error message visible to the operator.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS RT-30.7 (stub returns network-error from fetch; assertion that error message contains both "fetch" and the underlying text)

*** HOLD AC30.8 - the pre-flight guard runs before any SSH command to the host
:PROPERTIES:
:CUSTOM_ID: ac308---the-pre-flight-guard-runs-before-any-ssh-command-to-the-host
:END:

**** Requirement

When the divergence check aborts, no host state is touched. The call site is placed between =assertHostInHEAD= (local-only) and =uploadBinary= (first SSH-routing call) in =main.go=. A failing guard triggers =fatalf= -> =os.Exit(1)= before any later SSH-touching function executes.

**** Provenance

- *Introduced:* #30 (2026-05-21)

**** Tests

- PASS Structural - verified by code review of =deploy/deploy-nix/main.go= call-site placement (no RT; the property is design-level, not behaviour-level)

*** HOLD AC30.9 - make test exercises the deploy-nix Go test suite
:PROPERTIES:
:CUSTOM_ID: ac309---make-test-exercises-the-deploy-nix-go-test-suite
:END:

**** Requirement

The repo-level =make test= target invokes =go test ./...= against the =deploy/deploy-nix/= package after the bash regression suite. The Go RTs for #30 (RT-30.1 through RT-30.7) run as part of the same gate that the bash RTs do.

**** Provenance

- *Introduced:* #30 (backfilled 2026-05-21 - the gap was noticed during the /review pass and bunched into the same issue)

**** Status qualification

- *Status qualification:* (note: #38 broadened the =go test= invocation to cover both =cfdns= and =deploy-nix=; AC30.9's spirit is unchanged - deploy-nix's Go suite still runs under =make test=)
**** Tests

- PASS Behavioural - =make test= output now includes ===> go tests= followed by the =go test ./...= PASS line; if any Go RT regresses, =make test= fails

--------------

** Go module structure and test coverage (deploy/)
:PROPERTIES:
:CUSTOM_ID: go-module-structure-and-test-coverage-deploy
:END:
*** HOLD AC38.1 - deploy/ is a single Go module
:PROPERTIES:
:CUSTOM_ID: ac381---deploy-is-a-single-go-module
:END:

**** Requirement

A single =deploy/go.mod= exists at the =deploy/= root, declaring =module github.com/tigger-developer/exodan/deploy=. No =go.mod= files exist under =deploy/cfdns/= or =deploy/deploy-nix/=.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS RT-38.1 (deploy/go.mod present with unified module path), PASS RT-38.2 (no nested go.mod files under deploy/)

*** HOLD AC38.2 - cfdns has comprehensive Go test coverage
:PROPERTIES:
:CUSTOM_ID: ac382---cfdns-has-comprehensive-go-test-coverage
:END:

**** Requirement

The =cfdns= binary's public surface - =cfClient= methods (=findZone=, =listRecords=, =listAllRecords=, =createRecord=, =updateRecordProxied=, =deleteRecord=, =api()=), token loading (=loadToken= env-var/cwd-.env paths, =walkUpToRepoRoot=, =readTokenFromEnv= parsing including the literal-quote quirk), and command dispatchers (=cmdList=, =cmdAdd=, =cmdSet=, =cmdClear=, =cmdProxy=, =parseTypes=) - is covered by Go tests against =httptest.Server= mocks. The test suite locks current behaviour ahead of #39's lib refactor.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS RT-38.3 through RT-38.19 - 17 cfdns test functions, ~30 sub-tests in =deploy/cfdns/{api,token,commands}_test.go=

*** HOLD AC38.3 - deploy-nix builds and existing tests remain green under the unified module
:PROPERTIES:
:CUSTOM_ID: ac383---deploy-nix-builds-and-existing-tests-remain-green-under-the-unified-module
:END:

**** Requirement

The =deploy-nix= binary builds via =go build ./deploy-nix/...= from =deploy/=. All 7 pre-existing Go tests in =validate_test.go= and all 10 bash regression tests under =tests/regression/deploy-nix/= continue to pass after the module consolidation.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS RT-38.20 (build), PASS RT-38.21 (existing Go tests), PASS RT-38.22 (existing bash regression tests)

*** HOLD AC38.4 - deploy-nix's #39-refactor surfaces have happy + error path coverage
:PROPERTIES:
:CUSTOM_ID: ac384---deploy-nixs-39-refactor-surfaces-have-happy--error-path-coverage
:END:

**** Requirement

The four deploy-nix functions that the upcoming lib refactor (#39) will replace - =cfAPI=, =findZoneForDomain=, =loadYAML=, =loadEnvFile= - each have at least one happy-path test and one error-path test. The audit comment on #38 enumerates deferred non-critical functions for follow-up.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS RT-38.23a-d (happy paths), PASS RT-38.24a-d (error paths) - 10 new tests across =dns_test.go=, =config_test.go=, =main_test.go=; PASS OT-38.1 (audit comment posted)

*** HOLD AC38.5 - go test ./... from deploy/ is green
:PROPERTIES:
:CUSTOM_ID: ac385---go-test--from-deploy-is-green
:END:

**** Requirement

The aggregate test command =go test ./...= from =deploy/= exits 0 with all packages green.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS RT-38.25 (go test aggregate)

*** HOLD AC38.6 - make install resolves cfdns and deploy-nix from the unified module
:PROPERTIES:
:CUSTOM_ID: ac386---make-install-resolves-cfdns-and-deploy-nix-from-the-unified-module
:END:

**** Requirement

After =make install=, =~/.local/bin/cfdns= and =~/.local/bin/deploy-nix= resolve to executables built from =deploy/= and report version/help without error (cfdns supports =--version=; deploy-nix supports =--help=).

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS OT-38.2 (symlinks resolve under =deploy/=), PASS OT-38.3 (cfdns --version output is =cfdns 0.1.0=; deploy-nix --help prints usage)

*** HOLD AC38.7 - Live UT checklist exists for operator verification
:PROPERTIES:
:CUSTOM_ID: ac387---live-ut-checklist-exists-for-operator-verification
:END:

**** Requirement

The file =tests/manual/issue-38-uts.md= enumerates the manual one-shot verifications the operator runs against live Cloudflare and Hetzner state after merge: cfdns zone enumeration, throwaway TXT record round-trip, proxy toggle, deploy-nix smoke.

**** Provenance

- *Introduced:* #38 (closed 2026-05-27)

**** Tests

- PASS OT-38.4 (checklist file exists with the four items); PASS UT-38.1, PASS UT-38.2, PENDING UT-38.3 (operator skipped - pending if needed), PASS UT-38.4 - operator-verified during close ceremony

--------------

** Shared Go library and SDK wrappers (deploy/lib/)
:PROPERTIES:
:CUSTOM_ID: shared-go-library-and-sdk-wrappers-deploylib
:END:
*** HOLD AC39.1 - deploy/lib/ exposes the foundational orchestration helpers
:PROPERTIES:
:CUSTOM_ID: ac391---deploylib-exposes-the-foundational-orchestration-helpers
:END:

**** Requirement

The package =deploy/lib/= exports: =HTTP(ctx, method, url, headers, body)= returning =(status int, body []byte, err error)= atomically with =ErrNetwork= for transport failures; =ExtractMessage(body)= handling Cloudflare-shape (=.errors[0].message=) and Tailscale-shape (=.message=) error responses with safe fallback; =LoadYAML(path, out)= and =WriteYAML(path, value)= (atomic via tmp+rename, mode 0600); =LoadEnv(path)= returning =map[string]string= and =ApplyEnv(m)= setting os env without overwriting existing values; logging primitives =Info=/=Ok=/=Warn=/=Errortext= matching the =.qfuncs.sh= emoji-prefixed format.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.1 through RT-39.5 - one test function per helper in =deploy/lib/{http,apierror,yaml,env,log}_test.go=

*** HOLD AC39.2 - lib/hcloud wraps the official Hetzner Cloud Go SDK with fleet error conventions
:PROPERTIES:
:CUSTOM_ID: ac392---libhcloud-wraps-the-official-hetzner-cloud-go-sdk-with-fleet-error-conventions
:END:

**** Requirement

The package =deploy/lib/hcloud/= exports a =Client= type wrapping =github.com/hetznercloud/hcloud-go/v2=. =Client.DeleteServer(ctx, name)= returns =lib.ErrNotFound= when the server is absent, =lib.ErrAuth= on 401/403, and the underlying error otherwise.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.6 (happy path), PASS RT-39.7 (not-found), PASS RT-39.8 (auth error)

*** HOLD AC39.3 - lib/tailscale wraps the official Tailscale Go SDK with .name-segment matching
:PROPERTIES:
:CUSTOM_ID: ac393---libtailscale-wraps-the-official-tailscale-go-sdk-with-name-segment-matching
:END:

**** Requirement

The package =deploy/lib/tailscale/= exports a =Client= wrapping =tailscale.com/client/tailscale/v2=. =Client.FindDeviceByName(ctx, name)= matches by the unique first segment of =.Name= (e.g. =skys-edge-1= from =skys-edge-1.tail-scale.ts.net=), not =.Hostname= (which can repeat across sibling auto-numbered devices). =Client.DeleteDevice(ctx, deviceID)= returns =lib.ErrNotFound= and =lib.ErrAuth= appropriately.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.9 (delete happy), PASS RT-39.10 (not-found), PASS RT-39.11 (unique-segment match across siblings), PASS RT-39.12 (auth error)

*** HOLD AC39.4 - lib/github wraps go-github with title-based key deletion
:PROPERTIES:
:CUSTOM_ID: ac394---libgithub-wraps-go-github-with-title-based-key-deletion
:END:

**** Requirement

The package =deploy/lib/github/= exports a =Client= wrapping =github.com/google/go-github/v88= (Google-maintained, BSD-3, not officially endorsed by GitHub but the most-adopted Go binding for the REST API). =Client.DeleteUserKeyByTitle(ctx, title)= finds the authenticated user's SSH key whose title matches and deletes it; returns =lib.ErrNotFound= when no key matches, =lib.ErrAuth= on 401/403.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.13 (delete happy), PASS RT-39.14 (not-found), PASS RT-39.15 (auth error)

*** HOLD AC39.5 - cfdns uses lib helpers
:PROPERTIES:
:CUSTOM_ID: ac395---cfdns-uses-lib-helpers
:END:

**** Requirement

The =cfdns= source imports from =github.com/tigger-developer/exodan/deploy/lib=. Specifically: =api()= uses =lib.HTTP=; =readTokenFromEnv= uses =lib.LoadEnv=; logging helpers shim through to =lib.Info=/=Ok=/=Warn=. The cfdns-specific Cloudflare API client logic remains in cfdns (not moved to lib).

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.16 (cfdns source imports =github.com/tigger-developer/exodan/deploy/lib=), PASS RT-39.17 (cfdns smoke tests from #38 continue to pass against the refactored binary)

*** HOLD AC39.6 - deploy-nix uses lib helpers
:PROPERTIES:
:CUSTOM_ID: ac396---deploy-nix-uses-lib-helpers
:END:

**** Requirement

The =deploy-nix= source imports from =github.com/tigger-developer/exodan/deploy/lib=. Specifically: =cfAPI= uses =lib.HTTP=; =loadYAML= delegates to =lib.LoadYAML=; =loadEnvFile= delegates to =lib.LoadEnv= + =lib.ApplyEnv=; logging helpers shim through to =lib.Info=/=Ok=/=Warn=/=Errortext=. The deploy-nix-specific app-deployment logic remains in deploy-nix.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.18 (deploy-nix source imports =github.com/tigger-developer/exodan/deploy/lib=), PASS RT-39.19 (existing 7 Go tests in validate_test.go continue to pass), PASS RT-39.20 (existing 10 bash regression tests under =tests/regression/deploy-nix/= continue to pass)

*** HOLD AC39.7 - SDK versions pinned and verified
:PROPERTIES:
:CUSTOM_ID: ac397---sdk-versions-pinned-and-verified
:END:

**** Requirement

=deploy/go.mod= pins =hetznercloud/hcloud-go/v2= at v2.41.2+ (within v2.x), =tailscale.com/client/tailscale/v2= at v2.9.0+ (within v2.x), =google/go-github/v88= at v88.0.0+ (within v88.x). Each SDK wrapper's source ABOUTME comments cite the pkg.go.dev verification date (2026-05-27) and official-vs-third-party status.

**** Provenance

- *Introduced:* #39 (closed 2026-05-28)

**** Tests

- PASS RT-39.21 (=tests/regression/deploy/test_go_mod_pins.sh= checks =go list -m= against expected version prefixes), PASS OT-39.1 (ABOUTME comments inspected)

--------------

** Exodan repository identity
:PROPERTIES:
:CUSTOM_ID: exodan-repository-identity
:END:
*** HOLD AC41.1 - NixOS hosts use the Exodan repository and canonical host path
:PROPERTIES:
:CUSTOM_ID: ac411---nixos-hosts-use-the-exodan-repository-and-canonical-host-path
:END:

**** Requirement

Given a NixOS host managed by the fleet tooling, the host-side persistent repository clone is the Exodan repository at the canonical Exodan path rather than the legacy Hetzner repository slug or =/opt/hetzner= path.

**** Provenance

- *Introduced:* #41 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-41.1 (=test_create_nix_exodan_repo_path.sh= confirms =vps-create-nix= uses the Exodan GitHub remote and =/opt/exodan=), PASS RT-41.2 (=test_auto_upgrade_exodan_path.sh= confirms auto-upgrade rebuilds from =/opt/exodan=), PASS RT-41.3 (=TestRemoteNixosRebuildScript_UsesExodanHostPath_RT41_3= confirms deploy-nix rebuilds from =/opt/exodan=)

*** HOLD AC41.2 - Exodan tooling diagnostics use Exodan repo identity
:PROPERTIES:
:CUSTOM_ID: ac412---exodan-tooling-diagnostics-use-exodan-repo-identity
:END:

**** Requirement

Given the operator runs installed Exodan tooling from the current repository checkout, repo-root discovery and token fallback describe the repository as Exodan in user-facing diagnostics.

**** Provenance

- *Introduced:* #41 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-41.4 (deploy-nix repo-root discovery failure reports an Exodan repo-root diagnostic), PASS RT-41.5 (cfdns token fallback and repo-root diagnostics report Exodan as the repo-level =.env= fallback source), PASS RT-41.6 (deploy-nix dirty, ahead, behind, and diverged diagnostics name the Exodan repository)

*** HOLD AC41.3 - Active docs use Exodan repo identity
:PROPERTIES:
:CUSTOM_ID: ac413---active-docs-use-exodan-repo-identity
:END:

**** Requirement

Given user-facing documentation or generated project instructions describe the infrastructure repository, the repository is named Exodan and repo-local paths point at =~/code/exodan= or the installed command names instead of legacy =~/code/hetzner= paths.

**** Provenance

- *Introduced:* #41 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-41.7 (=test_exodan_active_docs.sh= confirms root docs and active project instructions use Exodan repo identity), PASS RT-41.8 (=test_exodan_active_docs.sh= and =test_install_symlink_targets_exist.sh= confirm deploy integration docs and install symlink targets no longer point at legacy paths or missing =deploy-hugo.sh=), PASS RT-41.9 (=test_exodan_active_docs.sh= confirms generated Hugo project instructions reference Exodan/deploy-nix)

*** HOLD AC41.4 - Provider and historical Hetzner identifiers are preserved
:PROPERTIES:
:CUSTOM_ID: ac414---provider-and-historical-hetzner-identifiers-are-preserved
:END:

**** Requirement

Given a reference identifies the cloud provider, hcloud CLI/API, official hcloud Go SDK, existing restic service/bucket names, historical issue links, or archived operational history, the project preserves the Hetzner name where it identifies that external provider, API package, existing service identifier, or historical fact.

**** Provenance

- *Introduced:* #41 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-41.10 (=test_provider_history_identifiers_preserved.sh= confirms =github.com/hetznercloud/hcloud-go/v2= remains in =deploy/go.mod=), PASS RT-41.11 (existing restic service identifiers remain intact), PASS RT-41.12 (historical Ubuntu runbook provenance remains intact)

--------------

** deploy-nix host app loading
:PROPERTIES:
:CUSTOM_ID: deploy-nix-host-app-loading
:END:
*** HOLD AC42.1 - Zero-app Nix hosts remain app-capable
:PROPERTIES:
:CUSTOM_ID: ac421---zero-app-nix-hosts-remain-app-capable
:END:

**** Requirement

Given a Nix host with no deployed app JSON files, the host configuration is app-capable and declares no app-specific services.

**** Provenance

- *Introduced:* #42 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-42.1 (fresh =vps-create-nix= host config imports the deployed-apps loader while no app JSON exists), PASS RT-42.2 (deployed-apps loader treats a missing apps directory as an empty app set)

*** HOLD AC42.2 - deploy-nix preserves existing host configuration while enabling app loading
:PROPERTIES:
:CUSTOM_ID: ac422---deploy-nix-preserves-existing-host-configuration-while-enabling-app-loading
:END:

**** Requirement

Given an existing host configuration with host-specific imports or settings, deploy-nix preserves the existing host configuration while making the deployed-apps loader available.

**** Provenance

- *Introduced:* #42 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-42.3 (existing simple =host.nix= gains the loader import without losing the hardened role import), PASS RT-42.4 (existing =host.nix= with additional host-specific settings retains those settings), PASS RT-42.5 (unsafe =host.nix= shapes fail before SSH with a diagnostic naming the missing loader import)

*** HOLD AC42.3 - Multiple app JSON files evaluate independently
:PROPERTIES:
:CUSTOM_ID: ac423---multiple-app-json-files-evaluate-independently
:END:

**** Requirement

Given multiple app JSON files for the same host, the evaluated Nix configuration contains independent app services, users, secrets, and Caddy snippets for each app.

**** Provenance

- *Introduced:* #42 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-42.6 (per-app JSON files map through the loader into independent systemd service names), PASS RT-42.7 (per-app Caddy snippets map into independent =/etc/caddy/sites/= entries), PASS RT-42.8 (per-app secret files map into independent agenix secret declarations)

*** HOLD AC42.4 - deploy-nix stops before remote side effects when the expected Go service is absent
:PROPERTIES:
:CUSTOM_ID: ac424---deploy-nix-stops-before-remote-side-effects-when-the-expected-go-service-is-absent
:END:

**** Requirement

Given a Go app deployment whose generated host configuration does not declare the expected app service, deploy-nix stops before remote deployment side effects.

**** Provenance

- *Introduced:* #42 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-42.9 (Nix evaluation errors fail local app-service validation with the expected service named), PASS RT-42.10 (missing expected Go service fails local app-service validation with a loader diagnostic)

*** HOLD AC42.5 - skys-edge loader repair was verified during migration
:PROPERTIES:
:CUSTOM_ID: ac425---skys-edge-loader-repair-was-verified-during-migration
:END:

**** Requirement

Given the existing skys-edge host config and generated writeback app files, the evaluated host configuration declares =writeback.service=.

**** Provenance

- *Introduced:* #42 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Status qualification

- *Status qualification:* for the migration verification; not a permanent host/app regression invariant
**** Tests

- PASS RT-42.11 (=skys-edge= host config imports the deployed-apps loader), PASS OT-42.12 (=skys-edge= Nix evaluation included the writeback Go service from =apps/writeback.json= during migration; reclassified from RT because it names one app on one host)

*** HOLD AC48.1 - deploy-nix preserves local data_dirs behaviour
:PROPERTIES:
:CUSTOM_ID: ac481---deploy-nix-preserves-local-data_dirs-behaviour
:END:

**** Requirement

Given an app config contains =data_dirs= entries without matching infra source definitions, =deploy-nix= treats those entries as repo-relative local directories and installs their contents at the same relative path under the app working directory.

**** Provenance

- *Introduced:* #48 (closed 2026-06-15)
- *Migrated:* 2026-06-15

**** Tests

- PASS RT-48.1 (local single-segment data dir resolves to a repo-relative upload plan with unchanged destination path), PASS RT-48.2 (local nested data dir resolves to a repo-relative upload plan with unchanged nested destination path)

*** HOLD AC48.2 - deploy-nix supports infra-sourced Git data dirs
:PROPERTIES:
:CUSTOM_ID: ac482---deploy-nix-supports-infra-sourced-git-data-dirs
:END:

**** Requirement

Given an infra app config defines a Git source for a declared data dir, =deploy-nix= installs the Git source contents at the declared data dir path without requiring the public app repo to contain the private source path.

**** Provenance

- *Introduced:* #48 (closed 2026-06-15)
- *Migrated:* 2026-06-15

**** Tests

- PASS RT-48.3 (Git source definition overrides local app-repo lookup for the same data dir id), PASS RT-48.4 (Git-sourced data dir planning succeeds when the app repo does not contain a matching local directory), PASS OT-48.1 (golink deploys to a NixOS host with =golink-resolvers= supplied from the private resolver repository)

*** HOLD AC48.3 - deploy-nix records Git data dir deployment metadata
:PROPERTIES:
:CUSTOM_ID: ac483---deploy-nix-records-git-data-dir-deployment-metadata
:END:

**** Requirement

Given a Git-sourced data dir is deployable, generated deployment state records the declared data dir path, source type, operator-provided ref, and resolved commit.

**** Provenance

- *Introduced:* #48 (closed 2026-06-15)
- *Migrated:* 2026-06-15

**** Tests

- PASS RT-48.5 (generated app JSON contains Git-sourced data dir metadata including requested ref and resolved commit), PASS RT-48.6 (CLI progress text names the Git-sourced data dir, requested ref, and resolved commit)

*** HOLD AC48.4 - deploy-nix rejects invalid data dir declarations before remote upload
:PROPERTIES:
:CUSTOM_ID: ac484---deploy-nix-rejects-invalid-data-dir-declarations-before-remote-upload
:END:

**** Requirement

Given a data dir entry is invalid, unsafe, missing, or inconsistent with infra source definitions, =deploy-nix= rejects the deployment before remote upload side effects.

**** Provenance

- *Introduced:* #48 (closed 2026-06-15)
- *Migrated:* 2026-06-15

**** Tests

- PASS RT-48.7 (absolute paths and parent-directory traversal in =data_dirs= are rejected before upload planning), PASS RT-48.8 (unknown data dir source types are rejected before upload planning), PASS RT-48.9 (a missing local data dir with no infra source is rejected before remote upload commands are prepared), PASS RT-48.10 (an infra source entry for an undeclared data dir is rejected before remote upload commands are prepared)

--------------

** NixOS app admin commands
:PROPERTIES:
:CUSTOM_ID: nixos-app-admin-commands
:END:
*** HOLD AC43.1 - Deployed apps expose an operator-safe admin CLI path
:PROPERTIES:
:CUSTOM_ID: ac431---deployed-apps-expose-an-operator-safe-admin-cli-path
:END:

**** Requirement

Given an app deployed through Exodan, members of the operator role have an operator-safe admin CLI execution path that runs approved app subcommands with the app's normal config, working directory, state access, and secrets environment, without broad operator read access to app state or secrets.

**** Provenance

- *Introduced:* #43 (closed 2026-06-10)
- *Migrated:* 2026-06-10

**** Tests

- PASS RT-43.1 (NixOS deployed-app configuration exposes an admin-command execution path for app subcommands), PASS RT-43.2 (the admin-command path runs as the app user rather than as the operator user), PASS RT-43.3 (the operator role has only the narrow permission needed to invoke the admin-command path), PASS RT-43.4 (the admin-command path supplies the deployed app runtime contract, including config, state, working directory, and secrets environment), PASS UT-43.1 (operator verified meet's helper can run a NixOS admin subcommand through the admin-command path on a deployed NixOS host)

--------------

** cfdns command-line contract
:PROPERTIES:
:CUSTOM_ID: cfdns-command-line-contract
:END:
*** HOLD AC59.1 - cfdns uses canonical domain-first positional syntax
:PROPERTIES:
:CUSTOM_ID: ac591---cfdns-uses-canonical-domain-first-positional-syntax
:END:

**** Requirement

Given a supported =cfdns= subcommand, its canonical positional syntax places the domain being operated on immediately after the subcommand, followed by the record type and any remaining operands.

**** Provenance

- *Introduced:* #59 (approved 2026-07-14)
- *Migrated:* 2026-07-14

**** Tests

- PASS RT-59.1 (the command entry points parse canonical domain-first forms for =list=, =add=, =set=, =clear=, and =proxy= and issue the expected Cloudflare requests), PASS RT-59.2 (the =copy= command entry point parses canonical =SOURCE TARGET TYPE= syntax and issues the expected Cloudflare requests)

*** HOLD AC59.2 - cfdns preserves deprecated type-first syntax during compatibility
:PROPERTIES:
:CUSTOM_ID: ac592---cfdns-preserves-deprecated-type-first-syntax-during-compatibility
:END:

**** Requirement

Given a valid invocation using the previous type-first syntax, =cfdns= preserves the requested operation and reports that the syntax is deprecated without changing the operation's Cloudflare semantics.

**** Provenance

- *Introduced:* #59 (approved 2026-07-14)
- *Migrated:* 2026-07-14

**** Tests

- PASS RT-59.3 (legacy forms for =add=, =set=, =clear=, and =proxy= execute against the expected domain and emit a deprecation warning), PASS RT-59.4 (legacy =TYPE SOURCE TARGET= copy syntax executes against the expected domains and emits a deprecation warning)

*** HOLD AC59.3 - cfdns rejects invalid arguments before Cloudflare requests
:PROPERTIES:
:CUSTOM_ID: ac593---cfdns-rejects-invalid-arguments-before-cloudflare-requests
:END:

**** Requirement

Given malformed, unsupported, excessive, or ambiguous command arguments, =cfdns= rejects the invocation locally before sending a Cloudflare request.

**** Provenance

- *Introduced:* #59 (approved 2026-07-14)
- *Migrated:* 2026-07-14

**** Tests

- PASS RT-59.5 (invalid type, wrong arity, unknown option, and ambiguous operand cases fail through the command entry points with zero HTTP requests)

*** HOLD AC59.4 - cfdns help and operator documentation state the canonical syntax
:PROPERTIES:
:CUSTOM_ID: ac594---cfdns-help-and-operator-documentation-state-the-canonical-syntax
:END:

**** Requirement

Given an operator requests =cfdns= help or consults the Exodan operator documentation, the canonical domain-first syntax and the temporary legacy compatibility behaviour are stated consistently.

**** Provenance

- *Introduced:* #59 (approved 2026-07-14)
- *Migrated:* 2026-07-14

**** Tests

- PASS RT-59.6 (CLI help output from every subcommand reports the canonical domain-first grammar), PASS UT-59.1 (the operator confirms the documented grammar is understandable and the deprecation warning is useful during interactive use)

--------------

** Regression runner temporary state
:PROPERTIES:
:CUSTOM_ID: regression-runner-temporary-state
:END:
*** SUPERSEDED AC65.1 - Regression runs use isolated, bounded temporary state - Cancelled by #66
:PROPERTIES:
:CUSTOM_ID: ac651---regression-runs-use-isolated-bounded-temporary-state---no_entry_sign-cancelled-by-66
:END:

**** Requirement

Given the regression runner executes any number of test files, its mutable captures are isolated to one unique operating-system temporary directory, retained output is bounded, and teardown removes only that directory after success, test failure, or handled interruption.

**** Provenance

- *Introduced:* #65 (closed 2026-07-16)
- *Migrated:* 2026-07-16

**** Status qualification

- *Status qualification:* CANCELLED; the #65 implementation and its architectural changes were reverted during #66 recovery. The criterion and test history are retained here as an audit record and are not requirements of the replacement implementation.
**** Tests

- PASS RT-65.1 (successful runner execution leaves no per-run capture directory), PASS RT-65.2 (failing runner execution prints the bounded failure evidence and leaves no per-run capture directory), PASS RT-65.3 (handled interruption terminates the active synthetic test and leaves no per-run capture directory), PASS RT-65.4 (output exceeding the documented limit fails visibly without retaining unbounded data), PASS RT-65.5 (an empty regression directory fails visibly instead of reporting a successful =0/0= suite)

--------------

** Migrated legacy acceptance criteria
:PROPERTIES:
:CUSTOM_ID: migrated-legacy-acceptance-criteria
:END:
These requirements were recovered from the immutable pre-SDLC v1 ticket archive. Original identifiers, wording, test relationships, provenance, and supersession history are retained.

*** ASSUMED_PASS AC1.1 - A =vps-template/= directory exists at the repo root containing all artefacts for VPS provisioning, with no scope leakage to other directories.
:PROPERTIES:
:CUSTOM_ID: ac1-1-a-vps-template-directory-exists-at-the-repo-root-containing-all-artefacts-for-vps-provisioning-with-no-scope-leakage-to-other-directories
:END:

**** Requirement

A =vps-template/= directory exists at the repo root containing all artefacts for VPS provisioning, with no scope leakage to other directories.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.1: =vps-template/= directory present; no =vps-template=-shaped files exist outside it

*** ASSUMED_PASS AC1.2 - After invoking =vps-template/create.sh= with valid inputs and prerequisites, a single new Hetzner Cloud server exists in the user's project, hardened per AC1.3--AC1.32, joined to the user's tailnet, and reachable via SSH only over Tailscale.
:PROPERTIES:
:CUSTOM_ID: ac1-2-after-invoking-vps-template-create-sh-with-valid-inputs-and-prerequisites-a-single-new-hetzner-cloud-server-exists-in-the-user-s-project-hardened-per-ac1-3-ac1-32-joined-to-the-user-s-tailnet-and-reachable-via-ssh-only-over-tailscale
:END:

**** Requirement

After invoking =vps-template/create.sh= with valid inputs and prerequisites, a single new Hetzner Cloud server exists in the user's project, hardened per AC1.3--AC1.32, joined to the user's tailnet, and reachable via SSH only over Tailscale.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.1: End-to-end smoke run --- create server, assert AC1.3--AC1.32 against it, teardown; ASSUMED PASS UT-1.1: Manual confirmation that the new server appears in the Hetzner Cloud Console under the user's project with the expected name and type

*** ASSUMED_PASS AC1.3 - The Hetzner Cloud firewall attached to the new server permits inbound TCP/443, UDP/443, and ICMP, and drops all other ingress including TCP/22.
:PROPERTIES:
:CUSTOM_ID: ac1-3-the-hetzner-cloud-firewall-attached-to-the-new-server-permits-inbound-tcp-443-udp-443-and-icmp-and-drops-all-other-ingress-including-tcp-22
:END:

**** Requirement

The Hetzner Cloud firewall attached to the new server permits inbound TCP/443, UDP/443, and ICMP, and drops all other ingress including TCP/22.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.2: External port probe from a non-tailnet machine --- TCP/443 connects, TCP/22 timed out, TCP/80 timed out, TCP/8080 timed out; PASS OT-1.3: External ICMP echo to public IP returns replies; PASS OT-1.4: External UDP/443 reaches the host (curl HTTP/3 against a temporary listener, or post-create ALPN probe)

*** ASSUMED_PASS AC1.4 - The new server has a non-root user account in the =wheel= and =sudo= groups with passwordless sudo, and root SSH login is disallowed.
:PROPERTIES:
:CUSTOM_ID: ac1-4-the-new-server-has-a-non-root-user-account-in-the-wheel-and-sudo-groups-with-passwordless-sudo-and-root-ssh-login-is-disallowed
:END:

**** Requirement

The new server has a non-root user account in the =wheel= and =sudo= groups with passwordless sudo, and root SSH login is disallowed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.5: SSH as the non-root user over tailnet succeeds and =sudo -n true= returns 0; PASS OT-1.6: SSH as =root= over tailnet is rejected by sshd

*** ASSUMED_PASS AC1.5 - sshd on the new server has =PermitRootLogin no= and =PasswordAuthentication no=, applied via daemon restart before the script exits.
:PROPERTIES:
:CUSTOM_ID: ac1-5-sshd-on-the-new-server-has-permitrootlogin-no-and-passwordauthentication-no-applied-via-daemon-restart-before-the-script-exits
:END:

**** Requirement

sshd on the new server has =PermitRootLogin no= and =PasswordAuthentication no=, applied via daemon restart before the script exits.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.7: =sshd -T= reports =permitrootlogin no= /and/ =passwordauthentication no=

*** ASSUMED_PASS AC1.6 - fail2ban is installed, enabled, and active, with a =jail.local= overriding =jail.conf= so future config changes survive package upgrades.
:PROPERTIES:
:CUSTOM_ID: ac1-6-fail2ban-is-installed-enabled-and-active-with-a-jail-local-overriding-jail-conf-so-future-config-changes-survive-package-upgrades
:END:

**** Requirement

fail2ban is installed, enabled, and active, with a =jail.local= overriding =jail.conf= so future config changes survive package upgrades.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.8: =systemctl is-active fail2ban= returns =active= and =/etc/fail2ban/jail.local= exists

*** ASSUMED_PASS AC1.7 - ufw is installed, enabled, default-deny inbound, and the allow-set matches the cloud firewall (defence in depth).
:PROPERTIES:
:CUSTOM_ID: ac1-7-ufw-is-installed-enabled-default-deny-inbound-and-the-allow-set-matches-the-cloud-firewall-defence-in-depth
:END:

**** Requirement

ufw is installed, enabled, default-deny inbound, and the allow-set matches the cloud firewall (defence in depth).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.9: =ufw status verbose= shows status active, default =deny (incoming)=, and allow rules for 443/tcp and 443/udp

*** ASSUMED_PASS AC1.8 - Tailscale is installed on the new server and the server has joined the user's tailnet.
:PROPERTIES:
:CUSTOM_ID: ac1-8-tailscale-is-installed-on-the-new-server-and-the-server-has-joined-the-user-s-tailnet
:END:

**** Requirement

Tailscale is installed on the new server and the server has joined the user's tailnet.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.10: =tailscale status= from a separate tailnet device shows the new server; ASSUMED PASS UT-1.2: Manual confirmation in the Tailscale admin console that the new server appears under the user's tailnet

*** ASSUMED_PASS AC1.9 - All secret values (Hetzner API token, Tailscale auth key, SSH public key path, etc.) are loaded from environment or a =.env= file at script-invocation time, and no secret value or password appears in any git-tracked file.
:PROPERTIES:
:CUSTOM_ID: ac1-9-all-secret-values-hetzner-api-token-tailscale-auth-key-ssh-public-key-path-etc-are-loaded-from-environment-or-a-env-file-at-script-invocation-time-and-no-secret-value-or-password-appears-in-any-git-tracked-file
:END:

**** Requirement

All secret values (Hetzner API token, Tailscale auth key, SSH public key path, etc.) are loaded from environment or a =.env= file at script-invocation time, and no secret value or password appears in any git-tracked file.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.2: =grep -r= for known secret patterns and password-shaped strings against tracked files returns no matches; PASS RT-1.3: =vps-template/.env.example= exists with placeholder values and documents every required variable

*** ASSUMED_PASS AC1.10 - The repo =.gitignore= excludes =.env= and any other secret-bearing files, such that =git status= is clean immediately after a successful script run.
:PROPERTIES:
:CUSTOM_ID: ac1-10-the-repo-gitignore-excludes-env-and-any-other-secret-bearing-files-such-that-git-status-is-clean-immediately-after-a-successful-script-run
:END:

**** Requirement

The repo =.gitignore= excludes =.env= and any other secret-bearing files, such that =git status= is clean immediately after a successful script run.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.4: =.gitignore= at repo root contains =.env= and matches secret-bearing patterns

*** ASSUMED_PASS AC1.11 - Re-invoking =create.sh= with a server name that already exists in the user's hcloud project exits non-zero without modifying any state.
:PROPERTIES:
:CUSTOM_ID: ac1-11-re-invoking-create-sh-with-a-server-name-that-already-exists-in-the-user-s-hcloud-project-exits-non-zero-without-modifying-any-state
:END:

**** Requirement

Re-invoking =create.sh= with a server name that already exists in the user's hcloud project exits non-zero without modifying any state.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.5: Unit test with mocked =hcloud server list= returning an existing name --- script exits non-zero with a clear message

*** ASSUMED_PASS AC1.12 - =vps-template/create.sh --help= documents all required and optional flags/env vars, prerequisites, and includes a copy-pasteable example invocation.
:PROPERTIES:
:CUSTOM_ID: ac1-12-vps-template-create-sh-help-documents-all-required-and-optional-flags-env-vars-prerequisites-and-includes-a-copy-pasteable-example-invocation
:END:

**** Requirement

=vps-template/create.sh --help= documents all required and optional flags/env vars, prerequisites, and includes a copy-pasteable example invocation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.6: =--help= exits 0 and output contains =Usage=, =Prerequisites=, and =Example= sections, plus every documented env var name

*** ASSUMED_PASS AC1.13 - All user-specific values (hostname, FQDN, IPs, usernames) in the cloud-init template are placeholders substituted at runtime; no user-specific data is hardcoded in tracked files.
:PROPERTIES:
:CUSTOM_ID: ac1-13-all-user-specific-values-hostname-fqdn-ips-usernames-in-the-cloud-init-template-are-placeholders-substituted-at-runtime-no-user-specific-data-is-hardcoded-in-tracked-files
:END:

**** Requirement

All user-specific values (hostname, FQDN, IPs, usernames) in the cloud-init template are placeholders substituted at runtime; no user-specific data is hardcoded in tracked files.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.7: Render the cloud-init template against a fixed fixture input set; rendered output matches a committed fixture file

*** ASSUMED_PASS AC1.14 - When =create.sh= is invoked from a TTY with required inputs missing, it prompts interactively for each missing input.
:PROPERTIES:
:CUSTOM_ID: ac1-14-when-create-sh-is-invoked-from-a-tty-with-required-inputs-missing-it-prompts-interactively-for-each-missing-input
:END:

**** Requirement

When =create.sh= is invoked from a TTY with required inputs missing, it prompts interactively for each missing input. When invoked without a TTY and with required inputs missing, it exits non-zero with a message naming the missing input. When all required inputs are provided via flags or env, no prompts occur.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.8: Piped invocation with missing required value exits non-zero with the variable name in the error; PASS RT-1.9: All values via env --- script reaches the dry-run/preflight phase without invoking any =read= call (verifiable by stubbing =read=); ASSUMED PASS UT-1.3: Interactive invocation from a real TTY prompts for each missing value in order

*** ASSUMED_PASS AC1.15 - The =create.sh= script sources =shell-and-scripting-helpers/.qfuncs.sh= and uses its functions (=info=, =warn=, =errortext=, =ok_confirm=) for all user-facing output and confirmation prompts; raw =echo=/=printf= are not used for status messages.
:PROPERTIES:
:CUSTOM_ID: ac1-15-the-create-sh-script-sources-shell-and-scripting-helpers-qfuncs-sh-and-uses-its-functions-info-warn-errortext-ok_confirm-for-all-user-facing-output-and-confirmation-prompts-raw-echo-printf-are-not-used-for-status-messages
:END:

**** Requirement

The =create.sh= script sources =shell-and-scripting-helpers/.qfuncs.sh= and uses its functions (=info=, =warn=, =errortext=, =ok_confirm=) for all user-facing output and confirmation prompts; raw =echo=/=printf= are not used for status messages.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.10: Source-level grep --- every status-message line in =create.sh= calls a qfuncs helper; no bare =echo=/=printf= outside the helpers themselves

*** ASSUMED_PASS AC1.16 - Every invocation of the =hcloud= CLI in =create.sh= is wrapped in =show_cmd_execute= so the literal command and its arguments are printed to stderr before execution.
:PROPERTIES:
:CUSTOM_ID: ac1-16-every-invocation-of-the-hcloud-cli-in-create-sh-is-wrapped-in-show_cmd_execute-so-the-literal-command-and-its-arguments-are-printed-to-stderr-before-execution
:END:

**** Requirement

Every invocation of the =hcloud= CLI in =create.sh= is wrapped in =show_cmd_execute= so the literal command and its arguments are printed to stderr before execution.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.11: Source-level grep --- every line containing =hcloud= is preceded by =show_cmd_execute= (or =scx= alias); no bare =hcloud= calls

*** ASSUMED_PASS AC1.17 - The new user account on the server has no usable password: the entry in =/etc/shadow= is locked (=!= or =*=), and no password was set, prompted for, or transmitted at any point during provisioning.
:PROPERTIES:
:CUSTOM_ID: ac1-17-the-new-user-account-on-the-server-has-no-usable-password-the-entry-in-etc-shadow-is-locked-or-and-no-password-was-set-prompted-for-or-transmitted-at-any-point-during-provisioning
:END:

**** Requirement

The new user account on the server has no usable password: the entry in =/etc/shadow= is locked (=!= or =*=), and no password was set, prompted for, or transmitted at any point during provisioning.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.11: =sudo passwd -S <user>= reports status =L= (locked) on the new server; PASS RT-1.12: =grep -r passwd= in =vps-template/= shows no password being set; cloud-init template contains no =passwd:= field for user creation

*** ASSUMED_PASS AC1.18 - =unattended-upgrades= is installed, enabled, and configured to automatically apply security updates on the new server.
:PROPERTIES:
:CUSTOM_ID: ac1-18-unattended-upgrades-is-installed-enabled-and-configured-to-automatically-apply-security-updates-on-the-new-server
:END:

**** Requirement

=unattended-upgrades= is installed, enabled, and configured to automatically apply security updates on the new server.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.12: =dpkg -s unattended-upgrades= shows installed; =/etc/apt/apt.conf.d/20auto-upgrades= enables =Update-Package-Lists= and =Unattended-Upgrade=; =systemctl is-enabled unattended-upgrades= returns =enabled=

*** ASSUMED_PASS AC1.19 - sshd_config on the new server restricts ciphers, MACs, and key-exchange algorithms to a modern allowlist (no legacy crypto).
:PROPERTIES:
:CUSTOM_ID: ac1-19-sshd_config-on-the-new-server-restricts-ciphers-macs-and-key-exchange-algorithms-to-a-modern-allowlist-no-legacy-crypto
:END:

**** Requirement

sshd_config on the new server restricts ciphers, MACs, and key-exchange algorithms to a modern allowlist (no legacy crypto).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.13: =sshd -T= reports =ciphers=, =macs=, and =kexalgorithms= lines containing only modern algorithms (e.g. =chacha20-poly1305=, =aes256-gcm=, =curve25519-sha256=, etc) and no legacy entries (e.g. no =cbc=, no =sha1=, no =diffie-hellman-group1=)

*** ASSUMED_PASS AC1.20 - Kernel hardening sysctl values are applied and persistent on the new server: =net.ipv4.conf.all.rp_filter=1=, =net.ipv4.tcp_syncookies=1=, =kernel.kptr_restrict=2=, =kernel.dmesg_restrict=1=, =net.ipv4.conf.all.accept_redirects=0=, =net.ipv6.conf.all.accept_redirects=0=.
:PROPERTIES:
:CUSTOM_ID: ac1-20-kernel-hardening-sysctl-values-are-applied-and-persistent-on-the-new-server-net-ipv4-conf-all-rp_filter1-net-ipv4-tcp_syncookies1-kernel-kptr_restrict2-kernel-dmesg_restrict1-net-ipv4-conf-all-accept_redirects0-net-ipv6-conf-all-accept_redirects0
:END:

**** Requirement

Kernel hardening sysctl values are applied and persistent on the new server: =net.ipv4.conf.all.rp_filter=1=, =net.ipv4.tcp_syncookies=1=, =kernel.kptr_restrict=2=, =kernel.dmesg_restrict=1=, =net.ipv4.conf.all.accept_redirects=0=, =net.ipv6.conf.all.accept_redirects=0=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.14: =sysctl -a= reports each named value matches the expected hardened setting; PASS OT-1.15: A file under =/etc/sysctl.d/= (e.g. =99-hardening.conf=) exists declaring these values so they survive reboot

*** ASSUMED_PASS AC1.21 - =chrony= is installed and active on the new server, and the system clock is synchronized to upstream NTP.
:PROPERTIES:
:CUSTOM_ID: ac1-21-chrony-is-installed-and-active-on-the-new-server-and-the-system-clock-is-synchronized-to-upstream-ntp
:END:

**** Requirement

=chrony= is installed and active on the new server, and the system clock is synchronized to upstream NTP.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.16: =systemctl is-active chrony= returns =active= and =chronyc tracking= reports a non-empty reference and a stratum < 16

*** ASSUMED_PASS AC1.22 - =/tmp= is mounted with =noexec=, =nosuid=, and =nodev= on the new server.
:PROPERTIES:
:CUSTOM_ID: ac1-22-tmp-is-mounted-with-noexec-nosuid-and-nodev-on-the-new-server
:END:

**** Requirement

=/tmp= is mounted with =noexec=, =nosuid=, and =nodev= on the new server.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.17: =findmnt /tmp= (or =mount=) reports all three options on =/tmp=

*** ASSUMED_PASS AC1.23 - journald is configured with a bounded =SystemMaxUse= (≤ 500M) so journal logs cannot fill the disk.
:PROPERTIES:
:CUSTOM_ID: ac1-23-journald-is-configured-with-a-bounded-systemmaxuse-500m-so-journal-logs-cannot-fill-the-disk
:END:

**** Requirement

journald is configured with a bounded =SystemMaxUse= (≤ 500M) so journal logs cannot fill the disk.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.18: =/etc/systemd/journald.conf.d/= (or =journald.conf=) sets =SystemMaxUse=500M= (or smaller) and =journalctl --disk-usage= reports a value within the bound

*** ASSUMED_PASS AC1.24 - Core dumps are disabled system-wide on the new server.
:PROPERTIES:
:CUSTOM_ID: ac1-24-core-dumps-are-disabled-system-wide-on-the-new-server
:END:

**** Requirement

Core dumps are disabled system-wide on the new server.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.19: =/etc/security/limits.d/= (or =limits.conf=) contains =* hard core 0=; =sysctl fs.suid_dumpable= returns 0; =ulimit -c= in a fresh shell returns 0

*** ASSUMED_PASS AC1.25 - AppArmor is loaded on the new server with at least one profile in enforce mode.
:PROPERTIES:
:CUSTOM_ID: ac1-25-apparmor-is-loaded-on-the-new-server-with-at-least-one-profile-in-enforce-mode
:END:

**** Requirement

AppArmor is loaded on the new server with at least one profile in enforce mode.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.20: =aa-status= reports the AppArmor module is loaded and at least one profile is in enforce mode

*** ASSUMED_PASS AC1.26 - A Lynis baseline report exists on the new server at a known path (e.g. =/var/log/lynis-baseline.json=) after cloud-init completes.
:PROPERTIES:
:CUSTOM_ID: ac1-26-a-lynis-baseline-report-exists-on-the-new-server-at-a-known-path-e-g-var-log-lynis-baseline-json-after-cloud-init-completes
:END:

**** Requirement

A Lynis baseline report exists on the new server at a known path (e.g. =/var/log/lynis-baseline.json=) after cloud-init completes.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.21: The baseline report file exists at the documented path and contains a non-zero =hardening_index=

*** ASSUMED_PASS AC1.27 - IPv6 is enabled on the new server and the server has at least one global IPv6 address bound to its public interface.
:PROPERTIES:
:CUSTOM_ID: ac1-27-ipv6-is-enabled-on-the-new-server-and-the-server-has-at-least-one-global-ipv6-address-bound-to-its-public-interface
:END:

**** Requirement

IPv6 is enabled on the new server and the server has at least one global IPv6 address bound to its public interface.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.22: =ip -6 addr show= reports a global scope address on the public interface (not just link-local)

*** ASSUMED_PASS AC1.28 - After successful provisioning, =create.sh= prints a final summary section to stdout containing (a) the server's vital stats --- server name, Hetzner server ID, type, image, location, public IPv4, public IPv6, tailnet hostname, tailnet IPv4 --- and (b) the status of every hardening measure applied by this issue, with one line per item indicating applied/verified/skipped: cloud firewall, sshd config (root login, password auth, ciphers), fail2ban, ufw, no-password user lock, unattended-upgrades, sysctl kernel hardening, chrony, /tmp mount hardening, journald limits, core dumps disabled, AppArmor enforcing, IPv6, Lynis hardening index, Tailscale enrolment.
:PROPERTIES:
:CUSTOM_ID: ac1-28-after-successful-provisioning-create-sh-prints-a-final-summary-section-to-stdout-containing-a-the-server-s-vital-stats-server-name-hetzner-server-id-type-image-location-public-ipv4-public-ipv6-tailnet-hostname-tailnet-ipv4-and-b-the-status-of-every-hardening-measure-applied-by-this-issue-with-one-line-per-item-indicating-applied-verified-skipped-cloud-firewall-sshd-config-root-login-password-auth-ciphers-fail2ban-ufw-no-password-user-lock-unattended-upgrades-sysctl-kernel-hardening-chrony-tmp-mount-hardening-journald-limits-core-dumps-disabled-apparmor-enforcing-ipv6-lynis-hardening-index-tailscale-enrolment
:END:

**** Requirement

After successful provisioning, =create.sh= prints a final summary section to stdout containing (a) the server's vital stats --- server name, Hetzner server ID, type, image, location, public IPv4, public IPv6, tailnet hostname, tailnet IPv4 --- and (b) the status of every hardening measure applied by this issue, with one line per item indicating applied/verified/skipped: cloud firewall, sshd config (root login, password auth, ciphers), fail2ban, ufw, no-password user lock, unattended-upgrades, sysctl kernel hardening, chrony, /tmp mount hardening, journald limits, core dumps disabled, AppArmor enforcing, IPv6, Lynis hardening index, Tailscale enrolment.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.23: After the smoke run, captured stdout contains a =Summary= section with each named vital stat present and non-empty; PASS OT-1.24: Captured stdout contains a hardening status line for each item enumerated in this AC, and each line indicates a positive status (applied/verified) for items that passed their respective ACs; PASS OT-1.25: The Lynis hardening index value reported in the summary equals the value in the persisted Lynis report file from AC1.26; PASS RT-1.13: Source-level grep --- =create.sh= contains a =print_summary= (or equivalently named) function invoked at the end of the success path

*** ASSUMED_PASS AC1.29 - A server name is a hard requirement with no default.
:PROPERTIES:
:CUSTOM_ID: ac1-29-a-server-name-is-a-hard-requirement-with-no-default
:END:

**** Requirement

A server name is a hard requirement with no default. If the script cannot resolve a server name from any input channel --- no =--name= flag, no =SERVER_NAME= environment variable, no =.env= setting, and (in interactive mode) no non-empty prompt response --- it exits non-zero with an error message identifying =name= as the missing required field, before any state-changing hcloud call is made.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.14: Non-interactive invocation (=</dev/null=) with no name in any input channel exits non-zero with the word =name= in the error message; PASS RT-1.15: Interactive invocation with an empty response to the name prompt exits non-zero with the same error; PASS RT-1.16: No =hcloud server create= invocation appears in the script's command trace before the name has been validated

*** ASSUMED_PASS AC1.30 - After successful provisioning, =create.sh= writes a per-server state file at =state/<server-name>.yaml= containing the server's vital stats (server name, Hetzner server ID, type, image, location, public IPv4, public IPv6, tailnet hostname, tailnet IPv4, user name, Lynis hardening index, hardening status).
:PROPERTIES:
:CUSTOM_ID: ac1-30-after-successful-provisioning-create-sh-writes-a-per-server-state-file-at-state-server-name-yaml-containing-the-server-s-vital-stats-server-name-hetzner-server-id-type-image-location-public-ipv4-public-ipv6-tailnet-hostname-tailnet-ipv4-user-name-lynis-hardening-index-hardening-status
:END:

**** Requirement

After successful provisioning, =create.sh= writes a per-server state file at =state/<server-name>.yaml= containing the server's vital stats (server name, Hetzner server ID, type, image, location, public IPv4, public IPv6, tailnet hostname, tailnet IPv4, user name, Lynis hardening index, hardening status). The state file is gitignored. /(Added post-Gate-1 to address bugs found during the OT-1.1 smoke run that escaped the original RT coverage.)/

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.18: =state_write= produces a YAML file with all named vital stats; =state_read= faithfully restores them; =state_delete= removes the file; PASS RT-1.19: A file under =state/= is excluded by =.gitignore= (=git check-ignore= reports it as ignored)

*** ASSUMED_PASS AC1.31 - When =create.sh --summary-only --name N= is invoked, the script reads =state/N.yaml=, populates the summary globals from it, and prints the summary block to stdout.
:PROPERTIES:
:CUSTOM_ID: ac1-31-when-create-sh-summary-only-name-n-is-invoked-the-script-reads-state-n-yaml-populates-the-summary-globals-from-it-and-prints-the-summary-block-to-stdout
:END:

**** Requirement

When =create.sh --summary-only --name N= is invoked, the script reads =state/N.yaml=, populates the summary globals from it, and prints the summary block to stdout. Validation steps that aren't relevant to summary mode (HCLOUD_LOCATION, TAILSCALE_AUTH_KEY) are skipped so the operator can re-print a summary without re-supplying create-time credentials. /(Added post-Gate-1.)/

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS OT-1.23: Smoke run captures =create.sh --summary-only= stdout; output contains the =Summary= section, the server name, and the public IPv4 from the state file; PASS OT-1.24: Same captured stdout contains hardening status lines for fail2ban, ufw, AppArmor, unattended-upgrades

*** ASSUMED_PASS AC1.32 - =load_env_file= parses common .env variants: plain =KEY=value=, =export KEY=value= (shell-sourceable form), single- and double-quoted values, comment lines, blank lines, indented comments.
:PROPERTIES:
:CUSTOM_ID: ac1-32-load_env_file-parses-common-env-variants-plain-keyvalue-export-keyvalue-shell-sourceable-form-single-and-double-quoted-values-comment-lines-blank-lines-indented-comments
:END:

**** Requirement

=load_env_file= parses common .env variants: plain =KEY=value=, =export KEY=value= (shell-sourceable form), single- and double-quoted values, comment lines, blank lines, indented comments. Values already set in the environment take precedence over .env file values (precedence rule). /(Added post-Gate-1 to cover the export-form parsing bug found when the operator's real .env used =export TAILSCALE_AUTH_KEY=...=.)/

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/1.md][#1 - Scriptable Hetzner VPS provisioning template (vps-template/)]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-1.17: Each variant produces the expected resolved value; existing env wins over .env; the original tskey-auth-shaped export form is exercised explicitly

*** HOLD AC3.1 - Deploy and teardown scripts contain no hardcoded references to any specific hostname, IP address, or host-specific filesystem path.
:PROPERTIES:
:CUSTOM_ID: ac3-1-deploy-and-teardown-scripts-contain-no-hardcoded-references-to-any-specific-hostname-ip-address-or-host-specific-filesystem-path
:END:

**** Requirement

Deploy and teardown scripts contain no hardcoded references to any specific hostname, IP address, or host-specific filesystem path. The target host is always supplied by the operator via CLI argument or environment variable.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.1: Grep all scripts under =deploy/= for literal hostnames (kepler-452, nc-tigger) and IPs --- none found; PENDING UT-3.2: Run =deploy-app --help= and =teardown-app --help= --- both show =--host= as required with no mention of a default host

*** HOLD AC3.2 - Each deployment target host is fully defined by its own configuration directory and its contents (Caddyfile, host-level settings), with no changes required to existing scripts, templates, or other hosts' directories when a new host is added.
:PROPERTIES:
:CUSTOM_ID: ac3-2-each-deployment-target-host-is-fully-defined-by-its-own-configuration-directory-and-its-contents-caddyfile-host-level-settings-with-no-changes-required-to-existing-scripts-templates-or-other-hosts-directories-when-a-new-host-is-added
:END:

**** Requirement

Each deployment target host is fully defined by its own configuration directory and its contents (Caddyfile, host-level settings), with no changes required to existing scripts, templates, or other hosts' directories when a new host is added.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.3: Create a mock =hosts/test-host/= with a Caddyfile --- no existing script or host directory is modified; PENDING UT-3.4: Inspect =hosts/kepler-452/= --- contains only host-specific artefacts (Caddyfile, README), no generic scripts

*** HOLD AC3.3 - App-specific artefacts (deps scripts, systemd overrides) are scoped to the app, not to a host.
:PROPERTIES:
:CUSTOM_ID: ac3-3-app-specific-artefacts-deps-scripts-systemd-overrides-are-scoped-to-the-app-not-to-a-host
:END:

**** Requirement

App-specific artefacts (deps scripts, systemd overrides) are scoped to the app, not to a host. The same =writeback.deps.sh= and =writeback.service.override= apply regardless of which host writeback is deployed to.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.5: Inspect =apps/writeback/= --- contains deps.sh and service.override with no host-specific references; PENDING UT-3.6: Confirm =deploy-app --host kepler-452 writeback= and =deploy-app --host nc-tigger writeback= both resolve deps from the same =apps/writeback/= path

*** HOLD AC3.4 - The operator's PATH-installed commands (=make install= symlinks) have names and =--help= output that make clear a target host must be specified.
:PROPERTIES:
:CUSTOM_ID: ac3-4-the-operator-s-path-installed-commands-make-install-symlinks-have-names-and-help-output-that-make-clear-a-target-host-must-be-specified
:END:

**** Requirement

The operator's PATH-installed commands (=make install= symlinks) have names and =--help= output that make clear a target host must be specified. No command assumes a default host that could lead to accidental operations against the wrong server.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.7: Run each installed command (=deploy-app=, =teardown-app=, =setup-caddy=) without =--host= --- each exits non-zero with an error naming =--host= as required; PENDING UT-3.8: Run each with =--help= --- output does not mention any default hostname

*** HOLD AC3.5 - The project-side deploy contract (=make deploy= interface documented in PROJECT-INTEGRATION.md) is unchanged when a new host is added --- the project Makefile requires no modification to target a different host beyond setting an environment variable or argument.
:PROPERTIES:
:CUSTOM_ID: ac3-5-the-project-side-deploy-contract-make-deploy-interface-documented-in-project-integration-md-is-unchanged-when-a-new-host-is-added-the-project-makefile-requires-no-modification-to-target-a-different-host-beyond-setting-an-environment-variable-or-argument
:END:

**** Requirement

The project-side deploy contract (=make deploy= interface documented in PROJECT-INTEGRATION.md) is unchanged when a new host is added --- the project Makefile requires no modification to target a different host beyond setting an environment variable or argument.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.9: Read PROJECT-INTEGRATION.md --- the Makefile template uses =DEPLOY_HOST= variable, not a hardcoded host; PENDING UT-3.10: Confirm =DEPLOY_HOST=kepler-452 make deploy= and =DEPLOY_HOST=nc-tigger make deploy= use the same Makefile target with no code change

*** HOLD AC3.6 - The repo's directory structure visually separates generic tools, per-host config, and per-app config so a reader can identify what belongs to what without reading file contents.
:PROPERTIES:
:CUSTOM_ID: ac3-6-the-repo-s-directory-structure-visually-separates-generic-tools-per-host-config-and-per-app-config-so-a-reader-can-identify-what-belongs-to-what-without-reading-file-contents
:END:

**** Requirement

The repo's directory structure visually separates generic tools, per-host config, and per-app config so a reader can identify what belongs to what without reading file contents.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.11: =ls= the repo root --- =deploy/=, =apps/=, =hosts/= are distinct top-level directories with self-explanatory names; PENDING UT-3.12: No file in =deploy/= is host-specific or app-specific; no file in =hosts/= is generic; no file in =apps/= is host-specific

*** HOLD AC3.7 - The deploy tooling provides a standard mechanism for apps to define periodic jobs (systemd timers).
:PROPERTIES:
:CUSTOM_ID: ac3-7-the-deploy-tooling-provides-a-standard-mechanism-for-apps-to-define-periodic-jobs-systemd-timers
:END:

**** Requirement

The deploy tooling provides a standard mechanism for apps to define periodic jobs (systemd timers). An app declares its timer schedule and command in its per-app config directory; the deploy script installs the corresponding systemd timer + service unit on the target host, sandboxed with the same hardening as the main service. Apps that don't need periodic jobs have no timer config and no timer is installed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.13: Deploy an app with a timer config --- =systemctl list-timers= on the host shows the timer active; PENDING UT-3.14: Deploy an app without a timer config --- no timer unit exists for that app on the host; PENDING UT-3.15: Inspect the installed timer's service unit --- it has the same sandboxing directives as the main service (DynamicUser, ProtectSystem, etc)

*** HOLD AC3.8 - The VPS provisioning script (=vps-create=) prompts for location before server type, and the server type prompt shows only types currently orderable in the chosen location, defaulting to the cheapest available.
:PROPERTIES:
:CUSTOM_ID: ac3-8-the-vps-provisioning-script-vps-create-prompts-for-location-before-server-type-and-the-server-type-prompt-shows-only-types-currently-orderable-in-the-chosen-location-defaulting-to-the-cheapest-available
:END:

**** Requirement

The VPS provisioning script (=vps-create=) prompts for location before server type, and the server type prompt shows only types currently orderable in the chosen location, defaulting to the cheapest available. The operator picks from what's actually available rather than discovering availability constraints after the fact.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/3.md][#3 - Restructure deploy tooling: separate generic scripts from per-host and per-app config]]

**** Tests

- PENDING UT-3.16: Run =vps-create= interactively --- location prompt appears before server type prompt; PENDING UT-3.17: After selecting a location (e.g. nbg1), the type prompt lists only types available there and the default shown in parentheses is the cheapest; PENDING UT-3.18: After selecting a US-only location (e.g. hil), the type list includes cpx11; after selecting an EU location, it does not

*** ASSUMED_PASS AC4.1 - The script validates all preconditions before any state-changing operation: missing tools (=hugo=/=gh=/=git=/=jq=/=curl=), unauthenticated =gh=, unset =CLOUDFLARE_API_TOKEN=, malformed =https://= base URL, and absent Cloudflare zone each cause an immediate non-zero exit with an error naming the missing requirement.
:PROPERTIES:
:CUSTOM_ID: ac4-1-the-script-validates-all-preconditions-before-any-state-changing-operation-missing-tools-hugo-gh-git-jq-curl-unauthenticated-gh-unset-cloudflare_api_token-malformed-https-base-url-and-absent-cloudflare-zone-each-cause-an-immediate-non-zero-exit-with-an-error-naming-the-missing-requirement
:END:

**** Requirement

The script validates all preconditions before any state-changing operation: missing tools (=hugo=/=gh=/=git=/=jq=/=curl=), unauthenticated =gh=, unset =CLOUDFLARE_API_TOKEN=, malformed =https://= base URL, and absent Cloudflare zone each cause an immediate non-zero exit with an error naming the missing requirement.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.1: Each of the five missing tools produces a non-zero exit and an error naming that tool (parametrised); PENDING RT-4.2: Unauthenticated =gh= exits non-zero citing =gh= authentication; PENDING RT-4.3: Unset =CLOUDFLARE_API_TOKEN= exits non-zero naming the variable; PENDING RT-4.4: Malformed base URLs (=http://x.com/=, =not-a-url=, =https://=) exit non-zero; =https://example.com/= passes; PENDING RT-4.5: Cloudflare API returning no matching zone exits non-zero naming the apex

*** ASSUMED_PASS AC4.2 - The required inputs (site name, base URL, copyright) are gathered via interactive prompts when not supplied as flags; empty or whitespace-only input at a required prompt re-prompts or rejects rather than accepts.
:PROPERTIES:
:CUSTOM_ID: ac4-2-the-required-inputs-site-name-base-url-copyright-are-gathered-via-interactive-prompts-when-not-supplied-as-flags-empty-or-whitespace-only-input-at-a-required-prompt-re-prompts-or-rejects-rather-than-accepts
:END:

**** Requirement

The required inputs (site name, base URL, copyright) are gathered via interactive prompts when not supplied as flags; empty or whitespace-only input at a required prompt re-prompts or rejects rather than accepts.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.6: Invocation with no input flags shows three sequential prompts for name, base URL, copyright; PENDING RT-4.7: Empty-string input at a required prompt does not proceed (either re-prompts or rejects)

*** ASSUMED_PASS AC4.3 - The theme input accepts three forms: bare token (resolved via known-theme lookup, default =tadg_ie=), =org/repo= (expanded to a GitHub git URL), and full URL containing =://= or =git@= (passed through to =git submodule add=).
:PROPERTIES:
:CUSTOM_ID: ac4-3-the-theme-input-accepts-three-forms-bare-token-resolved-via-known-theme-lookup-default-tadg_ie-org-repo-expanded-to-a-github-git-url-and-full-url-containing-or-git-passed-through-to-git-submodule-add
:END:

**** Requirement

The theme input accepts three forms: bare token (resolved via known-theme lookup, default =tadg_ie=), =org/repo= (expanded to a GitHub git URL), and full URL containing =://= or =git@= (passed through to =git submodule add=).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.8: Bare token =tadg_ie= resolves to the known-theme git URL; PENDING RT-4.9: =someone/footheme= expands to the corresponding GitHub git URL; PENDING RT-4.10: =https://github.com/someone/footheme.git= and =git@github.com:someone/footheme.git= pass through unchanged

*** ASSUMED_PASS AC4.4 - The script delegates Hugo skeleton generation to =hugo new site= and aborts if that delegation fails.
:PROPERTIES:
:CUSTOM_ID: ac4-4-the-script-delegates-hugo-skeleton-generation-to-hugo-new-site-and-aborts-if-that-delegation-fails
:END:

**** Requirement

The script delegates Hugo skeleton generation to =hugo new site= and aborts if that delegation fails.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.11: Stub =hugo new site= is invoked exactly once during scaffolding; PENDING RT-4.12: Stub =hugo new site= exiting non-zero causes the script to abort non-zero

*** ASSUMED_PASS AC4.5 - After scaffolding, the project contains the canonical layout: =hugo.yaml= (with operator's =baseURL=/=title=/=copyright= plus the omni/tadg.ie defaults), =.github/workflows/hugo.yml= (valid YAML using =actions/deploy-pages@v4= and triggered on push to =master=), =static/CNAME= (exactly the host, no surrounding whitespace), =Makefile= (with =serve=/=build=/=test= targets), =README.md= (interpolating site name and base URL), =content/_index.md= (title in front matter, body matching the blurb or empty), =.gitignore= (covering =public/=, =resources/=, =.hugo_build.lock=), and the theme as a git submodule under =themes/= referenced by =hugo.yaml='s =theme:= key.
:PROPERTIES:
:CUSTOM_ID: ac4-5-after-scaffolding-the-project-contains-the-canonical-layout-hugo-yaml-with-operator-s-baseurl-title-copyright-plus-the-omni-tadg-ie-defaults-github-workflows-hugo-yml-valid-yaml-using-actions-deploy-pages-v4-and-triggered-on-push-to-master-static-cname-exactly-the-host-no-surrounding-whitespace-makefile-with-serve-build-test-targets-readme-md-interpolating-site-name-and-base-url-content-_index-md-title-in-front-matter-body-matching-the-blurb-or-empty-gitignore-covering-public-resources-hugo_build-lock-and-the-theme-as-a-git-submodule-under-themes-referenced-by-hugo-yaml-s-theme-key
:END:

**** Requirement

After scaffolding, the project contains the canonical layout: =hugo.yaml= (with operator's =baseURL=/=title=/=copyright= plus the omni/tadg.ie defaults), =.github/workflows/hugo.yml= (valid YAML using =actions/deploy-pages@v4= and triggered on push to =master=), =static/CNAME= (exactly the host, no surrounding whitespace), =Makefile= (with =serve=/=build=/=test= targets), =README.md= (interpolating site name and base URL), =content/_index.md= (title in front matter, body matching the blurb or empty), =.gitignore= (covering =public/=, =resources/=, =.hugo_build.lock=), and the theme as a git submodule under =themes/= referenced by =hugo.yaml='s =theme:= key.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.13: =hugo.yaml= contains operator-supplied =baseURL=, =title=, =copyright=; PENDING RT-4.14: =.github/workflows/hugo.yml= parses as valid YAML, uses =actions/deploy-pages@v4=, triggers on push to =master=; PENDING RT-4.15: =static/CNAME= equals the host with no surrounding whitespace; PENDING RT-4.16: =Makefile= has =serve=, =build=, =test= targets; =test= references only =tests/regression/=; PENDING RT-4.17: =README.md= contains site name and base URL; PENDING RT-4.18: =content/_index.md= body matches the blurb when supplied, is empty otherwise; PENDING RT-4.19: =.gitignore= covers =public/=, =resources/=, =.hugo_build.lock=; PENDING RT-4.20: =themes/<theme>/= submodule is registered in =.gitmodules= and referenced by =hugo.yaml= =theme:=

*** ASSUMED_PASS AC4.6 - The target directory is a git repository whose existing HEAD is preserved across script reruns; the local default branch is =master= (matching the workflow trigger).
:PROPERTIES:
:CUSTOM_ID: ac4-6-the-target-directory-is-a-git-repository-whose-existing-head-is-preserved-across-script-reruns-the-local-default-branch-is-master-matching-the-workflow-trigger
:END:

**** Requirement

The target directory is a git repository whose existing HEAD is preserved across script reruns; the local default branch is =master= (matching the workflow trigger).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.21: First invocation against a non-git directory initializes the repo with =master= as the branch and creates an initial commit; PENDING RT-4.22: Second invocation preserves the existing HEAD commit hash

*** ASSUMED_PASS AC4.7 - A private GitHub repository exists under the configured owner; its GitHub Pages configuration has source set to "GitHub Actions", custom domain set to the operator-supplied host, and HTTPS enforcement enabled.
:PROPERTIES:
:CUSTOM_ID: ac4-7-a-private-github-repository-exists-under-the-configured-owner-its-github-pages-configuration-has-source-set-to-github-actions-custom-domain-set-to-the-operator-supplied-host-and-https-enforcement-enabled
:END:

**** Requirement

A private GitHub repository exists under the configured owner; its GitHub Pages configuration has source set to "GitHub Actions", custom domain set to the operator-supplied host, and HTTPS enforcement enabled. Pre-existing Pages configuration naming a different custom domain is a hard fail.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.23: Missing repo triggers =gh repo create --private= (verified via stub call log); present repo skips it; PENDING RT-4.24: Pages source, custom domain, and HTTPS enforcement are each set when absent, left untouched when already correct; PENDING RT-4.25: Pages configured with a different custom domain exits non-zero with an error naming both domains

*** ASSUMED_PASS AC4.8 - Cloudflare DNS for the host matches the GitHub Pages targets: four A + four AAAA records for an apex, or a single unproxied CNAME to =<repo-owner>.github.io= for a subdomain.
:PROPERTIES:
:CUSTOM_ID: ac4-8-cloudflare-dns-for-the-host-matches-the-github-pages-targets-four-a-four-aaaa-records-for-an-apex-or-a-single-unproxied-cname-to-repo-owner-github-io-for-a-subdomain
:END:

**** Requirement

Cloudflare DNS for the host matches the GitHub Pages targets: four A + four AAAA records for an apex, or a single unproxied CNAME to =<repo-owner>.github.io= for a subdomain. Correct records produce no writes; conflicting records are not overwritten without operator confirmation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.26: Apex with no records creates exactly the four A + four AAAA records (GH Pages IPs), unproxied; PENDING RT-4.27: Subdomain with no records creates exactly one unproxied CNAME to =<owner>.github.io=; PENDING RT-4.28: Records already correct (apex or subdomain) produce zero Cloudflare write operations; PENDING RT-4.29: Conflicting records trigger a confirm prompt; negative response exits non-zero with zero writes; positive response issues the overwrite

*** ASSUMED_PASS AC4.9 - Pre-existing =hugo.yaml= with values differing from the operator's inputs is not overwritten without explicit operator confirmation.
:PROPERTIES:
:CUSTOM_ID: ac4-9-pre-existing-hugo-yaml-with-values-differing-from-the-operator-s-inputs-is-not-overwritten-without-explicit-operator-confirmation
:END:

**** Requirement

Pre-existing =hugo.yaml= with values differing from the operator's inputs is not overwritten without explicit operator confirmation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.30: Differing existing =hugo.yaml= triggers a confirm prompt; negative response leaves the file byte-identical and exits non-zero

*** ASSUMED_PASS AC4.10 - Re-running the script against a site already in the desired state is a no-op: exit 0, zero writes across all systems (git, GitHub, Cloudflare, filesystem).
:PROPERTIES:
:CUSTOM_ID: ac4-10-re-running-the-script-against-a-site-already-in-the-desired-state-is-a-no-op-exit-0-zero-writes-across-all-systems-git-github-cloudflare-filesystem
:END:

**** Requirement

Re-running the script against a site already in the desired state is a no-op: exit 0, zero writes across all systems (git, GitHub, Cloudflare, filesystem).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.31: Second invocation in a fully-set-up sandbox exits 0 with zero invocations recorded by every stub (gh repo create, Cloudflare write endpoints, hugo.yaml writes, git commits); ASSUMED PASS UT-4.1: An end-to-end re-run against a real test domain visibly emits only =info= and =ok= messages with no UI changes in GitHub or Cloudflare

*** ASSUMED_PASS AC4.11 - =--help= and =-h= print usage, input flags, =CLOUDFLARE_API_TOKEN= requirement, and exit code semantics, then exit 0.
:PROPERTIES:
:CUSTOM_ID: ac4-11-help-and-h-print-usage-input-flags-cloudflare_api_token-requirement-and-exit-code-semantics-then-exit-0
:END:

**** Requirement

=--help= and =-h= print usage, input flags, =CLOUDFLARE_API_TOKEN= requirement, and exit code semantics, then exit 0.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.32: =--help= exits 0 and mentions each input flag and =CLOUDFLARE_API_TOKEN=; PENDING RT-4.33: =-h= produces equivalent output

*** ASSUMED_PASS AC4.12 - The scaffolded site lives at =$(pwd)/<site-name>/=; the operator controls the location by their choice of working directory when invoking the script.
:PROPERTIES:
:CUSTOM_ID: ac4-12-the-scaffolded-site-lives-at-pwd-site-name-the-operator-controls-the-location-by-their-choice-of-working-directory-when-invoking-the-script
:END:

**** Requirement

The scaffolded site lives at =$(pwd)/<site-name>/=; the operator controls the location by their choice of working directory when invoking the script.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/4.md][#4 - Add =new-hugo-site.sh=: Hugo site scaffolder with GH Pages + Cloudflare DNS]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-4.34: Invocation from an empty temp dir creates =<cwd>/<site-name>/= containing the scaffolded layout; PENDING RT-4.35: Invocation when =<cwd>/<site-name>/= already exists with content that is not the expected scaffolded layout exits non-zero and names the conflicting directory

*** HOLD AC7.1 - The provisioned NixOS VPS meets the same hardening posture as the Ubuntu VPS: sshd key-only with modern ciphers, firewall allowing only 443/tcp+udp and ICMP on the public interface, fail2ban active, kernel sysctl hardened, core dumps disabled, journald bounded, time sync active, Tailscale joined to the tailnet.
:PROPERTIES:
:CUSTOM_ID: ac7-1-the-provisioned-nixos-vps-meets-the-same-hardening-posture-as-the-ubuntu-vps-sshd-key-only-with-modern-ciphers-firewall-allowing-only-443-tcpudp-and-icmp-on-the-public-interface-fail2ban-active-kernel-sysctl-hardened-core-dumps-disabled-journald-bounded-time-sync-active-tailscale-joined-to-the-tailnet
:END:

**** Requirement

The provisioned NixOS VPS meets the same hardening posture as the Ubuntu VPS: sshd key-only with modern ciphers, firewall allowing only 443/tcp+udp and ICMP on the public interface, fail2ban active, kernel sysctl hardened, core dumps disabled, journald bounded, time sync active, Tailscale joined to the tailnet.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/7.md][#7 - Add vps-create-nix: NixOS VPS provisioning with declarative hardening]]

**** Tests

- PENDING OT-7.1: End-to-end smoke test provisions a NixOS VPS and verifies each hardening item (same sub-assertions as the Ubuntu OT-1.1)

*** HOLD AC7.2 - =deploy-app= and =deploy-hugo= work against the NixOS host without modification: git clone/pull, go build, hugo build, systemd unit install, Caddyfile deploy, and DNS all succeed.
:PROPERTIES:
:CUSTOM_ID: ac7-2-deploy-app-and-deploy-hugo-work-against-the-nixos-host-without-modification-git-clone-pull-go-build-hugo-build-systemd-unit-install-caddyfile-deploy-and-dns-all-succeed
:END:

**** Requirement

=deploy-app= and =deploy-hugo= work against the NixOS host without modification: git clone/pull, go build, hugo build, systemd unit install, Caddyfile deploy, and DNS all succeed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/7.md][#7 - Add vps-create-nix: NixOS VPS provisioning with declarative hardening]]

**** Tests

- PENDING OT-7.2: =deploy-app= deploys a Go app to the NixOS VPS and the app responds on its domain; PENDING OT-7.3: =deploy-hugo= deploys a Hugo site to the NixOS VPS and the site is served

*** HOLD AC7.3 - =vps-destroy= cleans up the NixOS host identically to an Ubuntu host: Hetzner server deleted, Tailscale device removed, DNS records removed, state file removed.
:PROPERTIES:
:CUSTOM_ID: ac7-3-vps-destroy-cleans-up-the-nixos-host-identically-to-an-ubuntu-host-hetzner-server-deleted-tailscale-device-removed-dns-records-removed-state-file-removed
:END:

**** Requirement

=vps-destroy= cleans up the NixOS host identically to an Ubuntu host: Hetzner server deleted, Tailscale device removed, DNS records removed, state file removed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/7.md][#7 - Add vps-create-nix: NixOS VPS provisioning with declarative hardening]]

**** Tests

- PENDING OT-7.4: =vps-destroy= against the NixOS VPS completes with zero warnings

*** HOLD AC10.1 - The flake at =vps-template/nixos/flake.nix= evaluates without errors and produces a =nixosConfigurations.hetzner-vps= output.
:PROPERTIES:
:CUSTOM_ID: ac10-1-the-flake-at-vps-template-nixos-flake-nix-evaluates-without-errors-and-produces-a-nixosconfigurations-hetzner-vps-output
:END:

**** Requirement

The flake at =vps-template/nixos/flake.nix= evaluates without errors and produces a =nixosConfigurations.hetzner-vps= output.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.1: =nix flake check= exits 0; PENDING RT-10.2: =nix eval .#nixosConfigurations.hetzner-vps.config.system.stateVersion= returns a version string

*** HOLD AC10.2 - sshd is configured for key-only authentication with modern ciphers (chacha20-poly1305, aes256-gcm, aes128-gcm), modern KEX (curve25519-sha256), and no root login.
:PROPERTIES:
:CUSTOM_ID: ac10-2-sshd-is-configured-for-key-only-authentication-with-modern-ciphers-chacha20-poly1305-aes256-gcm-aes128-gcm-modern-kex-curve25519-sha256-and-no-root-login
:END:

**** Requirement

sshd is configured for key-only authentication with modern ciphers (chacha20-poly1305, aes256-gcm, aes128-gcm), modern KEX (curve25519-sha256), and no root login.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.3: Evaluated config has PasswordAuthentication=false, PermitRootLogin="no", and the expected cipher list

*** HOLD AC10.3 - The firewall allows only 443/tcp, 443/udp, and ICMP on the public interface, and trusts the Tailscale interface.
:PROPERTIES:
:CUSTOM_ID: ac10-3-the-firewall-allows-only-443-tcp-443-udp-and-icmp-on-the-public-interface-and-trusts-the-tailscale-interface
:END:

**** Requirement

The firewall allows only 443/tcp, 443/udp, and ICMP on the public interface, and trusts the Tailscale interface.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.4: Evaluated config has allowedTCPPorts=[443], allowedUDPPorts=[443], trustedInterfaces contains "tailscale0"

*** HOLD AC10.4 - fail2ban is active with an SSH jail.
:PROPERTIES:
:CUSTOM_ID: ac10-4-fail2ban-is-active-with-an-ssh-jail
:END:

**** Requirement

fail2ban is active with an SSH jail.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.5: Evaluated config has services.fail2ban.enable=true

*** HOLD AC10.5 - Kernel sysctl hardening is applied: rp_filter, syncookies, kptr_restrict, dmesg_restrict, accept_redirects disabled.
:PROPERTIES:
:CUSTOM_ID: ac10-5-kernel-sysctl-hardening-is-applied-rp_filter-syncookies-kptr_restrict-dmesg_restrict-accept_redirects-disabled
:END:

**** Requirement

Kernel sysctl hardening is applied: rp_filter, syncookies, kptr_restrict, dmesg_restrict, accept_redirects disabled.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.6: Evaluated config contains the expected boot.kernel.sysctl keys and values

*** HOLD AC10.6 - chrony is enabled for time synchronization.
:PROPERTIES:
:CUSTOM_ID: ac10-6-chrony-is-enabled-for-time-synchronization
:END:

**** Requirement

chrony is enabled for time synchronization.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.7: Evaluated config has services.chrony.enable=true

*** HOLD AC10.7 - journald is bounded at 500M.
:PROPERTIES:
:CUSTOM_ID: ac10-7-journald-is-bounded-at-500m
:END:

**** Requirement

journald is bounded at 500M.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.8: Evaluated config contains SystemMaxUse=500M in journald settings

*** HOLD AC10.8 - Core dumps are disabled system-wide.
:PROPERTIES:
:CUSTOM_ID: ac10-8-core-dumps-are-disabled-system-wide
:END:

**** Requirement

Core dumps are disabled system-wide.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.9: Evaluated config has a PAM loginLimits entry for hard core 0

*** HOLD AC10.9 - /tmp is mounted as tmpfs with noexec, nosuid, nodev.
:PROPERTIES:
:CUSTOM_ID: ac10-9-tmp-is-mounted-as-tmpfs-with-noexec-nosuid-nodev
:END:

**** Requirement

/tmp is mounted as tmpfs with noexec, nosuid, nodev.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.10: Evaluated config declares a tmpfs mount for /tmp with the restrictive options

*** HOLD AC10.10 - Automatic NixOS upgrades are configured to rebuild from the flake daily.
:PROPERTIES:
:CUSTOM_ID: ac10-10-automatic-nixos-upgrades-are-configured-to-rebuild-from-the-flake-daily
:END:

**** Requirement

Automatic NixOS upgrades are configured to rebuild from the flake daily.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.11: Evaluated config has system.autoUpgrade.enable=true with the flake URL

*** HOLD AC10.11 - CrowdSec agent and firewall bouncer are declared with community blocklists and escalating ban durations.
:PROPERTIES:
:CUSTOM_ID: ac10-11-crowdsec-agent-and-firewall-bouncer-are-declared-with-community-blocklists-and-escalating-ban-durations
:END:

**** Requirement

CrowdSec agent and firewall bouncer are declared with community blocklists and escalating ban durations.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.12: CrowdSec service is declared in the evaluated config (or an equivalent overlay/derivation if not in nixpkgs)

*** HOLD AC10.12 - Tailscale is enabled with MagicDNS and the tailscale0 interface trusted by the firewall.
:PROPERTIES:
:CUSTOM_ID: ac10-12-tailscale-is-enabled-with-magicdns-and-the-tailscale0-interface-trusted-by-the-firewall
:END:

**** Requirement

Tailscale is enabled with MagicDNS and the tailscale0 interface trusted by the firewall.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.13: Evaluated config has services.tailscale.enable=true

*** HOLD AC10.13 - Caddy is declared with the caddy-dns/cloudflare plugin compiled in, not requiring xcaddy at runtime.
:PROPERTIES:
:CUSTOM_ID: ac10-13-caddy-is-declared-with-the-caddy-dns-cloudflare-plugin-compiled-in-not-requiring-xcaddy-at-runtime
:END:

**** Requirement

Caddy is declared with the caddy-dns/cloudflare plugin compiled in, not requiring xcaddy at runtime.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.14: Evaluated Caddy package is built with the cloudflare plugin

*** HOLD AC10.14 - A non-root deploy user exists with passwordless sudo, SSH key auth, and a locked password.
:PROPERTIES:
:CUSTOM_ID: ac10-14-a-non-root-deploy-user-exists-with-passwordless-sudo-ssh-key-auth-and-a-locked-password
:END:

**** Requirement

A non-root deploy user exists with passwordless sudo, SSH key auth, and a locked password.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.15: Evaluated config has a user with isNormalUser, extraGroups including "wheel", and hashedPassword="!"

*** HOLD AC10.15 - Go toolchain and Hugo binary are on PATH.
:PROPERTIES:
:CUSTOM_ID: ac10-15-go-toolchain-and-hugo-binary-are-on-path
:END:

**** Requirement

Go toolchain and Hugo binary are on PATH.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.16: Evaluated config includes pkgs.go and pkgs.hugo in environment.systemPackages

*** HOLD AC10.16 - Base operator tools are installed: git, jq, curl, age, fd, ripgrep, trash-cli.
:PROPERTIES:
:CUSTOM_ID: ac10-16-base-operator-tools-are-installed-git-jq-curl-age-fd-ripgrep-trash-cli
:END:

**** Requirement

Base operator tools are installed: git, jq, curl, age, fd, ripgrep, trash-cli.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.17: Evaluated config includes all listed packages in environment.systemPackages

*** HOLD AC10.17 - Restic backup is configured on a daily 5am schedule.
:PROPERTIES:
:CUSTOM_ID: ac10-17-restic-backup-is-configured-on-a-daily-5am-schedule
:END:

**** Requirement

Restic backup is configured on a daily 5am schedule.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/10.md][#10 - Phase 1a: Write NixOS flake with base hardening modules]]

**** Tests

- PENDING RT-10.18: Evaluated config has services.restic or an equivalent backup timer

*** HOLD AC11.1 - The script provisions a Hetzner VPS running NixOS with the Phase 1a flake applied, joined to the Tailscale tailnet.
:PROPERTIES:
:CUSTOM_ID: ac11-1-the-script-provisions-a-hetzner-vps-running-nixos-with-the-phase-1a-flake-applied-joined-to-the-tailscale-tailnet
:END:

**** Requirement

The script provisions a Hetzner VPS running NixOS with the Phase 1a flake applied, joined to the Tailscale tailnet.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/11.md][#11 - Phase 1b: vps-create-nix provisioning script]]

**** Tests

- PENDING OT-11.1: =vps-create-nix test-host= provisions a server, =tailscale status= shows it

*** HOLD AC11.2 - The script has the same operator interface as =vps-create=: positional NAME, location/type prompts, =--no-rollback=, =--help=, =--version=.
:PROPERTIES:
:CUSTOM_ID: ac11-2-the-script-has-the-same-operator-interface-as-vps-create-positional-name-location-type-prompts-no-rollback-help-version
:END:

**** Requirement

The script has the same operator interface as =vps-create=: positional NAME, location/type prompts, =--no-rollback=, =--help=, =--version=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/11.md][#11 - Phase 1b: vps-create-nix provisioning script]]

**** Tests

- PENDING RT-11.1: =--help= exits 0 with usage text; PENDING RT-11.2: =--version= exits 0

*** HOLD AC11.3 - Port 22 is only open during provisioning (for nixos-anywhere) and closed afterwards.
:PROPERTIES:
:CUSTOM_ID: ac11-3-port-22-is-only-open-during-provisioning-for-nixos-anywhere-and-closed-afterwards
:END:

**** Requirement

Port 22 is only open during provisioning (for nixos-anywhere) and closed afterwards.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/11.md][#11 - Phase 1b: vps-create-nix provisioning script]]

**** Tests

- PENDING OT-11.2: After provisioning completes, =hcloud firewall describe= shows no port 22 rule

*** HOLD AC11.4 - The provisioned host is compatible with =deploy-app= and =deploy-hugo= (SSH access, Go, Hugo, Caddy, git all present).
:PROPERTIES:
:CUSTOM_ID: ac11-4-the-provisioned-host-is-compatible-with-deploy-app-and-deploy-hugo-ssh-access-go-hugo-caddy-git-all-present
:END:

**** Requirement

The provisioned host is compatible with =deploy-app= and =deploy-hugo= (SSH access, Go, Hugo, Caddy, git all present).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/11.md][#11 - Phase 1b: vps-create-nix provisioning script]]

**** Tests

- PENDING OT-11.3: SSH as deploy user succeeds, =go version=, =hugo version=, =caddy version=, =git --version= all return 0

*** HOLD AC12.1 - A NixOS VPS provisioned by =vps-create-nix= has the same hardening posture as the Ubuntu VPS: all 15 items from the hardening checklist are active.
:PROPERTIES:
:CUSTOM_ID: ac12-1-a-nixos-vps-provisioned-by-vps-create-nix-has-the-same-hardening-posture-as-the-ubuntu-vps-all-15-items-from-the-hardening-checklist-are-active
:END:

**** Requirement

A NixOS VPS provisioned by =vps-create-nix= has the same hardening posture as the Ubuntu VPS: all 15 items from the hardening checklist are active.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/12.md][#12 - Phase 1c: POC - provision + deploy to NixOS VPS]]

**** Tests

- PENDING OT-12.1: SSH in and verify each hardening item (same sub-assertions as OT-1.1): sshd ciphers, firewall rules, fail2ban active, sysctl values, chrony running, journald limits, core dumps disabled, AppArmor loaded, CrowdSec active, Tailscale joined

*** HOLD AC12.2 - =deploy-app= deploys a Go app (golink with private submodule) to the NixOS host and the app responds on its domain.
:PROPERTIES:
:CUSTOM_ID: ac12-2-deploy-app-deploys-a-go-app-golink-with-private-submodule-to-the-nixos-host-and-the-app-responds-on-its-domain
:END:

**** Requirement

=deploy-app= deploys a Go app (golink with private submodule) to the NixOS host and the app responds on its domain.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/12.md][#12 - Phase 1c: POC - provision + deploy to NixOS VPS]]

**** Tests

- PENDING OT-12.2: =deploy-app <host>= from golink directory exits 0; =curl -sI https://<domain>= returns a response from golink

*** HOLD AC12.3 - =deploy-hugo= deploys a Hugo site (readers) to the NixOS host and the site is served.
:PROPERTIES:
:CUSTOM_ID: ac12-3-deploy-hugo-deploys-a-hugo-site-readers-to-the-nixos-host-and-the-site-is-served
:END:

**** Requirement

=deploy-hugo= deploys a Hugo site (readers) to the NixOS host and the site is served.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/12.md][#12 - Phase 1c: POC - provision + deploy to NixOS VPS]]

**** Tests

- PENDING OT-12.3: =deploy-hugo <host>= from readers directory exits 0; site files present at =/srv/readers/public/=

*** HOLD AC12.4 - CrowdSec is active on the NixOS host, reading Caddy access logs from journald, and the firewall bouncer is blocking decisions.
:PROPERTIES:
:CUSTOM_ID: ac12-4-crowdsec-is-active-on-the-nixos-host-reading-caddy-access-logs-from-journald-and-the-firewall-bouncer-is-blocking-decisions
:END:

**** Requirement

CrowdSec is active on the NixOS host, reading Caddy access logs from journald, and the firewall bouncer is blocking decisions.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/12.md][#12 - Phase 1c: POC - provision + deploy to NixOS VPS]]

**** Tests

- PENDING OT-12.4: =cshost <host> metrics= shows Caddy log lines parsed; =cshost <host> bouncers list= shows the firewall bouncer connected

*** HOLD AC12.5 - =vps-destroy= cleans up the NixOS host identically to an Ubuntu host: Hetzner server deleted, Tailscale device removed, DNS records removed, SSH key removed, state file and hosts/ directory removed.
:PROPERTIES:
:CUSTOM_ID: ac12-5-vps-destroy-cleans-up-the-nixos-host-identically-to-an-ubuntu-host-hetzner-server-deleted-tailscale-device-removed-dns-records-removed-ssh-key-removed-state-file-and-hosts-directory-removed
:END:

**** Requirement

=vps-destroy= cleans up the NixOS host identically to an Ubuntu host: Hetzner server deleted, Tailscale device removed, DNS records removed, SSH key removed, state file and hosts/ directory removed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/12.md][#12 - Phase 1c: POC - provision + deploy to NixOS VPS]]

**** Tests

- PENDING OT-12.5: =vps-destroy <host>= exits 0 with no warnings

*** HOLD AC18.1 - The writeback service runs as a static system user with a fixed UID, not a transient DynamicUser
:PROPERTIES:
:CUSTOM_ID: ac18-1-the-writeback-service-runs-as-a-static-system-user-with-a-fixed-uid-not-a-transient-dynamicuser
:END:

**** Requirement

The writeback service runs as a static system user with a fixed UID, not a transient DynamicUser

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/18.md][#18 - Bug: DynamicUser causes readonly database on writeback restart]]

**** Tests

- PENDING UT-18.1: Restart writeback 3 times, verify UID is identical each time via =id -u writeback=

*** HOLD AC18.2 - The SQLite database retains correct ownership across service restarts
:PROPERTIES:
:CUSTOM_ID: ac18-2-the-sqlite-database-retains-correct-ownership-across-service-restarts
:END:

**** Requirement

The SQLite database retains correct ownership across service restarts

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/18.md][#18 - Bug: DynamicUser causes readonly database on writeback restart]]

**** Tests

- PENDING UT-18.2: Restart writeback, verify =/var/lib/writeback/writeback.db= is owned by the static writeback user

*** HOLD AC18.3 - The daily timer job runs as the same static user as the main service
:PROPERTIES:
:CUSTOM_ID: ac18-3-the-daily-timer-job-runs-as-the-same-static-user-as-the-main-service
:END:

**** Requirement

The daily timer job runs as the same static user as the main service

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/18.md][#18 - Bug: DynamicUser causes readonly database on writeback restart]]

**** Tests

- PENDING UT-18.3: Verify =systemctl show writeback-timer -p User= matches =systemctl show writeback -p User=

*** HOLD AC18.4 - =deploy-app= automatically creates a static system user when the service override sets =DynamicUser=no=
:PROPERTIES:
:CUSTOM_ID: ac18-4-deploy-app-automatically-creates-a-static-system-user-when-the-service-override-sets-dynamicuserno
:END:

**** Requirement

=deploy-app= automatically creates a static system user when the service override sets =DynamicUser=no=

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/18.md][#18 - Bug: DynamicUser causes readonly database on writeback restart]]

**** Tests

- PENDING UT-18.4: Deploy to a host where the user does not yet exist, verify user is created

*** HOLD AC18.5 - =deploy-app= uses =getent passwd= (not =id=) to detect existing users, avoiding false positives from transient DynamicUser entries
:PROPERTIES:
:CUSTOM_ID: ac18-5-deploy-app-uses-getent-passwd-not-id-to-detect-existing-users-avoiding-false-positives-from-transient-dynamicuser-entries
:END:

**** Requirement

=deploy-app= uses =getent passwd= (not =id=) to detect existing users, avoiding false positives from transient DynamicUser entries

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/18.md][#18 - Bug: DynamicUser causes readonly database on writeback restart]]

**** Tests

- PENDING UT-18.5: Review deploy-app.sh, confirm =getent passwd= is used for user detection

*** ASSUMED_PASS AC22.1 - Go apps deployed via deploy-nix run from a pre-compiled binary with no source compilation on the server
:PROPERTIES:
:CUSTOM_ID: ac22-1-go-apps-deployed-via-deploy-nix-run-from-a-pre-compiled-binary-with-no-source-compilation-on-the-server
:END:

**** Requirement

Go apps deployed via deploy-nix run from a pre-compiled binary with no source compilation on the server

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/22.md][#22 - deploy-nix: pre-compiled binary releases instead of build-from-source]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-22.1: Generated app JSON contains binary_url and binary_hash, not repo/rev/vendor_hash; PENDING RT-22.2: Nix derivation for a Go app uses fetchurl, not buildGoModule; ASSUMED PASS OT-22.1: Deployed Go app on a live NixOS host serves requests correctly

*** ASSUMED_PASS AC22.2 - The NixOS server has no Go toolchain dependency for running deployed Go apps
:PROPERTIES:
:CUSTOM_ID: ac22-2-the-nixos-server-has-no-go-toolchain-dependency-for-running-deployed-go-apps
:END:

**** Requirement

The NixOS server has no Go toolchain dependency for running deployed Go apps

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/22.md][#22 - deploy-nix: pre-compiled binary releases instead of build-from-source]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-22.3: The caddy-hardened role does not import the Go module; ASSUMED PASS UT-22.1: A provisioned host with deployed Go app has no =go= binary on PATH

*** ASSUMED_PASS AC22.3 - Hugo site deployment is unchanged --- source fetch and Hugo build on server
:PROPERTIES:
:CUSTOM_ID: ac22-3-hugo-site-deployment-is-unchanged-source-fetch-and-hugo-build-on-server
:END:

**** Requirement

Hugo site deployment is unchanged --- source fetch and Hugo build on server

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/22.md][#22 - deploy-nix: pre-compiled binary releases instead of build-from-source]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-22.4: Hugo app JSON retains repo and rev fields; PENDING RT-22.5: Hugo app type in deployed-apps.nix uses builtins.fetchGit

*** ASSUMED_PASS AC22.4 - A release cannot be created from a dirty or unpushed working tree
:PROPERTIES:
:CUSTOM_ID: ac22-4-a-release-cannot-be-created-from-a-dirty-or-unpushed-working-tree
:END:

**** Requirement

A release cannot be created from a dirty or unpushed working tree

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/22.md][#22 - deploy-nix: pre-compiled binary releases instead of build-from-source]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-22.6: Uncommitted changes cause deploy-nix to exit before any release; PENDING RT-22.7: Unpushed commits cause deploy-nix to exit before any release

*** ASSUMED_PASS AC22.5 - =deploy-app= and =deploy-hugo= refuse to operate on a NixOS host
:PROPERTIES:
:CUSTOM_ID: ac22-5-deploy-app-and-deploy-hugo-refuse-to-operate-on-a-nixos-host
:END:

**** Requirement

=deploy-app= and =deploy-hugo= refuse to operate on a NixOS host

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/22.md][#22 - deploy-nix: pre-compiled binary releases instead of build-from-source]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PENDING RT-22.8: deploy-app exits with error when target host has /etc/NIXOS; PENDING RT-22.9: deploy-app error message names deploy-nix as the correct tool

*** HOLD AC24.1 - =deploy-nix= refuses to deploy a Go app when no ref is provided via positional arg or =deploy.yaml=.
:PROPERTIES:
:CUSTOM_ID: ac24-1-deploy-nix-refuses-to-deploy-a-go-app-when-no-ref-is-provided-via-positional-arg-or-deploy-yaml
:END:

**** Requirement

=deploy-nix= refuses to deploy a Go app when no ref is provided via positional arg or =deploy.yaml=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.1: No ref arg and no =ref= in deploy.yaml --- deploy exits non-zero with descriptive error; PASS RT-24.2: No ref arg and empty =ref:= in deploy.yaml --- deploy exits non-zero

*** HOLD AC24.2 - =deploy-nix= refuses to deploy a Hugo app when no ref is provided via positional arg or =deploy.yaml=.
:PROPERTIES:
:CUSTOM_ID: ac24-2-deploy-nix-refuses-to-deploy-a-hugo-app-when-no-ref-is-provided-via-positional-arg-or-deploy-yaml
:END:

**** Requirement

=deploy-nix= refuses to deploy a Hugo app when no ref is provided via positional arg or =deploy.yaml=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.3: Hugo app with no ref anywhere --- deploy exits non-zero; PASS RT-24.4: Hugo app with ref arg pointing to nonexistent tag --- deploy exits non-zero

*** HOLD AC24.3 - =deploy-nix= accepts a ref from the positional arg and records it in the generated JSON.
:PROPERTIES:
:CUSTOM_ID: ac24-3-deploy-nix-accepts-a-ref-from-the-positional-arg-and-records-it-in-the-generated-json
:END:

**** Requirement

=deploy-nix= accepts a ref from the positional arg and records it in the generated JSON.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.5: =deploy-nix host v1.0= with valid tag --- JSON contains ="ref": "v1.0"= and correct =rev=; PASS RT-24.6: =deploy-nix host main= with valid branch --- JSON contains ="ref": "main"= and correct =rev=

*** HOLD AC24.4 - =deploy-nix= reads the default ref from =deploy.yaml= when no positional arg is provided.
:PROPERTIES:
:CUSTOM_ID: ac24-4-deploy-nix-reads-the-default-ref-from-deploy-yaml-when-no-positional-arg-is-provided
:END:

**** Requirement

=deploy-nix= reads the default ref from =deploy.yaml= when no positional arg is provided.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.7: =ref: v2.1= in deploy.yaml, no positional arg --- JSON contains ="ref": "v2.1"=; PASS RT-24.8: =ref: v2.1= in deploy.yaml, =deploy-nix host v3.0= on CLI --- CLI wins, JSON contains ="ref": "v3.0"=

*** HOLD AC24.5 - =deploy-nix= validates that the specified ref resolves to an existing branch or tag in the app repo.
:PROPERTIES:
:CUSTOM_ID: ac24-5-deploy-nix-validates-that-the-specified-ref-resolves-to-an-existing-branch-or-tag-in-the-app-repo
:END:

**** Requirement

=deploy-nix= validates that the specified ref resolves to an existing branch or tag in the app repo.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.9: Ref is a valid tag --- deploy proceeds; PASS RT-24.10: Ref is a nonexistent string --- deploy exits non-zero with descriptive error

*** SUPERSEDED AC24.6 - =deploy-app= refuses to deploy when no ref positional arg is provided. Superseded by issue #50, which removed the legacy Ubuntu deployment surface.
:PROPERTIES:
:CUSTOM_ID: ac24-6-deploy-app-refuses-to-deploy-when-no-ref-positional-arg-is-provided-superseded-by-issue-50-which-removed-the-legacy-ubuntu-deployment-surface
:END:

**** Requirement

+=deploy-app= refuses to deploy when no ref positional arg is provided.+ Superseded by issue #50, which removed the legacy Ubuntu deployment surface.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- PASS RT-24.11: No ref arg --- deploy exits non-zero with usage error; PASS RT-24.12: Empty string as ref arg --- deploy exits non-zero

*** SUPERSEDED AC24.7 - =deploy-app= checks out the specified ref on the server instead of =git pull --ff-only=. Superseded by issue #50, which removed the legacy Ubuntu deployment surface.
:PROPERTIES:
:CUSTOM_ID: ac24-7-deploy-app-checks-out-the-specified-ref-on-the-server-instead-of-git-pull-ff-only-superseded-by-issue-50-which-removed-the-legacy-ubuntu-deployment-surface
:END:

**** Requirement

+=deploy-app= checks out the specified ref on the server instead of =git pull --ff-only=.+ Superseded by issue #50, which removed the legacy Ubuntu deployment surface.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- PENDING UT-24.1: Deploy with ref =v1.0= to a test host --- server checkout is at the correct rev for that tag; PENDING UT-24.2: Deploy with ref =main= --- server checkout is at the branch HEAD

*** HOLD AC24.8 - The generated JSON for both Go and Hugo apps includes the named ref alongside the SHA.
:PROPERTIES:
:CUSTOM_ID: ac24-8-the-generated-json-for-both-go-and-hugo-apps-includes-the-named-ref-alongside-the-sha
:END:

**** Requirement

The generated JSON for both Go and Hugo apps includes the named ref alongside the SHA.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/24.md][#24 - Require named git ref (branch or tag) for all deployments]]

**** Tests

- PASS RT-24.13: Go app deploy --- JSON has both =ref= and =version= fields; PASS RT-24.14: Hugo app deploy --- JSON has both =ref= and =rev= fields

*** HOLD AC27.1 - A single =load_env_file= implementation exists at =lib/env_loader.sh=; =vps-template/lib/inputs.sh= re-exports it.
:PROPERTIES:
:CUSTOM_ID: ac27-1-a-single-load_env_file-implementation-exists-at-lib-env_loader-sh-vps-template-lib-inputs-sh-re-exports-it
:END:

**** Requirement

A single =load_env_file= implementation exists at =lib/env_loader.sh=; =vps-template/lib/inputs.sh= re-exports it.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS RT-1.17 still passes against the relocated helper

*** HOLD AC27.2 - No shell file in the repo invokes =source= (or =.=) on a =.env= file directly; all =.env= loads go through =load_env_file=.
:PROPERTIES:
:CUSTOM_ID: ac27-2-no-shell-file-in-the-repo-invokes-source-or-on-a-env-file-directly-all-env-loads-go-through-load_env_file
:END:

**** Requirement

No shell file in the repo invokes =source= (or =.=) on a =.env= file directly; all =.env= loads go through =load_env_file=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS Verified by behavioural RT-27.1 (Phase 3) - if any script regresses, the marker token would not survive

*** HOLD AC27.3 - Sourcing =.env= does not overwrite existing environment variables in any production script.
:PROPERTIES:
:CUSTOM_ID: ac27-3-sourcing-env-does-not-overwrite-existing-environment-variables-in-any-production-script
:END:

**** Requirement

Sourcing =.env= does not overwrite existing environment variables in any production script.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS RT-1.17 condition 6: =PRECEDENCE_KEY=from_environment= survives =load_env_file=

*** HOLD AC27.4 - Test-driven invocations run with a baseline that does not inherit the operator shell's environment.
:PROPERTIES:
:CUSTOM_ID: ac27-4-test-driven-invocations-run-with-a-baseline-that-does-not-inherit-the-operator-shell-s-environment
:END:

**** Requirement

Test-driven invocations run with a baseline that does not inherit the operator shell's environment.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS =_test_helpers.sh= exports =run_clean=; RT-1.9 uses it

*** HOLD AC27.5 - =make test= passes from a shell where =.env= has been sourced.
:PROPERTIES:
:CUSTOM_ID: ac27-5-make-test-passes-from-a-shell-where-env-has-been-sourced
:END:

**** Requirement

=make test= passes from a shell where =.env= has been sourced.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS UT-27.1: verified by Taḋg

*** HOLD AC27.6 - =make lint= covers every tracked =*.sh= in =lib/=, =deploy/=, =vps-template/=, =tests/=.
:PROPERTIES:
:CUSTOM_ID: ac27-6-make-lint-covers-every-tracked-sh-in-lib-deploy-vps-template-tests
:END:

**** Requirement

=make lint= covers every tracked =*.sh= in =lib/=, =deploy/=, =vps-template/=, =tests/=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS Verified - found and fixed three previously-hidden warnings

*** HOLD AC27.7 - SC1083 false positives resolved at source.
:PROPERTIES:
:CUSTOM_ID: ac27-7-sc1083-false-positives-resolved-at-source
:END:

**** Requirement

SC1083 false positives resolved at source.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS Plus bonus SC2034 fix in =whatismyip.sh=

*** HOLD AC27.8 - The no-override =.env= loader is exercised end-to-end.
:PROPERTIES:
:CUSTOM_ID: ac27-8-the-no-override-env-loader-is-exercised-end-to-end
:END:

**** Requirement

The no-override =.env= loader is exercised end-to-end.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS =test_dotenv_no_override.sh=: stubs hcloud, runs =create.sh --dry-run= with =HCLOUD_TOKEN=marker= via =run_clean=, asserts every recorded value is =marker= not the production value

*** SUPERSEDED AC27.9 - RT-1.10 honestly categorized.
:PROPERTIES:
:CUSTOM_ID: ac27-9-rt-1-10-honestly-categorized
:END:

**** Requirement

+RT-1.10 honestly categorized.+

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +⏭️ Initially relocated to =lint/=; superseded by Phase 3a (deleted)+

*** SUPERSEDED AC27.10 - RT-1.11 honestly categorized.
:PROPERTIES:
:CUSTOM_ID: ac27-10-rt-1-11-honestly-categorized
:END:

**** Requirement

+RT-1.11 honestly categorized.+

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +⏭️ Initially relocated to =lint/=; superseded by Phase 3a (deleted)+

*** HOLD AC27.11 - RT-1.13 (print_summary) verified by running the script.
:PROPERTIES:
:CUSTOM_ID: ac27-11-rt-1-13-print_summary-verified-by-running-the-script
:END:

**** Requirement

RT-1.13 (print_summary) verified by running the script.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS =test_print_summary_function.sh=: plants fixture state file, runs =create.sh --summary-only= via =run_clean=, asserts canonical summary markers in stderr

*** HOLD AC27.12 - =test_function_definitions.sh= honestly described.
:PROPERTIES:
:CUSTOM_ID: ac27-12-test_function_definitions-sh-honestly-described
:END:

**** Requirement

=test_function_definitions.sh= honestly described.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS ABOUTME tightened: it sources the lib chain (runtime) and queries =declare -F= (runtime) - it is not source-grep. New ID RT-1.21

*** HOLD AC27.13 - Guard-rail tests relocated under =tests/regression/lint/= with documenting README.
:PROPERTIES:
:CUSTOM_ID: ac27-13-guard-rail-tests-relocated-under-tests-regression-lint-with-documenting-readme
:END:

**** Requirement

Guard-rail tests relocated under =tests/regression/lint/= with documenting README.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS Refined under Phase 3a - lint/ now contains only two genuine guard rails (secrets, gitignore patterns)

*** HOLD AC27.14 - The lint/ directory contains only tests where source-grep is genuinely the only sensible expression - i.e.
:PROPERTIES:
:CUSTOM_ID: ac27-14-the-lint-directory-contains-only-tests-where-source-grep-is-genuinely-the-only-sensible-expression-i-e
:END:

**** Requirement

The lint/ directory contains only tests where source-grep is genuinely the only sensible expression - i.e. where the committed source IS the user-observable artefact.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS Final lint/ contents: =test_no_secrets_committed.sh=, =test_gitignore_required_patterns.sh=. Both defended in the directory README.

*** HOLD AC27.15 - The "no plaintext password reaches the server" guard is expressed against rendered output, not template source.
:PROPERTIES:
:CUSTOM_ID: ac27-15-the-no-plaintext-password-reaches-the-server-guard-is-expressed-against-rendered-output-not-template-source
:END:

**** Requirement

The "no plaintext password reaches the server" guard is expressed against rendered output, not template source.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/27.md][#27 - Test harness review improvements]]

**** Tests

- PASS RT-1.12 assertions present in =test_template_render_fixture.sh=: rendered output has no =^passwd:= line; rendered output has =lock_passwd: true=

*** HOLD AC33.1 - Hosts provisioned with the =hetzner-vps-caddy-hardened= role have =pkgs.restic= available on PATH and a =restic-backups-hetzner= systemd unit defined.
:PROPERTIES:
:CUSTOM_ID: ac33-1-hosts-provisioned-with-the-hetzner-vps-caddy-hardened-role-have-pkgs-restic-available-on-path-and-a-restic-backups-hetzner-systemd-unit-defined
:END:

**** Requirement

Hosts provisioned with the =hetzner-vps-caddy-hardened= role have =pkgs.restic= available on PATH and a =restic-backups-hetzner= systemd unit defined.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING RT-33.1 (NixOS configuration test: =nixos-rebuild dry-build= with the role evaluates without error and the resulting toplevel includes the unit)

*** HOLD AC33.2 - Hosts with the =hetzner-vps-base= role (Phase 1 only, no Phase 2) do NOT have the restic backup service active.
:PROPERTIES:
:CUSTOM_ID: ac33-2-hosts-with-the-hetzner-vps-base-role-phase-1-only-no-phase-2-do-not-have-the-restic-backup-service-active
:END:

**** Requirement

Hosts with the =hetzner-vps-base= role (Phase 1 only, no Phase 2) do NOT have the restic backup service active.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING RT-33.2 (NixOS configuration test: =dry-build= of base role does not include =restic-backups-hetzner=)

*** SUPERSEDED AC33.3 - The systemd timer for =restic-backups-hetzner= is scheduled daily at 05:00 UTC with =RandomizedDelaySec=1h= and =Persistent=true=.
:PROPERTIES:
:CUSTOM_ID: ac33-3-the-systemd-timer-for-restic-backups-hetzner-is-scheduled-daily-at-05-00-utc-with-randomizeddelaysec1h-and-persistenttrue
:END:

**** Requirement

The systemd timer for =restic-backups-hetzner= is scheduled daily at 05:00 UTC with =RandomizedDelaySec=1h= and =Persistent=true=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Status qualification

- *Status qualification:* by AC81.5, fleet backup at 01:00 host-local with up to 45 minutes of spread
**** Tests

- +SUPERSEDED RT-33.3 (config test asserts =OnCalendar=, =RandomizedDelaySec=, =Persistent= values) superseded by RT-81.15 under AC81.5+

*** HOLD AC33.4 - Restic credentials (=restic-b2-env.age=, =restic-password.age=) live in =vps-template/nixos/secrets/= and decrypt to =/run/agenix/restic-b2-env= and =/run/agenix/restic-password= at activation time, owned by root, mode 0400.
:PROPERTIES:
:CUSTOM_ID: ac33-4-restic-credentials-restic-b2-env-age-restic-password-age-live-in-vps-template-nixos-secrets-and-decrypt-to-run-agenix-restic-b2-env-and-run-agenix-restic-password-at-activation-time-owned-by-root-mode-0400
:END:

**** Requirement

Restic credentials (=restic-b2-env.age=, =restic-password.age=) live in =vps-template/nixos/secrets/= and decrypt to =/run/agenix/restic-b2-env= and =/run/agenix/restic-password= at activation time, owned by root, mode 0400.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.1 (operator inspects =ls -l /run/agenix/restic-*= on a Phase-2 host, confirms ownership and mode)

*** HOLD AC33.5 - =hetzner-backup= wrapper is on PATH and accepts the subcommands =backup=, =snapshots=, =snapshots --all=, =status=, =mount=, =restore=, =check=, =stats=, =forget=, =forget --apply=, =unlock=.
:PROPERTIES:
:CUSTOM_ID: ac33-5-hetzner-backup-wrapper-is-on-path-and-accepts-the-subcommands-backup-snapshots-snapshots-all-status-mount-restore-check-stats-forget-forget-apply-unlock
:END:

**** Requirement

=hetzner-backup= wrapper is on PATH and accepts the subcommands =backup=, =snapshots=, =snapshots --all=, =status=, =mount=, =restore=, =check=, =stats=, =forget=, =forget --apply=, =unlock=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.2 (operator runs =hetzner-backup --help= on a Phase-2 host, confirms all subcommands appear)

*** HOLD AC33.6 - A manual =hetzner-backup backup= run on a fresh Phase-2 host writes a snapshot to the B2 repository, visible via =hetzner-backup snapshots=.
:PROPERTIES:
:CUSTOM_ID: ac33-6-a-manual-hetzner-backup-backup-run-on-a-fresh-phase-2-host-writes-a-snapshot-to-the-b2-repository-visible-via-hetzner-backup-snapshots
:END:

**** Requirement

A manual =hetzner-backup backup= run on a fresh Phase-2 host writes a snapshot to the B2 repository, visible via =hetzner-backup snapshots=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.3 (real-world: operator provisions a test host, runs the backup manually, confirms snapshot exists)

*** SUPERSEDED AC33.7 - The first scheduled backup after host provisioning runs successfully at the next 05:00 UTC window without operator intervention.
:PROPERTIES:
:CUSTOM_ID: ac33-7-the-first-scheduled-backup-after-host-provisioning-runs-successfully-at-the-next-05-00-utc-window-without-operator-intervention
:END:

**** Requirement

The first scheduled backup after host provisioning runs successfully at the next 05:00 UTC window without operator intervention.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Status qualification

- *Status qualification:* by AC81.5; successful scheduled backup operation remains covered by the active fleet backup criteria
**** Tests

- ~~SUPERSEDED UT-33.4 (real-world: operator waits ~24h after provisioning a long-lived host, checks =journalctl -u restic-backups-hetzner= for a =snapshot ... saved= line) superseded by the schedule contract and RT-81.15 under AC81.5~~

*** HOLD AC33.8 - Snapshots are namespaced per host via restic's =--host= snapshot tag (set per-backup by the module's =extraBackupArgs=).
:PROPERTIES:
:CUSTOM_ID: ac33-8-snapshots-are-namespaced-per-host-via-restic-s-host-snapshot-tag-set-per-backup-by-the-module-s-extrabackupargs
:END:

**** Requirement

Snapshots are namespaced per host via restic's =--host= snapshot tag (set per-backup by the module's =extraBackupArgs=). All hosts share one B2 repository; =hetzner-backup snapshots --host <name>= filters server-side to one host.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.5 (operator confirms =restic snapshots --host <hostname>= shows only that host's snapshots after multiple hosts are backed up to the shared bucket)

*** HOLD AC33.9 - Retention policy is enforced: snapshots older than the keep-daily/weekly/monthly/yearly window are pruned automatically (via =pruneOpts= on the NixOS module).
:PROPERTIES:
:CUSTOM_ID: ac33-9-retention-policy-is-enforced-snapshots-older-than-the-keep-daily-weekly-monthly-yearly-window-are-pruned-automatically-via-pruneopts-on-the-nixos-module
:END:

**** Requirement

Retention policy is enforced: snapshots older than the keep-daily/weekly/monthly/yearly window are pruned automatically (via =pruneOpts= on the NixOS module).

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.6 (deferred --- requires 1+ week of accumulated snapshots to observe pruning; verification done at +7 days post-deployment)

*** HOLD AC33.10 - The summary block at the end of a successful =vps-create-nix= run reports =16/16 hardening measures applied=, with ="restic backup"= appearing in the listed item set.
:PROPERTIES:
:CUSTOM_ID: ac33-10-the-summary-block-at-the-end-of-a-successful-vps-create-nix-run-reports-16-16-hardening-measures-applied-with-restic-backup-appearing-in-the-listed-item-set
:END:

**** Requirement

The summary block at the end of a successful =vps-create-nix= run reports =16/16 hardening measures applied=, with ="restic backup"= appearing in the listed item set.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/33.md][#33 - Restic off-host backup for NixOS hosts (port from samizdat, wire into Phase 2)]]

**** Tests

- PENDING UT-33.7 (operator runs =vps-create-nix <new-host>= and confirms the final summary reads 16/16 and includes restic backup)

*** HOLD AC44.1 - Given a per-app NixOS infrastructure config, =deploy-nix= stores service invocation, resource limits, sandbox relaxations, runtime-directory requirements, and timer definitions as structured generated app configuration, while legacy Ubuntu drop-in and timer files remain outside the Nix config source of truth.
:PROPERTIES:
:CUSTOM_ID: ac44-1-given-a-per-app-nixos-infrastructure-config-deploy-nix-stores-service-invocation-resource-limits-sandbox-relaxations-runtime-directory-requirements-and-timer-definitions-as-structured-generated-app-configuration-while-legacy-ubuntu-drop-in-and-timer-files-remain-outside-the-nix-config-source-of-truth
:END:

**** Requirement

Given a per-app NixOS infrastructure config, =deploy-nix= stores service invocation, resource limits, sandbox relaxations, runtime-directory requirements, and timer definitions as structured generated app configuration, while legacy Ubuntu drop-in and timer files remain outside the Nix config source of truth.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/44.md][#44 - Move Nix per-app service overrides and timers into deploy.yaml]]

**** Tests

- PASS RT-44.1: A fixture app with structured =apps/APP/deploy.yaml= service settings generates app JSON containing service args, resource limits, sandbox relaxations, runtime-directory settings, and timer definitions; PASS RT-44.2: A fixture app with only legacy =service.override= and =timer.conf= files generates app JSON without importing those legacy settings

*** HOLD AC44.2 - Given generated NixOS app configuration with service settings, the evaluated NixOS app service has the declared executable arguments, resource limits, sandbox relaxations, runtime-directory environment, and static app identity.
:PROPERTIES:
:CUSTOM_ID: ac44-2-given-generated-nixos-app-configuration-with-service-settings-the-evaluated-nixos-app-service-has-the-declared-executable-arguments-resource-limits-sandbox-relaxations-runtime-directory-environment-and-static-app-identity
:END:

**** Requirement

Given generated NixOS app configuration with service settings, the evaluated NixOS app service has the declared executable arguments, resource limits, sandbox relaxations, runtime-directory environment, and static app identity.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/44.md][#44 - Move Nix per-app service overrides and timers into deploy.yaml]]

**** Tests

- PASS RT-44.3: Nix evaluation of a generated fixture app service contains the declared ExecStart arguments, memory limits, task limits, file limits, ProcSubset setting, and SystemCallFilter setting; PASS RT-44.4: Nix evaluation of a generated fixture app service contains a writable runtime-directory contract for subprocesses that require =XDG_RUNTIME_DIR=

*** HOLD AC44.3 - Given generated NixOS app configuration with timer definitions, the evaluated NixOS host contains app-scoped systemd timer units whose oneshot services run as the app identity with the app runtime contract, and apps without timer definitions have no app timer units.
:PROPERTIES:
:CUSTOM_ID: ac44-3-given-generated-nixos-app-configuration-with-timer-definitions-the-evaluated-nixos-host-contains-app-scoped-systemd-timer-units-whose-oneshot-services-run-as-the-app-identity-with-the-app-runtime-contract-and-apps-without-timer-definitions-have-no-app-timer-units
:END:

**** Requirement

Given generated NixOS app configuration with timer definitions, the evaluated NixOS host contains app-scoped systemd timer units whose oneshot services run as the app identity with the app runtime contract, and apps without timer definitions have no app timer units.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/44.md][#44 - Move Nix per-app service overrides and timers into deploy.yaml]]

**** Tests

- PASS RT-44.5: Nix evaluation of a fixture app with a timer definition contains the expected timer and oneshot service units; PASS RT-44.6: Nix evaluation of a fixture app without timer definitions contains no app timer units

*** SUPERSEDED AC44.4 - Given the dual-running period, legacy Ubuntu deployments retain =deploy-app= support for =apps/APP/service.override= and =apps/APP/timer.conf=, while NixOS deployments use only structured =deploy.yaml= for equivalent per-app service and timer behaviour.
:PROPERTIES:
:CUSTOM_ID: ac44-4-given-the-dual-running-period-legacy-ubuntu-deployments-retain-deploy-app-support-for-apps-app-service-override-and-apps-app-timer-conf-while-nixos-deployments-use-only-structured-deploy-yaml-for-equivalent-per-app-service-and-timer-behaviour
:END:

**** Requirement

Given the dual-running period, legacy Ubuntu deployments retain =deploy-app= support for =apps/APP/service.override= and =apps/APP/timer.conf=, while NixOS deployments use only structured =deploy.yaml= for equivalent per-app service and timer behaviour.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/44.md][#44 - Move Nix per-app service overrides and timers into deploy.yaml]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- PASS RT-44.7: A stubbed legacy =deploy-app= deployment still installs a service override and timer from =apps/APP/service.override= and =apps/APP/timer.conf=; PASS RT-44.8: A stubbed =deploy-nix= deployment ignores legacy =service.override= and =timer.conf= when no structured =deploy.yaml= fields are present

*** HOLD AC44.5 - Given writeback's NixOS infrastructure config includes its required service settings, the evaluated writeback service provides the LibreOffice-compatible runtime and sandbox contract needed for document conversion without relying on Ubuntu drop-ins.
:PROPERTIES:
:CUSTOM_ID: ac44-5-given-writeback-s-nixos-infrastructure-config-includes-its-required-service-settings-the-evaluated-writeback-service-provides-the-libreoffice-compatible-runtime-and-sandbox-contract-needed-for-document-conversion-without-relying-on-ubuntu-drop-ins
:END:

**** Requirement

Given writeback's NixOS infrastructure config includes its required service settings, the evaluated writeback service provides the LibreOffice-compatible runtime and sandbox contract needed for document conversion without relying on Ubuntu drop-ins.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/44.md][#44 - Move Nix per-app service overrides and timers into deploy.yaml]]

**** Tests

- PASS OT-44.1: Nix evaluation of writeback's service contains the declared runtime-directory, ProcSubset, SystemCallFilter, resource, and executable-argument settings from =apps/writeback/deploy.yaml=; PASS UT-44.1: Operator verifies a representative writeback document conversion on a NixOS host completes without the =/run/user/UID= permission failure

*** HOLD AC45.1 - Given a deployed Go application or Hugo website, =deploy-nix --undeploy HOST APP= presents a non-mutating plan identifying the deployment type, generated host artefacts to be withdrawn, and application data retained.
:PROPERTIES:
:CUSTOM_ID: ac45-1-given-a-deployed-go-application-or-hugo-website-deploy-nix-undeploy-host-app-presents-a-non-mutating-plan-identifying-the-deployment-type-generated-host-artefacts-to-be-withdrawn-and-application-data-retained
:END:

**** Requirement

Given a deployed Go application or Hugo website, =deploy-nix --undeploy HOST APP= presents a non-mutating plan identifying the deployment type, generated host artefacts to be withdrawn, and application data retained.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/45.md][#45 - Add deploy-nix undeploy workflow for Go apps and Hugo sites]]

**** Tests

- PASS RT-45.1: Go deployment plan enumerates the ordered apply steps through the CLI, invokes none of them, and leaves its fixture repository byte-for-byte unchanged; PASS RT-45.2: Hugo deployment plan is observed through the CLI and leaves its fixture repository byte-for-byte unchanged; PASS RT-45.3: Plans distinguish present, absent, and optional generated artefacts without treating retained paths as removal targets; PASS RT-45.18: Only first-position =--undeploy= selects withdrawal; =undeploy= remains a valid deployment host name, and legacy or surplus deployment operands fail before side effects; PASS RT-45.20: Successful plan and apply output ends with alert-marked DNS retirement guidance for both A and AAAA records; PASS UT-45.1: Operator confirms the plan makes the removal and retention boundaries understandable before authorizing a change

*** HOLD AC45.2 - Given an authorized undeployment, the named deployment has no declarative NixOS service, timer, Caddy route, operator command, user, or host secret wiring, while its application data, DNS, fleet policy, and every other deployment remain unchanged.
:PROPERTIES:
:CUSTOM_ID: ac45-2-given-an-authorized-undeployment-the-named-deployment-has-no-declarative-nixos-service-timer-caddy-route-operator-command-user-or-host-secret-wiring-while-its-application-data-dns-fleet-policy-and-every-other-deployment-remain-unchanged
:END:

**** Requirement

Given an authorized undeployment, the named deployment has no declarative NixOS service, timer, Caddy route, operator command, user, or host secret wiring, while its application data, DNS, fleet policy, and every other deployment remain unchanged.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/45.md][#45 - Add deploy-nix undeploy workflow for Go apps and Hugo sites]]

**** Tests

- PASS RT-45.4: Go undeployment through the CLI removes only its three generated host artefact classes; PASS RT-45.5: Hugo undeployment through the CLI removes only its generated app and Caddy artefacts; PASS RT-45.6: Nix evaluation of a withdrawn Go app lacks its service, timers, admin command, sudo rule, user, group, Caddy entry, and agenix secret; PASS RT-45.7: Nix evaluation of a withdrawn Hugo site lacks its deploy service and Caddy entry; PASS RT-45.8: A second deployment on the same host remains present and evaluable; PASS RT-45.9: Command-boundary capture proves no remote file deletion, DNS mutation, or application-repository mutation is attempted; PASS OT-45.1: An operator-authorized disposable Go deployment is withdrawn from a NixOS host while retained state and unrelated services remain available; PASS OT-45.2: An operator-authorized disposable Hugo deployment is withdrawn from a NixOS host while unrelated sites remain available

*** HOLD AC45.3 - Given an absent, partially withdrawn, invalid, or previously interrupted deployment, undeployment either converges to the intended declarative state or fails before an unvalidated or unpublished configuration reaches the host.
:PROPERTIES:
:CUSTOM_ID: ac45-3-given-an-absent-partially-withdrawn-invalid-or-previously-interrupted-deployment-undeployment-either-converges-to-the-intended-declarative-state-or-fails-before-an-unvalidated-or-unpublished-configuration-reaches-the-host
:END:

**** Requirement

Given an absent, partially withdrawn, invalid, or previously interrupted deployment, undeployment either converges to the intended declarative state or fails before an unvalidated or unpublished configuration reaches the host.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/45.md][#45 - Add deploy-nix undeploy workflow for Go apps and Hugo sites]]

**** Tests

- PASS RT-45.10: Repeated plan and apply operations are idempotent when all generated artefacts are already absent; PASS RT-45.11: Partial generated state is enumerated and converged without broad directory deletion; PASS RT-45.12: Malformed or identity-mismatched deployment JSON is rejected before mutation; PASS RT-45.13: Invalid host and app identifiers, including traversal input, are rejected at the CLI boundary; PASS RT-45.14: Prospective Nix or Caddy validation failure leaves Git and host execution untouched; PASS RT-45.15: Commit or push failure prevents host execution and reports the recoverable repository state; PASS RT-45.16: A failed host rebuild can be retried after publication even though the generated deployment artefacts are already absent; PASS RT-45.17: Unrelated dirty worktree content is preserved, while conflicting changes to targeted artefacts are refused; PASS RT-45.19: A missing APP before =--apply= is rejected with command usage before plan construction or any undeployment boundary

*** HOLD AC54.1 - Given the real =murderbot= host configuration, Exodan enables Hermes in pinned native systemd mode with the Git-notes adapter, persistent Hermes-owned state, declarative settings that take precedence on activation, and mutable interactive authentication outside the Nix store; a disabled Hermes configuration has no active Hermes service, adapter or secret wiring and does not erase retained Hermes state.
:PROPERTIES:
:CUSTOM_ID: ac54-1-given-the-real-murderbot-host-configuration-exodan-enables-hermes-in-pinned-native-systemd-mode-with-the-git-notes-adapter-persistent-hermes-owned-state-declarative-settings-that-take-precedence-on-activation-and-mutable-interactive-authentication-outside-the-nix-store-a-disabled-hermes-configuration-has-no-active-hermes-service-adapter-or-secret-wiring-and-does-not-erase-retained-hermes-state
:END:

**** Requirement

Given the real =murderbot= host configuration, Exodan enables Hermes in pinned native systemd mode with the Git-notes adapter, persistent Hermes-owned state, declarative settings that take precedence on activation, and mutable interactive authentication outside the Nix store; a disabled Hermes configuration has no active Hermes service, adapter or secret wiring and does not erase retained Hermes state.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.1: Nix evaluation of a host-shaped configuration declares the upstream Hermes service in native mode and persistent state without requiring a provider secret.; PASS RT-54.2: Nix evaluation wires an optional agenix provider environment file without embedding its contents in Nix store values.; PASS RT-54.14: Nix evaluation of the real =murderbot= host declares the native Hermes service with the current Git-notes adapter.; PASS RT-54.29: Nix evaluation of the enabled-to-disabled transition removes active units and secret wiring without declaring deletion of retained Hermes state.; PASS OT-54.1: The deployed Nix closure builds, the host service starts after interactive Codex authentication, and Hermes state remains present across service restart.

*** SUPERSEDED AC54.2 - Given Nextcloud data adapters are configured for Hermes, only declared Nextcloud paths are synchronized locally and each declared path is exposed to Hermes with its configured read-only or read-write mode. Removed: superseded by AC54.6 and AC54.10.
:PROPERTIES:
:CUSTOM_ID: ac54-2-given-nextcloud-data-adapters-are-configured-for-hermes-only-declared-nextcloud-paths-are-synchronized-locally-and-each-declared-path-is-exposed-to-hermes-with-its-configured-read-only-or-read-write-mode-removed-superseded-by-ac54-6-and-ac54-10
:END:

**** Requirement

+Given Nextcloud data adapters are configured for Hermes, only declared Nextcloud paths are synchronized locally and each declared path is exposed to Hermes with its configured read-only or read-write mode.+ Removed: superseded by AC54.6 and AC54.10.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +SUPERSEDED RT-54.3: Nix evaluation declares rclone synchronization only for configured Nextcloud paths.+; +SUPERSEDED RT-54.4: Nix evaluation maps declared Nextcloud paths to their configured read-only or read-write Hermes volumes.+; +SUPERSEDED RT-54.5: Invalid Nextcloud modes or missing source paths fail Nix evaluation with a diagnostic naming the entry.+; +SUPERSEDED OT-54.2: A controlled rclone operation shows only declared Nextcloud paths entering adapter state.+

*** SUPERSEDED AC54.3 - Given Gmail access is enabled for Hermes, Gmail data is synchronized and indexed by a dedicated host-side sync identity and exposed to Hermes as read-only mail context with no SMTP authority. Removed: superseded first by AC54.7 and AC54.11, then withdrawn entirely by AC54.13.
:PROPERTIES:
:CUSTOM_ID: ac54-3-given-gmail-access-is-enabled-for-hermes-gmail-data-is-synchronized-and-indexed-by-a-dedicated-host-side-sync-identity-and-exposed-to-hermes-as-read-only-mail-context-with-no-smtp-authority-removed-superseded-first-by-ac54-7-and-ac54-11-then-withdrawn-entirely-by-ac54-13
:END:

**** Requirement

+Given Gmail access is enabled for Hermes, Gmail data is synchronized and indexed by a dedicated host-side sync identity and exposed to Hermes as read-only mail context with no SMTP authority.+ Removed: superseded first by AC54.7 and AC54.11, then withdrawn entirely by AC54.13.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +SUPERSEDED RT-54.6: Nix evaluation declares the lieer/notmuch sync identity, state, timer and indexing step.+; +SUPERSEDED RT-54.7: Nix evaluation mounts the mail corpus read-only without exposing Gmail OAuth files.+; +SUPERSEDED RT-54.8: Nix evaluation contains no SMTP service, SMTP secret or Hermes mail-send capability.+; +SUPERSEDED UT-54.1: The operator completes lieer Gmail OAuth and confirms the first local indexed corpus.+

*** HOLD AC54.4 - Given Hermes MCP capabilities are configured through Exodan, Hermes receives only explicitly enabled uniquely named servers and tools; duplicate identities, ambiguous command or URL definitions, unsupported URL schemes, empty allowlists, missing secrets, out-of-scope filesystem roots and symlink escapes fail closed.
:PROPERTIES:
:CUSTOM_ID: ac54-4-given-hermes-mcp-capabilities-are-configured-through-exodan-hermes-receives-only-explicitly-enabled-uniquely-named-servers-and-tools-duplicate-identities-ambiguous-command-or-url-definitions-unsupported-url-schemes-empty-allowlists-missing-secrets-out-of-scope-filesystem-roots-and-symlink-escapes-fail-closed
:END:

**** Requirement

Given Hermes MCP capabilities are configured through Exodan, Hermes receives only explicitly enabled uniquely named servers and tools; duplicate identities, ambiguous command or URL definitions, unsupported URL schemes, empty allowlists, missing secrets, out-of-scope filesystem roots and symlink escapes fail closed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.9: Nix evaluation renders only enabled Exodan MCP server entries.; PASS RT-54.10: Nix evaluation scopes each MCP secret through an agenix-backed file rather than an inline store value.; PASS RT-54.11: Filesystem MCP evaluation permits only declared Hermes data roots.; +SUPERSEDED RT-54.18: Gmail MCP evaluation renders only its enabled read-only tool allowlist.+; PASS RT-54.19: Invalid MCP definitions, duplicate identities, unsupported schemes, empty allowlists, missing secrets and path or symlink escapes fail closed.; +SUPERSEDED RT-54.26: Nix and transport tests establish local-only equivalent IPv4/IPv6 Gmail listeners.+; +SUPERSEDED RT-54.30: Failure to bind either Gmail loopback family tears down both listeners and leaves the adapter not ready.+; +SUPERSEDED RT-54.51: A rendered Hermes MCP configuration connects to fake Gmail loopback adapters and discovers exactly the declared tools.+

*** HOLD AC54.5 - Given Hermes is enabled with current adapters, Exodan documentation matches evaluated defaults and describes service and adapter state, bootstrap, model-pin review, access modes, authentication ownership, backup, rotation, divergence, recovery, host regeneration and the authoritative Git, agenix or manual re-authentication source for each state class.
:PROPERTIES:
:CUSTOM_ID: ac54-5-given-hermes-is-enabled-with-current-adapters-exodan-documentation-matches-evaluated-defaults-and-describes-service-and-adapter-state-bootstrap-model-pin-review-access-modes-authentication-ownership-backup-rotation-divergence-recovery-host-regeneration-and-the-authoritative-git-agenix-or-manual-re-authentication-source-for-each-state-class
:END:

**** Requirement

Given Hermes is enabled with current adapters, Exodan documentation matches evaluated defaults and describes service and adapter state, bootstrap, model-pin review, access modes, authentication ownership, backup, rotation, divergence, recovery, host regeneration and the authoritative Git, agenix or manual re-authentication source for each state class.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- +SUPERSEDED RT-54.12: Documentation names the former Nextcloud and lieer/notmuch paths and access modes.+; +SUPERSEDED RT-54.13: Documentation describes the former lieer Gmail OAuth bootstrap.+; +SUPERSEDED RT-54.22: Documentation regression covers current paths, authentication, model routing, regeneration, rotation, failure and recovery boundaries.+ Removed: semantic documentation correctness belongs to documentation-impact review and UT-54.2.; PASS UT-54.2: The operator confirms the documented access, ownership, deliberate backup exclusion, rotation, accepted data-loss boundary and recovery-source model matches the intended boundaries.

*** HOLD AC54.6 - Given the Git notes adapter is enabled, a dedicated host identity refreshes the configured private repository every five minutes under the same checkout lock used by the draft publisher; safe fast-forwards atomically replace Hermes complete read-only notes directory, reuse unchanged regular files through hard links between immutable generations, remove files deleted upstream, retain a bounded number of old generations without reclaiming the active generation, and leave the previous generation intact on initialization, remote-update, divergence, authentication, validation, replacement, cross-filesystem, corrupt-current or cleanup failure; concurrent runs serialize, unsupported file types are rejected, only the separate =drafts/= directory is writable, and =.git=, SSH credentials, the synchronizer checkout, external symlink targets and undeclared submodule content remain inaccessible.
:PROPERTIES:
:CUSTOM_ID: ac54-6-given-the-git-notes-adapter-is-enabled-a-dedicated-host-identity-refreshes-the-configured-private-repository-every-five-minutes-under-the-same-checkout-lock-used-by-the-draft-publisher-safe-fast-forwards-atomically-replace-hermes-complete-read-only-notes-directory-reuse-unchanged-regular-files-through-hard-links-between-immutable-generations-remove-files-deleted-upstream-retain-a-bounded-number-of-old-generations-without-reclaiming-the-active-generation-and-leave-the-previous-generation-intact-on-initialization-remote-update-divergence-authentication-validation-replacement-cross-filesystem-corrupt-current-or-cleanup-failure-concurrent-runs-serialize-unsupported-file-types-are-rejected-only-the-separate-drafts-directory-is-writable-and-git-ssh-credentials-the-synchronizer-checkout-external-symlink-targets-and-undeclared-submodule-content-remain-inaccessible
:END:

**** Requirement

Given the Git notes adapter is enabled, a dedicated host identity refreshes the configured private repository every five minutes under the same checkout lock used by the draft publisher; safe fast-forwards atomically replace Hermes complete read-only notes directory, reuse unchanged regular files through hard links between immutable generations, remove files deleted upstream, retain a bounded number of old generations without reclaiming the active generation, and leave the previous generation intact on initialization, remote-update, divergence, authentication, validation, replacement, cross-filesystem, corrupt-current or cleanup failure; concurrent runs serialize, unsupported file types are rejected, only the separate =drafts/= directory is writable, and =.git=, SSH credentials, the synchronizer checkout, external symlink targets and undeclared submodule content remain inaccessible.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.15: Nix evaluation declares the dedicated Git identity, five-minute timer, shared checkout lock, read-only notes parent mount and separate read-write drafts mount without exposing credentials, the checkout or =.git=.; PASS RT-54.16: Real refresh-entry-point fixtures cover first clone, no-op refresh, safe fast-forward, atomic replacement, hard-link reuse and changed-file replacement.; PASS RT-54.17: Real refresh-entry-point fixtures reject external symlinks, special files and undeclared submodules and expose neither Git metadata nor the private checkout.; PASS RT-54.31: Upstream deletion and bounded generation cleanup remove obsolete files while preserving the active generation.; PASS RT-54.32: Initialization, remote-update, divergence, authentication, validation, replacement, cross-filesystem, corrupt-current and cleanup failures preserve the previous generation.; PASS RT-54.33: Concurrent refresh and publish entry points contend on the same lock and both leave recoverable state.; PASS RT-54.34: Refresh never overwrites or incorporates the separate writable drafts directory.; PASS OT-54.3: One guarded minimum-request run synchronizes the configured private repository without automatic authentication retry while Hermes remains unable to access Git metadata or credentials.

*** SUPERSEDED AC54.7 - Given Gmail access is enabled, a dedicated host-side MCP service exposes only bounded read-only folder and message listing, structured search and stable-identifier message-read operations through Himalaya; hostile mail content remains structured and diagnostics redacted, arbitrary execution, OAuth files, SMTP or outbound-mail authority and raw mail-directory access remain unavailable, and successful operations leave Gmail flags and remote metadata unchanged. Removed: all mail access moved outside Hermes by AC54.13.
:PROPERTIES:
:CUSTOM_ID: ac54-7-given-gmail-access-is-enabled-a-dedicated-host-side-mcp-service-exposes-only-bounded-read-only-folder-and-message-listing-structured-search-and-stable-identifier-message-read-operations-through-himalaya-hostile-mail-content-remains-structured-and-diagnostics-redacted-arbitrary-execution-oauth-files-smtp-or-outbound-mail-authority-and-raw-mail-directory-access-remain-unavailable-and-successful-operations-leave-gmail-flags-and-remote-metadata-unchanged-removed-all-mail-access-moved-outside-hermes-by-ac54-13
:END:

**** Requirement

+Given Gmail access is enabled, a dedicated host-side MCP service exposes only bounded read-only folder and message listing, structured search and stable-identifier message-read operations through Himalaya; hostile mail content remains structured and diagnostics redacted, arbitrary execution, OAuth files, SMTP or outbound-mail authority and raw mail-directory access remain unavailable, and successful operations leave Gmail flags and remote metadata unchanged.+ Removed: all mail access moved outside Hermes by AC54.13.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +SUPERSEDED RT-54.18: Nix evaluation declares only the Gmail read-tool allowlist and dedicated adapter identity.+; +SUPERSEDED RT-54.19: Invalid Gmail MCP definitions and secret exposure fail evaluation.+; +SUPERSEDED RT-54.20: The packaged service exposes exactly the list/search/read tools through Streamable HTTP and maps validated requests to fixed Himalaya argv without a shell or mutation path.+; +SUPERSEDED RT-54.27: Component tests return bounded structured errors for hostile, missing, malformed, oversized, rate-limited, unavailable and concurrent inputs and isolate Hermes from adapter failure.+; +SUPERSEDED RT-54.35: List operations preserve synthetic flags and metadata and return bounded structured results.+; +SUPERSEDED RT-54.36: Structured search validates folders and search fields while preserving synthetic flags and metadata.+; +SUPERSEDED RT-54.37: Stable-identifier reads bound and structure hostile headers and bodies, redact diagnostics and preserve synthetic flags and metadata.+; +SUPERSEDED UT-54.3: The operator confirms the deployed custom adapter reads through the persisted OAuth credentials without send authority or metadata changes.+

*** HOLD AC54.8 - Given the declarative inference policy, Hermes uses =gpt-5.6-terra= through =openai-codex= by default, routes supported auxiliary work to =gpt-5.6-luna=, and uses =z-ai/glm-5.2= through the named key-backed =nous-api= custom provider when that revision fallback engine activates; trigger semantics remain owned by the pinned revision and are reviewed when its pin changes, deliberate valid alternatives leave the declarative default unchanged, invalid selections fail explicitly, and simultaneous primary and fallback failure remains an explicit provider error.
:PROPERTIES:
:CUSTOM_ID: ac54-8-given-the-declarative-inference-policy-hermes-uses-gpt-5-6-terra-through-openai-codex-by-default-routes-supported-auxiliary-work-to-gpt-5-6-luna-and-uses-z-ai-glm-5-2-through-the-named-key-backed-nous-api-custom-provider-when-that-revision-fallback-engine-activates-trigger-semantics-remain-owned-by-the-pinned-revision-and-are-reviewed-when-its-pin-changes-deliberate-valid-alternatives-leave-the-declarative-default-unchanged-invalid-selections-fail-explicitly-and-simultaneous-primary-and-fallback-failure-remains-an-explicit-provider-error
:END:

**** Requirement

Given the declarative inference policy, Hermes uses =gpt-5.6-terra= through =openai-codex= by default, routes supported auxiliary work to =gpt-5.6-luna=, and uses =z-ai/glm-5.2= through the named key-backed =nous-api= custom provider when that revision fallback engine activates; trigger semantics remain owned by the pinned revision and are reviewed when its pin changes, deliberate valid alternatives leave the declarative default unchanged, invalid selections fail explicitly, and simultaneous primary and fallback failure remains an explicit provider error.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.21: Nix evaluation renders the Codex default, Luna auxiliary slots, ordered named Nous fallback and =NOUS_API_KEY= indirection without unsupported reasoning keys.; +SUPERSEDED RT-54.22: Documentation names the deliberate alternatives, selection boundary and unchanged declarative default.+ Removed: semantic documentation correctness belongs to documentation-impact review and UT-54.2.; PASS RT-54.38: Local provider fixtures exercise the pinned revision supported fallback trigger path without a live authentication failure.; PASS RT-54.39: Explicit valid alternatives leave the declarative default unchanged and unknown provider or model selections fail explicitly.; PASS RT-54.40: Simultaneous local-fixture primary and fallback failure remains a bounded explicit provider error.; PASS OT-54.4: One minimum-request Codex operation identifies the active primary provider and model without automatic authentication retry.; PASS OT-54.8: One minimum-request Nous operation identifies the configured fallback provider and model without forcing a live Codex authentication failure.

*** HOLD AC54.9 - Given static credentials for =murderbot=, agenix declares the operator recipient and current retained murderbot host recipient---and no fleet, backup or unrelated-host recipient---for the Nous API key and writable notes deploy key; each decrypts with consumer-only ownership and modes, rotation failure preserves the previously usable generation, only affected consumers restart, Codex OAuth remains outside agenix, and secrets remain absent from the Nix store, arguments, evaluation output, logs and fixtures.
:PROPERTIES:
:CUSTOM_ID: ac54-9-given-static-credentials-for-murderbot-agenix-declares-the-operator-recipient-and-current-retained-murderbot-host-recipient-and-no-fleet-backup-or-unrelated-host-recipient-for-the-nous-api-key-and-writable-notes-deploy-key-each-decrypts-with-consumer-only-ownership-and-modes-rotation-failure-preserves-the-previously-usable-generation-only-affected-consumers-restart-codex-oauth-remains-outside-agenix-and-secrets-remain-absent-from-the-nix-store-arguments-evaluation-output-logs-and-fixtures
:END:

**** Requirement

Given static credentials for =murderbot=, agenix declares the operator recipient and current retained murderbot host recipient---and no fleet, backup or unrelated-host recipient---for the Nous API key and writable notes deploy key; each decrypts with consumer-only ownership and modes, rotation failure preserves the previously usable generation, only affected consumers restart, Codex OAuth remains outside agenix, and secrets remain absent from the Nix store, arguments, evaluation output, logs and fixtures. The generic repository deploy-key command accepts configurable host, repository, title, output, recipient and permission inputs, defaults to read-only registration, confirms encrypted-output decryptability through standard CLIs and leaves no plaintext temporary key material.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.23: Evaluated recipient declarations establish the operator plus retained murderbot host set, exclude fleet, backup and unrelated-host recipients, and prove consumer-only metadata, two distinct inputs, excluded Codex paths and absence of plaintext store-facing values without consuming private identities.; PASS RT-54.28: Fake-CLI subprocess tests cover help, validation, side-effect-free dry-run, configurable recipients and output, read-only default, explicit write opt-in, encrypted-output verification, GitHub registration and plaintext temporary-key cleanup without real SSH keys or provider calls.; PASS RT-54.41: Synthetic consumers remove exactly one optional LF or CRLF and reject empty or multiply terminated raw values.; +SUPERSEDED RT-54.42: Partial Gmail payload sets fail consumer readiness and restart wiring remains scoped to affected services.+; PASS RT-54.43: Declarative recipient-set fixtures reject stale or additional declared recipients, and synthetic failed-rotation fixtures preserve the prior usable generation.; PASS OT-54.5: Read-only host checks establish current-host decryptability through successful agenix activation, decryption ownership and mode, consumer wiring, scoped restart declarations and redacted diagnostics without reading, rotating or revoking credentials.

*** HOLD AC54.10 - Given Hermes changes supported regular files under =drafts/=, the host publisher stages no other path, represents controlled draft deletion, creates a bounded identifiable commit only for changed draft content, and pushes only from a safe remote relation; symlinks and special files are rejected, dirty non-draft state, concurrent changes, divergence, authentication failure, network failure, crash and uncertain prior push outcomes preserve unpublished work without force-push, reset, duplicate commit or automatic conflict resolution, and a later safe publication cycle reconciles and publishes it.
:PROPERTIES:
:CUSTOM_ID: ac54-10-given-hermes-changes-supported-regular-files-under-drafts-the-host-publisher-stages-no-other-path-represents-controlled-draft-deletion-creates-a-bounded-identifiable-commit-only-for-changed-draft-content-and-pushes-only-from-a-safe-remote-relation-symlinks-and-special-files-are-rejected-dirty-non-draft-state-concurrent-changes-divergence-authentication-failure-network-failure-crash-and-uncertain-prior-push-outcomes-preserve-unpublished-work-without-force-push-reset-duplicate-commit-or-automatic-conflict-resolution-and-a-later-safe-publication-cycle-reconciles-and-publishes-it
:END:

**** Requirement

Given Hermes changes supported regular files under =drafts/=, the host publisher stages no other path, represents controlled draft deletion, creates a bounded identifiable commit only for changed draft content, and pushes only from a safe remote relation; symlinks and special files are rejected, dirty non-draft state, concurrent changes, divergence, authentication failure, network failure, crash and uncertain prior push outcomes preserve unpublished work without force-push, reset, duplicate commit or automatic conflict resolution, and a later safe publication cycle reconciles and publishes it.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.24: Real publisher-entry-point fixtures cover draft-only staging, controlled deletion, bounded commit identity, unchanged-content no-op, serialization and safe publication.; PASS RT-54.25: Dirty non-draft state, divergence, concurrent change, authentication failure and network failure preserve work and permit a later safe retry without force, reset or conflict resolution.; PASS RT-54.33: Concurrent refresh and publish entry points contend on the same lock and both leave recoverable state.; PASS RT-54.44: Publisher fixtures reject symlink and special-file drafts without staging undeclared paths.; PASS RT-54.45: Crash and uncertain-push fixtures reconcile the remote relation before retry and avoid duplicate commits.; PASS OT-54.6: One guarded minimum-request run sends a controlled host draft through a draft-only commit while a controlled divergence remains local and unmodified.

*** SUPERSEDED AC54.11 - Given the Gmail MCP adapter is enabled, its transport becomes ready only when equivalent =127.0.0.1= and =::1= listeners are local-only; missing or expired OAuth, missing Himalaya, malformed or oversized output, invalid account, folder or message input, timeout, rate limit, unavailable messages and concurrent operations remain bounded structured errors without widened access, shell execution, Gmail mutation, partial-family readiness or Hermes failure. Removed: all mail access moved outside Hermes by AC54.13.
:PROPERTIES:
:CUSTOM_ID: ac54-11-given-the-gmail-mcp-adapter-is-enabled-its-transport-becomes-ready-only-when-equivalent-127-0-0-1-and-1-listeners-are-local-only-missing-or-expired-oauth-missing-himalaya-malformed-or-oversized-output-invalid-account-folder-or-message-input-timeout-rate-limit-unavailable-messages-and-concurrent-operations-remain-bounded-structured-errors-without-widened-access-shell-execution-gmail-mutation-partial-family-readiness-or-hermes-failure-removed-all-mail-access-moved-outside-hermes-by-ac54-13
:END:

**** Requirement

+Given the Gmail MCP adapter is enabled, its transport becomes ready only when equivalent =127.0.0.1= and =::1= listeners are local-only; missing or expired OAuth, missing Himalaya, malformed or oversized output, invalid account, folder or message input, timeout, rate limit, unavailable messages and concurrent operations remain bounded structured errors without widened access, shell execution, Gmail mutation, partial-family readiness or Hermes failure.+ Removed: all mail access moved outside Hermes by AC54.13.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +SUPERSEDED RT-54.26: Nix and transport tests establish local-only equivalent IPv4 and IPv6 loopback listeners.+; +SUPERSEDED RT-54.27: Component tests cover bounded dependency, input, output, timeout, rate-limit, unavailable-message and concurrency errors without shell execution, access widening, metadata mutation or Hermes-process failure.+; +SUPERSEDED RT-54.30: Failure to bind either loopback family tears down both listeners and leaves the adapter not ready.+; +SUPERSEDED OT-54.7: One minimum-request read-only Gmail operation compares flags and remote metadata before and after access without automatic authentication retry.+; +SUPERSEDED OT-54.9: Host transport checks establish both loopback listeners are reachable locally and neither public IPv4 nor public IPv6 can reach the adapter.+

*** SUPERSEDED AC54.12 - Given the three Gmail OAuth payloads are consumer-ready, the adapter remains network-idle at startup, performs one non-redirecting and non-retrying refresh-token exchange at first read, stores the short-lived access token with consumer-only mode, reuses it until near expiry, refreshes it near expiry, and rejects malformed or failed token responses before Himalaya invocation without broadening access. Removed: all Gmail credentials and runtime state moved outside murderbot by AC54.13.
:PROPERTIES:
:CUSTOM_ID: ac54-12-given-the-three-gmail-oauth-payloads-are-consumer-ready-the-adapter-remains-network-idle-at-startup-performs-one-non-redirecting-and-non-retrying-refresh-token-exchange-at-first-read-stores-the-short-lived-access-token-with-consumer-only-mode-reuses-it-until-near-expiry-refreshes-it-near-expiry-and-rejects-malformed-or-failed-token-responses-before-himalaya-invocation-without-broadening-access-removed-all-gmail-credentials-and-runtime-state-moved-outside-murderbot-by-ac54-13
:END:

**** Requirement

+Given the three Gmail OAuth payloads are consumer-ready, the adapter remains network-idle at startup, performs one non-redirecting and non-retrying refresh-token exchange at first read, stores the short-lived access token with consumer-only mode, reuses it until near expiry, refreshes it near expiry, and rejects malformed or failed token responses before Himalaya invocation without broadening access.+ Removed: all Gmail credentials and runtime state moved outside murderbot by AC54.13.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Status qualification

- *Status qualification:* ; history retained
**** Tests

- +SUPERSEDED RT-54.46: Service startup with a fake token endpoint makes no network request.+; +SUPERSEDED RT-54.47: First read performs exactly one non-redirecting, non-retrying refresh-token exchange before fixed Himalaya invocation.+; +SUPERSEDED RT-54.48: An unexpired mode-0600 access-token cache is reused without token-endpoint contact.+; +SUPERSEDED RT-54.49: A near-expiry cache causes one refresh and atomic consumer-only cache replacement.+; +SUPERSEDED RT-54.50: Malformed, redirected, rate-limited and failed token responses remain bounded errors and never invoke Himalaya.+; +SUPERSEDED RT-54.52: The actual Nix package build contains OAuth-capable Himalaya and the packaged adapter without contacting Google.+; +SUPERSEDED OT-54.7: One minimum-request live Gmail operation establishes the persisted refresh-token path without automatic authentication retry.+

*** HOLD AC54.13 - Given murderbot runs Hermes, no Exodan-managed service, environment, secret, mount, MCP definition, state path or recovery source introduces Gmail credentials, a mail adapter or an automated mail-origin data path; human-authored trusted notes remain outside content classification.
:PROPERTIES:
:CUSTOM_ID: ac54-13-given-murderbot-runs-hermes-no-exodan-managed-service-environment-secret-mount-mcp-definition-state-path-or-recovery-source-introduces-gmail-credentials-a-mail-adapter-or-an-automated-mail-origin-data-path-human-authored-trusted-notes-remain-outside-content-classification
:END:

**** Requirement

Given murderbot runs Hermes, no Exodan-managed service, environment, secret, mount, MCP definition, state path or recovery source introduces Gmail credentials, a mail adapter or an automated mail-origin data path; human-authored trusted notes remain outside content classification.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.53: Real murderbot Nix evaluation contains no Gmail secret, adapter, environment, mount, unit, MCP entry or declared mail-origin recovery input while retaining the declared notes and inference capabilities.; PASS RT-54.54: Enabled-to-disabled and regeneration fixtures prove no generic module default, retained wiring or declared recovery input introduces an automated mail path; tests do not inspect or classify human-authored note content.; PASS OT-54.10: Read-only host evidence establishes the absence of Gmail units, credentials, mounts and MCP declarations without reading any secret value.

*** HOLD AC54.14 - Given murderbot is a non-web Hermes host, its evaluated role contains the hardened base and Hermes capabilities while fleet restic, Caddy, CrowdSec and Hugo remain absent; murderbot possesses no fleet-restic secret, repository configuration, timer or network authority, and fleet verification treats absent optional components as not applicable without weakening the remaining strict measures.
:PROPERTIES:
:CUSTOM_ID: ac54-14-given-murderbot-is-a-non-web-hermes-host-its-evaluated-role-contains-the-hardened-base-and-hermes-capabilities-while-fleet-restic-caddy-crowdsec-and-hugo-remain-absent-murderbot-possesses-no-fleet-restic-secret-repository-configuration-timer-or-network-authority-and-fleet-verification-treats-absent-optional-components-as-not-applicable-without-weakening-the-remaining-strict-measures
:END:

**** Requirement

Given murderbot is a non-web Hermes host, its evaluated role contains the hardened base and Hermes capabilities while fleet restic, Caddy, CrowdSec and Hugo remain absent; murderbot possesses no fleet-restic secret, repository configuration, timer or network authority, and fleet verification treats absent optional components as not applicable without weakening the remaining strict measures.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.55: Real murderbot Nix evaluation contains base hardening and Hermes while omitting restic secrets, repository configuration and timers as well as Caddy, CrowdSec and Hugo declarations.; PASS RT-54.56: Evaluated fleet-verifier applicability classifies the absent optional components as not applicable and retains strict failure for enabled-but-unhealthy components.; PASS OT-54.11: One post-deployment strict verification establishes the reduced measure set and healthy required measures on murderbot, and read-only evidence confirms no restic unit, secret or repository configuration is present.

*** HOLD AC54.15 - Given Hermes skills, memory, sessions, Codex authentication, drafts and adapter state are mutable host state, their posture and persistence paths are declared rather than inherited accidentally, no fleet backup covers them, host destruction may lose unpublished or non-Git state, regeneration restores only declarative and Git-backed state without widening Hermes filesystem access, and interactive provider re-authentication remains explicit.
:PROPERTIES:
:CUSTOM_ID: ac54-15-given-hermes-skills-memory-sessions-codex-authentication-drafts-and-adapter-state-are-mutable-host-state-their-posture-and-persistence-paths-are-declared-rather-than-inherited-accidentally-no-fleet-backup-covers-them-host-destruction-may-lose-unpublished-or-non-git-state-regeneration-restores-only-declarative-and-git-backed-state-without-widening-hermes-filesystem-access-and-interactive-provider-re-authentication-remains-explicit
:END:

**** Requirement

Given Hermes skills, memory, sessions, Codex authentication, drafts and adapter state are mutable host state, their posture and persistence paths are declared rather than inherited accidentally, no fleet backup covers them, host destruction may lose unpublished or non-Git state, regeneration restores only declarative and Git-backed state without widening Hermes filesystem access, and interactive provider re-authentication remains explicit.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/54.md][#54 - Deploy Hermes on murderbot with Git notes and no mail access]]

**** Tests

- PASS RT-54.57: Nix evaluation declares the supported skill and memory posture, persistent state paths and scoped mounts while proving that no restic credential or backup wiring is present.; PASS RT-54.58: Regeneration fixtures restore declarative and Git-backed state only, begin other mutable state empty, preserve ownership and access boundaries, and surface missing provider authentication as a bounded failure.; PASS OT-54.12: Read-only host evidence establishes persistent ownership and mount modes, confirms the absence of fleet-restic material, and does not read state or credential contents.; PASS UT-54.4: The operator confirms the delivered memory, skills, deliberate no-backup posture, accepted data-loss boundary and recovery model match the intended Hermes value and trust boundary.

*** ASSUMED_PASS AC55.1 - Given AIO admin firewall rules include descriptions, =nc-aio close= removes the described rules using the full Hetzner rule identity.
:PROPERTIES:
:CUSTOM_ID: ac55-1-given-aio-admin-firewall-rules-include-descriptions-nc-aio-close-removes-the-described-rules-using-the-full-hetzner-rule-identity
:END:

**** Requirement

Given AIO admin firewall rules include descriptions, =nc-aio close= removes the described rules using the full Hetzner rule identity.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/55.md][#55 - nc-aio close fails to delete described Hetzner firewall rules]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-55.1: A stubbed =hcloud= close run receives =firewall delete-rule= calls for 8080 and 8443 that include each rule's source IP and description.; ASSUMED PASS OT-55.1: From an authenticated operator shell with live AIO firewall rules present, =nc-aio close= removes the remaining 8080/8443 rules and =nc-aio status= reports the AIO admin ports closed.

*** ASSUMED_PASS AC55.2 - Given an operator opens AIO admin access, =nc-aio open= exposes only the 8080 self-signed admin interface and does not create new 8443 firewall rules.
:PROPERTIES:
:CUSTOM_ID: ac55-2-given-an-operator-opens-aio-admin-access-nc-aio-open-exposes-only-the-8080-self-signed-admin-interface-and-does-not-create-new-8443-firewall-rules
:END:

**** Requirement

Given an operator opens AIO admin access, =nc-aio open= exposes only the 8080 self-signed admin interface and does not create new 8443 firewall rules.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/55.md][#55 - nc-aio close fails to delete described Hetzner firewall rules]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-55.2: A stubbed =hcloud= open run receives 8080 add-rule calls for detected IPv4 and IPv6 operator addresses, receives no 8443 add-rule call, and prints 8080 URLs.

*** ASSUMED_PASS AC56.1 - Given a NixOS host imports the shared Exodan host baseline, the host font configuration includes the standard public font families needed by PDF-rendering apps.
:PROPERTIES:
:CUSTOM_ID: ac56-1-given-a-nixos-host-imports-the-shared-exodan-host-baseline-the-host-font-configuration-includes-the-standard-public-font-families-needed-by-pdf-rendering-apps
:END:

**** Requirement

Given a NixOS host imports the shared Exodan host baseline, the host font configuration includes the standard public font families needed by PDF-rendering apps.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/56.md][#56 - Add fleet font baseline for NixOS PDF rendering]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-56.1: Nix evaluation of a representative host shows =fonts.packages= contains the public font packages for Liberation, Libertinus, and Agave.; PASS RT-56.2: Nix evaluation of the shared role shows the font baseline module is imported by hosts that use the standard Exodan NixOS role.; ASSUMED PASS OT-56.1: On a rebuilt NixOS host, =fc-match= resolves representative Liberation, Libertinus, and Agave public family names.

*** ASSUMED_PASS AC56.2 - Given the Exodan flake is evaluated without access to operator home directories, the requested custom font families are available from reproducible Nix sources.
:PROPERTIES:
:CUSTOM_ID: ac56-2-given-the-exodan-flake-is-evaluated-without-access-to-operator-home-directories-the-requested-custom-font-families-are-available-from-reproducible-nix-sources
:END:

**** Requirement

Given the Exodan flake is evaluated without access to operator home directories, the requested custom font families are available from reproducible Nix sources.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/56.md][#56 - Add fleet font baseline for NixOS PDF rendering]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-56.3: Nix evaluation of the custom font package succeeds without references to =/Users/tigger=, =~/tfont=, or =~/fonting= in evaluated host configuration.; PASS RT-56.4: The custom font package exposes installable font files for Asap, Iosevka Custom, Iosevka Quasi-Proportional, and Special Elite.; ASSUMED PASS OT-56.2: On a rebuilt NixOS host, =fc-match= resolves representative names from the custom font set.

*** ASSUMED_PASS AC56.3 - Given a deployed app service uses fontconfig-backed rendering on a NixOS host, the service user can resolve the fleet font set without app-local bundled font files.
:PROPERTIES:
:CUSTOM_ID: ac56-3-given-a-deployed-app-service-uses-fontconfig-backed-rendering-on-a-nixos-host-the-service-user-can-resolve-the-fleet-font-set-without-app-local-bundled-font-files
:END:

**** Requirement

Given a deployed app service uses fontconfig-backed rendering on a NixOS host, the service user can resolve the fleet font set without app-local bundled font files.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/56.md][#56 - Add fleet font baseline for NixOS PDF rendering]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-56.5: Nix evaluation of a deployed-app service keeps the service runtime independent of app =data_dirs= for the fleet font set.; ASSUMED PASS OT-56.3: On a rebuilt validation host with =writeback= deployed, =writeback-admin= or an equivalent app-user command resolves representative fleet fonts through fontconfig.; ASSUMED PASS UT-56.1: A human compares a generated =writeback= PDF against the expected typography and confirms the intended fonts are used.

*** ASSUMED_PASS AC57.1 - Given the Mac remote desktop guard is installed, macOS has a persistent local firewall boundary that permits TCP 5900 from Tailscale addresses and denies TCP 5900 from non-tailnet sources without editing Apple-owned =pf= base files.
:PROPERTIES:
:CUSTOM_ID: ac57-1-given-the-mac-remote-desktop-guard-is-installed-macos-has-a-persistent-local-firewall-boundary-that-permits-tcp-5900-from-tailscale-addresses-and-denies-tcp-5900-from-non-tailnet-sources-without-editing-apple-owned-pf-base-files
:END:

**** Requirement

Given the Mac remote desktop guard is installed, macOS has a persistent local firewall boundary that permits TCP 5900 from Tailscale addresses and denies TCP 5900 from non-tailnet sources without editing Apple-owned =pf= base files.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.1: Go unit tests render the planned Exodan =pf= main ruleset, anchor, and launchd plist from typed config and verify Tailscale IPv4 and IPv6 allow rules precede the final deny rule.; PASS RT-57.2: Go unit tests use a fake runner/filesystem to verify install validates generated =pf= content before load and mutates only Exodan-owned paths.; PASS RT-57.3: Go unit tests verify uninstall unloads only Exodan-owned launchd jobs, flushes only the Exodan anchor, and leaves Apple-owned =pf= files untouched.; ASSUMED PASS OT-57.1: On the target Mac, TCP 5900 is reachable over Tailscale and not reachable over the ordinary LAN interface.

*** ASSUMED_PASS AC57.2 - Given the operator uses the guard tooling, the non-root installation boundary and sudo mutation boundary are explicit, predictable, and tested without depending on sudo's PATH.
:PROPERTIES:
:CUSTOM_ID: ac57-2-given-the-operator-uses-the-guard-tooling-the-non-root-installation-boundary-and-sudo-mutation-boundary-are-explicit-predictable-and-tested-without-depending-on-sudo-s-path
:END:

**** Requirement

Given the operator uses the guard tooling, the non-root installation boundary and sudo mutation boundary are explicit, predictable, and tested without depending on sudo's PATH.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.4: Go unit tests inject effective UID and verify mutating host commands self-escalate through =/usr/bin/sudo= using the resolved absolute executable path, while read-only =plan= remains non-root.; PASS RT-57.5: Go unit tests verify launchd plists use an absolute executable path captured from the installed command, not =sh -c=, =bash -c=, or an unresolved PATH lookup.; ASSUMED PASS OT-57.2: A one-off Makefile/install check runs =make install-mac-screen-sharing= as the normal user, then runs a mutating installed-command path without prefixing sudo and verifies the tool invokes the native sudo password prompt for live mutation.

*** ASSUMED_PASS AC57.3 - Given Tailscale policy support is included, the generated policy is grants-based, scoped to user-owned devices, and validation prevents wider remote desktop access by default.
:PROPERTIES:
:CUSTOM_ID: ac57-3-given-tailscale-policy-support-is-included-the-generated-policy-is-grants-based-scoped-to-user-owned-devices-and-validation-prevents-wider-remote-desktop-access-by-default
:END:

**** Requirement

Given Tailscale policy support is included, the generated policy is grants-based, scoped to user-owned devices, and validation prevents wider remote desktop access by default.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.6: Go unit tests generate policy JSON/HuJSON with =src= user selector, =dst= =autogroup:self=, and =ip= =tcp:5900=.; PASS RT-57.7: Go unit tests reject raw tailnet IP and daily-driver device-tag strategies unless an explicit override is supplied.; ASSUMED PASS OT-57.3: The generated policy is validated locally, reviewed by the operator, and then applied manually through the Tailscale admin UI or a separately validated API workflow; policy preview shows only intended TCP 5900 reachability.

*** ASSUMED_PASS AC57.4 - Given the Mac remote desktop guard is ready for operator use, documentation describes the trust model, manual macOS settings, sudo boundary, iPad client setup, temporary LAN exception tradeoff, rollback, and required user validation.
:PROPERTIES:
:CUSTOM_ID: ac57-4-given-the-mac-remote-desktop-guard-is-ready-for-operator-use-documentation-describes-the-trust-model-manual-macos-settings-sudo-boundary-ipad-client-setup-temporary-lan-exception-tradeoff-rollback-and-required-user-validation
:END:

**** Requirement

Given the Mac remote desktop guard is ready for operator use, documentation describes the trust model, manual macOS settings, sudo boundary, iPad client setup, temporary LAN exception tradeoff, rollback, and required user validation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.8: Go unit tests or CLI tests verify =--help= exposes the supported command surface and keeps mutation commands behind explicit =--apply=.; PASS RT-57.9: Documentation regression verifies the operator guide covers manual Screen Sharing setup, Tailscale as encrypted transport, =pf= as local boundary, normal-user Makefile install, absolute-path sudo use, and rollback.; PASS UT-57.1: The operator reviews the documented trust model and confirms the final balance between tailnet-wide/self-device access, temporary LAN exceptions, and daily-driver device identity is acceptable.

*** ASSUMED_PASS AC57.5 - Given the operator needs temporary high-fidelity local network access, the guard can open a source-specific local LAN exception for a bounded time and reliably return to tailnet-only access without depending on operator memory.
:PROPERTIES:
:CUSTOM_ID: ac57-5-given-the-operator-needs-temporary-high-fidelity-local-network-access-the-guard-can-open-a-source-specific-local-lan-exception-for-a-bounded-time-and-reliably-return-to-tailnet-only-access-without-depending-on-operator-memory
:END:

**** Requirement

Given the operator needs temporary high-fidelity local network access, the guard can open a source-specific local LAN exception for a bounded time and reliably return to tailnet-only access without depending on operator memory.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.10: Go unit tests model the =local-open= state transition and verify it accepts exactly one IPv4 source address, rejects subnet/wildcard inputs, enforces the maximum duration, and renders the local pass rule before the final deny rule.; PASS RT-57.11: Go unit tests verify expiry setup is completed before the local exception is loaded into active PF state, and failure at any expiry step leaves the active firewall in tailnet-only state.; PASS RT-57.12: Go unit tests verify =local-close= is idempotent and restores the tailnet-only anchor even when no local exception is active.; ASSUMED PASS OT-57.4: On the target Mac and trusted LAN, a temporary local exception allows the specified local iPad IPv4 address during the window and is closed early or expires back to tailnet-only access.

*** ASSUMED_PASS AC57.6 - Given any supported subcommand is run repeatedly or after a partial prior attempt, the command is idempotent where possible, fails safely where not possible, and reports the current state clearly without widening access.
:PROPERTIES:
:CUSTOM_ID: ac57-6-given-any-supported-subcommand-is-run-repeatedly-or-after-a-partial-prior-attempt-the-command-is-idempotent-where-possible-fails-safely-where-not-possible-and-reports-the-current-state-clearly-without-widening-access
:END:

**** Requirement

Given any supported subcommand is run repeatedly or after a partial prior attempt, the command is idempotent where possible, fails safely where not possible, and reports the current state clearly without widening access.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/57.md][#57 - Design secure Mac Screen Sharing over Tailscale]]

**** Status qualification

- *Status qualification:* by migration heuristic [fn:migration-heuristic]
**** Tests

- PASS RT-57.13: Go unit tests verify repeated =install --apply=, =uninstall --apply=, =local-open=, and =local-close= calls converge to the expected state without duplicate launchd/PF artefacts or widened firewall rules.; PASS RT-57.14: Go unit tests inject partial-state fixtures (missing plist, stale anchor, active main ruleset missing the Exodan anchor, expired local exception metadata) and verify recovery either repairs the owned state or fails before changing active PF exposure.; PASS RT-57.15: Go unit tests verify =status= distinguishes installed, partially installed, local-exception-active, expired-exception, and absent states in user-facing output.; PASS RT-57.16: Installed CLI regression builds the real binary, installs it behind a user-facing symlink, runs =mac-screen-sharing status=, and verifies observable output plus sudo-backed PF inspection through executable test doubles.; PASS OT-57.5: On the target Mac, the installed CLI runs live install/status/local-open/local-close/uninstall cycles against real sudo, pfctl, launchctl, and PF state; each step verifies the expected active firewall/launchd state and returns the host to no Exodan guard state at the end.

*** HOLD AC58.1 - Given a deployable Go app declares scheduled routine intent in app-owned configuration, deploy-nix represents that intent in generated host app state as structured timer declarations while raw host unit content remains outside the app contract.
:PROPERTIES:
:CUSTOM_ID: ac58-1-given-a-deployable-go-app-declares-scheduled-routine-intent-in-app-owned-configuration-deploy-nix-represents-that-intent-in-generated-host-app-state-as-structured-timer-declarations-while-raw-host-unit-content-remains-outside-the-app-contract
:END:

**** Requirement

Given a deployable Go app declares scheduled routine intent in app-owned configuration, deploy-nix represents that intent in generated host app state as structured timer declarations while raw host unit content remains outside the app contract.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/58.md][#58 - deploy-nix: accept app-declared timer intent]]

**** Status qualification

- *Status qualification:* ; partially superseded by AC81.1/AC81.2; history retained
**** Tests

- PASS RT-58.1: App config fixture with =timers= in app-owned YAML appears in generated deployment JSON as timer declarations.; PASS RT-58.2: App config fixture with no timer declarations preserves an empty timer set in generated deployment JSON.; PASS RT-58.3: App contract documentation describes app-declared scheduled routine intent and excludes raw host timer ownership.

*** HOLD AC58.2 - Given structured timer declarations are present in generated NixOS host state, timer jobs use the app runtime contract: app identity, working directory, config paths, secrets path when present, state directory, dependency PATH, and isolated timer runtime directory where configured.
:PROPERTIES:
:CUSTOM_ID: ac58-2-given-structured-timer-declarations-are-present-in-generated-nixos-host-state-timer-jobs-use-the-app-runtime-contract-app-identity-working-directory-config-paths-secrets-path-when-present-state-directory-dependency-path-and-isolated-timer-runtime-directory-where-configured
:END:

**** Requirement

Given structured timer declarations are present in generated NixOS host state, timer jobs use the app runtime contract: app identity, working directory, config paths, secrets path when present, state directory, dependency PATH, and isolated timer runtime directory where configured.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/58.md][#58 - deploy-nix: accept app-declared timer intent]]

**** Tests

- PASS RT-58.4: Nix evaluation fixture for an app timer confirms systemd service user/group, working directory, config environment, secrets environment, state directory, and dependency PATH.; PASS RT-58.5: Nix evaluation fixture for an app with runtime directories confirms timer-specific runtime directory naming.; PASS RT-58.6: Nix evaluation fixture confirms timer =ExecStart= is the deployed app binary plus escaped argument vector, with no shell command interpolation.

*** HOLD AC58.3 - Given timer intent contains malformed, duplicate, or host-control fields, deploy-nix refuses the deployment before generated host state is changed.
:PROPERTIES:
:CUSTOM_ID: ac58-3-given-timer-intent-contains-malformed-duplicate-or-host-control-fields-deploy-nix-refuses-the-deployment-before-generated-host-state-is-changed
:END:

**** Requirement

Given timer intent contains malformed, duplicate, or host-control fields, deploy-nix refuses the deployment before generated host state is changed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/58.md][#58 - deploy-nix: accept app-declared timer intent]]

**** Status qualification

- *Status qualification:* ; partially superseded by AC81.1/AC81.2; history retained
**** Tests

- PASS RT-58.7: Invalid timer names fail validation with a clear deploy-nix error.; PASS RT-58.8: Missing timer names, missing schedules, and empty schedules fail validation with clear deploy-nix errors.; PASS RT-58.9: Unknown host-control fields such as =command=, =exec_start=, =unit=, =user=, =working_directory=, =environment=, and =path= fail validation.

*** HOLD AC58.4 - Given Exodan operator timer policy exists for an app, deploy-nix applies a deterministic precedence model that preserves app-declared defaults while allowing explicit infra-owned additions, overrides, and disables.
:PROPERTIES:
:CUSTOM_ID: ac58-4-given-exodan-operator-timer-policy-exists-for-an-app-deploy-nix-applies-a-deterministic-precedence-model-that-preserves-app-declared-defaults-while-allowing-explicit-infra-owned-additions-overrides-and-disables
:END:

**** Requirement

Given Exodan operator timer policy exists for an app, deploy-nix applies a deterministic precedence model that preserves app-declared defaults while allowing explicit infra-owned additions, overrides, and disables.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/58.md][#58 - deploy-nix: accept app-declared timer intent]]

**** Tests

- PASS RT-58.10: Exodan deploy config can add an infra-owned timer that is absent from app-owned config.; PASS RT-58.11: Exodan deploy config can explicitly override schedule and args for an app-declared timer by name.; PASS RT-58.12: Exodan deploy config can explicitly disable an app-declared timer by name.; PASS RT-58.13: Duplicate timer names fail validation unless the operator config uses the explicit override or disable mechanism.

*** HOLD AC58.5 - Given the timer contract is published, app-facing and operator-facing documentation describe the same app/infra boundary and exclude app-authored host timers, cron jobs, shell snippets, users, paths, and unit files.
:PROPERTIES:
:CUSTOM_ID: ac58-5-given-the-timer-contract-is-published-app-facing-and-operator-facing-documentation-describe-the-same-app-infra-boundary-and-exclude-app-authored-host-timers-cron-jobs-shell-snippets-users-paths-and-unit-files
:END:

**** Requirement

Given the timer contract is published, app-facing and operator-facing documentation describe the same app/infra boundary and exclude app-authored host timers, cron jobs, shell snippets, users, paths, and unit files.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/58.md][#58 - deploy-nix: accept app-declared timer intent]]

**** Tests

- PASS RT-58.14: Documentation regression confirms =deploy/docs/PROJECT-INTEGRATION.md= and =docs/deploy-nix.md= describe the same timer schema and ownership boundary.; PASS RT-58.15: Legacy =apps/APP/timer.conf= fixture remains absent from deploy-nix generated timer state.; PASS UT-58.1: Operator review confirms the documented timer boundary is clear enough for app authors and infra maintainers.

*** HOLD AC60.1 - Given a supported local audio file, a valid Exodan publication configuration, and a nominated book and item identifier, =media-upload publish= stores the file under an immutable deterministic public key and merges an entry containing its HTTPS URL, media type, byte length, =ffprobe=-derived duration, and SHA-256 digest into =./media.yaml= at the current project repository root under =media.<book>.<item>=.
:PROPERTIES:
:CUSTOM_ID: ac60-1-given-a-supported-local-audio-file-a-valid-exodan-publication-configuration-and-a-nominated-book-and-item-identifier-media-upload-publish-stores-the-file-under-an-immutable-deterministic-public-key-and-merges-an-entry-containing-its-https-url-media-type-byte-length-ffprobe-derived-duration-and-sha-256-digest-into-media-yaml-at-the-current-project-repository-root-under-media-book-item
:END:

**** Requirement

Given a supported local audio file, a valid Exodan publication configuration, and a nominated book and item identifier, =media-upload publish= stores the file under an immutable deterministic public key and merges an entry containing its HTTPS URL, media type, byte length, =ffprobe=-derived duration, and SHA-256 digest into =./media.yaml= at the current project repository root under =media.<book>.<item>=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.1: The publish subcommand publishes a synthetic supported audio fixture through a local S3-compatible endpoint and writes a =./media.yaml= entry whose fields match the received object bytes, key, and headers.; PASS RT-60.2: Table-driven publish cases cover MP3, Ogg/Opus, FLAC, WAV, and m4a and produce their canonical media types and canonical extensions.; PASS RT-60.3: Repeating publication of identical input preserves the same object key, remote metadata, and =./media.yaml= entry without creating another object version.; PASS RT-60.4: Publishing a second item into an existing =./media.yaml= preserves hand-authored comments, unrelated entries, and top-level key ordering.; PASS OT-60.3: =make install= installs the real =media-upload= entry point and its global and subcommand help execute successfully from outside the repository.

*** HOLD AC60.2 - Given absent, malformed, unsupported, conflicting, or unauthorized publication input, =media-upload= leaves remote storage and the local =./media.yaml= unchanged and reports a specific failure without disclosing credentials.
:PROPERTIES:
:CUSTOM_ID: ac60-2-given-absent-malformed-unsupported-conflicting-or-unauthorized-publication-input-media-upload-leaves-remote-storage-and-the-local-media-yaml-unchanged-and-reports-a-specific-failure-without-disclosing-credentials
:END:

**** Requirement

Given absent, malformed, unsupported, conflicting, or unauthorized publication input, =media-upload= leaves remote storage and the local =./media.yaml= unchanged and reports a specific failure without disclosing credentials.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.5: Missing and malformed configuration, missing files, directories, and empty files fail through the publish subcommand before storage or manifest mutation.; PASS RT-60.6: Unsupported content, extension/content mismatch, and video-carrying ISO Base Media containers fail before storage or manifest mutation.; PASS RT-60.7: Missing credentials and simulated B2 authentication failures produce non-zero exits without writing to =./media.yaml=.; PASS RT-60.8: A =./media.yaml= whose top-level shape lacks a supported =version= field is refused before any mutation.; PASS RT-60.9: An existing remote object whose recorded metadata conflicts with the local file is neither overwritten remotely nor merged into =./media.yaml=.; PASS RT-60.10: CLI output and diagnostics omit configured B2 and Cloudflare credential values across success and failure paths.

*** HOLD AC60.3 - Given a valid Exodan public-media configuration and pre-existing B2 bucket, =media-upload setup= maps absent Cloudflare state to the configured proxied DNS record, bucket-constraining URL-rewrite rule, and audio cache rule; performs no mutation when observed state matches; and refuses to replace unrelated Cloudflare state.
:PROPERTIES:
:CUSTOM_ID: ac60-3-given-a-valid-exodan-public-media-configuration-and-pre-existing-b2-bucket-media-upload-setup-maps-absent-cloudflare-state-to-the-configured-proxied-dns-record-bucket-constraining-url-rewrite-rule-and-audio-cache-rule-performs-no-mutation-when-observed-state-matches-and-refuses-to-replace-unrelated-cloudflare-state
:END:

**** Requirement

Given a valid Exodan public-media configuration and pre-existing B2 bucket, =media-upload setup= maps absent Cloudflare state to the configured proxied DNS record, bucket-constraining URL-rewrite rule, and audio cache rule; performs no mutation when observed state matches; and refuses to replace unrelated Cloudflare state.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.11: The setup subcommand maps absent Cloudflare state to the configured proxied DNS record, bucket-constraining URL-rewrite rule, and audio cache rule.; PASS RT-60.12: The setup subcommand performs no mutation when observed Cloudflare state already matches configuration.; PASS RT-60.13: Conflicting unmanaged DNS or ruleset state is rejected without replacing unrelated configuration.; PASS RT-60.14: The status subcommand distinguishes matching, absent, and drifted Cloudflare state from API and public HTTP observations.; PASS OT-60.1: A live configured B2 and Cloudflare path is reconciled twice through the installed CLI and the second reconciliation reports no changes.

*** HOLD AC60.4 - Given a published audio object, its canonical URL is reachable over IPv4 and IPv6 with the configured media type and byte length, supports HTTP byte ranges, and retains the same bytes and URL across repeated publication.
:PROPERTIES:
:CUSTOM_ID: ac60-4-given-a-published-audio-object-its-canonical-url-is-reachable-over-ipv4-and-ipv6-with-the-configured-media-type-and-byte-length-supports-http-byte-ranges-and-retains-the-same-bytes-and-url-across-repeated-publication
:END:

**** Requirement

Given a published audio object, its canonical URL is reachable over IPv4 and IPv6 with the configured media type and byte length, supports HTTP byte ranges, and retains the same bytes and URL across repeated publication.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.15: The public-object verifier accepts correct full and ranged responses and rejects incorrect media type, length, range status, digest, and body bytes.; PASS RT-60.16: The publish subcommand treats a matching existing remote object as a successful no-op with unchanged canonical =./media.yaml= entry.; PASS OT-60.2: A live published fixture resolves through both IPv4 and IPv6, returns the configured headers and byte length, serves a byte range with HTTP 206, and becomes a Cloudflare cache hit within a bounded poll.; PASS UT-60.1: The operator confirms that a First Folio Hugo site consuming the generated =./media.yaml= loads, plays, and seeks through the canonical published URL in a browser.

*** HOLD AC60.5 - Given the Exodan public-audio contract, Go applications and Hugo sites consume identical =./media.yaml= shapes containing canonical media metadata without publication credentials, provider-specific storage endpoints, or theme-specific naming.
:PROPERTIES:
:CUSTOM_ID: ac60-5-given-the-exodan-public-audio-contract-go-applications-and-hugo-sites-consume-identical-media-yaml-shapes-containing-canonical-media-metadata-without-publication-credentials-provider-specific-storage-endpoints-or-theme-specific-naming
:END:

**** Requirement

Given the Exodan public-audio contract, Go applications and Hugo sites consume identical =./media.yaml= shapes containing canonical media metadata without publication credentials, provider-specific storage endpoints, or theme-specific naming.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.17: =./media.yaml= entries follow the documented neutral schema (top-level =version= and =media=, entries keyed by book identifier then item identifier, per-item =src=/=mimeType=/=byteLength=/=duration=/=sha256=) and contain no B2 endpoint, bucket, Cloudflare rule identifier, or credential field.; PASS RT-60.18: The publish subcommand succeeds without a Cloudflare token once matching delivery infrastructure exists.; PASS UT-60.2: The operator confirms that the integration-contract instructions are sufficient to add published metadata to a Go application configuration or Hugo content page without accessing Exodan storage configuration.

*** HOLD AC60.6 - Given =--dry-run= is supplied to a mutating subcommand, =media-upload= performs local validation, remote read-only checks, and the intended =./media.yaml= merge in memory, reports the planned state, and mutates no remote storage, no Cloudflare configuration, and no local file.
:PROPERTIES:
:CUSTOM_ID: ac60-6-given-dry-run-is-supplied-to-a-mutating-subcommand-media-upload-performs-local-validation-remote-read-only-checks-and-the-intended-media-yaml-merge-in-memory-reports-the-planned-state-and-mutates-no-remote-storage-no-cloudflare-configuration-and-no-local-file
:END:

**** Requirement

Given =--dry-run= is supplied to a mutating subcommand, =media-upload= performs local validation, remote read-only checks, and the intended =./media.yaml= merge in memory, reports the planned state, and mutates no remote storage, no Cloudflare configuration, and no local file.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/60.md][#60 - Add media-upload CLI and public audio delivery contract]]

**** Tests

- PASS RT-60.19: The publish subcommand with =--dry-run= performs no S3 PUT and no =./media.yaml= write and reports the planned entry.; PASS RT-60.20: The setup subcommand with =--dry-run= performs no Cloudflare API mutation and reports the planned state changes.

*** HOLD AC66.1 - The fleet-verify command reports PASS or FAIL by name for each selected hardening measure with a truthful aggregate, and exits zero only when every selected measure passes.
:PROPERTIES:
:CUSTOM_ID: ac66-1-the-fleet-verify-command-reports-pass-or-fail-by-name-for-each-selected-hardening-measure-with-a-truthful-aggregate-and-exits-zero-only-when-every-selected-measure-passes
:END:

**** Requirement

The fleet-verify command reports PASS or FAIL by name for each selected hardening measure with a truthful aggregate, and exits zero only when every selected measure passes.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/66.md][#66 - Verify Go fleet verifier end-to-end, migrate cloud firewall check, restore post-quantum SSH]]

**** Tests

- PASS Go unit tests: every check has healthy and principal-negative coverage using a mock system harness. CLI contract, dry-run, subprocess execution, and error-context passing also unit-tested.

*** HOLD AC66.2 - Each of the 17 host-observable measures moves PASS -> FAIL -> PASS when its underlying dependency is disabled and restored on a real hardened host.
:PROPERTIES:
:CUSTOM_ID: ac66-2-each-of-the-17-host-observable-measures-moves-pass-fail-pass-when-its-underlying-dependency-is-disabled-and-restored-on-a-real-hardened-host
:END:

**** Requirement

Each of the 17 host-observable measures moves PASS -> FAIL -> PASS when its underlying dependency is disabled and restored on a real hardened host.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/66.md][#66 - Verify Go fleet verifier end-to-end, migrate cloud firewall check, restore post-quantum SSH]]

**** Tests

- PASS 15 of 17 probed by hand on live host issue-66-probe-0721192643 (details below). PASS Mocked negative coverage in Go unit tests for all 17. Two measures skipped for live probing with documented reasons; both remain covered by the mocked unit tests.

*** HOLD AC66.3 - The operator-side cloud_firewall check PASSes when at least one firewall is attached to the named server and FAILs by name when zero are attached.
:PROPERTIES:
:CUSTOM_ID: ac66-3-the-operator-side-cloud_firewall-check-passes-when-at-least-one-firewall-is-attached-to-the-named-server-and-fails-by-name-when-zero-are-attached
:END:

**** Requirement

The operator-side cloud_firewall check PASSes when at least one firewall is attached to the named server and FAILs by name when zero are attached.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/66.md][#66 - Verify Go fleet verifier end-to-end, migrate cloud firewall check, restore post-quantum SSH]]

**** Tests

- PASS Go unit tests for the check (mock provider, four paths). PASS Go unit tests for the Hetzner client method behind it (httptest server, four paths). PASS Human-executed UT on 2026-07-21 against skynet: baseline PASS, detach -> FAIL naming skynet, reattach -> PASS.

*** HOLD AC66.4 - Operators can invoke any single hardening measure or the default set of measures via one CLI, with -h/--help, --version, --dry-run, standard exit codes, and help text as documentation rather than inline heredoc.
:PROPERTIES:
:CUSTOM_ID: ac66-4-operators-can-invoke-any-single-hardening-measure-or-the-default-set-of-measures-via-one-cli-with-h-help-version-dry-run-standard-exit-codes-and-help-text-as-documentation-rather-than-inline-heredoc
:END:

**** Requirement

Operators can invoke any single hardening measure or the default set of measures via one CLI, with -h/--help, --version, --dry-run, standard exit codes, and help text as documentation rather than inline heredoc.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/66.md][#66 - Verify Go fleet verifier end-to-end, migrate cloud firewall check, restore post-quantum SSH]]

**** Tests

- PASS CLI-selection unit tests. PASS Help output includes usage, environment, examples, and per-check documentation. PASS Version flag prints and exits zero.

*** HOLD AC1 - Given an operator checkout with =HCLOUD_TOKEN= in the repository-root =.env=, when an active provisioning entry point invokes =hcloud=, then the child process receives =HCLOUD_TOKEN= without requiring a named hcloud context or an entry in =cli.toml=.
:PROPERTIES:
:CUSTOM_ID: ac1-given-an-operator-checkout-with-hcloud_token-in-the-repository-root-env-when-an-active-provisioning-entry-point-invokes-hcloud-then-the-child-process-receives-hcloud_token-without-requiring-a-named-hcloud-context-or-an-entry-in-cli-toml
:END:

**** Requirement

Given an operator checkout with =HCLOUD_TOKEN= in the repository-root =.env=, when an active provisioning entry point invokes =hcloud=, then the child process receives =HCLOUD_TOKEN= without requiring a named hcloud context or an entry in =cli.toml=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/71.md][#71 - Remove the hcloud context dependency from operator configuration]]

**** Tests

- PASS RT-71.1 (HCLOUD_TOKEN remains required and exported while HCLOUD_CONTEXT is absent); PASS RT-71.2 (active provisioning documentation and validation carry no context requirement)

*** HOLD AC2 - Given the active provisioning scripts, when they describe Hetzner authentication or project-scoped uniqueness, then they use token/project terminology and do not instruct the operator to create or select an hcloud context.
:PROPERTIES:
:CUSTOM_ID: ac2-given-the-active-provisioning-scripts-when-they-describe-hetzner-authentication-or-project-scoped-uniqueness-then-they-use-token-project-terminology-and-do-not-instruct-the-operator-to-create-or-select-an-hcloud-context
:END:

**** Requirement

Given the active provisioning scripts, when they describe Hetzner authentication or project-scoped uniqueness, then they use token/project terminology and do not instruct the operator to create or select an hcloud context.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/71.md][#71 - Remove the hcloud context dependency from operator configuration]]

**** Tests

- PASS RT-71.2 (active provisioning documentation and validation carry no context requirement)

*** HOLD AC3 - Given the repository regression suite, when configuration-contract tests run, then they fail if =HCLOUD_CONTEXT= is reintroduced into active operator configuration or documentation, or if the provisioning entry points cease exporting =HCLOUD_TOKEN=.
:PROPERTIES:
:CUSTOM_ID: ac3-given-the-repository-regression-suite-when-configuration-contract-tests-run-then-they-fail-if-hcloud_context-is-reintroduced-into-active-operator-configuration-or-documentation-or-if-the-provisioning-entry-points-cease-exporting-hcloud_token
:END:

**** Requirement

Given the repository regression suite, when configuration-contract tests run, then they fail if =HCLOUD_CONTEXT= is reintroduced into active operator configuration or documentation, or if the provisioning entry points cease exporting =HCLOUD_TOKEN=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/71.md][#71 - Remove the hcloud context dependency from operator configuration]]

**** Tests

- PASS RT-71.1 (HCLOUD_TOKEN remains required and exported while HCLOUD_CONTEXT is absent); PASS RT-71.2 (active provisioning documentation and validation carry no context requirement)

*** HOLD AC80.1 - Given murderbot is the selected Hermes host, the complete child delivery provides the confirmed inference, Git notes, drafts, skills, memory, no-mail, no-restic, non-web, secret, identity-scoped egress, deployment and verification boundaries without implementing excluded lifecycle or unrelated infrastructure work.
:PROPERTIES:
:CUSTOM_ID: ac80-1-given-murderbot-is-the-selected-hermes-host-the-complete-child-delivery-provides-the-confirmed-inference-git-notes-drafts-skills-memory-no-mail-no-restic-non-web-secret-identity-scoped-egress-deployment-and-verification-boundaries-without-implementing-excluded-lifecycle-or-unrelated-infrastructure-work
:END:

**** Requirement

Given murderbot is the selected Hermes host, the complete child delivery provides the confirmed inference, Git notes, drafts, skills, memory, no-mail, no-restic, non-web, secret, identity-scoped egress, deployment and verification boundaries without implementing excluded lifecycle or unrelated infrastructure work.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/80.md][#80 - Deliver murderbot as the Hermes VPS host]]

**** Tests

- PASS RT-80.1: Master roll-up confirms every in-scope child AC has passing evidence at its declared boundary and every exclusion remains absent.; PASS RT-80.4: The generated operator command rejects caller arguments and unauthorized identities, creates a fixed hardened transient unit, and launches Hermes only through an exact positive =hermes= UID branch.; +SUPERSEDED UT-80.1: Use Hermes on murderbot to read notes, create a draft and read Gmail.+ Removed because murderbot must have no mail surface.; PASS UT-80.3: The operator uses Hermes to read notes and create a draft, confirms non-draft notes remain read-only, confirms skills and memory match the intended posture, and confirms no mail or fleet-backup surface is available.

*** HOLD AC80.2 - Given live verification is required, normal regression execution contacts no external provider, while explicitly guarded one-off checks make only the minimum allocated real requests, never retry authentication automatically, preserve IPv4/IPv6 evidence where applicable, and are retired from routine execution after evidence is recorded.
:PROPERTIES:
:CUSTOM_ID: ac80-2-given-live-verification-is-required-normal-regression-execution-contacts-no-external-provider-while-explicitly-guarded-one-off-checks-make-only-the-minimum-allocated-real-requests-never-retry-authentication-automatically-preserve-ipv4-ipv6-evidence-where-applicable-and-are-retired-from-routine-execution-after-evidence-is-recorded
:END:

**** Requirement

Given live verification is required, normal regression execution contacts no external provider, while explicitly guarded one-off checks make only the minimum allocated real requests, never retry authentication automatically, preserve IPv4/IPv6 evidence where applicable, and are retired from routine execution after evidence is recorded.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/80.md][#80 - Deliver murderbot as the Hermes VPS host]]

**** Tests

- PASS RT-80.2: Regression entry points reject or omit live-provider and live-Git execution without the explicit one-off gate.; +SUPERSEDED OT-80.1: Allocated Google, Nous and Codex checks establish the live contract.+ Removed because Google/Gmail is excluded from murderbot.; PASS OT-80.2: Guarded Codex, Nous, notes-fetch, draft-publication and permitted/refused egress checks establish the live contract with no Google request.

*** HOLD AC80.3 - Given delivery reaches human review, this master contains the child evidence, deployment evidence, documentation impact, unresolved risks, deliberate data-loss boundary and complete pending user-test instructions in one review package.
:PROPERTIES:
:CUSTOM_ID: ac80-3-given-delivery-reaches-human-review-this-master-contains-the-child-evidence-deployment-evidence-documentation-impact-unresolved-risks-deliberate-data-loss-boundary-and-complete-pending-user-test-instructions-in-one-review-package
:END:

**** Requirement

Given delivery reaches human review, this master contains the child evidence, deployment evidence, documentation impact, unresolved risks, deliberate data-loss boundary and complete pending user-test instructions in one review package.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/80.md][#80 - Deliver murderbot as the Hermes VPS host]]

**** Tests

- PASS RT-80.3: Delivery roll-up records child issues, commits, automated evidence, operational evidence, removed history and residual risks.; PASS UT-80.2: The operator completes the consolidated user tests and records the results on this master.

*** HOLD AC81.1 - The app-facing scheduled routine contract accepts a polling interval written as a positive integer followed by one of the units =min=, =hour=, =day=, =week=, or =month=, with the existing name and argument fields unchanged.
:PROPERTIES:
:CUSTOM_ID: ac81-1-the-app-facing-scheduled-routine-contract-accepts-a-polling-interval-written-as-a-positive-integer-followed-by-one-of-the-units-min-hour-day-week-or-month-with-the-existing-name-and-argument-fields-unchanged
:END:

**** Requirement

The app-facing scheduled routine contract accepts a polling interval written as a positive integer followed by one of the units =min=, =hour=, =day=, =week=, or =month=, with the existing name and argument fields unchanged. The accepted grammar contains no sub-minute unit and no abbreviated unit.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.1: a declared interval appears in generated deployment state with its duration preserved; PASS RT-81.2: sub-minute and abbreviated forms such as =30s=, =1h=, and =1M= fail validation with a diagnostic naming the accepted units; PASS RT-81.3: an app-owned calendar schedule fails validation with a diagnostic naming the replacement field; PASS RT-81.4: =1min=, =10min=, =12hours=, =7days=, =4weeks=, and =4months= are accepted, while unrecognized text and spaced forms such as =5 min= fail validation

*** HOLD AC81.2 - Timer intent that is malformed, duplicated by name, duplicated by argument vector, or expressed as a calendar schedule leaves the deployment refused, generated host state unchanged, and no host contacted.
:PROPERTIES:
:CUSTOM_ID: ac81-2-timer-intent-that-is-malformed-duplicated-by-name-duplicated-by-argument-vector-or-expressed-as-a-calendar-schedule-leaves-the-deployment-refused-generated-host-state-unchanged-and-no-host-contacted
:END:

**** Requirement

Timer intent that is malformed, duplicated by name, duplicated by argument vector, or expressed as a calendar schedule leaves the deployment refused, generated host state unchanged, and no host contacted.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.5: a refused deployment leaves no generated app JSON or Caddy snippet for the app; PASS RT-81.6: duplicate timer names fail validation naming both entries; PASS RT-81.7: two timers with identical argument vectors fail validation naming both entries; PASS RT-81.8: timer validation completes before the first host-routing call, by recorded call order

*** HOLD AC81.3 - A generated app timer for a declared polling interval is monotonic, anchored from host boot and from the previous activation, and carries no missed-window catch-up.
:PROPERTIES:
:CUSTOM_ID: ac81-3-a-generated-app-timer-for-a-declared-polling-interval-is-monotonic-anchored-from-host-boot-and-from-the-previous-activation-and-carries-no-missed-window-catch-up
:END:

**** Requirement

A generated app timer for a declared polling interval is monotonic, anchored from host boot and from the previous activation, and carries no missed-window catch-up.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.9: Nix evaluation declares =OnBootSec= and =OnUnitActiveSec= derived from the declared interval; PASS RT-81.10: Nix evaluation of an app interval timer declares no =Persistent= setting; PASS RT-81.11: Nix evaluation confirms the timer job retains app identity, working directory, config paths, secrets path, state directory, and dependency PATH; +SUPERSEDED OT-81.2: on a live host, a deployed interval timer is present with the expected cadence after a rebuild+; PASS UT-81.2: operator confirms a deployed interval timer's cadence on a live host, after writeback moves to the new contract

*** HOLD AC81.4 - A timer declared by operator policy in Exodan is aligned to the host wall clock and retains missed-window catch-up.
:PROPERTIES:
:CUSTOM_ID: ac81-4-a-timer-declared-by-operator-policy-in-exodan-is-aligned-to-the-host-wall-clock-and-retains-missed-window-catch-up
:END:

**** Requirement

A timer declared by operator policy in Exodan is aligned to the host wall clock and retains missed-window catch-up.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.12: Nix evaluation of an infra-declared calendar timer declares =OnCalendar= with the operator value; PASS RT-81.13: Nix evaluation of an infra-declared calendar timer declares =Persistent=; PASS RT-81.14: operator policy converts a named app polling routine to a calendar routine without the app repository changing

*** HOLD AC81.5 - On a NixOS fleet host, the backup window opens at 01:00 host-local time with up to 45 minutes of spread, and the auto-upgrade window opens at 03:00 host-local time with up to 30 minutes of spread.
:PROPERTIES:
:CUSTOM_ID: ac81-5-on-a-nixos-fleet-host-the-backup-window-opens-at-01-00-host-local-time-with-up-to-45-minutes-of-spread-and-the-auto-upgrade-window-opens-at-03-00-host-local-time-with-up-to-30-minutes-of-spread
:END:

**** Requirement

On a NixOS fleet host, the backup window opens at 01:00 host-local time with up to 45 minutes of spread, and the auto-upgrade window opens at 03:00 host-local time with up to 30 minutes of spread.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.15: Nix evaluation declares the backup timer at 01:00 with 45 minutes of randomized delay and =Persistent=; PASS RT-81.16: Nix evaluation declares the upgrade timer at 03:00 with 30 minutes of randomized delay and =Persistent=; PASS RT-81.17: Nix evaluation declares =time.timeZone = "Europe/Dublin"=, the premise the window arithmetic depends on

*** HOLD AC81.6 - While a fleet backup is in progress the auto-upgrade is skipped rather than failed, and an unsuccessful previous backup leaves the auto-upgrade eligible.
:PROPERTIES:
:CUSTOM_ID: ac81-6-while-a-fleet-backup-is-in-progress-the-auto-upgrade-is-skipped-rather-than-failed-and-an-unsuccessful-previous-backup-leaves-the-auto-upgrade-eligible
:END:

**** Requirement

While a fleet backup is in progress the auto-upgrade is skipped rather than failed, and an unsuccessful previous backup leaves the auto-upgrade eligible.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/81.md][#81 - Fleet scheduling contract: interval-only app timers and operator-owned overnight windows]]

**** Tests

- PASS RT-81.18: the guard exits with a skip status while the backup unit is active; PASS RT-81.19: the guard exits with a proceed status while the backup unit is inactive, including after an unsuccessful backup; PASS RT-81.20: Nix evaluation declares =ExecCondition= on the upgrade service naming the guard; +SUPERSEDED OT-81.1: on a live host, an upgrade window opening during an in-progress backup leaves a skipped upgrade rather than a failed unit+; PASS UT-81.1: operator confirms an upgrade window opening during an in-progress backup leaves a skipped upgrade rather than a failed unit

*** HOLD AC85.1 - Given the installed =vps-rebuild= entry point, the command accepts exactly one positional host, an optional =--user= override, =--help=, =--version=, and =--dry-run=; the SSH identity comes from =--user= before =USER_NAME=, and missing, duplicate, malformed, or unknown input fails before remote contact.
:PROPERTIES:
:CUSTOM_ID: ac85-1-given-the-installed-vps-rebuild-entry-point-the-command-accepts-exactly-one-positional-host-an-optional-user-override-help-version-and-dry-run-the-ssh-identity-comes-from-user-before-user_name-and-missing-duplicate-malformed-or-unknown-input-fails-before-remote-contact
:END:

**** Requirement

Given the installed =vps-rebuild= entry point, the command accepts exactly one positional host, an optional =--user= override, =--help=, =--version=, and =--dry-run=; the SSH identity comes from =--user= before =USER_NAME=, and missing, duplicate, malformed, or unknown input fails before remote contact.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.1: CLI subprocess coverage establishes help, version, positional host, option ordering, user precedence, and documented exit status.; PASS RT-85.2: CLI subprocess coverage establishes fail-closed handling for missing and duplicate hosts, absent user identity, malformed host/user values, missing option values, and unknown options.

*** HOLD AC85.2 - Given the shell entry point is reached directly or through its installed symlink, it resolves the Exodan repository, sources the repository =.env= as Bash syntax with ordinary assignments exported to the Go process, and transfers control to a fixed repository-owned Go executable without interpreting configuration, deployment state, or secret values itself.
:PROPERTIES:
:CUSTOM_ID: ac85-2-given-the-shell-entry-point-is-reached-directly-or-through-its-installed-symlink-it-resolves-the-exodan-repository-sources-the-repository-env-as-bash-syntax-with-ordinary-assignments-exported-to-the-go-process-and-transfers-control-to-a-fixed-repository-owned-go-executable-without-interpreting-configuration-deployment-state-or-secret-values-itself
:END:

**** Requirement

Given the shell entry point is reached directly or through its installed symlink, it resolves the Exodan repository, sources the repository =.env= as Bash syntax with ordinary assignments exported to the Go process, and transfers control to a fixed repository-owned Go executable without interpreting configuration, deployment state, or secret values itself.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.3: A temporary installed-symlink fixture establishes repository resolution, shell evaluation and export of quoted/expanded assignments, comment handling, exact argument forwarding, and fixed-binary execution.; PASS RT-85.4: Wrapper failure fixtures establish visible refusal for a missing Go executable or invalid repository layout without leaking environment values.; PASS RT-85.13: A full subprocess fixture passes from the installed wrapper through the real Go command, a real temporary Git repository, and fake OpenSSH to the exact pinned activation plan.

*** HOLD AC85.3 - Given a normal rebuild, only a clean tracked host configuration at the exact local commit currently published as =origin/master= is eligible; an absent host declaration, unpublished commit, target-subtree modification, unavailable prerequisite, or dirty managed remote checkout fails before destructive alignment or NixOS activation.
:PROPERTIES:
:CUSTOM_ID: ac85-3-given-a-normal-rebuild-only-a-clean-tracked-host-configuration-at-the-exact-local-commit-currently-published-as-origin-master-is-eligible-an-absent-host-declaration-unpublished-commit-target-subtree-modification-unavailable-prerequisite-or-dirty-managed-remote-checkout-fails-before-destructive-alignment-or-nixos-activation
:END:

**** Requirement

Given a normal rebuild, only a clean tracked host configuration at the exact local commit currently published as =origin/master= is eligible; an absent host declaration, unpublished commit, target-subtree modification, unavailable prerequisite, or dirty managed remote checkout fails before destructive alignment or NixOS activation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.5: Temporary Git-repository subprocess coverage establishes eligibility for a clean published host and refusal for missing, untracked, modified, staged, or unpushed host configuration.; PASS RT-85.6: Fake-command boundary coverage establishes exact-commit selection, prerequisite refusal, and managed-remote cleanliness refusal before alignment or activation.; PASS RT-85.13: The full subprocess fixture establishes that the commit observed at the wrapper boundary is the commit used at the remote activation boundary.

*** HOLD AC85.4 - Given an eligible rebuild, the command uses bounded fixed-argv OpenSSH phases to align the managed =/opt/exodan= checkout to the exact validated commit and activate =path:/opt/exodan/vps-template/nixos#HOST=; every failure, timeout, or interruption stops later phases, OpenSSH credential state is unchanged, and sourced provider secrets are absent from child environments, remote argv, and diagnostics.
:PROPERTIES:
:CUSTOM_ID: ac85-4-given-an-eligible-rebuild-the-command-uses-bounded-fixed-argv-openssh-phases-to-align-the-managed-opt-exodan-checkout-to-the-exact-validated-commit-and-activate-path-opt-exodan-vps-template-nixos-host-every-failure-timeout-or-interruption-stops-later-phases-openssh-credential-state-is-unchanged-and-sourced-provider-secrets-are-absent-from-child-environments-remote-argv-and-diagnostics
:END:

**** Requirement

Given an eligible rebuild, the command uses bounded fixed-argv OpenSSH phases to align the managed =/opt/exodan= checkout to the exact validated commit and activate =path:/opt/exodan/vps-template/nixos#HOST=; every failure, timeout, or interruption stops later phases, OpenSSH credential state is unchanged, and sourced provider secrets are absent from child environments, remote argv, and diagnostics.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.7: Fake-OpenSSH subprocess coverage establishes the clean-status, remote update, exact-commit alignment, and NixOS activation sequence with fixed argv and the selected user/host.; PASS RT-85.8: Per-phase failure and environment-capture coverage establishes immediate termination, preserved diagnostics, absent later phases, minimal child environment, default OpenSSH credential handling, and no secret-bearing environment, argv, or output.; PASS RT-85.12: Timeout and handled-interruption coverage establishes cancellation of the active child and absence of later phases or leaked processes.; PASS OT-85.1: One authorized operator invocation activates the published murderbot configuration through =vps-rebuild murderbot= and reports the resulting NixOS generation.

*** HOLD AC85.5 - Given =--dry-run=, the command presents a redacted ordered plan while leaving local Git, network, OpenSSH, remote checkout, and NixOS state unchanged.
:PROPERTIES:
:CUSTOM_ID: ac85-5-given-dry-run-the-command-presents-a-redacted-ordered-plan-while-leaving-local-git-network-openssh-remote-checkout-and-nixos-state-unchanged
:END:

**** Requirement

Given =--dry-run=, the command presents a redacted ordered plan while leaving local Git, network, OpenSSH, remote checkout, and NixOS state unchanged.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.9: A subprocess dry-run with fake Git and OpenSSH boundaries establishes the complete exact-commit plan and zero state-changing or network commands.; PASS RT-85.10: Dry-run fixtures containing sentinel secret values establish that neither stdout nor stderr contains those values.

*** HOLD AC85.6 - Given the Exodan operator-tool lifecycle, =vps-rebuild= is built and installed through the existing Makefile convention, remains portable across the supported macOS operator and Linux target boundary, documents its environment and safety contract, and preserves SSH reachability without forcing either IP address family.
:PROPERTIES:
:CUSTOM_ID: ac85-6-given-the-exodan-operator-tool-lifecycle-vps-rebuild-is-built-and-installed-through-the-existing-makefile-convention-remains-portable-across-the-supported-macos-operator-and-linux-target-boundary-documents-its-environment-and-safety-contract-and-preserves-ssh-reachability-without-forcing-either-ip-address-family
:END:

**** Requirement

Given the Exodan operator-tool lifecycle, =vps-rebuild= is built and installed through the existing Makefile convention, remains portable across the supported macOS operator and Linux target boundary, documents its environment and safety contract, and preserves SSH reachability without forcing either IP address family.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/85.md][#85 - Add the vps-rebuild NixOS host operator command]]

**** Tests

- PASS RT-85.11: Go tests and wrapper subprocess coverage pass on the operator platform without hard-coded temporary or home paths and without a forced OpenSSH address-family flag.; PASS OT-85.2: The existing Makefile build/install entry point exposes the documented command and help text through an installation rooted in a unique operating-system temporary directory.; PASS OT-85.3: Read-only diagnostics establish murderbot OpenSSH reachability over both available tailnet address families without a remote write.

*** HOLD AC87.1 - =vps-rebuild= refreshes the =exodan-src= pin before publication, so a rebuild activates the host with verifier source matching the reviewed commit.
:PROPERTIES:
:CUSTOM_ID: ac87-1-vps-rebuild-refreshes-the-exodan-src-pin-before-publication-so-a-rebuild-activates-the-host-with-verifier-source-matching-the-reviewed-commit
:END:

**** Requirement

=vps-rebuild= refreshes the =exodan-src= pin before publication, so a rebuild activates the host with verifier source matching the reviewed commit.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/87.md][#87 - vps-rebuild: refresh the fleet-verify source pin by default]]

**** Tests

- PASS RT-87.1: a run whose pin omits committed verifier inputs refreshes the lock and publishes it; PASS RT-87.2: a run whose pin is already current makes no lock commit

*** HOLD AC87.2 - A documented flag rebuilds against the pre-existing pin without refreshing it, and the run states that the pin was left as it stood.
:PROPERTIES:
:CUSTOM_ID: ac87-2-a-documented-flag-rebuilds-against-the-pre-existing-pin-without-refreshing-it-and-the-run-states-that-the-pin-was-left-as-it-stood
:END:

**** Requirement

A documented flag rebuilds against the pre-existing pin without refreshing it, and the run states that the pin was left as it stood.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/87.md][#87 - vps-rebuild: refresh the fleet-verify source pin by default]]

**** Tests

- PASS RT-87.4: the flag is accepted and recorded, and the default does not set it; PASS RT-87.5: the flag is rejected when combined with an option it cannot coexist with, rather than silently ignored

*** HOLD AC87.3 - A refresh that changes the lock is published before the host is contacted, and the commit contains the lock file and nothing else.
:PROPERTIES:
:CUSTOM_ID: ac87-3-a-refresh-that-changes-the-lock-is-published-before-the-host-is-contacted-and-the-commit-contains-the-lock-file-and-nothing-else
:END:

**** Requirement

A refresh that changes the lock is published before the host is contacted, and the commit contains the lock file and nothing else.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/87.md][#87 - vps-rebuild: refresh the fleet-verify source pin by default]]

**** Tests

- PASS RT-87.7: the commit names only =vps-template/nixos/flake.lock=, by pathspec; PASS RT-87.8: nothing is staged, so unrelated dirty paths cannot be swept in; PASS RT-87.9: a refresh that cannot be published stops the run instead of contacting a host

*** HOLD AC87.4 - A dry run reports the pin action it would take and changes no lock, commits nothing, and contacts nothing.
:PROPERTIES:
:CUSTOM_ID: ac87-4-a-dry-run-reports-the-pin-action-it-would-take-and-changes-no-lock-commits-nothing-and-contacts-nothing
:END:

**** Requirement

A dry run reports the pin action it would take and changes no lock, commits nothing, and contacts nothing.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/87.md][#87 - vps-rebuild: refresh the fleet-verify source pin by default]]

**** Tests

- PASS RT-87.10: a dry run against a stale pin reports the intended refresh and runs no write command

*** HOLD AC92.1 - Given any installed operator-invoked Go command, its command symlink resolves through one production launcher to the executable at =deploy/COMMAND/COMMAND=, with the invoked basename constrained to lowercase letters, digits, and dashes.
:PROPERTIES:
:CUSTOM_ID: ac92-1-given-any-installed-operator-invoked-go-command-its-command-symlink-resolves-through-one-production-launcher-to-the-executable-at-deploy-command-command-with-the-invoked-basename-constrained-to-lowercase-letters-digits-and-dashes
:END:

**** Requirement

Given any installed operator-invoked Go command, its command symlink resolves through one production launcher to the executable at =deploy/COMMAND/COMMAND=, with the invoked basename constrained to lowercase letters, digits, and dashes.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/92.md][#92 - Unify operator Go launchers and remove Go-side .env parsing]]

**** Tests

- PENDING RT-92.1: Every Makefile-installed Go command resolves through the production launcher to its conventionally located fixture executable; PENDING RT-92.2: Invocation from outside Exodan preserves command identity and locates the repository through the launcher's resolved source path; PENDING RT-92.3: Malformed command names, direct launcher invocation, missing binaries, non-executable binaries, and paths outside the fixed command directory fail before child startup; PENDING RT-92.4: The separately installed =mac-screen-sharing= command retains its installation lifecycle while using the common production launcher

*** HOLD AC92.2 - Given production operator configuration, Bash supplies the repository =.env= semantics to every operator-invoked Go command, while arguments, working directory, signals, and child exit status cross the launcher unchanged and secret values remain absent from diagnostics.
:PROPERTIES:
:CUSTOM_ID: ac92-2-given-production-operator-configuration-bash-supplies-the-repository-env-semantics-to-every-operator-invoked-go-command-while-arguments-working-directory-signals-and-child-exit-status-cross-the-launcher-unchanged-and-secret-values-remain-absent-from-diagnostics
:END:

**** Requirement

Given production operator configuration, Bash supplies the repository =.env= semantics to every operator-invoked Go command, while arguments, working directory, signals, and child exit status cross the launcher unchanged and secret values remain absent from diagnostics.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/92.md][#92 - Unify operator Go launchers and remove Go-side .env parsing]]

**** Tests

- PENDING RT-92.5: A synthetic =.env= containing export syntax, quoting, expansion, and shell assignment semantics reaches a fixture child exactly as Bash evaluates it; PENDING RT-92.6: Arguments including whitespace and empty values, repository working directory, child exit codes, and termination signals survive the production launch boundary; PENDING RT-92.7: Help and version remain available without requiring operator credentials; PENDING RT-92.8: Missing or invalid operator configuration reports bounded field names and paths without printing synthetic secret values

*** HOLD AC92.5 - Given operator installation and NixOS packaging, operator symlinks and build outputs follow the common launch convention, while host-runtime Nix packages contain direct Go executables and no dependency on the operator repository =.env=.
:PROPERTIES:
:CUSTOM_ID: ac92-5-given-operator-installation-and-nixos-packaging-operator-symlinks-and-build-outputs-follow-the-common-launch-convention-while-host-runtime-nix-packages-contain-direct-go-executables-and-no-dependency-on-the-operator-repository-env
:END:

**** Requirement

Given operator installation and NixOS packaging, operator symlinks and build outputs follow the common launch convention, while host-runtime Nix packages contain direct Go executables and no dependency on the operator repository =.env=.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/92.md][#92 - Unify operator Go launchers and remove Go-side .env parsing]]

**** Tests

- PENDING RT-92.17: Build and installation regressions cover every operator Go command, the standardized binary paths, and absence of per-command production wrappers; PENDING RT-92.18: Nix package builds for =fleet-status= and =fleet-verify= retain direct executable entry points without the operator launcher; PENDING RT-92.19: Active operator and project-integration documentation distinguishes production, test, and host-runtime launch vectors; PENDING OT-92.1: Installed operator command help/version smoke confirms every symlink reaches its intended binary without provider access

*** HOLD AC92.6 - Given the operator invokes =fleet-backup= through the production launcher, it consumes =B2_APPLICATION_KEY_ID=, =B2_APPLICATION_KEY=, and =RESTIC_REPOSITORY= from the process environment, decrypts =restic-fleet-password.age= through the retained fleet identity and configured operator SSH identity without plaintext disk staging, maps the two Backblaze credentials only into the restic child process, defaults repository initialization to a non-mutating plan requiring =--apply=, provides snapshots and bounded integrity checks, creates and restores a synthetic canary before reporting success, and mounts only an existing empty directory through restic's read-only mount command without exposing credentials.
:PROPERTIES:
:CUSTOM_ID: ac92-6-given-the-operator-invokes-fleet-backup-through-the-production-launcher-it-consumes-b2_application_key_id-b2_application_key-and-restic_repository-from-the-process-environment-decrypts-restic-fleet-password-age-through-the-retained-fleet-identity-and-configured-operator-ssh-identity-without-plaintext-disk-staging-maps-the-two-backblaze-credentials-only-into-the-restic-child-process-defaults-repository-initialization-to-a-non-mutating-plan-requiring-apply-provides-snapshots-and-bounded-integrity-checks-creates-and-restores-a-synthetic-canary-before-reporting-success-and-mounts-only-an-existing-empty-directory-through-restic-s-read-only-mount-command-without-exposing-credentials
:END:

**** Requirement

Given the operator invokes =fleet-backup= through the production launcher, it consumes =B2_APPLICATION_KEY_ID=, =B2_APPLICATION_KEY=, and =RESTIC_REPOSITORY= from the process environment, decrypts =restic-fleet-password.age= through the retained fleet identity and configured operator SSH identity without plaintext disk staging, maps the two Backblaze credentials only into the restic child process, defaults repository initialization to a non-mutating plan requiring =--apply=, provides snapshots and bounded integrity checks, creates and restores a synthetic canary before reporting success, and mounts only an existing empty directory through restic's read-only mount command without exposing credentials.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/92.md][#92 - Unify operator Go launchers and remove Go-side .env parsing]]

**** Tests

- PENDING RT-92.20: Go CLI fixtures cover help/version, command parsing, required environment validation, credential mapping, secret redaction, fixed restic argv, and child exit propagation.; PENDING RT-92.21: Fake-restic subprocess tests prove =init= is a plan by default, =init --apply= is the sole mutating initialization path, and an existing or failed repository is not reported initialized.; PENDING RT-92.22: Fake-restic subprocess tests prove snapshots, metadata check, and explicit =check --read-data= argv and failure semantics.; PENDING RT-92.23: Mount tests cover missing, non-directory, non-empty and valid empty paths, foreground execution, signal propagation, and read-only restic mount semantics.; PENDING RT-92.24: Canary tests prove bounded temporary state, backup snapshot-ID capture, restore, byte comparison, cleanup on success/failure/interruption, and no implicit forget or prune.; PENDING RT-92.25: Synthetic fake-age subprocess tests prove nested fleet-identity and repository-password decryption, single-line normalization, in-memory-only plaintext handling, replacement of alternate restic password sources, fail-closed behaviour, and secret-free diagnostics.; PENDING OT-92.2: Once-only operator run initializes the exclusive repository, runs metadata and read-data checks, completes the canary restoration, and mounts the resulting snapshot tree read-only.

*** HOLD AC93.1 - Given the murderbot service graph is evaluated, the post-entrypoint Hermes process and every process it launches have direct outbound networking denied by default for IPv4 and IPv6 under the =hermes= host UID, the shared notes units have the same direct denial under the =hermes-notes= UID, only their declared local gateway paths remain reachable, no new public listener exists, and missing, partial-family or incomplete policy fails before activation.
:PROPERTIES:
:CUSTOM_ID: ac93-1-given-the-murderbot-service-graph-is-evaluated-the-post-entrypoint-hermes-process-and-every-process-it-launches-have-direct-outbound-networking-denied-by-default-for-ipv4-and-ipv6-under-the-hermes-host-uid-the-shared-notes-units-have-the-same-direct-denial-under-the-hermes-notes-uid-only-their-declared-local-gateway-paths-remain-reachable-no-new-public-listener-exists-and-missing-partial-family-or-incomplete-policy-fails-before-activation
:END:

**** Requirement

Given the murderbot service graph is evaluated, the post-entrypoint Hermes process and every process it launches have direct outbound networking denied by default for IPv4 and IPv6 under the =hermes= host UID, the shared notes units have the same direct denial under the =hermes-notes= UID, only their declared local gateway paths remain reachable, no new public listener exists, and missing, partial-family or incomplete policy fails before activation.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/93.md][#93 - Restrict murderbot egress by service identity]]

**** Tests

- PASS RT-93.1: Real murderbot Nix evaluation asserts both restricted UIDs, equivalent IPv4/IPv6 direct-deny rules, declared local gateway paths and absence of a public listener.; PASS RT-93.2: Negative evaluations reject a missing UID, policy, listener, family, endpoint class or cross-identity path.

*** HOLD AC93.2 - Given Hermes requires inference, its bounded local CONNECT gateway accepts only =auth.openai.com:443=, =chatgpt.com:443= and =inference-api.nousresearch.com:443=, as declared from the pinned Hermes revision; arbitrary domains, wildcard suffixes, non-443 ports, literal addresses, malformed authorities and DNS-based direct bypasses are refused; endpoint changes require declarative pin review; and authentication failure never widens access or triggers an automatic authentication retry.
:PROPERTIES:
:CUSTOM_ID: ac93-2-given-hermes-requires-inference-its-bounded-local-connect-gateway-accepts-only-auth-openai-com-443-chatgpt-com-443-and-inference-api-nousresearch-com-443-as-declared-from-the-pinned-hermes-revision-arbitrary-domains-wildcard-suffixes-non-443-ports-literal-addresses-malformed-authorities-and-dns-based-direct-bypasses-are-refused-endpoint-changes-require-declarative-pin-review-and-authentication-failure-never-widens-access-or-triggers-an-automatic-authentication-retry
:END:

**** Requirement

Given Hermes requires inference, its bounded local CONNECT gateway accepts only =auth.openai.com:443=, =chatgpt.com:443= and =inference-api.nousresearch.com:443=, as declared from the pinned Hermes revision; arbitrary domains, wildcard suffixes, non-443 ports, literal addresses, malformed authorities and DNS-based direct bypasses are refused; endpoint changes require declarative pin review; and authentication failure never widens access or triggers an automatic authentication retry.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/93.md][#93 - Restrict murderbot egress by service identity]]

**** Tests

- PASS RT-93.3: Gateway component tests plus rendered-policy fixtures establish permitted CONNECT authorities, resolver-controlled IPv4/IPv6 dialling and direct-path refusal without contacting providers.; PASS RT-93.4: Empty, malformed, wildcard, suffix-confusion, literal-address, resolver-failure, timeout, bounded-request, concurrency and teardown fixtures fail closed; authentication-failure fixtures prove no retry or fail-open path.; PASS OT-93.1: One guarded Codex request and one guarded Nous request traverse the installed controlled path, while installed probes under the restricted Hermes identity refuse one unlisted name and direct IPv4 and IPv6 destinations.

*** HOLD AC93.3 - Given the notes synchronizer and draft publisher run as the dedicated shared =hermes-notes= service identity, both use normal OpenSSH authentication through a mode-restricted local Unix-socket relay fixed to =github.com:22=, cannot reach the inference proxy or any direct IPv4/IPv6 Internet destination, and Hermes cannot open the Git relay socket, read the private checkout or credential, or acquire the notes identity's network authority.
:PROPERTIES:
:CUSTOM_ID: ac93-3-given-the-notes-synchronizer-and-draft-publisher-run-as-the-dedicated-shared-hermes-notes-service-identity-both-use-normal-openssh-authentication-through-a-mode-restricted-local-unix-socket-relay-fixed-to-github-com-22-cannot-reach-the-inference-proxy-or-any-direct-ipv4-ipv6-internet-destination-and-hermes-cannot-open-the-git-relay-socket-read-the-private-checkout-or-credential-or-acquire-the-notes-identity-s-network-authority
:END:

**** Requirement

Given the notes synchronizer and draft publisher run as the dedicated shared =hermes-notes= service identity, both use normal OpenSSH authentication through a mode-restricted local Unix-socket relay fixed to =github.com:22=, cannot reach the inference proxy or any direct IPv4/IPv6 Internet destination, and Hermes cannot open the Git relay socket, read the private checkout or credential, or acquire the notes identity's network authority.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/93.md][#93 - Restrict murderbot egress by service identity]]

**** Tests

- PASS RT-93.5: Service, socket-permission and relay fixtures prove the shared identity's fixed Git path, cross-channel refusal and Hermes credential/checkout isolation without inspecting real OpenSSH material.; PASS OT-93.2: One guarded notes fetch and one controlled draft publication use the installed relay, while probes executed under =hermes-notes= refuse inference and direct dual-stack destinations and a Hermes-identity probe cannot open the Git relay.

*** HOLD AC93.4 - Given other murderbot base services require networking, this child leaves their existing network policy unchanged and does not place them in either restricted UID; the restricted identities cannot inherit base-service authority; restic, B2 backup access and fleet-backup credentials remain absent; and failure of the gateway or a required base service is visible rather than silently bypassed.
:PROPERTIES:
:CUSTOM_ID: ac93-4-given-other-murderbot-base-services-require-networking-this-child-leaves-their-existing-network-policy-unchanged-and-does-not-place-them-in-either-restricted-uid-the-restricted-identities-cannot-inherit-base-service-authority-restic-b2-backup-access-and-fleet-backup-credentials-remain-absent-and-failure-of-the-gateway-or-a-required-base-service-is-visible-rather-than-silently-bypassed
:END:

**** Requirement

Given other murderbot base services require networking, this child leaves their existing network policy unchanged and does not place them in either restricted UID; the restricted identities cannot inherit base-service authority; restic, B2 backup access and fleet-backup credentials remain absent; and failure of the gateway or a required base service is visible rather than silently bypassed.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/93.md][#93 - Restrict murderbot egress by service identity]]

**** Tests

- PASS RT-93.6: Nix evaluation proves the restricted UIDs are used only by the declared Hermes/notes consumers, base services are not accidentally assigned either UID, gateway failure is visible, and restic/B2 configuration and credentials are absent.; PASS OT-93.3: After activation, read-only service evidence and strict fleet verification establish healthy declared host networking, visible gateway state and the absence of restic units, repository configuration and credentials.

*** HOLD AC93.5 - Given the policy is activated or changed, its effective identity-to-destination mapping, listener/socket metadata and refusal evidence are observable without exposing secrets; NixOS generation rollback restores the last declared policy; and operator documentation explains endpoint-pin review, dual-stack diagnosis, failure symptoms and recovery without direct SSH writes.
:PROPERTIES:
:CUSTOM_ID: ac93-5-given-the-policy-is-activated-or-changed-its-effective-identity-to-destination-mapping-listener-socket-metadata-and-refusal-evidence-are-observable-without-exposing-secrets-nixos-generation-rollback-restores-the-last-declared-policy-and-operator-documentation-explains-endpoint-pin-review-dual-stack-diagnosis-failure-symptoms-and-recovery-without-direct-ssh-writes
:END:

**** Requirement

Given the policy is activated or changed, its effective identity-to-destination mapping, listener/socket metadata and refusal evidence are observable without exposing secrets; NixOS generation rollback restores the last declared policy; and operator documentation explains endpoint-pin review, dual-stack diagnosis, failure symptoms and recovery without direct SSH writes.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/93.md][#93 - Restrict murderbot egress by service identity]]

**** Tests

- PASS RT-93.7: Rendered policy and local status output provide bounded, secret-free identity/listener/endpoint diagnostics and the documented generation rollback path.; PASS UT-93.1: The operator confirms the delivered allowlist, refusal behaviour, observability and recovery model match the intended murderbot trust boundary.

*** HOLD AC94.1 - Given host roles are evaluated, Caddy/web composition and Restic backup composition are independent; the Caddy-hardened role grants no backup capability by itself, an authorized host receives Restic packages, units, repository configuration, agenix credentials and backup decryption authority only through explicit backup composition, and murderbot receives neither web nor backup authority; base auto-upgrade and other required maintenance continue normally when backup is disabled.
:PROPERTIES:
:CUSTOM_ID: ac94-1-given-host-roles-are-evaluated-caddy-web-composition-and-restic-backup-composition-are-independent-the-caddy-hardened-role-grants-no-backup-capability-by-itself-an-authorized-host-receives-restic-packages-units-repository-configuration-agenix-credentials-and-backup-decryption-authority-only-through-explicit-backup-composition-and-murderbot-receives-neither-web-nor-backup-authority-base-auto-upgrade-and-other-required-maintenance-continue-normally-when-backup-is-disabled
:END:

**** Requirement

Given host roles are evaluated, Caddy/web composition and Restic backup composition are independent; the Caddy-hardened role grants no backup capability by itself, an authorized host receives Restic packages, units, repository configuration, agenix credentials and backup decryption authority only through explicit backup composition, and murderbot receives neither web nor backup authority; base auto-upgrade and other required maintenance continue normally when backup is disabled.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.1: Real Nix evaluation proves the Caddy role alone has no Restic package, units, repository configuration or credentials and that adding explicit backup composition enables them.; PASS RT-94.2: Real murderbot evaluation proves Caddy, CrowdSec, Hugo, backup identity and all Restic/B2 material are absent while required base services remain present and upgrades are not blocked by an absent backup unit.; PASS RT-94.13: Recipient and Nix evaluation proves the general fleet identity cannot decrypt or activate the dedicated repository payloads and that only backup-enabled composition declares the backup identity path.

*** HOLD AC94.2 - Given the operator invokes =vps-create-nix=, backup is enabled by default; =--no-backup= explicitly generates a host without the backup identity or module; help, dry-run and the final success summary report the selected posture with truthful hardening pass/not-applicable/applied totals; missing backup key material, malformed, repeated or conflicting backup arguments fail before secrets, prompts or provider calls; an existing host configuration that conflicts with the requested posture is not silently accepted or overwritten; and the fleet-scoped backup identity survives successful or failed creation, rollback and every per-host destroy operation without being created, rotated or removed as host lifecycle state.
:PROPERTIES:
:CUSTOM_ID: ac94-2-given-the-operator-invokes-vps-create-nix-backup-is-enabled-by-default-no-backup-explicitly-generates-a-host-without-the-backup-identity-or-module-help-dry-run-and-the-final-success-summary-report-the-selected-posture-with-truthful-hardening-pass-not-applicable-applied-totals-missing-backup-key-material-malformed-repeated-or-conflicting-backup-arguments-fail-before-secrets-prompts-or-provider-calls-an-existing-host-configuration-that-conflicts-with-the-requested-posture-is-not-silently-accepted-or-overwritten-and-the-fleet-scoped-backup-identity-survives-successful-or-failed-creation-rollback-and-every-per-host-destroy-operation-without-being-created-rotated-or-removed-as-host-lifecycle-state
:END:

**** Requirement

Given the operator invokes =vps-create-nix=, backup is enabled by default; =--no-backup= explicitly generates a host without the backup identity or module; help, dry-run and the final success summary report the selected posture with truthful hardening pass/not-applicable/applied totals; missing backup key material, malformed, repeated or conflicting backup arguments fail before secrets, prompts or provider calls; an existing host configuration that conflicts with the requested posture is not silently accepted or overwritten; and the fleet-scoped backup identity survives successful or failed creation, rollback and every per-host destroy operation without being created, rotated or removed as host lifecycle state.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.3: The real operator entry point under the existing isolated subprocess harness proves default and =--no-backup= parsing, help, dry-run and success-summary output, generated host composition, truthful hardening totals and fixed exit semantics without provider access.; PASS RT-94.4: Negative entry-point fixtures cover repeated/conflicting arguments, missing host name, missing backup key material and pre-existing matching/conflicting host configurations, proving failure precedes secrets, prompts and provider calls.; PASS RT-94.5: Generated configurations for default and excluded hosts evaluate successfully through the real flake boundary.; PASS RT-94.14: Provisioning fixtures prove only a default backup-enabled installation stages the backup identity and that =--no-backup= never stages it.; PASS RT-94.17: Real lifecycle entry-point fixtures prove create success, create rollback, =--no-backup= and destroy of present or already-absent hosts never create, rotate or remove the fleet-scoped backup identity.

*** HOLD AC94.3 - Given the role migration is applied, kepler452, perihelion and nothingbot2 retain explicit backup composition and backup identity authority with equivalent evaluated paths, units and timers, while murderbot is the deliberate exclusion in this delivery.
:PROPERTIES:
:CUSTOM_ID: ac94-3-given-the-role-migration-is-applied-kepler452-perihelion-and-nothingbot2-retain-explicit-backup-composition-and-backup-identity-authority-with-equivalent-evaluated-paths-units-and-timers-while-murderbot-is-the-deliberate-exclusion-in-this-delivery
:END:

**** Requirement

Given the role migration is applied, kepler452, perihelion and nothingbot2 retain explicit backup composition and backup identity authority with equivalent evaluated paths, units and timers, while murderbot is the deliberate exclusion in this delivery.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.6: Real checked-in host outputs assert the complete expected inventory mapping: backup enabled for kepler452, perihelion and nothingbot2 and disabled for murderbot.; PASS RT-94.7: Synthetic temporary host outputs prove Caddy-only, backup-only, Caddy-plus-backup and base-only compositions evaluate to four distinct capability sets without inspecting source shape.

*** HOLD AC94.4 - Given fleet hardening assessment on a managed host, Restic applicability comes only from evaluated Nix policy: enabled and healthy passes, enabled but absent or unhealthy fails strictly, disabled is not applicable with detail =not enabled in evaluated Nix configuration=, and missing or malformed applicability policy is unknown rather than inferred from runtime absence.
:PROPERTIES:
:CUSTOM_ID: ac94-4-given-fleet-hardening-assessment-on-a-managed-host-restic-applicability-comes-only-from-evaluated-nix-policy-enabled-and-healthy-passes-enabled-but-absent-or-unhealthy-fails-strictly-disabled-is-not-applicable-with-detail-not-enabled-in-evaluated-nix-configuration-and-missing-or-malformed-applicability-policy-is-unknown-rather-than-inferred-from-runtime-absence
:END:

**** Requirement

Given fleet hardening assessment on a managed host, Restic applicability comes only from evaluated Nix policy: enabled and healthy passes, enabled but absent or unhealthy fails strictly, disabled is not applicable with detail =not enabled in evaluated Nix configuration=, and missing or malformed applicability policy is unknown rather than inferred from runtime absence.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.8: Public fleet-verify CLI fixtures cover enabled-pass, enabled-fail, disabled/not-applicable and missing/malformed-policy unknown outcomes with correct aggregate counts and exit status.; PASS RT-94.9: Real Nix evaluation emits the Restic expectation from declared backup configuration for both enabled and disabled hosts.

*** HOLD AC94.5 - Given fleet-status collects a managed host, a declared Restic exclusion is displayed as =⚪ not enabled=; enabled backups retain healthy, failed and no-record semantics; the disabled state does not fail the report or gate; and missing or inconsistent host capability metadata remains unknown rather than being mislabeled as disabled.
:PROPERTIES:
:CUSTOM_ID: ac94-5-given-fleet-status-collects-a-managed-host-a-declared-restic-exclusion-is-displayed-as-not-enabled-enabled-backups-retain-healthy-failed-and-no-record-semantics-the-disabled-state-does-not-fail-the-report-or-gate-and-missing-or-inconsistent-host-capability-metadata-remains-unknown-rather-than-being-mislabeled-as-disabled
:END:

**** Requirement

Given fleet-status collects a managed host, a declared Restic exclusion is displayed as =⚪ not enabled=; enabled backups retain healthy, failed and no-record semantics; the disabled state does not fail the report or gate; and missing or inconsistent host capability metadata remains unknown rather than being mislabeled as disabled.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.10: Fleet and local-report fixtures cover not-enabled, healthy, failed, no-record, missing and malformed metadata with visible labels and exit semantics.; PASS RT-94.11: The real Nix-generated status manifest represents enabled and disabled backup capability explicitly, so omission cannot masquerade as a deliberate exclusion.; PASS RT-94.12: Host configurations generated by the real provisioning entry point are evaluated, and their resulting verifier expectation and status manifest drive the public fleet-verify/fleet-status CLIs to enabled or not-enabled outcomes end to end.

*** HOLD AC94.6 - Given the capability and repository migration is delivered, operator documentation explains default backup creation, =--no-backup=, explicit per-host enablement, backup-recipient authority, dedicated-repository setup, verifier/status meanings, mounting, restoration and safe migration/recovery without exposing secret contents.
:PROPERTIES:
:CUSTOM_ID: ac94-6-given-the-capability-and-repository-migration-is-delivered-operator-documentation-explains-default-backup-creation-no-backup-explicit-per-host-enablement-backup-recipient-authority-dedicated-repository-setup-verifier-status-meanings-mounting-restoration-and-safe-migration-recovery-without-exposing-secret-contents
:END:

**** Requirement

Given the capability and repository migration is delivered, operator documentation explains default backup creation, =--no-backup=, explicit per-host enablement, backup-recipient authority, dedicated-repository setup, verifier/status meanings, mounting, restoration and safe migration/recovery without exposing secret contents.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS UT-94.1: The operator reviews the updated provisioning, Restic, monitoring and architecture documentation and confirms the capability, authority and recovery model.

*** HOLD AC94.7 - Given dedicated repository credentials are configured, the B2 API ID, B2 API key and repository location are one root-only environment payload, the repository password is a separate root-only raw payload, both are encrypted only to the backup recipient, enabled hosts consume only those dedicated payloads, and both historical Restic payloads are absent from the current tracked tree.
:PROPERTIES:
:CUSTOM_ID: ac94-7-given-dedicated-repository-credentials-are-configured-the-b2-api-id-b2-api-key-and-repository-location-are-one-root-only-environment-payload-the-repository-password-is-a-separate-root-only-raw-payload-both-are-encrypted-only-to-the-backup-recipient-enabled-hosts-consume-only-those-dedicated-payloads-and-both-historical-restic-payloads-are-absent-from-the-current-tracked-tree
:END:

**** Requirement

Given dedicated repository credentials are configured, the B2 API ID, B2 API key and repository location are one root-only environment payload, the repository password is a separate root-only raw payload, both are encrypted only to the backup recipient, enabled hosts consume only those dedicated payloads, and both historical Restic payloads are absent from the current tracked tree.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.15: Real Nix evaluation proves the dedicated environment and password paths, root-only ownership and mode, separate password-file consumption and absence of both historical payloads from active configuration.; PASS RT-94.16: Evaluation with synthetic public recipient strings proves the dedicated payload declarations exclude the general fleet recipient and murderbot; the test creates no private identity and does not inspect ciphertext contents.; PASS OT-94.1: Read-only host evidence records dedicated decrypted paths, ownership and modes on each enabled host without reading their contents.

*** HOLD AC94.8 - Given the fleet repository migration has completed, kepler452, perihelion and nothingbot2 each have a current host-tagged snapshot in =b2:exodan-fleet-restic=, a controlled file can be restored from the new repository, murderbot has no repository surface, and the historical repository remains unmodified by this delivery.
:PROPERTIES:
:CUSTOM_ID: ac94-8-given-the-fleet-repository-migration-has-completed-kepler452-perihelion-and-nothingbot2-each-have-a-current-host-tagged-snapshot-in-b2-exodan-fleet-restic-a-controlled-file-can-be-restored-from-the-new-repository-murderbot-has-no-repository-surface-and-the-historical-repository-remains-unmodified-by-this-delivery
:END:

**** Requirement

Given the fleet repository migration has completed, kepler452, perihelion and nothingbot2 each have a current host-tagged snapshot in =b2:exodan-fleet-restic=, a controlled file can be restored from the new repository, murderbot has no repository surface, and the historical repository remains unmodified by this delivery.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS OT-94.2: Guarded repository observation records one successful host-tagged snapshot from each enabled host after its standard scheduled unit runs or after a separately operator-authorized one-shot start of that installed unit; polling is bounded, read-only and never reimplements backup orchestration.; PASS OT-94.3: A guarded non-destructive repository check and controlled restore prove the new snapshots are readable.; PASS OT-94.4: Read-only murderbot evidence proves backup identity, credential paths, Restic units and repository commands are absent.

*** HOLD AC94.9 - Given historical fleet backup authority is retired, the former master application credential recorded in repository history is unusable, this operator host authenticates with its rotated replacement, active fleet configuration contains neither historical Restic payload, and rollback cannot restore usable historical repository authority.
:PROPERTIES:
:CUSTOM_ID: ac94-9-given-historical-fleet-backup-authority-is-retired-the-former-master-application-credential-recorded-in-repository-history-is-unusable-this-operator-host-authenticates-with-its-rotated-replacement-active-fleet-configuration-contains-neither-historical-restic-payload-and-rollback-cannot-restore-usable-historical-repository-authority
:END:

**** Requirement

Given historical fleet backup authority is retired, the former master application credential recorded in repository history is unusable, this operator host authenticates with its rotated replacement, active fleet configuration contains neither historical Restic payload, and rollback cannot restore usable historical repository authority.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.18: Real Nix and repository-boundary tests prove the current evaluated generation uses only the dedicated payloads and has no active historical repository source.; PASS UT-94.2: On 2026-08-27 the operator confirmed that the master application key used by this host was rotated, invalidating the former credential retained in Git history and any older generation that references it.

*** HOLD AC94.10 - Given application-consistent backup hooks are declared, each hook has fixed Nix-store argv, an application identity, an isolated environment, bounded runtime, a unique normalized staging directory, required regular-file artefacts and an optional exact validation contract; preparation follows declared dependencies, cleanup follows reverse dependency order after success or failure, and any preparation, validation or cleanup failure prevents a successful backup outcome; kepler452 has validated SQLite online-backup consumers whose staged databases are the canonical restore sources while raw database, WAL and SHM files remain available as fallback evidence.
:PROPERTIES:
:CUSTOM_ID: ac94-10-given-application-consistent-backup-hooks-are-declared-each-hook-has-fixed-nix-store-argv-an-application-identity-an-isolated-environment-bounded-runtime-a-unique-normalized-staging-directory-required-regular-file-artefacts-and-an-optional-exact-validation-contract-preparation-follows-declared-dependencies-cleanup-follows-reverse-dependency-order-after-success-or-failure-and-any-preparation-validation-or-cleanup-failure-prevents-a-successful-backup-outcome-kepler452-has-validated-sqlite-online-backup-consumers-whose-staged-databases-are-the-canonical-restore-sources-while-raw-database-wal-and-shm-files-remain-available-as-fallback-evidence
:END:

**** Requirement

Given application-consistent backup hooks are declared, each hook has fixed Nix-store argv, an application identity, an isolated environment, bounded runtime, a unique normalized staging directory, required regular-file artefacts and an optional exact validation contract; preparation follows declared dependencies, cleanup follows reverse dependency order after success or failure, and any preparation, validation or cleanup failure prevents a successful backup outcome; kepler452 has validated SQLite online-backup consumers whose staged databases are the canonical restore sources while raw database, WAL and SHM files remain available as fallback evidence.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/94.md][#94 - Make fleet backup an explicit host capability]]

**** Tests

- PASS RT-94.19: Real Nix evaluation proves the kepler452 consumers, fixed-command contract, identity and environment isolation, bounds, required outputs, dependency ordering, reverse cleanup and explicit non-zero preparation, validation and cleanup failure paths.; PASS RT-94.20: A real local SQLite WAL fixture proves direct =.backup= captures committed WAL-resident data in a valid standalone database.; PASS RT-94.21: Negative Nix evaluations reject unsafe paths, non-store executables, shared staging, unknown dependencies and dependency cycles.; PASS OT-94.5: The installed kepler452 unit prepares and validates both staged databases, creates a snapshot containing them, removes hook staging afterwards and exposes the standard successful service outcome. Read-only repository inspection proves both staged databases are valid and raw database/WAL/SHM fallback artefacts remain present.

*** HOLD AC98.1 - orderable Hetzner type/location validation
:PROPERTIES:
:CUSTOM_ID: ac98-1-orderable-hetzner-type-location-validation
:END:

**** Requirement

Given a Hetzner server-type and location pair is validated before server creation, an orderable pair is accepted, while a deprecated or unavailable pair is rejected with the rejected pair and available alternatives identified.

**** Provenance

- *Introduced by:* [[file:archive/migrated-tickets/98.md][#98 - pre-migration baseline, 2026-09-03]]

**** Tests

- PASS RT-1.20 (orderable Hetzner type/location validation)

** Migration notes and footnotes

The source ledger's migration heuristic is preserved as a footnote and is not
treated as contemporaneous test evidence.

[fn:migration-heuristic] Inferred migration status, not a contemporaneous test result. The marked ticket or criterion was treated as delivered because maintained regression evidence remained current for the ticket, or because every other recorded test passed and no more than two unresolved user or one-off tests remained.

--------------

*** Maintained regression traceability supplements
:PROPERTIES:
:CUSTOM_ID: maintained-regression-traceability-supplements
:END:
These entries preserve maintained regression identifiers absent from their original AC table. They add no requirements. Each identifier remains current passing evidence at the mapped boundary; cancelled or superseded lineage is labelled rather than silently promoted.

- PASS RT-38.11 --- issue #38; AC relationship: AC38.2; =loadToken= returns env-var value when =CLOUDFLARE_API_TOKEN= is set
- PASS RT-38.12 --- issue #38; AC relationship: AC38.2; =loadToken= falls through to =.env= file when env var unset; reads the value correctly
- PASS RT-38.13 --- issue #38; AC relationship: AC38.2; =loadToken= falls through to repo-root =.env= when neither env var nor cwd =.env= provides the token
- PASS RT-38.14 --- issue #38; AC relationship: AC38.2; =readTokenFromEnv= handles =export = prefix, quoted values (rejects gracefully), and missing key
- PASS RT-38.23b --- issue #38; AC relationship: AC38.4; AC38.4: findZoneForDomain returns longest-suffix-matching zone ID.
- PASS RT-38.23c --- issue #38; AC relationship: AC38.4; AC38.4: loadYAML reads + parses a YAML file into the target struct.
- PASS RT-38.23d --- issue #38; AC relationship: AC38.4; AC38.4: loadEnvFile parses .env content and exports to environment,
- PASS RT-38.24b --- issue #38; AC relationship: AC38.4; AC38.4: findZoneForDomain returns error when no matching zone.
- PASS RT-38.24c --- issue #38; AC relationship: AC38.4; AC38.4: loadYAML returns error on missing file.
- PASS RT-38.24d --- issue #38; AC relationship: AC38.4; AC38.4: loadEnvFile returns error on missing file.
- PASS RT-38.4 --- issue #38; AC relationship: AC38.2; =cfClient.findZone= returns correct zone ID for exact-match and best-suffix-match cases against =httptest.Server= mock
- PASS RT-38.5 --- issue #38; AC relationship: AC38.2; =cfClient.listRecords= returns the expected slice filtered by name+type
- PASS RT-38.6 --- issue #38; AC relationship: AC38.2; =cfClient.listAllRecords= returns records, with optional name/type filters applied correctly
- PASS RT-38.7 --- issue #38; AC relationship: AC38.2; =cfClient.createRecord= issues correct POST body and handles success
- PASS RT-38.8 --- issue #38; AC relationship: AC38.2; =cfClient.updateRecordProxied= issues correct PUT body preserving other fields
- PASS RT-38.9 --- issue #38; AC relationship: AC38.2; =cfClient.deleteRecord= issues correct DELETE and handles success
- PASS RT-39.4 --- issue #39; AC relationship: AC39.1; =.env= loader produces correct map and handles =export = prefix, comments, blank lines
- PASS RT-44.10 --- issue #44; AC relationship: AC44.1; Maintained regression RT-44.10
- PASS RT-44.11 --- issue #44; AC relationship: AC44.5; Maintained regression RT-44.11
- PASS RT-44.12 --- issue #44; AC relationship: AC44.1; AC44.1: Nix stderr warnings do not corrupt JSON boolean validation.
- PASS RT-48.3b --- issue #48; AC relationship: AC48.2; Maintained regression RT-48.3b
- PASS RT-60.21 --- issue #60; AC relationship: AC60.1; Maintained regression RT-60.21
- PASS RT-60.22 --- issue #60; AC relationship: AC60.1, AC60.5; Maintained regression RT-60.22
- PASS RT-60.23 --- issue #60; AC relationship: AC60.1; Maintained regression RT-60.23
- PASS RT-60.24 --- issue #60; AC relationship: AC60.1; Maintained regression RT-60.24
- PASS RT-60.25 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.25
- PASS RT-60.26 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.26
- PASS RT-60.27 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.27
- PASS RT-60.28 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.28
- PASS RT-60.29 --- issue #60; AC relationship: AC60.1, AC60.5; Maintained regression RT-60.29
- PASS RT-60.30 --- issue #60; AC relationship: AC60.2; Maintained regression RT-60.30
- PASS RT-60.31 --- issue #60; AC relationship: AC60.2, AC60.3; Maintained regression RT-60.31
- PASS RT-60.32 --- issue #60; AC relationship: AC60.4; Maintained regression RT-60.32
- PASS RT-60.33 --- issue #60; AC relationship: AC60.5, AC60.6; Maintained regression RT-60.33
- PASS RT-60.34 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.34
- PASS RT-60.35 --- issue #60; AC relationship: AC60.3, AC60.4; Maintained regression RT-60.35
- PASS RT-60.36 --- issue #60; AC relationship: AC60.3, AC60.4; Maintained regression RT-60.36
- PASS RT-60.37 --- issue #60; AC relationship: AC60.2; Maintained regression RT-60.37
- PASS RT-60.38 --- issue #60; AC relationship: AC60.1, AC60.3, AC60.5; Maintained regression RT-60.38
- PASS RT-60.40 --- issue #60; AC relationship: AC60.2; Maintained regression RT-60.40
- PASS RT-60.41 --- issue #60; AC relationship: AC60.3; Maintained regression RT-60.41
- PASS RT-63.24 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.3); issue 63 cancelled/superseded, lineage only; Restic timer and secret compliant/failing states are distinguished
- PASS RT-63.26 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.3); issue 63 cancelled/superseded, lineage only; external-command failure and malformed-output fixtures fail with the responsible check named
- PASS RT-63.27 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; evaluated base and full roles install the same Go =fleet-verify= package
- PASS RT-63.28 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; removed --- whole-provisioning simulation duplicated external platforms rather than testing a stable boundary~~
- PASS RT-63.29 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; removed --- whole-provisioning simulation duplicated external platforms rather than testing a stable boundary~~
- PASS RT-63.30 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; removed --- whole-provisioning simulation duplicated external platforms rather than testing a stable boundary~~
- PASS RT-63.31 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; removed --- whole-provisioning simulation duplicated external platforms rather than testing a stable boundary~~
- PASS RT-63.33 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.5); issue 63 cancelled/superseded, lineage only; the executable operator boundary invokes the stable installed command and propagates its exit status without depending on implementation language
- PASS RT-63.34 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.5); issue 63 cancelled/superseded, lineage only; subprocess CLI dry-run contains 17 observable checks and excludes an unconditional cloud-firewall pass
- PASS RT-63.35 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.5); issue 63 cancelled/superseded, lineage only; the executable operator boundary reports =18/18= only when one independently observed cloud-firewall result and all 17 host results pass
- PASS RT-63.36 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.4); issue 63 cancelled/superseded, lineage only; tailnet preflight rejects failed or non-JSON Tailscale CLI output with the responsible dependency named
- PASS RT-63.4 --- issue #63; AC relationship: successor AC66.1/AC66.2/AC66.4 (archived source AC63.1); issue 63 cancelled/superseded, lineage only; invalid mode, unknown item, and missing hostname each exit with CLI-misuse status and diagnostics
- PASS RT-66.1 --- issue #66; AC relationship: AC66.1, AC66.2; Maintained regression RT-66.1
- PASS RT-66.2 --- issue #66; AC relationship: AC66.1, AC66.2, AC66.4; Maintained regression RT-66.2
- PASS RT-66.3 --- issue #66; AC relationship: AC66.1, AC66.4; Maintained regression RT-66.3
- PASS RT-66.4 --- issue #66; AC relationship: AC66.5; Maintained regression RT-66.4
- PASS RT-66.5 --- issue #66; AC relationship: AC66.4; evaluated roles share the Go verifier package.
- PASS RT-80.31 --- issue #80; AC relationship: AC80.1; Maintained regression RT-80.31
- PASS RT-82.1 --- issue #82; AC relationship: AC87.1/AC87.3 and AC66.5 (archived source AC63.4, AC63.5); emergency successor evidence; Maintained regression RT-82.1
- PASS RT-84.10b --- issue #84; AC relationship: AC84.4; source AC84.4 is superseded by AC91.3; Maintained regression RT-84.10b
- PASS RT-84.4b --- issue #84; AC relationship: AC84.2; Maintained regression RT-84.4b
- PASS RT-95.3 --- issue #95; AC relationship: AC85.3, AC85.4; Maintained regression RT-95.3
- PASS RT-95.4 --- issue #95; AC relationship: AC85.4; Maintained regression RT-95.4
* Migration record
:PROPERTIES:
:VISIBILITY: folded
:END:

** DONE SDLC migration - v2 to v3                      :migration:
:PROPERTIES:
:CUSTOM_ID: sdlc-migration
:MIGRATED: 2026-09-06
:END:

- *Pre-migration state:* sdlc_v2_state_2026-09-06
- *Migration branch:* sdlc-v3-migration-2026-09-06
- *Ticket archive:* Not applicable.
- *Historical requirements:* [[#legacy-acceptance-criteria][Legacy Acceptance Criteria (SDLC v1)]].
- *Archived v2 work:* [[file:archive/sdlc-v2/][Archived Spec Kit work]].
- *Assumptions:* None recorded by project initialization.
- *Unresolved items:* None recorded by project initialization.
